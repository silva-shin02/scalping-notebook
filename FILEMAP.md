# FILEMAP — 分割ファイル対応表
読み込み順: vendor.js → app-01.js → ... (index.html の末尾に記載)
各 app-*.js は「トップレベル文の境界」で分割。連結すると元コードと一致。

## 全体の約束事

**数値入力欄は全角で打たれても打った瞬間に半角へ 2026-08-05x**（ユーザー要望「数値入力欄はすべて自動で半角に」・app-02/04/06/07/09 を横断）
- 変換関数は3つ。**用途で使い分ける**＝間違えると打てなくなる。
  - `_toHankaku`（app-03:203）… 全角→半角に**変換するだけで文字を消さない**。`．`→`.`／`ー`→`-`／`：`→`:` も直る。**小数やマイナスを打つ欄・時刻欄はこれ**。
  - `_toHankakuNum`（app-05:6211）… 変換＋**数字以外を全部捨てる**。整数だけの欄（株数・円・%）用。
  - `_toHankakuDecimal`（app-05:6218）… 変換＋数字と`.`だけ残す（小数点は1つに畳む）。
  - ⚠️**小数やマイナスを許す欄で `_toHankakuNum` を既定にしてはいけない**＝`.`や`-`を打った瞬間に消えて入力できなくなる。
- 効かせ方は3通り。**新しい数値欄を足すときはどれかに必ず乗せる**。
  1. **`FastInput`（app-02:243）は自動**＝`transformInput` 未指定でも `inputMode:"numeric"/"decimal"` か `type:"number"` なら `_toHankaku` を既定で当てる。キャレット保持も既存実装のまま。明示した `transformInput` が最優先。**inputMode を書かない数値欄は対象外**なので、数値欄には必ず `inputMode` を付ける。
  2. **素の`<input>`は onChange で包む**＝`setX(_toHankakuNum(e.target.value))`。controlled input なので state が半角になれば表示も半角になる。
  3. **`e` を渡し回す共通部品**（app-06 `_cfgStepField`）は `onCh({ target:{ value: 半角 } })` と**器を作って渡す**。⚠️DOMの `e.target.value` を書き換える方式は取らない（controlled なら state 経由で戻るので不要・キャレットが飛ぶ）。
- ⚠️**`type:"number"` は使わない**（2026-08-05x に残り4箇所を `type:"text"`＋`inputMode` へ統一）。全角で入力するとブラウザ側の値サニタイズで `e.target.value` が `""` に潰れ、こちらの変換関数まで文字が届かない＝どう頑張っても半角化できない。
- ⚠️**`//` コメントを1行のJSX風呼び出しの途中に足さない**（2026-08-05x に app-02/app-06 で実際にやらかした）。`onChange: ...,   // 説明 style: {...} })` となり**行の残りが丸ごとコメントに飲まれて構文エラー**。説明は行の上に置く。

## app-01.js (元 L378-6573)
_React, useState, useEffect, useRef, useCallback, useMemo, IS_TOUCH, **_SN_THEME_KEY/_snGetTheme/_snThemeIsDark/_snApplyTheme（ダークモード 2026-06-19=端末ローカル保存(localStorage sn_theme_v1: light/dark/auto)。html.sn-darkへCSSフィルタ invert(1) hue-rotate(180deg) で全体を明度反転＝色相保持で損益の赤/緑等は維持・img/canvasは逆フィルタで原状復元・SVG折れ線は反転のまま暗背景で明るい線。初期適用とautoのOS追従はindex.htmlの早期<script>・トグルUIはSettingsModal app-04「🎨表示」タブ）**, debounce, IMG_H, DEF_STOCKS, DEF_FM_INDICATORS, DEF_FM_STOCKS, DEF_NEWS_CATS, _DEF_STOCKS_FROZEN, _DEF_NEWS_CATS_FROZEN, MACRO_LEVELS, MACRO_LABELS, MACRO_LABELS_STOCK, MACRO_COLORS, getMC, DAYS_JP, _fmtDow, OPEN_TAGS, MOVE_TAGS, TECH_TAGS, GRADES, TAG_SEL, TAG_UNS, _TAG_QC, _isDarkColor, _tagColorTriple, EMPTY, ST_KEY, CF_KEY, getFC, dateFmt, todayStr, stripCat, stripHtml, _hasText, _sigNameNode, calcSim, **_imgToWebpMaybe（取り込み画像をWebP q0.88へ再エンコード=容量削減2026-06-15。gif/svg対象外・24x24縮小比較でiOS空化検証・8%未満縮小は元のまま＝壊さない。**2026-06-17: 長辺をMAXEDGE=1600pxまで縮小してから再エンコード＝1枚あたり容量を桁で削減・縮小時は多少でも縮めば採用。WebP非対応端末はJPEG q0.82/PNG(透過保持)へフォールバック。既webpも縮小対象に変更**）, fileToImg/_fileToImg（**2026-06-20: 取り込みは再エンコード/縮小せず原寸・原画質で保存＝書き込み画面を最高画質に。_imgToWebpMaybe経由のWebP化+1600px縮小を撤廃（容量はニュース画像の自動削除で管理・手動圧縮は設定の📉で随時）。addedAt(追加日時)/star(保護)付与は維持**）,** getAllNewsCatsData, getCatData, hasCatContent, getDayEvents, hasEventsContent, getEventColor, getEventCategoryName, _buildHolidayDateSet, _collectCatTagsFromTrades, _filterTagPoolForCat, migrateData（移行群。_migEpScheme3=OS1〜5導入前の旧記録(schemeなし・osVal有)をscheme:3化＝旧H1(holdHighVal)→OS2・旧H2(hold2HighVal)→OS3へ_epLegsベースで移しhold欄空に。scheme:2/3は対象外・損益保存・冪等。**_migMinBarDefault1=分足(minBar)未設定の既存記録を[1](1分足)に補完＝有効な1/5を持たない記録のみObject.assignで[1]に・既設定([1]/[5]/[1,5]・旧single)は不変・冪等 2026-06-24**。**_migLineCoexist 2026-07-08g=旧「ライン併存/併存ライン」を詳細(sigDetail b/k/f)・EPナビitem・候補マスターから除去しsignal/item.lineCoexist=true付与＝履歴のbaseAlphaVal/alphaVal/epは非書換・条件ベース冪等**）, stLoad, _stSaveTimer, _stSaveLatest, stSave, _stStrip, _snEvictExpendableCaches（localStorage容量超過時、再取得可能なキャッシュを退避してユーザー記録の保存を優先2026-06-16。**2026-06-17: 日足CSV sn_dc_csv_v1_* に加えCAバー sn_dcc_ca_bar_v1_* も退避対象に追加＝退避の穴を塞いだ。設定画面(SettingsModal app-04 メンテナンス欄)の「🧽 不要キャッシュを削除」ボタンもこの関数を再利用**）, _stWriteToStorage（容量超過時: 画像IDB退避→CSVキャッシュ退避→再保存→なお失敗ならalert警告。記録の「黙って保存失敗→消失」を防止）, _stFlush, cfLoad, cfSave, _FB_USAGE_PREFIX, _fbUsageCache, _fbUsageFlushTimer, _fbUsageKey, _fbUsageGet, _fbUsageFlush, _fbUsageSave, _fbUsageFormat, _FB_FREE, _fbWarnShown, _fbAutoPauseCb（未使用＝自動停止は廃止2026-06-16）, _fbSetAutoPauseCb（同左・未使用）, _fbTrack, _fbCheckLimits（90%は警告バナーのみ表示し同期は継続＝自動停止は廃止2026-06-16）, _fbShowBanner, _fbBase, _fbAuth, makeFbUrl, _makeFbDataUrl, _HEAVY_KEYS, _stripHeavy, _buildMeta, **_unionIds/_applyDelTombstones（削除トムストーン(削除id集合)方式 2026-06-19: signals削除は物理削除＋削除idを charts[ck]._delSig に記録し、_mergeRemoteMeta末尾でlocal/remoteのunionで対象配列から再除去＝相手端末が新しくても削除済みが復活しない。_delSig/_delItem キー不在の既存データには no-op。itemsは現状能動削除UI無しで_delItemは将来用）**, _mergeRemoteMeta（末尾で_applyDelTombstones適用）, fbGet, _fbLocalV, _fbLastPutHash, _fbHashStr, fbPut, _fbPut, fbPollV, _fbPollV, **_canonStr（キー順非依存の正準JSON）, **_rtdbNorm（署名前にRTDB保存形へ正規化＝空{}/空[]/nullはキーごと捨てる。書き手JS値と読み手のRTDB往復後値で署名が食い違い該当ckを毎回無駄再DLするのを防ぐ 2026-06-19）, _secSig（サブキー/セクション署名=正準JSON(rtdbNorm(stripHeavy))の長さ:FNV）, _SHARD_SECTIONS={charts,trades}/_SHARD_FULL_THRESHOLD=30/_SIG_EMPTY_OBJ/_SIG_EMPTY_ARR**, fbGetDiff（差分同期2026-06-15→**サブキー粒度シャーディング2026-06-19**: remoteの署名マップ(新:/meta/_svc.json=charts/tradesはck/日付ごとの署名・他は文字列／旧:/meta/_sv.json=セクション粒度)とローカルを比較し、**変更されたサブキー(ck)だけ/meta/charts/<ck>.jsonでDL**→「ローカル未変更＋取得した変更分」で完全なremote相当を再構成し既存_mergeRemoteMetaに全件渡す＝欠落削除リスク無し。1チャート編集で全charts再DLしていたのを当該ckのみに削減＝RTDB帯域の主因対策。**変更サブキーが多い(>=30 or 半数以上)時はセクション丸ごと1回DLへ切替(ローカル限定ckはObject.assignで保護)。null本体は署名が空相当(_SIG_EMPTY_*)なら局保持・非空なら競合で{status:full}。_svc無し→_svへ、署名無し/不確実時は{status:full}で全件フォールバック。トップレベル/サブキー消滅は伝播させない**。後方互換: 旧版端末は_svのみ見て従来動作・新writerは_sv/_svc両方書く）, _fbPollFetch（ポーリング版数変化時の取得=fbGetDiff→ダメなら全件fbGet・{ok,data,nochange}を返す）,** fbInitialLoad（起動時同期。**2026-06-19: 全件fbGetでなくfbGetDiffで変更トップレベルセクションのみDL＝起動時(file://は開く度に起動)の全件meta DLを回避しRTDBダウンロード帯域を削減。_sv無し旧DB/取得失敗/ローカル空など不確実時は内部_full()で従来どおり全件fbGetにフォールバック。nochange時はDL省略。戻り値(skip/ok+data/pushed/err)は不変で呼び出し側=app-08のL379/L784は無改修**）, _snIdb, _snIdbReady, snIdbGet, snIdbSet, snIdbDel, _SN_CACHE_NAME, snCacheGet, snCachePut, _stSaveImagesToIdb, _evictNonActiveImages, _fbStorageRef, _FB_STORAGE_BUCKET, fbStorageInit, base64ToBlob, _uploadToStorage, **_snStoragePathFromUrl/_snCollectReferencedStoragePaths/_snStorageAudit/_snStorageDeleteOrphans（Firebase Storage孤児GC 2026-06-17: dataの全/o/URL＋fbGetリモート全meta＋CAドラフトthumbUrl(chart-annotator-drafts)で参照集合を作り、listAllのうち本アプリ製(img_/orig_/html_に加え旧命名d_charts_*/d_trades_*も対象に拡張2026-06-17＝notebook-imagesフォルダ自体がオーナーシップ境界・CAはchart-images/chart-thumbsに書くので来ない)で未参照かつ30日以上前のみdeleteObject。remoteOk/caOk取得失敗時は削除中止＝多端末/CA参照の巻き込み防止。設定→メンテナンスの「🗂クラウド画像の整理」から診断/削除。敵対的検証2ラウンド済み）**, **_snRecompressImages/_snApplyImgMaps/_snRemapImg/_snForEachImg/_snImgIsImageObj/_snRecompressGetBytes/_snStorageBreakdown/_snImgUsableB64/_snImgB64Sig/_snB64Bytes（過去画像の再圧縮＋容量内訳診断 2026-06-17。当初ニュース限定→全画像に一般化）: **_snRecompressImages**=data全体の画像オブジェクト(ニュース/チャート/メモ。_snForEachImgで走査・base64/orig_base64/strokesへは降りない)をWebP/長辺1600pxへ再エンコード(_imgToWebpMaybe)・小さくなる時のみ新パス(notebook-images/img_<hash>)へ再アップロードし参照を張替え。**_imgと_origが同一内容なら同じhash→同じオブジェクトに集約＝重複保存を解消。注釈付き(strokes有)のorigは再編集元として温存(縮小で位置ズレ防止)**。**_snApplyImgMaps**=data全体を非破壊・不変に深く走査しURL/内容シグネチャ照合で画像参照をremap(変更無い枝は参照維持)。現バイト取得はライブb64→IDB→Cache→networkの順でegress最小化(_snRecompressGetBytes)。削除はせず旧オブジェクトを孤児化し既存GC(_snStorageDeleteOrphans。旧命名d_*も回収対象に拡張済)で同期後に回収＝多端末巻き込み防止。**_snStorageBreakdown**=バケット全フォルダをroot再帰listAll＋同時実行12のgetMetadataで件数/合計バイトとファイル上位を集計(読み取り専用)＝CA(chart-images/chart-thumbs)等notebook-images以外も可視化。**_snStorageCategoryAudit/_snCatOfKey（種類別内訳 2026-06-17）**=ハッシュ命名(img_/orig_)は名前から用途不明なので、data(ローカル＋fbGetリモート)全文字列の/o/<path>を拾い出現キー経路の目印(_snCatOfKey: newsCats→ニュース/chartImg・chartImgs→チャート画像/chartMemo・chartMemoHtml→チャートメモ/tradesMemo→取引メモ/summaryHtml・summaryMemo・tradesSummaryMemo→総括メモ/stockInfo→銘柄情報タブ/signals→エントリー記録)で分類→notebook-imagesをgetMetadataし種類別bytes/件数＋orig内数を集計。どの参照にも無いものは未参照(孤児)。設定の「📋種類別に分析」。カテゴリ判定はJScriptユニットテスト済。**_snStripOldNewsImages（古いニュース画像整理 2026-06-18）**=trades日付がcutoff(YYYY-MM-DD)より前のニュース画像(newsItems/newsMemo/subCatMemosのimages[])を「画像だけ」空配列化＝テキスト/タグ/記録は残す・非破壊不変で{data,count}返却。外したStorageは孤児化→「全部削除(grace=0)」で回収。設定の「🗞古いニュース画像を削除」(日付指定→対象枚数プレビュー→削除)。ユニットテスト14/14 pass。設定→メンテ(SettingsModal app-04)の「📊全フォルダの内訳を測定」「📉画像を圧縮」。データ変換はJScriptユニットテスト計13/13 pass）**, **_snImgAddedAt/_snPruneNewsImagesCore/_snAutoPruneNewsImages（★保護付きニュース画像の自動削除 2026-06-19）**=各ニュース画像に追加日時addedAt＋保護フラグstarを付与（fileToImgで採番・ImageAnnotator保存でも温存）。_snPruneNewsImagesCore=共通走査(dateOk(date)×keepImg(img)で判定・件数は実除去数・非破壊不変)。_snStripOldNewsImages(手動・日付前)は★画像を保護するよう変更。_snAutoPruneNewsImages(data,cutoffMs)=全日付で「★なし＆addedAt(無ければid)<cutoffMs」のみ除去＝起動時自動削除の本体。設定はdata.custom.newsImgAutoDelete{enabled(既定true=オン出荷),periodDays(既定7)}でmigrateDataがseed＋_newsImgAddedAtMigで既存画像にaddedAt/star一括backfill(idを実追加日時として使用)。自動実行はApp(app-08)のuseEffect([fbStatus])=enabled&&fbStatus==='ok'&&非空trades&&1日1回(localStorage sn_news_autoprune_day_v1)・初回のみsn_news_autoprune_ack_v1でwindow.confirm(対象枚数提示=大量削除の事前確認)。**push完了後に_snReclaimPrunedStorage(app-01)でStorage実体も自動回収**=pruneで全参照を失ったパス(before/after差)のみを、afterData＋リモート全fbGet＋CAドラフトで再診断(_snStorageAudit)し未参照(孤児)のものだけdeleteObject(remoteOk/caOk失敗時は中止＝安全・他端末の新規画像はlost範囲外で巻き込まない・リモート未反映なら削除0で次回回収)。**全端末でのアプリ上削除は既存_mergeRemoteMetaのid付き配列マージ(リモートmembershipに従い脱落＝復活しない＋images等は_DELETABLE_KEYS)で伝播**。設定UIはSettingsModal(app-04)の「画像・メンテ」タブ。ImgGrid/ImgThumb(app-02)に★トグル＋バッジ・NewsTab(app-03)/MemoSection(app-02)/NewsHistoryView(app-07)にonToggleStar配線。JScript構文＋機能テスト pass。**設定画面をタブ式(同期/データ・銘柄/画像・メンテ)に刷新＋死コード(通信量モニタ false&&)整理 2026-06-19**, urlToBase64, imgSrc, origImgSrc, _imgUploadStore, _imgUploadSubs, _imgUploadSet, _imgUploadSubscribe, _imgUploadGet, _imgUploadAnyPending, CaThumbImg, _preloadRunning, preloadImages, _collectAndUploadHtmlDataUrls, _replaceHtmlDataUrls, _applyHtmlUrlMapToData, **_applyImgUrlMapToData（構造化画像images[](ニュース等の{base64,mt,id})にアップロード済みStorage URLをライブstateへ書き戻す＝_stStripがbase64をlocalStorageから剥離可能にする。_applyHtmlUrlMapToDataの画像オブジェクト版・base64キーで照合・app-08の_snImgUploadCbから呼ぶ。ニュース画像のlocalStorage肥大→容量超過アラート修正 2026-06-17）,** _uploadAllImages（**2026-06-17: 構造化画像アップロード成功URLをimgUrlMap(base64→url)に集約しwindow._snImgUploadCbでライブstateへ書き戻し**）, analyzeChart, _analyzeChart, ANNOT_PRESET_KEY, DEF_PRESETS, _clipStroke, ColorPicker, TextEditor, ZoomLightbox, **_snMaxCanvasDim（端末が描けるcanvas最大1辺を16384/8192/4096でプローブ→キャッシュ。iOSストロークcanvas上限に使用＝M1等は最大8192/非対応は4096自動降格 2026-06-15。**2026-06-21: ストロークcanvasの1辺上限を8192固定→_snMaxCanvasDim(16384)＝デバイス実上限まで拡大**）, **_snMaxCanvasArea（canvas総画素=面積の上限を実機プローブ＝候補33.5M/25.2M/16.8Mを大きい順に試し遠端ピクセルが描けた最大を採用→キャッシュ・失敗時15.7M降格。テストcanvasは判定後即1px化でメモリ即解放。iOSストロークcanvasの面積上限(従来15.7M固定)に使用＝大きい画像でストロークを論理(保存)解像度より高精細に焼ける(背面の原寸ベース画像とのボヤけ差を解消・拡大描画ほど効く)。保存LOGICAL_MAXは据え置き＝保存サイズ/容量は不変。2026-06-21）**, ImageAnnotator（②方式: ベース画像は背面<img>で高精細表示・canvasは透明な手書きストローク専用＝iPad上限とは無関係にガビガビ解消。ストロークcanvasは_snMaxCanvasDim(8192)等で物理上限制限・保存_pngWorkは論理解像度ベクター合成→**PNG(可逆・最高画質。2026-06-20にWebP q0.88から復帰＝書き込み画面の画質を元の最高に)**。**線の太さ正規化 2026-06-18: lineWは論理座標(画像naturalサイズ)基準の絶対値→画像サイズで見かけ太さが約20倍ばらつく問題を_lineWScale(画像長辺/1200・0.5〜4クランプ)×lineW=_geomLineWで補正。ライブ描画/確定(buildStroke/freehand)/消しゴム半径/保存時確定の全経路に適用＝どの画像でもスライダー値=同じ見かけ太さ。スライダー値(toolPrefs)と保存済みs.lineWは不変=過去注釈は変わらない。**文字サイズ(fontSize)も同じ_lineWScaleで正規化＝新規テキスト作成時のみ(onStart idx:-1のtextInputState.fontSize)・再編集/+−は実寸調整 2026-06-18**）,** Pill
**2026-08-03e ニュース記事の保存(keep)と移行**
- `_snNiKept(ni)` / `_snNiKeepStocks(ni)` / `_snMakeKeep(cat,sub,stocks)` / `_snHashId(s)` を追加。**2026-08-03i `_isValidImg`(migrateData内)がローカルIDB退避済み(`lk`)の画像を「中身なし」と誤判定して捨てていた不具合を修正**＝`_stStrip`は`imageUrl`だけでなく`lk`でも`base64`をnull化する(2026-06-23)のに、`_isValidImg`が`lk`を見ておらず、Storage未アップロードの画像が再読込のたびに記録から消えていた（実体はIDBに孤児として残る）。復元は`_snPreloadImages`が`lk`から行う。記事に`keep:{at,cat,sub,stocks[]}`が1つあれば**保存済み**（中身が空なら未分類で保存）。
- **自動削除の保護が画像1枚→記事1件へ**: `_snPruneNewsImagesCore`の`keepImg`は`(img, ni)`で受ける。`_snAutoPruneNewsImages`/`_snStripOldNewsImages`は`_snNiKept(ni)`なら中の画像を全部残す（旧`im.star`も後方互換で保護継続）。`_snNewsItemEmpty`も`keep`があれば空と見なさない。メモ(newsMemo/subCatMemos)の画像には`ni=null`を渡す。
- **移行（migrateData・冪等なのでフラグで囲わず毎回走る）**: ①鍵付き画像`im.star`／②記事★`ni.starred` → その記事を保存済みへ昇格し当時のカテゴリ・サブを`keep.cat`/`keep.sub`へ継承（`keep.stocks`は空）。③`newsMemo`/`subCatMemos[サブ]` → `fromMemo:true`の記事カードへ変換して`newsItems`の先頭へ（id は`_snHashId(日付|カテゴリ|由来)`＝端末をまたいでも同じidになる）＋保存済みにする。④`newsCatDefaults`/`newsSubCatDefaults`/`stockSubCatRefs`はcustomから削除（`EMPTY`と`_LOCAL_WINS_KEYS`からも除去）。`im.star`/`ni.starred`のフィールド自体は消さない＝旧版の端末が読んでも壊れない。


## app-02.js (元 L6574-12637)
CPill, FlowChips, DeleteDlg, AddBtn, FastInput, _fiFlushAll, AutoTextarea, RichTextEditor, PasteZone, useImgUploadStatus, ImgThumb, ImgGrid, RichMemoEditor, LightTextarea, _summaryMemoToHtml, MemoSection, TagPicker, makeShapeTagPoolHandlers, makeTagPoolHandlers, _sigId, **出現シグナル・テクニカル(2026-06-18): _apByTime/_apSigNames/_apRowsForDay(data,stock,date=取引記録signals[]の自動シグナル(src:auto・stripCat名寄せ)＋手動appearances(src:manual)を時間順マージ。auto行メモは取引記録rationaleでなく出現欄固有=charts[ck].apMemos[s|sigId|名前]・_apSetAutoMemoで編集)/_apCollectAll(data=全銘柄日付横断)/_apSave/_apDelete(charts[ck].appearances編集)。TechnicalManageModal(custom.technicals=[{id,name}]の追加/削除/改名/並び替え)。AppearanceSection(銘柄×日付の「📡出現シグナル・テクニカル」欄=時間/シグナル・テクニカル/メモのエクセル風表＋＋追加(テクニカルはマスター/シグナルはプールから・自動行は読取・手動行は✎🗑)。ChartSectionのチャートメモ直下に描画・全銘柄(日経含む・外国市場除く))**, _sigStats, _sigStockHistory, SignalHistoryPopup, EntrySignalSection, _nikkeiMacroFromChange, _NikkeiManualInput, ChartSection, WeeklyPnlPanel（単一銘柄の「今週の損益データ」パネル＝取引タブ今週欄と同システムを独立再現。ChartSection末尾📝メモ下に表示・hideSignals(日経)/外国市場は非表示。自前state(週送り/α・損切りシミュ/明細行展開recExp/日別折りたたみdayExp)・週はdate基準の月〜金。**表示は取引タブ今週欄と同形の日別サマリー折りたたみに変更(2026-06-14)**＝曜日/件/勝/引/負/未達/平均OS値/EP損益/H１/H２/実現損益/タグの行(_sumRow=取引タブ_wkRowと同ロジック:_elCalcStats/_elAllMissRow/_elDynPlanned/_elDynHold/_elHold2TotParts)を週合計＋月〜金で表示し、行タップで展開(_expRowFor)。週合計展開=理想α表(_elIdealAlphaTableV2)＋α/損切りシミュ操作(_simControlsBar)＋週全体の明細、日別展開=その日の明細。明細テーブル(_detailTableFor/_detailRowsFor/_detailTotRowFor=旧フラット表を関数化)のセルは取引タブと同じグローバルヘルパー(_epOsChainCell/_epECell/_epPnlCell/_elHoldTd2/_elRPnlDispW)・合計は_elTotAccumで取引タブと一致。DayViewには非依存=既存今週欄は無改修）

## app-03.js (元 L12638-17335)
_fmIsBizDay, _fmPrevValue, _fmPrevValues, _fmPrevDates, _FMSparkline, _toHankaku, _fmToHalfWidth, _FMValueInput, _FMPriceChart, _FMHayamihyoTable, ForeignMarketTable, ForeignMarketSettingsModal, StockTabs, _RenameRow, **_ncsMapNewsItems/NewsClassSettings（ニュースの分類の管理 2026-08-03e。旧NewsCatTabsを置き換え）**, EventsTab, NewsTab

**2026-08-03e ニュース欄の再設計（カテゴリ／サブのタブを全廃・分類は保存時だけ）**
- **NewsTab**: カテゴリタブ・サブタブ・「⚙自動タグ」・記事★(`ni.starred`)・サブの`<select>`・独立メモ欄(`MemoSection`)をすべて撤去。その日の**全カテゴリのnewsItemsを串刺し**で1つのカードgridに並べる（B案＝画像主役のボード）。並びは`id`昇順＝追加順。上部に**使用中の材料タグをチップ（件数付き）**で出し、押すと絞り込み（複数選択＝AND）。カードは 画像／本文(`MemoEditableField`)／材料タグ／**「🔖この記事を保存」**。札の追加は「＋」への貼り付け・ファイル選択・D&Dのみ。**2026-08-03f 複数枚をまとめて追加できる**（`addNewsWithFiles`＝全部読み終えてから1回だけ書き込み・既存idを避けて採番するのでid衝突なし。1枚＝1札）。**2026-08-03h 並び順はファイルの更新日時`lastModified`の早い順**＝スクショを撮った順＝時系列に揃う（ドロップされる順はエクスプローラの表示設定しだいで変わるので当てにしない。日時が同じ／取れない分は渡された順のまま＝`Array#sort`は安定）。板の並び自体は`id`昇順なので、後からドロップした分は必ず後ろに付く。**2026-08-03g「ニュース画像1枚＝記事1件」を前提に、画像を持つ札には貼り付け枠を出さない**（画像が無い札＝旧メモ由来・自動削除で画像が消えた札にだけ出し、`PasteZone`へ`single:true`を渡して1枚しか受けない）。`PasteZone`(app-02)自体は複数ドロップ／複数選択／複数枚の貼り付けに対応（`single`未指定の他画面はまとめて足せる）。
- **2026-08-03i 札のレイアウトと並べ替え**: 画像を持つ札から本文欄を撤去（画像が無い札＝旧メモ由来と、すでに本文がある札にだけ出す＝移行したメモが見えなくならない）。「🏷️ タグ付け」と「🔖 記事を保存」は横並び（`TagPicker`に`addLabel`/`trailing`を追加。`trailing`が来た時だけ横並びの行を作るので他画面のレイアウトは不変）。横並びは最大3枚（`repeat(IS_TOUCH?2:3, minmax(0,1fr))`）にして1枚を広く取り、`ImgGrid`に`maxHeight`プロパティを追加してサムネイルを大きく（PC420/タッチ300・既定は従来のIMG_H=180）。
- **ドラッグ並べ替えを復活**（`_snNiOrderCmp`/`reorderNiTo`/`onNewsDragStart`）。PCは画像を直接掴める・タッチは☰ハンドル（画像でtouchstartを塞ぐと縦スクロールできなくなるため）。落とし先はカード中心との距離で判定（gridなので上下左右）。並べ替えると**その日の全札に`ord`(0,1,2…)を焼き込む**＝以後は`ord`順、`ord`が無い札は後ろに`id`昇順。クローンは同じ`ord`。`ord`は位置情報なので`_snNewsItemEmpty`(app-01)の無視キーに追加した（`ord:0`だけの空札が掃除されなくなるのを防ぐ）。
- **2026-08-03j 横長の画像はカードを2列ぶんに広げる**（`imgAspects`/`isWideNi`・比1.6以上＝16:9より横長）。縦横比はデータに持っていないので描画後にDOMの`naturalWidth/Height`から実測する＝以前から入っている画像にも効く。`gridAutoFlow:dense`は使わない＝空きができても並び順(`ord`)と画面の並びが食い違わないようにする（ドラッグ並べ替えと両立させるため）。
- **2026-08-03k 画像の形で出し方を3通りに分ける**（`_niShape`/`isWideNi`/`isTallNi`・実測した`naturalWidth/Height`で判定）。
  - **縦長(比0.95以下)** → `ImgGrid`の`fillHeight`（高さ固定＋`object-fit:cover`＋上端そろえ）でカード幅いっぱいに敷く。旧: 高さ上限420で頭打ち→幅が241pxに痩せて左右に61pxの死に領域＋行の高さがバラバラ。新: 全札同じ高さ＝行が揃い、画像も約25%大きい（**2026-08-03qに固定pxをやめ正方形の枠`fillAspect`へ**）。切れるのは記事下部（元々この幅では読めない）で、全体はクリックで拡大。
  - **横長(比1.6以上)かつ元が600px以上** → `span 2`。元が小さい横長を2列にすると拡大されず右側が丸ごと余るだけなので幅の下限を付けている。
  - **正方形〜ゆるい横長** → 従来どおり比率のまま（`cover`にすると左右が切れて記事の端が失われる）。
  - `ImgGrid`に`fillHeight`/`fillZoom`/**`fillAspect`(2026-08-03q)**、`ImgThumb`に`wrapStyle`（外枠に足すスタイル＝高さ固定＋`overflow:hidden`の窓）を追加。どちらも未指定なら従来の描画のまま＝他画面は不変。
- **2026-08-03m 横長スクショは「列」を検出して左端のブロックだけを出す**（`_snDetectImgColumns`/`_COL_SCAN_RATIO`/`niCrop`/`_effShape`・app-03）。ニュースサイトのPC表示を撮ると本文の横に別記事が並ぶので、そのままだと全部が小さくなる。縦にずっと伸びる「余白の帯(ガター)」を探してブロックに割り、2つ以上あれば先頭ブロックを`ImgGrid`の`fillCrop{left,width,ratio}`で切り出す。判定は背景色（ふちの最頻色＝白とは限らない）との差分で列ごとの中身の濃さを見る。canvasが汚染されて読めない場合（Storage経由のimageUrl等）は`null`＝従来どおり全体表示にフォールバック。
  - **2026-08-03n 実物で効かなかったのを修正**。①走査条件が`比1.25以上`＝実物の2段組スクショは全体がほぼ正方形(比1.07)で走査すらされていなかった→`0.85`へ。②ガター判定が「中身が全く無い列」＝実物はサイトのヘッダ・フッタ・全幅バーが必ず縦の帯を横切るので永久に見つからなかった→**本文列の濃さ(上寄り中央値)の28%を下回ればガター**という相対判定へ変更し、上端12%(ヘッダ)は最初から見ない。細い隙間はガターにせず・細い断片(縦罫線やアイコン列)はブロックにしない前処理を追加。1段組を割らない歯止めとして左ブロック幅0.18〜0.78・右ブロック幅0.12以上を要求。
  - 切り出し後が枠より縦長なら高さ揃えにも乗せる＝縦長札と同じ枠で行が揃う。それより横長なら切り出し後の比率で高さを決める（下に空きを作らない）。**2026-08-03qに枠が正方形になったので、この閾値は0.70→`_TALL_RATIO`(0.95)へ統一**（比1以下なら幅で決まる高さが枠を超える＝下に空きが出ない）。
  - **2026-08-03o さらに2点修正**（実物でまだ効かない札が残っていたため）。①**縦横比による足切り(`_COL_SCAN_RATIO`)を全廃**＝実物は2段組でも全体が縦長(比0.78)になることがあり、比で絞ると取りこぼす。1段組は受け入れ条件（左ブロック幅0.18〜0.78・右0.12以上）で弾かれるので、全画像を調べてよい（縦長1段組・細長い縦長・写真1枚・1段組の横長で誤検出なしを実測）。②**計測を`<img>`の`onLoad`からも行う**（`measureNiImg`・`ImgGrid`/`ImgThumb`に`onImgLoad`を追加）＝画像はIDB/Storageから後で差し込まれるので、描画直後の一括走査だけでは実寸を取れず、形の判定も列検出も走らないまま残る札があった。
  - **2026-08-03p CORS汚染への対処**。`imgSrc`がStorageのURL(`imageUrl`)を返す画像はcanvasが汚染されて`getImageData`が例外になる＝画素を読めない。従来はこれを握りつぶして`null`（＝列なし）にしていたため、**同じ画像でもbase64を持っている端末では効き、アップロード済みの端末では効かない**という分かりにくい挙動になっていた。`_snDetectImgColumns`は読めなかったとき`"blocked"`を返すようにし、`measureNiImg`が`crossOrigin="anonymous"`の別画像で読み直す（表示中の`<img>`には付けない＝CORSヘッダの無いサーバで画像自体が出なくなるのを避ける）。取れなければ従来どおり全体表示。
  - 切り分け用に`_snNewsColDebug()`（コンソールで実行）を追加。画面上の各札の実寸・比・src・「画素を読めない(CORS汚染)／列なし／Nブロック」を返す。
  - **2026-08-03q 判定を「列の中身の平均」から「縦に連続した空白の長さ」へ作り直した**（`_snColProfile`/`_COL_GUTTER_RUN`=0.75）。平均方式は紙面の内容量に左右されて不安定＝右カラムが下半分空白だと平均が下がって余白と誤り、左カラムが写真とグラフで埋まっていると基準が上がって本文列を余白と誤る。目で見て「太い余白の帯」と分かるのは上から下までつながっているからで、その特徴（縦の連続空白の最長割合）を直接測る。実測: 本物のガター0.99／記事が短く下が空白の列0.65。
  - **全幅の帯（サイトのヘッダ・区切り線・全幅の広告や写真＝その行の85%以上が中身）は行ごと除外**してから縦の連続を数える。「短い中断なら帯を切らない」という許容で処理しようとすると、本文の行間まで空白としてつながって本文列が丸ごと余白に見えてしまう（一度これで2ケース退行させた）。中断を許すのではなく、横に伸びるものを先に取り除くのが正しい。
  - 検証は9パターン（実物の日経記事2948x2750の再現・3段組・右パネル灰色・縦長2段組・写真+グラフ2段組／縦長1段組・細長い縦長・写真1枚・1段組の横長）で、検出と誤検出なしの両方を確認している。
  - 切り出した後は**切り出し後の形**で列数を決める（`_effShape`）＝1600x700の2段組は左ブロック780x700(比1.11)なので1列。それでも元の2.4倍に見える。
  - 実測: 2段組(1600x700)→左列 x38〜702・倍率0.189→0.452（2.4倍）／3段組(1520x660)→左列 x29〜431・0.199→0.747（3.8倍）／段組なしの写真と1段組は検出なしで従来表示（誤検出なし）。
  - **縦長は拡大しない**（2026-08-03lで入れた`_NEWS_HEAD_ZOOM`は撤回）。縦長は従来どおり高さ揃え＋全幅表示。
- **2026-08-03q 札を正方形に／操作ボタンを画像の外へ**（ユーザー指摘「縦が長い」「☰・移動・✕が画像に被る」）。
  - 縦長の枠を**固定px(PC 420 / タッチ 300)から縦横比(`_NEWS_BOX_R`=1＝正方形)へ**。`ImgGrid`に`fillAspect`（枠の幅に追従＝`aspectRatio`。`fillHeight`より優先・未指定なら従来描画のまま＝他画面は不変）を追加。PCで370x420→370x370。列数や画面幅が変わっても形は正方形のまま（固定pxだと幅だけ変わって縦長に戻る）。もっと縦を出したいときは`_NEWS_BOX_R`を下げる。
  - `maxHeight`（比率のまま出す札の高さ上限）も420→380（タッチ300→240）。
  - **2026-08-03r 余白を削って札を広げた**（ユーザー要望「もう少し大きく／欄内の余白をもっと無くす」）。ニュース欄の外枠だけ`Card`を`_objectSpread`で上書きし`padding:"16px 14px"→"12px 8px"`（他の`Card`使用箇所は不変）・グリッドの`gap:12→8`・札内テキスト欄`padding:"6px 8px"→"4px 6px"`・**幅の頭打ち`maxWidth:1140→1600`**。実測(1枚あたり)＝窓1115px:348→355／1440px:372→463／1920px:372→528＝**狭い窓では+2%しか増えず、効くのは頭打ちを外したぶん**。列数は3列のまま（2列にすれば1115pxでも537pxになるが一覧性が落ちるためユーザー判断で見送り）。
  - ☰/↪/🔗/✕ は札に`paddingTop:28`で空けた上端の帯へ収め、画像への重ねをやめた（記事の見出し＝一番読みたい所が隠れていた）。帯は札の地色なので、ボタンは半透明黒の丸→白地＋枠線の角丸に変更（☰は枠なし・✕は赤字）。絶対配置のまま`top:4→3`・☰は24→22pxで帯(28px)に収まる。
- カードの**保存シート**（`keepSheet`）でカテゴリ・サブ・銘柄（複数可）を選ぶ。**全部任意**＝何も選ばず保存すると「未分類で保存」。シート内の「＋新規」でカテゴリ／サブを追加できる（改名・削除・並べ替えは設定画面の`NewsClassSettings`）。保存＝記事に`keep:{at,cat,sub,stocks[]}`を足すだけで、**保存キー`trades[日付].newsCats[カテゴリ]`は動かさない**（画面だけ串刺し・後戻りできるように）。`saveNiKeep`/`unsaveNiKeep`はクローン(`groupId`)へも伝播。
- **廃止**: `newsCatDefaults`/`newsSubCatDefaults`（自動タグ）・`stockSubCatRefs`（銘柄←→サブの紐付け→記事の`keep.stocks`へ）・`newsMemo`/`subCatMemos`（→記事カードへ変換）。カテゴリの一覧`newsCategories`とサブ`newsSubCats`は**分類の語彙として存続**。
- **ドラッグ並べ替えは廃止**（カテゴリをまたいで1列に混ぜる以上、カテゴリ配列内の手並べ替えが画面の並びと対応できないため）。移動/複製(`moveTarget`)モーダルは従来どおり残す（日付をまたぐ移動に使う）。**→2026-08-03sに「日付移動だけ」へ縮小（下記）。**
- **2026-08-03s ↪「記事を移動/複製」を 📅「別の日に移す」へ縮小**（ユーザー指摘「移動ボタンはもう不要では？日付移動はいるか」）。フラット化の結果、3機能のうち日付移動しか効かなくなっていた。
  - **カテゴリ/サブ移動＝画面上は無変化だった**。`boardItems`は`getAllNewsCatsData`で全カテゴリを串刺しに集め`_snNiOrderCmp`(手並び順)だけで並べるので、保存キー`trades[日付].newsCats[カテゴリ]`がどこでも表示位置は変わらない。分類の正本は保存シートの`keep.cat`/`keep.sub`側。→移動先カテゴリ/サブの`select`を撤去し、`moveNewsItem`には**移動元のcat/subCatをそのまま渡す**（保存先は据え置き・日付だけ変える）。
  - **複製(クローン)＝画面上は増えなかった**。`cloneNewsItem`は同じ日の別カテゴリへコピーするが、板も`_nhvCollectFlat`も`groupId`で1枚に間引くため🔗バッジが付くだけ。→複製タブとチェックリストUIを撤去。**`cloneNewsItem`/`_findClones`/🔗バッジは残置**（過去データの表示と、戻したくなったとき用）。
  - モーダルは日付`input`＋「移動する」だけに。同じ日付ならボタン無効＋「同じ日付です」。未使用になった`moveToCat`/`moveToSubCat`/`moveMode`/`cloneTargets`のstateは削除。カード上のボタンは`↪`→`📅`(title「この記事を別の日に移す」)。
  - 実データで検証: 8/3の2件→1件・8/4へ1件移動を確認。札309x391／画像305x305（正方形）／☰・📅・✕とも画像との重なり0を実測。
- `_delThisClone`は現在タブのカテゴリだけでなく**全カテゴリを横断**して削除するよう変更。`updCatField`は「受け皿カテゴリ(`inboxCat`=先頭カテゴリ)」への追加専用。

## app-04.js (元 L17336-22687)
TradeForm, TradeItemCard, CalendarPreview, SearchView, SettingsModal（**メンテナンス欄に「🧽不要キャッシュを削除」(_snEvictExpendableCaches再利用)＋「🗂クラウド画像の整理」(_snStorageAudit/_snStorageDeleteOrphans=Firebase Storage孤児診断/削除・_runStAudit/_runStDelete・削除直前に再診断しremoteOk/caOk falseなら中止・_runStDelete(graceDays)はgrace=30の通常削除に加え「新しい孤児も含めて全部削除」(grace=0)ボタンも提供＝圧縮で置き換えた直後の旧画像を即回収。grace=0でもrefSet(ローカルdata＋remote＋CA)で参照中は保護2026-06-17)＋「📊全フォルダの内訳を測定」(_runBreakdown=_snStorageBreakdown・進捗_stBdP/結果_stBd・フォルダ別サイズ/件数+大ファイル上位を表示・CA画像も可視化)＋「📋種類別に分析」(_runCatAudit=_snStorageCategoryAudit・進捗_stCatP/結果_stCat・notebook-imagesをニュース/チャート/メモ/銘柄情報等の種類別に集計)＋「📉画像を圧縮」(_runRecompress=_snRecompressImages→saveで_snApplyImgMaps適用・進捗_stRcP/結果_stRc・全画像対象に一般化・圧縮後は孤児削除を案内)＋「🗞古いニュース画像を削除」(_runNewsPreview/_runNewsStrip=_snStripOldNewsImages・日付input _stNewsCut/結果_stNewsPrev・容量主犯のニュース画像を日付指定で整理)を追加 2026-06-17〜18**・**2026-07-08 「📊 データ・銘柄」タブに「📡 シグナル管理」セクションを新設＝既存の_EpnChipMgr＋_sig*(add/delete/reorder)＋_sigRename(=_elSignalRenameData呼び出し)をSettingsModalに移植（custom.signalTagsの追加/改名/削除/ドラッグ並び替え・改名で全記録・分析・EPナビ等へ全データ移行＝既存名へは統合・削除は候補から外すのみで記録の名前は残す）。3つ目のdataタブ兄弟ブロックとしてCA連携ブロックの直後に挿入・sw v74**）, NikkeiPriceChart, StockQuickRefTable, StockQuickRefTableWithChart, **_epnTagsOf/_epnReasonsOf/_epnPickWin/_epnCascade/_epnPut/_epnDelete/_epnRecalcBase/_epnCollectSignals/_epnBaseLevelKey/_epnSpecialRecoFrom/_epnSpecialReco/_rsnMaster/_rsnSetMaster/_rsnAddData/_rsnDeleteData/_rsnReorderData/_rsnRenameData/_EpnChipMgr/_EpnAddSection/_EpnCalcForm/EpNaviPanel（⚡EPナビ＝場中のEP計算・保存早見。取引タブのエントリー記録カードの【上】に独立表示。**2026-07-08: 計算フォームを銘柄列ごとに常設化＝_EpnCalcForm（最大3銘柄を同時に独立計算・①銘柄セレクタと＋計算ボタン廃止＝列に固定・番号は①基準分足〜④その他特徴へ繰り上げ・旧左右2カラムは縦積み1カラム化・早見上段/フォーム下段の2段グリッド整列・カードタップ編集/削除通知はregister(stock,api)のrefマップで親子連携・推奨カスケードは列ごと常時計算）**。**2026-07-08b: 早見カード刷新＝面色は分足（5分含む=緑/1分=オレンジ）・追加αありはEP/内訳の文字だけ赤＋「追」バッジ・「済」トグル新設（×の左・item.doneに保存・灰色薄く・並び=未済みEP降順→済み(済み内もEP降順)・再タップで解除・編集保存でdone維持=editDone state）**。推奨ロジックはEntryRecordForm._refSigAlphaと同一＝変更時は両方直す・保存はcharts[銘柄_日付].epNavi（item.minBars/reasons含む）・_EpnChipMgr＝チップの追加/改名/削除/ドラッグ共通部品・**2026-07-08c: 早見カードに「編集」ボタン新設（済の左＝loadForEditで全項目を計算フォームへ読込＋focus()でスクロール・カード全体タップ編集は廃止＝入力中フォームの誤上書き防止）・編集中は計算フォームを琥珀色＋見出し「✎編集：」に（_editing=!!editId）。併存ライン（シグナル/③底抜け/③起点/④その他特徴のどこで選んでも）選択で基本α欄へ1を自動入力＝EPナビ_EpnCalcForm＋記録フォームEntryRecordFormの両方に実装（_isKyozon判定＋_kyozPrevRef遷移useEffectでsetNBase/setFBaseAlpha("1")・手修正可・解除で1なら空に戻し推奨へ・二重実装＝変更時は両方直す・sw v72）**・**2026-07-08d 早見カードにインライン編集を追加（「編集」ボタン無しで直接編集・案C）＝③起点(select単一)/④その他特徴(トグルチップ)/追加α(±ステッパー)を各カードで直接編集→即_epnPut保存＋EP自動再計算。③④変更時は新helper_epnRecalcBase（_epnCascade＋autoPickと同じ段階フォールバック・併存ライン→1）で推奨基本αを再導出、追加αは基本α据え置きで直接再計算。「編集」ボタンは編集中(editId一致)だけ「💾保存」に変わりフォーム編集を上書き保存＝register APIにsave追加＋onEditingで編集idを親_editingMapへ通知。**罠**: register useEffect(deps無し=毎レンダー再実行)のcleanupでonEditing(null)を呼ぶと親再描画のたび打ち消される→通知は専用effect(deps[editId,stock])のみ・unmountクリアは別effectに分離。編集中カードはインライン編集を隠し「下の計算フォームで編集中」表示。sw v73**・2026-07-07e〜f＝詳細は変更ログ）**, DayView（末尾に「📊本日のデータ分析」=EP→OS5ホールド検証(_epHoldLadder)＋銘柄別OS1値分析(本日/今週/全期間比較・_elOsStatsV2)＋OS連鎖分析(_elOsChainSection dense=本日のOS1→OS2…帯別の次OS分布/遷移ヒートマップ/成績)。旧ホールド変動分析(_haEl)・本日のデータ(時間帯別/連勝)は2026-06-13削除。**銘柄別記録タブの活性銘柄末尾＋取引タブ本日の損益データ表(_pbMainEl)末尾に「この日 vs 全期間（銘柄別）」比較=_elDayStockBenchV2(app-06)を追加 2026-06-15**）

## app-05.js (元 L22688-27460)
Calendar, EventCategoryManagementModal, _hdRecentRecords, _hdEnteredOnly, _hdTagKey, _hdGroupByTag, _hdWinTop3, _hdLoseTop3, _hdConsecutiveLosses, SignalDashboard, CA_URL, DEFAULT_STOCK_MAP, _caGetStockInfo, _caResolveDate, _caBuildUrl, _caOpen, _caMetaCache, _caMetaInFlight, CA_META_TTL, _caFetchMeta, _caFilterByStockDate, _caDraftInFlight, _caFetchDraftData, _caExtractCode, _CA_BAR_CACHE_PREFIX, _caBarCacheGet, _caBarCacheSet, _caFindPrevDraftMeta, _businessDaysBetween, _MACRO_LOCAL_RANGES, _macroLocalFromPct, _macroLastClose, _macroLocalDetectFromCa, _simResample, _simComputeFeatures, _simCosine, _simHammingMatch, _simScoreDetail, _simScore, _simFeatureMemCache, _simBulkFeaturesLoaded, _simBulkFeaturesPromise, _simLoadFeature, _simSaveFeature, _simBulkLoadAllFeatures, _simEnsureFeature, _EVT_ROUND_GRIDS, _evtDetectIndicatorCrosses, _evtDetectRoundNumberCrosses, _evtDetectHighLowUpdates, _evtDetectVolumeSpikes, _evtDetectConsecutiveCandles, _evtDetectOpenType, _evtDetectAll, _patDetectBreakdownReject, _patDetectAll, _evtEnsureEvents, _patEnsurePatterns, PATTERN_DEFS, PatternSearchDialog, SimilarSearchDialog, CAChartSection, _elCollectAllSignals, _elIsEntered, **_elInclTotal（合計額算入フラグ: signal.includeInTotal!==false。false の記録だけ合計額/データ分析から除外・undefined/null/旧記録=算入true=後方互換・_elIsEntered(E成立)とは別概念）/_elFilterIncl（recs配列を算入対象だけに絞る）2026-06-18=除外は分析/合計の派生配列だけに適用し一覧/カレンダー/検索/明細展開の表示は全件＝チョークポイント:app-06 v2recs(1818)/_elBestAlphaBadgeV2/_elDayStockBenchV2・app-05 _hdRecentRecords/gradeByDate/_elCalcChartGrades(関数内)・app-08 _mAgg・app-04 _trEntryAgg(totRecords)/_pbMainEl(_pbAllRecsT/_skT/スカラーガード)/_wkRow/_dayRecs/_collectOS/_pnlDetailTableEl(_inclTpb)/検索日カード・app-02 EntrySignalSection(_recsForTot)/WeeklyPnlPanel(_sumRow/_detailTotRowFor)/_iaRecs/_sigStats。**不算入(includeInTotal===false)の可視化2026-06-18: _elIsExcluded/_elOutOfTotalBadge（**合計から外れた理由バッジ 2026-07-29**=スルー/不算入/データのみ(選外銘柄)を出し分け。includeInTotal未設定の選外記録が無印で消えるのを防ぐ）, _elNotInclBadge(水色「不算入」)/_elNotInclRowStyle(淡色+水色背景+左ライン)/_elExclDot(水色ドット)/_elExclCountRecs/_elExclCountSigs/_elDayStockExclCount/_elDayExclCount。各記録表の行に不算入バッジ+行スタイル(app-04 _pnlDetailTableEl/取引dataRows・app-02 EntrySignalSection/_detailRowsFor・app-06 _recTable・app-05 EntryLogCard)。銘柄タブ水色ドット(app-03 StockTabs←app-04でexclCount渡す)。カレンダー日ドット(app-05 Calendar/app-06記録帳カレンダー=filtered母数)。本日/今週損益データ表見出しに「不算入N件」(app-04)。記録帳一覧に「不算入のみN件」絞り込み(app-06 listExclOnly)。表示は全件のまま=不算入も見えて編集で再算入できる**, _compatSignal, _vcol, _elSignedVal, _epIsV2（EP起算方式判定 scheme:2/3）, _epIsV3（OS1〜5固定欄方式scheme:3判定）, _epOwnAlpha（記録固有の採用α・未設定は予想OS度α）, **_EL_RN_BANDS/_elRnAutoAt/_elRnAutoFrom/_elRnAutoOfRec（RN加算自動判定 2026-07-20c・`_elRnAdd`の直後）＝予定EPの下二桁がバンド内ならキリ番ちょうどまでの加算額を返す単一源。`_EL_RN_BANDS`=[{from:41,to:49,target:50},{from:91,to:99,target:100}]（**40・90は含めない**・後から調整できるよう定数外出し）。`_elRnAutoAt(epPre)`→**null=判定不可／0=対象外(自動×)／>0=加算額**。`_elRnAutoFrom(levelPrice, preAlpha)`。`_elRnAutoOfRec(s, alpha)`＝採用αからRN分を引き戻して判定（RN〇/×どちらの記録でも使える）。**⚠️判定は必ずRN加算“前”のEP（水準線＋基底α＋浮き足加算）に対して行う**＝予定EP＝水準線＋採用α で採用αにRNが含まれるため、RN込みEPで判定すると循環する。使用先＝記録フォーム(app-05 `fRnAuto`)・EPナビ(app-04 `nRnAuto`)・シミュの動的再判定(app-06 `_simRnAt`)・RN加算候補一覧(app-06 `_rnCandRecs`)**, **_elRnPreEpOfRec/_elRnDistAt/_elRnTierAt/_elRnAddAtT（RN加算閾値スイープの単一源 2026-07-20e・`_elRnAutoOfRec`の直後）＝「RNは何円手前から〇にすべきか」分析(app-06 `_elRnThresholdBoardV2`)の土台。`_elRnPreEpOfRec(s, alpha)`＝**RN加算“前”EP**＝水準線＋(採用α−`_elRnAdd`)・null=判定不可(水準線未入力/α不明)。**`_elRnAutoOfRec`もこれに委譲＝「RN前EP」の算出式は1箇所だけ**（2026-07-20dのリファクタ・結果は従来と同一・936通りの差分テストでpass）。`_elRnDistAt(epPre)`＝直近キリ番までの距離**1〜49／0=すでに…50・…00ちょうど／null=判定不可**。`_elRnTierAt(epPre)`＝"50"(下二桁1〜49)/"00"(51〜99)/null＝段別トグルの振り分け。`_elRnAddAtT(epPre, T)`＝閾値T(距離≤T円なら〇)での加算額・**T=9で現行バンド`_elRnAutoAt`と完全一致（1000通り検証済）・T≥10で…40/…90も含む**＝バンド定数を書き換えずに反実仮想スイープができる。⚠️運用の自動判定(`_EL_RN_BANDS`)は41-49/91-99のまま不変＝この4関数は**分析専用**で、記録フォーム/EPナビ/シミュの挙動には一切影響しない**, **_elAlphaBreakdownNode（α値欄の各数値の下に「（基本α+追加α）」の内訳を出す小ノード=baseAlphaVal/addAlphaValから算出・片方欠損は合計alphaValから逆算・基本α無の旧記録やα値シミュ中(表示α≠内訳合計)はnullで非表示。**追加α3状態(addAlphaUsed)対応 2026-06-25: ×不要(add=0)は「（基本α円）」表記(例（10円）・2026-06-25f修正＝当初は非表示にしていたが、本来は基本αを括弧で囲んで表示が正・算入/不算入とも)／未選択(addAlphaUsedがtrue/false以外=null)は「（未選択）」表記／〇要(add>0)は「（基本+追加）」。**2026-06-25b〜d修正: 当初「（不算入）」水色表記を入れたが勘違い依頼として撤回／旧記録(baseAlphaVal無し)への（未選択）付与も撤回。最終仕様(2026-06-25e)＝「（未選択）」は《新形式(baseAlphaVal有り)》で addAlphaUsedが〇/×以外なら表示＝**不算入記録でも出す**(ユーザー要望: 不算入でも追加αの〇×が未選択なら（未選択）を表記)。旧記録(baseAlphaVal無し=base==nullで早期null返し)だけは追加α3状態の概念が無いため対象外。途中で入れた「不算入は（未選択）を出さない」抑止(25d)は逆要望のため撤回。α値シミュ中は全状態でnull**。使用先=EntrySignalSection明細/WeeklyPnlPanel明細(app-02)・DayView本日/銘柄別明細(app-04)・EntryLogView一覧(app-06)のα値セル 2026-06-24）**, **_minBarBadge（各記録の分足signal.minBar(1/5)を表の時間欄の下に「1分/5分」緑バッジで表示）。新規エントリー記録フォーム(EntryRecordForm)に分足(1/5)選択欄をエントリーシグナル見出しの上にボタン式(「1」「5」を独立トグルで複数選択可・再タップで解除)で再導入(fMinBars→signal.minBar=number配列[1]/[5]/[1,5]・旧形式の単一numberも_minBarListが読む・バッジは両方選択時「1分/5分」・2026-06-18実装→α節改修で一旦廃止→2026-06-24再導入)＋水準線値欄の右に「予定EP(自動)」欄を新設(=水準線値fLevelPrice＋合計α_fAlphaの和を自動表示・非保存)。分足バッジ使用先=EntrySignalSection明細/WeeklyPnlPanel明細(app-02)・DayView本日/銘柄別明細(app-04)・EntryLogView一覧(app-06)の時間欄。バッジは緑枠の四角・中央寄せ・11pxに拡大 2026-06-24**, **_epEpDiffNode（E欄=_epECellの〇/×/未達の下に「EPとの差」を表示=OS1〜3の最高高値(_elOsMaxAll・アウトカム盲目)−α・ともに水準線比。〇/×(EP成立)は＋＝ピークがαを何円超えたか・未達は−＝あと何円でα到達か・0は±0・色↑赤/↓緑。基準はユーザー選択でOS1〜3最高に統一 2026-06-24）**, _epLegs（足配列[{h,c,exp,role}]構築=OS1〜3+H1+H2・符号規約:OS系sign"-"=↓負/hold系sign"+"=↓負・水準線比で統一。scheme:2のみ旧ミラー同期の写しレグ＝OS側と高値・確定値が完全一致するものを採用α基準のEP位置で除去（同期導入前の実データは残す）。scheme:3はhold*=常に4・5本目で除去なし）, _epIsXSkip（×宣言後の到達=judge'x'判定）, _epIsTriEntry（EP足の到達期待度△＝△の確信度でエントリー。2026-06-16: EP損益を（）内のみ算入・（）外は0＝H1/H2の△と同様。judge ok かつ ep.exp==="△"。旧記録/×見送り/未設定はfalse=○扱い）, **_elOsMaxFiltered（OS値列用の各記録OS値 2026-06-23）**＝損益サマリー表のOS値列(中央/平均)用。OS1→OS3を左から見て算入足の最高値を返す＝EP足は常に算入・それ以外は期待度×/損切り済でその足以降打ち切り(○/△/未設定は算入)・EP前=α到達期待(os1Exp/os2Exp)/EP後=H期待(holdExp/hold2Exp)・算入足無→null・旧記録(非v2)=OS1。alpha未指定は_epOwnAlpha。実関数で全11ケース機能テストpass。**適用先(損益サマリーのOS値列＝×/損切りで打ち切り維持)**=本日の損益データ(app-04 _pbRow×2/明細)・今週(app-02 WeeklyPnlPanel)・早見等(app-05 _elCalcStats.osVals/osAvg)。**_elOsMaxAll（分析用の各記録OS値 2026-06-23）**＝OS1〜3の最高値を結果に依らず全足から取る(×/損切り足も含む＝アウトカム盲目)。○△に絞らず全足を母数に入れα目安/帯別の選択バイアス・アウトカム漏れを避ける版。旧記録(非v2)=OS1(osVal)。**適用先**=記録帳の期間別/比較データ(app-06 _elPeriodStatsV2→_elDayStockBenchV2の中央/平均OS)＋OS総合分析(_elOsSectionV2の中央値/α目安/円グラフ/帯別成績_elOsBandPerfV2・分位_elOsPctlV2)。**2026-06-25: OS値の分析の数値クリックで取引展開＝ヒストの棒(_elOsHistV2にrecs/aiOf渡し時のみクリック有効・他2箇所は不変)／OS値別の成績の各行(_elOsBandPerfV2)をクリックすると、その帯の取引を「本日の損益データ」欄と同じ列構成の読み取り専用ミニ表_elOsTradeMini(新規グローバル・グローバルセルヘルパー_epOsChainCell/_epECell/_epPnlCell/_elHoldTd2/_elRPnlDispWで構築)で真下に展開。α別の総当たり_elBaseAlphaDetailV2に「平均H1損益(円/件=ΣH1損益pnl÷件数scN・_elBaseAlphaEvalのpnl/scN)」列を追加**。2026-06-23オプションCで統一＝_elOsStatsV2/_elOsPctlV2にosOf引数追加(既定OS1・OS総合は_elOsMaxAllを渡す)。**据え置き(OS1のまま=寄り付き初動)**=DayViewの銘柄別OS1値分析(app-04・寄り付き専用)・OS連鎖分析(_elOsChainSection 各足ベース)・銘柄/群別OS要約(_elOsStatsV2既定osOf)。, _epAsTraded（×宣言を無視した仮想signal=os1/os2Exp除去でjudge ok化→見送り記録の「取引していたら」参考損益を共通ヘルパーで算出）, _epResolve（EP解決:先頭3本以内で高値≥αの最初の足=EP。judge:"ok"E成立/"x"×宣言後到達(EPより手前のOSが×=xBefore。EP足=エントリー足なのでEP-OS自身×はありえず＝EP/OS1がEPは無条件算入)=参考/"miss"E未達。h1/h2=EPの次の足から配列位置で導出→αシミュ追従。旧記録はnull返し=各ヘルパーの従来ロジックへ。_elDynResult/_elDynPlanned/_elDynHold/_elDynHold2/_elPlanIsStop/_elHoldIsStop/_elHoldIsStop2/_elH2Miss/_elHoldMaxHighにv2分岐あり）, _epSignedNode（符号付き値ノード↑正/↓負）, _epOsChainCell（OS欄=OS足連鎖「8(↑3)→9(↑5)→15(↓2)」高値(確定値)・旧記録はOS1のみ。エントリー記録欄で使用）, _epECell（E欄=○E成立/×見送り/未達。旧記録は未達=_elH2Miss・他○）, _epPnlCell（EP損益欄=旧OS値・確定値・α値比・結果/EP損益を1セル統合「高値→確定値/α比/結果・損益」。pnlDisp引数で各表のランク表示を注入可・miss=「ー」のみ(Qバッジ廃止)・×は参考表示）, （_elIsValidReach＝有効な到達は2026-07-29cに廃止＝到達列は各表のE成立母数を使う）, _epReachedAt（成立判定: v2=3本以内α到達・旧=OS値≥α。成立率/α意思決定表で使用）, _elGradeBadge18（ランクバッジ18px・旧_esBadge/_trBadge/_pbBadgeを統合。各表のローカル名はエイリアス）, _elLane（固定幅レーン・旧_esLane/_trLane/_lane）, _elPnlColor, _elPnlFmt, _elRPnlDispW（ランク+金額レーン表示(valW,showZ)・旧_esRPnlDisp/_trRPnlDisp/_rPnlDisp）, _elRPnlDispABAll（合計表示・旧4コピー統合）, _elDeriveHoldProfit（損益変化○△ー×導出(hp,pp,res,fallback)・旧_dynHoldProfitES/_dynHPtr）, **時間かぶりの合計除外2026-07-07[[project_scalping_total_pnl_system #2]]: _elCollMemoCharts/_elCollMemoSet（data.charts参照のmemo）・_elCollKey(stock,date,signal)・_elCollisionExcludedSet(data)（同一日・全銘柄のv2算入記録を時刻昇順→隣接≤5分を上限2個の貪欲ペアリング→各ペアの（）外手じまい損益(_elHold2TotPartsのmain・採用α/損切り)が良い方（高い方）を除外・悪い方を残す＝{excluded,marked}。同額は後の時刻を除外・main無しペア/単独は除外なし）・_elCollExcluded(data,r)/_elCollMarked(data,r)・_elCollMarkNode(data,r)（残した側の明細行「※被り有」バッジ）。配線=表示総計のみ（_elTotAccumのget.excluded＋app-02 _sumRow/app-04 _pbRow/_wkRow/_pnlDetailTableElフッターのinline複製）・α総当たり/理想α/推奨基本αには非適用**, _elTotAccum（合計行の共通集計{real,plan,planAB,holdPlanCap,holdRaw,holdRef,holdPlanStopDiff,hold2,hold2Ref+各Cnt}。get.excluded=時間かぶり除外（表示総計だけ配線・trueは金額もCntも全スキップ）。H1=早見表と同一のキャップ後差分基準・H2=_elHold2TotParts・get={signal,alpha,cut,real?,norm?}で株数正規化注入。取引テーブルが使用・app-02/本日今週ループは移行候補）, _elPlanIsStop, _elHoldIsStop, _h2sig（H2=hold2*をhold*名にマップした仮想signal）, _epHoldLadder（EP→以降の足を順にホールドした損益ラダー OS1〜5対応・各足で手仕舞い損益と損切り発生{epIdx,items[{depth,role,leg,pnl,isStop,afterStop}],stopDepth,maxPnl,finalPnl}・本日のデータ分析で使用）, _epIncompleteMark（OS4/OS5未入力の記録の【未記録】マーク＝各表の時間欄の下に表示・v2/v3でhold/hold2欄空の時。完備/旧記録はnull）, _epHoldView（表示用仮想hold-signal: v2/v3はαで解決したH1/H2レグをhold*名に詰め替え＝H明細表示が役割に追従・旧記録はs/_h2sig）, _elDynHoldNoStop（損切りルール非適用のホールド損益＝値幅/確定値ベース。H2で想定orH1が既に損切りの時に使用）, _elDynHold2（想定orH1が既損切りならH2に損切りルールを適用せずNoStop損益を返す）, _elHoldIsStop2, _elHoldMaxHigh（OS(H0)〜H2の高値水準線比の最高値{ex:×除く,all:×含む}。OS値=常に↑正・H1/H2はsign"+"=↓負）, _elHighNode（符号付き高値の↑/↓ノード）, _elHoldMaxHighCell（日別集計「H中最高値」セル＝↑18（↑42）形式・括弧内=×含む最高値）, _elRealizedOutcome（日別集計「実現結果」判定: miss(E未達=_elH2Miss)/stop(想定orH1orH2損切り)/profit/loss/zero。利益損失は結果損益H2→無ければH1）, _elOutcomeCell（実現結果セル: E未達/損切り(緑pill)/利益(赤)/損失(緑)/±0）, _EL_IDEAL_ALPHAS（理想α候補=**0〜50円・1円刻み**。2026-06-21に5刻み0/5/10/15/20→1刻み0〜50へ変更＝理想α表/一括α理想値/記録帳の理想α分析が1円単位に精密化・上限20→50）, _elIdealAlpha（per-record理想αボタン用＝EP損益+H1が最大の単一α・候補0〜50円1刻み）, _elIdealAlphaV2（理想α完全刷新2026-06-13＝EP/H1/H2各指標について候補0〜50円1刻みを_elTotAccumで総当たりし合計最大のαを返す{ep,h1,h2}・各{maxA,maxSum,tgtA(=2500円以上の最小α),tgtSum}・合計行と同基準）, _elIdealAlphaTableV2（**2026-06-21に本日/今週/チャートの表は_elBaseAlphaTableV2(推奨基本α5〜20)へ置換＝呼び出し消滅で死蔵。_elIdealAlpha/_elIdealCut/_elIdealAlphaV2/_EL_IDEAL_ALPHAS(0〜50)は行ごとの個別α/損切りボタンで継続使用**・旧=銘柄別理想α表＝groups[{label,recs}]からEP/H1/H2列×最大α/目標α＋損益。本日/今週の損益データ表＋チャート解析(ChartSection)で使用）, _EL_IDEAL_CUTS, _elIdealCut（理想損切り値=候補10/15/20で損切り回避できる最小値・無ければ10。本日/今週「損切り値シミュ／理想損切り値」用）, _elHas2Data（v2/v3はαで解決したh2レグの有無・alpha省略時は採用α）, _elH2Miss（想定もH1もE基準未達=OS値<αかつH1高値もα未達の判定。trueでH2は非成立。表ではH2期待度「ー」＋Q ー円表示・合計除外）, _elH2ExpCounts, _elHoldGradeBadge, _elHoldStopPnlNode（EP損益が損切りの時のH損益セル「想定額（H額）」を両方ランク付きで描画。左=EP損益額そのまま・括弧内=H1/H2損益）, _elStopDoneLabel（損切り済ラベル「ー（損切り済）」。縦積み表では明細列にcolSpan中央寄せ）, _elHoldStopDetail（損切り行の明細「（高値→確定値/α値比）」を薄くインライン描画。hs=対象signal(H2は_h2sig)）, _elHoldStopAmtNode（損切り額ノード「ランク 損切額 損」。損益列。損益変化「損」(丸囲みなし)を損益額の右に配置）, _elHoldStopDoneNode（インライン損切り表示「損切 （高値→確定値/α値比） / ランク 損切額 損」。H1は想定損切り時・H2は想定orH1損切り時。縦積み表(_elHoldStackInner)の_stopRowは明細列(colSpan5)に（高値→確定値/α値比）を薄く＋損益列に「ランク額 損」で描画。明細無し時のみ「ー」）, _elHoldFlow（統合Hセル「高値→確定値/α値比値幅/損益・勝敗(○△×記号は損益額の右)」描画。isH2でhold2*使用。想定が損切りなら_elHoldStopPnlNode使用）, _elHold2Cell（H2期待度セル: ○/△=記号+統合表示・×=ー(統合)・未選択=空・H1までE基準未達=「ー」＋フロー(Q ー円)）, _elHoldBoth（旧:H1とH2を1セルに。現在は不使用＝表は2列に戻した）, _elHoldSumBoth（①H1合計｜②H2合計を1セルに。refH2/refCntで×参考合計を併記）, _elHold2RefSuffix（合計の（）参考表示：本合計（）外（=○のみ）＋△参考refの○△合計を「（Ⓐ+9900円）」で描画＝引数(mainSum,refSum,refCnt)でmainSum+refSumを表示。×は一切算入しない（[[project_scalping_total_pnl_system]]）。全合計欄で使用）, _elHold1TotParts（H1合計の1記録寄与{main,ref}。【合計損益2026-06-16改 [[project_scalping_total_pnl_system]]】（）外＝○のみ/（）内＝○△/×・未設定は一切算入しない: EP×→{null,null}・H1期待度×/未設定→想定額mainのみ(参考無)・△→想定額main＋H1結果との差ref(（）内=H1結果)・損切り済→従来どおりmain+ref・○のみ→H1結果(planCap)main（判定 holdExp!=="○"でフォールバック・未設定=×扱い 2026-06-16）。全H1合計サイトが本関数or同ロジック）, _elH1HeldBase（H2カスケード基準=「1段下(H1まで保有)で手仕舞いした損益」。H1×/損切り済→EP損益・他(○/△/未設定)→H1損益。H1=△でも実保有のH1損益を返す＝（）外H1列フォールバックとは別物。_elHold2TotPartsが使用）, _elHold2TotParts（H2合計の1記録寄与{main,ref}。【合計損益2026-06-16改 [[project_scalping_total_pnl_system]]】（）外＝○のみ/（）内＝○△/×は一切算入しない: EP×→{null,null}・H1期待度×/未設定→1段下(EP損益=_elH1HeldBase)mainのみ(参考無・H1×/未設定はH2期待度に関わらず優先)・H1損切り済→1段下main+参考(従来どおり)・H2期待度×/未設定→H1損益(1段下)mainのみ(参考無)・H2△→1段下main＋H2保有との差ref(（）内=H2損益)・H2損切り済→1段下main+参考・想定orH1損切り(H2=○)→損切り額main・H2○非損切り→_elDynHold2 main。判定 hold2Exp!=="○"でフォールバック・未設定=×扱い 2026-06-16。カスケード基準は_elH1HeldBase(H1未設定→EP損益)。planCapフッターで使用。**2026-06-17 損切り＝×統一: H1保有済(○/△)かつ(H1足損切り_elHoldIsStop or hold2Exp∈{×,損切り済,未設定})→自分の損益を出さず return _elHold1TotParts(=H1合計を丸ごと引き継ぐ)。例 H1=△・H2はH1足損切り→200(-1100)＝H1と一致。×期待度も同挙動に統一。H2=△/○は自前寄与のまま・EP×/_elH2Miss/H1撤退(4109)は不変。全表フッター自動追従・敵対的検証で退行なし** [[project_scalping_total_pnl_system]]）, _elHoldParts（H各部位[高値/値幅/α値比/損益]の描画ノードを返す＝縦積み用）, _elHoldStackInner（フロー明細のH1上/H2下を内部テーブルで縦揃え・H2行左端に期待度○/△/×。**2026-06-17 _stopRow刷新: 損切り行を「×（損）」(×=赤/（損）薄)＋明細(高値→確定値/α値比)＋損切額を _row(paren=2)へ委譲＝（）囲み+opacity0.6で薄く・H1/H2通常行と同一列で縦揃え。H1/H2両損切り行に適用。記録帳明細のみ**）, _elHoldTd2（フロー明細表用:H列を1セル(colSpan:2)に統合しH1上/H2下で縦積み＝_elHoldStackInner使用。集計/早見/損益データ欄は対象外で2列のまま）, _elHoldSumTd2（集計/早見表用:「H１合計」td＋「H２合計」tdの2セル配列＝統合しない）, _elCalcStats, _profitGradeFromPnl, _profitGradeFromPnlReal, _GRADE_STYLE, _GRADE_DESC, _GRADE_DESC_REAL（ランクQ=E基準未達のため非表示）, _qMissCell（miss時のEP損益欄「ー」のみ表示＝Qバッジ廃止2026-06-13）, _qZeroCell（集計が全記録E基準未達の時「Ⓠ ー円」セル表示）, _elAllMissRow（recs各記録を行と同じαで判定し全miss=trueをrecsベースで返す。本日/今週表で使用）, _elStatAllMiss（_elCalcStats結果がst.miss===st.total>0なら全miss。app-06集計表で使用）, _elCalcChartGrades（早見表の集計＝_elTotAccumと別関数だがルール共通で値一致。E×=planRefSum/holdRefSum/hold2RefSumに参考算入・本合計除外・allMissはx除外）, _GradeBadges3, _elPnlPeriodFilter, _elFilterPeriod, _elTagLabel, _elTagEntries, _elGetItemCandidates, _elSetItemSignalRef, _elSaveSignal, _elDeleteSignal, _toHankakuNum, _toHankakuDecimal, _lpState, _clearLpState, _stepBtn, VirtualAlphaCalc, EntryRecordForm（OS1〜5固定欄フォーム=scheme:3保存(2026-06-13)。**α値セクション 2026-06-21刷新(予想OS度欄の下)= 基本α値(fBaseAlpha/signal.baseAlphaVal)＋追加α値(fAddAlpha/signal.addAlphaVal)＝合計α値(自動計算・読取専用＝_fAlpha=_fBaseA+_fAddA・基本未入力は予想OS度の_gradeAlpha・追加未入力は0)。合計α値が従来の採用α＝signal.alphaValに保存(全計算で使用＝下流不変)。基本α/追加α/合計α/損切り値を1行に横並び＋αメモはその下(2026-06-21調整: 見出しαはtextTransform:noneでΑ大文字化を解消・合計αは薄色#F8FAFCのスマート表示・損切りを合計αの右へ移動)。見出し「α値」の右に推奨基本α(直近1週/1か月/3か月/全期間)の参考値を表示(_refBaseAlpha=useMemoで保存済み記録[この銘柄・v2・算入分]から_elBaseAlphaPick。期間はカレンダーの今週/今月でなくfDate起点の移動窓＝週初/月初の標本不足を避け「最近の傾向」を安定表示・記録の参考用)2026-06-21。予想OS度と基本αは連動しない(2026-06-21ユーザー指示で解除)。**さらに2026-06-21=予想OS度欄(A〜Eボタン)を廃止し「α到達の見立て」セクションに置換(fReachPred/signal.reachPred=◎○△×)。◎基本α不十分・追加α要/○基本αで十分/△基本αまでも微妙/×届かない＝基準線からのOSが基本αに対しどこまで伸びるかの予想。損益計算には不使用＝的中検証専用。上部にα参考(推奨基本α直近1週・理想αEP/H1/H2=_elIdealAlphaV2・追加α目安=_elBaseAlphaA(基本αに何円足すと損切り↓H1利益↑) を_refAlphaAux useMemoで全期間集計し表示のみ・到達率別α[OS分位a70/a80]は2026-06-22の再設計で撤去)。バリデーションは予想OS度→α到達予想に差し替え。difficultyフィールドは旧記録のα補完・編集保持のためデータは温存(新記録はnull)。各表の予想OS度列を「到達見立」(reachPred ◎○△×)へ置換済 2026-06-21＝共有ヘルパー`_reachPredBadge`(セル・色付きバッジ)/`_reachPredHead`(2行見出し「到達」「見立」)をapp-05に新設し、取引(app-04 _trTh/cell)・本日損益明細(app-04 _rTh)・EntrySignalSection(app-02 _esTh)・WeeklyPnlPanel明細(app-02 _rTh)・記録帳一覧(app-06 _th)の5表＋EntryLogCardチップ(app-05)＋並び替え「予想OS度順→到達見立順」(app-02 _esSortByDifficulty=reachPredでソート)に適用。旧記録(reachPred無)は「—」。**残=AB高確信集計の廃止(s.difficulty==="A"/"B"のAB専用合計・app-04/02・表示は内部のみ)。difficultyのα補完フォールバック(_gradeAlpha)は旧記録用に温存。****旧仕様: 予想OS度ボタンはfDifficultyのみ設定。新規記録は基本αに直近1週間の推奨基本α(_defBaseA=w1→無ければm1→m3→all)をuseEffectで自動入力(isEdit除外・_baTouchedRefで手動操作後は不介入・_baAutoRefで銘柄/日付変更時は追従)。_fBaseA/合計α/入力欄の未入力フォールバックも_gradeAlpha(fDifficulty)でなく_defBaseAに変更。基本α(_stepBA)/追加α(_stepAA)のステッパは関数型更新setX(prev=>)へ＝長押し中の連続増減に対応(旧=stale closureで絶対値setのため連続増減せず)。既存記録は基本α=旧alphaVal・追加α=0で初期化(合計=従来値で不変)。旧 分足(minBar/fMinBar)のフォーム欄は廃止＋**水準線(levelPrice/fLevelPrice)は2026-06-22にOS見出しの右へ再導入**(OS値=水準線比なので基準実価格を記録・signal.levelPrice・損益計算には不使用・priceInと同じ_toHankakuDecimal小数ステッパ+_stepBtn・灰色UI#F1F5F9/#CBD5E1)、分足のみ廃止のまま(保存もしない＝表の分足列は新記録で既定1)。****末尾(反省・メモの後・保存ボタン前)に「計算・データ算入」チェックボックス欄(旧「合計額算入」)=signal.includeInTotalに保存・既定チェック済(算入)・外すとその記録を合計額/データ分析から除外。状態fIncl=useState(initSig.includeInTotal!==false) 2026-06-18**。さらに各記録表(取引表app-04・損益データ表app-04・銘柄別記録app-02 _esTh/_detailTableFor・記録帳一覧app-06 _recTable full)のシグナル列の左に「分足」列（**2026-06-21に全テーブルから削除＝フォーム欄廃止で形骸化・ヘッダー/セル/合計/展開のcolSpan-1で整合**）(s.minBar===5?"5":"1"・既定1)を追加＝ヘッダー/行/totRowのcolSpan/展開行colSpanを各+1で整合****: 上段OS1〜3・下段OS4〜5(=hold*/hold2*フィールド)＋役割チップEP/H1/H2＋EPセクション(EP損益/メモ・想定値幅欄は表に出るためフォーム非表示)。H1/H2セクションは結果損益・損益変化・メモのみ＝高値/確定値は足ブロックで入力・期待度スロットは足ブロックに役割連動(OS1〜4)。ミラー同期廃止・scheme:2記録は_epLegsベースの取り込み変換(_initHold)でOS2〜OS5欄へ展開。バリデーション必須=OS1〜EP＋EPの次・その次の足(全欄未入力は確認のうえ保存可)。プレビュー損益は_fVSig(仮想signal)経由で表と同一基準）, _epOsChainCell（OS欄: 各足の数値＋下に期待度/↑EP/実E。実E=s.entryOsNoの足・EP足と被れば↑EP/実E・judge x時↑EP（×）/実E）。**2026-06-24: OS欄の最下部中央に「OS1〜3の最高値」を追加(_epOsMaxChainNode)＝通常は最高値1つ・到達期待度×/H期待度×/損切り済の足より後ろに更に高い足がある時だけ「打ち切り最高値（全体最高値）」例 12（21）。打ち切り版_elOsMaxCapped(×足自身は算入してから打ち切り・EP足は対象外・_elOsMaxFilteredは×足を除外する別物)・全体_elOsMaxAll**）の補足はFILEMAP既存項目参照, EntryLogCard, _tradeAlphaChip（取引時の採用α値チップ。実現損益の左に表示。signal.tradeAlpha）, **_epNextExpAt（次足期待度・2026-07-06統一の正本読み取り: 足x=0..3(OS1..OS4)の引けの「次の足へ継続するか」判断。読み順=新フィールドsignal.nextExp1〜4→レガシー導出(待ち足=os(x+1)Exp/EP足=holdExp/EP+1=hold2Exp/それより深い=null=×扱い)。足iの保有ガバナンス＝_epNextExpAt(s,i-1)＝_elHold1TotParts/_elHold2TotParts/_elH1HeldBase/_epIsTriEntryが使用。_epExpAtは表示専用に降格）**

**【シグナル詳細＋スルー 2026-07-06】新規=_elSigDetailOf(s,t)/_elTagDisp(s,t)（signal.sigDetail={タグ名:詳細名}の読取と表示用「タグ（詳細）」文字列・候補マスター=custom.sigDetails={タグ名:[詳細名]}・分析グループ化_elTagEntriesは素のタグ名のまま＝シグナル全体集計不変）・_elIsThru(s)（スルー判定=passThrough===true）。変更=_elInclTotal（スルーは算入チェックに関わらず常に不算入）・_elIsExcluded（不算入∪スルー）・_elNotInclBadge(extra,s)（第2引数sでスルー時は灰色「スルー」バッジ・従来水色「不算入」）・_elNotInclRowStyle（スルー=灰色行 #F5F5F4/#9CA3AF・不算入=水色のまま）・_elExclCountSigs（_elIsExcluded経由＝スルーも件数に含む）。EntryRecordForm=実エントリー欄3択化（あり/見送り/スルー・スルー=entered:false＋passThrough:true保存・選択時に不算入注意書き・実現損益欄非表示）＋シグナルチップ直下に選択タグごとの「└ タグ の詳細」行（案A階層型＝チップ1つ選択/再タップ解除・＋追加=window.prompt・「編集」=候補削除モード・save(prev=>)でcustom.sigDetails更新・未選択OK=未分類）＋推奨基本α行の下に「シグナル別参考」行（_refSigAlpha useMemo=先頭タグ/選択詳細で絞った_elBaseAlphaPick・全期間・fDate前日まで・表示のみ＝自動入力は従来どおり銘柄全体で値の統一性維持）。保存フィールド=sig.sigDetail（選択タグ分のみ・無ければnull）/sig.passThrough（true/null）。表示適用先=EntryLogCard（状態チップ「スルー」灰色・タグチップに（詳細）付与）・記録帳一覧app-06 _recTable/取引タブapp-04/銘柄別記録app-02の各明細＝シグナル列_elTagDisp・実エントリー列（スルー=灰「ス」）・時間欄バッジ_elNotInclBadge(null,s)。**追補（2026-07-06g）詳細・根拠チップの選択が効かない不具合を修正**＝ドラッグ対応で「span（ドラッグ＋setPointerCapture）＋内側button（選択onClick）」の入れ子にした際、setPointerCaptureをpointerdownで呼ぶとタップ時にclickがキャプチャ先のspanへretargetされ内側buttonのonClickが発火しない（＝選択できない）。修正＝setPointerCaptureをpointerdown→ドラッグ開始判定（移動7px超）時のpointermoveへ移動＝タップ時は非キャプチャでclickが内側buttonへ正常到達・ドラッグ時のみキャプチャ。シグナル詳細チップ・追加α根拠チップの両方。実クリック(preview_click=CDP)で選択/複数選択/解除/ドラッグ並び替えを検証。**追補（2026-07-06f）シグナル詳細を複数選択化**＝signal.sigDetail[タグ]を単一文字列→配列に変更（旧記録の文字列は_elSigDetailList(s,t)が配列へ正規化＝全読み取りはこれ経由・後方互換）。**→2026-07-07c 3セクション化＝sigDetail[タグ]={b:底抜け,k:起点,f:[その他特徴...]}（①②単一・③複数）・_EL_SIG_SECS/_elSigDetailSec(s,t)=セクション読み取りの正本（旧文字列/フラット配列は③(f)として読む）・_elSigDetailRenameSig=セクション内改名の記録追従・候補はcustom.sigDetails2[タグ]={b,k,f}（migrateDataで旧sigDetailsから複製シード・旧キーは旧端末互換で凍結）・_elTagDispは「底:X・起:Y」接頭辞表記・推奨基本αの自動入力は詳細別→シグナル別→銘柄全体の段階フォールバック（_refSigAlpha/_pickWin/_autoBaseA・★表示・推奨追加αも同カスケード_refAddAlphaSrc）。2026-07-07d 追加α根拠の詳細（_elAddReasonDetailList/signal.addReasonDetail）は**2026-07-09完全廃止**＝フォーム/ヘルパー/保存/改名追従削除＋migrateData._migDropAddReasonDetailで既存データ一掃（sw v84・下記app-05項）**。新ヘルパー_elSigDetailList・_elSigDetailOfは先頭値互換・_elTagDispは「タグ（詳細・詳細）」でjoin。フォーム=チップは複数トグル（membership add/remove・見出し「（任意・複数可）」）・保存は非空配列のみ・改名/削除/ドラッグ/_refSigAlpha（選択詳細ごとに参考α行）も配列対応。記録帳詳細サブタブ（app-06）=_detOf→_detHas(membership)/_detEmptyでフィルタしCount重複可（1記録が複数詳細バケットに算入・全体=実件数）・詳細ラベルにツールチップ注記。**追補（2026-07-06e）追加α根拠の選択肢＝ドラッグ並び替え＋prompt全廃・詳細候補に✎改名追加**＝①根拠チップ（EntryRecordForm・fAddAlphaUsed○時の「根拠」欄）をラッパーspan（data-rsn）にPointer Eventsドラッグ＝custom.addAlphaReasonsの配列順を保存（未設定時は既定_DEF_ADD_REASONSを実体化）・fRsnOrder/_rsnDragRef/_rsnMovedRef。②根拠の「＋追加」「✎改名」のwindow.promptを廃止→インライン入力行（fRsnInput={val}/{old,val}・_rsnValRef・_fiFlushAll確定・改名は既存_renameR＝過去記録一括追従のまま）。③シグナル詳細の編集モードを2タップ削除→根拠と同型の✎/×ミニボタンに統一（fDetDelPend廃止）＝✎改名はfDetAdd={tag,val,old}で入力行を改名モード化し、マスター＋過去記録のsignal.sigDetail[タグ]を一括変更（同名既存はno-op）・×は即削除。ドラッグハンドラはチップbutton→ラッパーspanへ移動。**残存window.prompt=app-03（外国市場の指標/銘柄追加・改名、銘柄タブ改名）とapp-07（銘柄情報タブ名）＝未対応（iPad standaloneでは同様に無反応のはず）**。**追補（2026-07-06d）詳細の＋追加/削除をネイティブダイアログ廃止**＝iPadホーム画面起動(standalone)でwindow.prompt/confirmが無反応（即null）＝「＋追加を押しても反応しない」の原因。＋追加→インライン入力欄（fDetAdd={tag,val}・FastInput type:text・値の正本は_detAddValRef・「追加」ボタンで_fiFlushAll→確定・開いている間は「✕閉じる」）／編集モードの削除confirm→2タップ方式（fDetDelPend・1回目=赤「もう一度タップで削除」表示・2回目で削除・編集切替/＋追加で解除）。pointercancel時の_detMovedRef戻し忘れも修正（ドラッグ中断後の初回タップが食われる穴）。**追補（2026-07-06c）スルーの根拠メモ**＝実エントリー欄でスルー選択時のみ、注意書きの直下に「スルーの根拠メモ」欄（反省・メモ欄と同システム=FastInput multiline/autoResize/rows2/minHeight56）。保存=signal.thruMemo（スルー時のみ・空はnull）。EntryLogCardに灰色左ライン付き「スルー根拠」表示（rationaleの上）・SearchView(app-04)のキーワード検索対象にもthruMemo追加。**追補（2026-07-06b）詳細チップのドラッグ並び替え**＝Pointer Events（onPointerDown/Move/Up/Cancel・touchAction:none=iPadタッチ対応・iOS13+/旧端末はタップのみに自然縮退）。移動量7px未満=タップ（選択/削除・従来どおり）/超えたらドラッグ開始＝fDetOrder={tag,list}で並びプレビュー（正本はrefの_detDragRef.list）→elementFromPoint+closest("[data-dettag]")で重なったチップ位置へsplice挿入→pointerupでcustom.sigDetails[tag]へ保存（並びはフォームと記録帳「詳細」バー両方に反映）。ドラッグ直後のclickは_detMovedRefで1回抑止・孤児チップ（マスター外の過去詳細）はドラッグ不可/ドロップ先にもならない・候補2つ以上で「チップをドラッグで並び替え」ヒント表示。

**【要審議 2026-07-14c】** 実エントリー第4状態＝_elIsReview(s)（signal.review===true）。要審議=無エントリー扱い（entered:false・realizedPnl等はentered条件でnull＝実現損益なし）＝合計損益に不算入だが、スルー（_elInclTotalで分析からも除外）と違い**_elInclTotalは通過する**ので分析母数（α推奨_elBaseAlphaEval/OS/reach/損切り率/件数/シグナル別）には算入。合計（仮想最終損益含む）からの除外は**_elTotAccum**（先頭で`if(_elIsReview(s))return`）と**_elCumPnlSectionV2**（累積・recsからreview除外）の2箇所＝見送りは仮想損益として合計に入るが要審議は入らない点が機能的差分。保存=signal.review（true/null）/signal.reviewMemo（要審議根拠メモ・スルーのthruMemoと同型・handleSaveで保存）。EntryRecordForm=実エントリー4択（あり/見送り/スルー/**要審議**）＝要審議ボタン（選択時#DB2777）/選択チップ/根拠メモ欄をピンク（#FCE7F3/#9D174D/#FBCFE8）・fReview/fReviewMemo state。表示適用先=EntryLogCard（状態チップ「要審議」ピンク・memo「審議根拠」ピンク左ライン）・コンパクト実エントリー列「審」ピンク（app-02×2/app-04/app-06のスルー「ス」の右）・SearchView(app-04)検索対象にreviewMemo追加。migrateData不要（review無記録は_elIsReview=falseで従来どおり・後方互換）。sw v147。

## app-06.js (元 L27461-31104)
_EL_OSC_BANDS（OS連鎖分析用OS帯=下落/0〜4/5〜9/10〜14/15〜19/20円〜・OS2以降の基準線割れマイナス対応）, _elOscBandIdx, _elOscHighs（signalのOS1〜5高値を_epLegsのrole→[0..4]位置にマップ・OS4=H1/OS5=H2・未入力null）, _elOscAgg（指定OS足legIdxを帯別集計＝次OS中央値(nextVals配列→_elMedian・2026-06-14b平均から変更)/遷移行列matrix/成績(E成立率/勝率/EP損益/H1/損切り率・採用α基準・E成立分のみ)・**2026-06-15 第4引数granで帯/1円刻み(_elOscBucket)両対応・rows/colsは出現バケットキー昇順**）, _elOscChip（OS帯チップ）, **_elGranToggle（帯⇄1円刻みの粒度切替トグル 2026-06-15）, _elOscBucket（OS高値→粒度別バケットキー: band=帯index/each=整数値・負値=下落含む）, _elOscBucketLabel/_elOscBucketColor/_elOscChipG（粒度別のラベル/色/チップ・each時は値が属する帯色を継承）**, _elOsChainSection（**OS連鎖分析 2026-06-14**: OS1→OS2→OS3…の数値帯ごとの次OS分布を「遷移ヒートマップ＋ドリルダウン(行orヒートマップ行タップでpath絞り込み)＋成績テーブル＋自動読み取り」で表示。OS2以降はマイナス(下落)帯あり。シグナル絞り込みセレクタ内蔵・props recs/data/aiOf?/dense?。記録帳「OS連鎖」タブとDayView本日のデータ分析で共用・state=path[バケットキー]/sigFil/**gran（帯⇄1円刻み・切替でpathリセット・ヒートマップ/成績/絞り込み/見出し/読み取り全て粒度連動）2026-06-15**。aiOf省略時_elAlphaInfo）, _elOsLegsSectionV2（**各足OS値プロファイル 2026-06-14b**=OS1〜OS5(固定足位置・OS4=H1足/OS5=H2足)各足の高値(水準線比)の中央値/平均/下落率(高値<0=基準線割れ)/6帯分布(_EL_OSC_BANDS下落帯込み)。OS1専用の_elOsSectionV2を全足に拡張＝「OS2以降を同じ中央値目線で」。_elOscHighsで足高値取得・OS連鎖タブ上部に設置。**2026-06-15 コンポーネント化(props recs・useState)＋帯⇄1円刻みトグル＝分布列を6帯バー(_bar6)/1円刻みヒストグラム(_bar1=整数値ごとの件数棒・帯色)で切替**。**前足比 伸長率列を追加 2026-06-15c**＝記録ごとに「この足の高値＞前の足の高値」だった割合(伸長率%)＋下段に上回った時/下回った時の平均差(円)。母数=両足とも高値入力ありの記録。OS1→OS2・OS2→OS3のオーバーシュート伸長のみ算出(OS4/OS5=EP後足は符号系が異なり「最適ホールド本数」が担当→「—」)。読み取りに「OS2がOS1を上回ったのは◯%」を追加＝ユーザー質問「OS2がOS1より大きい割合はどの程度か」への回答）, **【OS値1円刻み化 2026-06-25】5円帯(_EL_OS_BANDS_V2/_EL_OSC_BANDS)を廃止し全OS分析を1円刻みへ。新バケット=下落(<0・連鎖のみ)/0〜4=帯/5〜24=1円/25〜=帯。色は赤系単色グラデ(_EL_OS_GLO#F4A6A6→_EL_OS_GHI#6E1414・_elOsShade(v)・高いほど濃い／下落のみグレー)。中核ヘルパー=_EL_OS_TOP(25)/_elOsLerpHex/_elOsShade/_elOsBucketKey(v,includeNeg)/_elOsBucketOrd/_elOsBucketLabel/_elOsBucketColor/_elOsBucketExpandable/_elOsCountMap(vals→{vc:整数件数,neg,tot})/_elOsBucketCounts/_elOsBucketChip/_elOsGradLegend。分布表示は円グラフ(_elOsPieV2=死蔵)→棒ヒストグラム_elOsHistV2(props{vals,includeNeg,w,barH,**markVal**}・各棒に件数・hoverで件数+%・25〜は棒/ボタンで1円内訳に展開・useState exp。**markVal=現在の推奨基本α(円)で該当OS値帯を青字+▲+青枠 2026-06-28**)、**_elFloatReasonSectionV2(recs,aiOf,data,secH)＝底抜け前足浮き(数値根拠)の追加α分析。α値タブ_elAddAlphaSectionV2から移設しシグナル別_groupPanelで描画 2026-06-28**。_elOsStatsV2は.dist[5]廃止→.vals/.bcounts/.bmode(最頻バケット)を返す。_elOsPctlV2は.bandMode→.bucketMode。_elOsDistBarV2(vals,w,h,incNeg)=1円バケットのグラデ積み上げ。_elOsBandLegendV2=_elOsGradLegend。_elOsBandPerfV2=トグル廃止し完全1円刻み+「25円〜を展開」ボタン。OS連鎖(_elOscBucket/Label/Color/Chip/ChipG・_elOscAgg・_elOsChainSection)と各足(_elOsLegsSectionV2)と未達最高値分布(_elMissSectionV2①)も粒度トグル廃止し1円刻み固定(数値キー: 下落=-1/0〜4=0/5〜24=整数/25〜=25)。EntryLogCardのOS帯バッジ・groupPanel最頻もバケット化。app-04のDayView比較データ分布バーは_elOsStatsV2(...).vals渡しに変更。旧の_EL_OS_BANDS_V2/_elOsBandIdxV2/_EL_OSC_BANDS/_elOscBandIdx/_elGranToggle/_elOsPieV2/legsの_bar6/_bar1は死蔵(参照なし・要掃除)。[[project_scalping_analysis_design]]**, _EL_OS_BANDS_V2（旧OS値帯定義・現在は死蔵 0〜4/5〜9/10〜14/15〜19/20円〜・予想OS度A〜Eの配色）, _elOsBandIdxV2, _elOsStatsV2（OS値統計: 平均/中央値/最頻/最小最大/帯別分布dist[5]）, _elOsPctlV2（OS値分位p25/p50/p75・歪みskewRight(平均-中央値>15%)・到達率別αa50/a70/a80/a90(=(1-p)分位を整数円)・帯最頻bandMode 2026-06-14）, _elOsSectionV2（**OS総合分析 2026-06-14**=中央値主軸の統計ライン＋右偏バッジ（成立率→α分位表(α目安50/70/80/90%)は2026-06-28に新関数_elOsAlphaPctlTableV2でα値タブへ移設）＋OS値帯別成績(E成立率/勝率/EP/H1/損切り率・**帯別表は子コンポ_elOsBandPerfV2に分離＝帯⇄1円刻みトグルで行を5帯/OS1値1円刻みに切替・集計ロジックは本体のbands集計と共通ルールで値一致 2026-06-15**)。集計タブに常設・OS質問の結論「重視は平均でなく中央値=α到達確率と直結」を反映）, ****_HScrollBox（記録帳の横長テーブル用ラッパー 2026-06-26）**＝内容が枠からはみ出す時だけ「タップして初めて横スクロール」(普段は固定でページの縦スクロールを妨げない・右端フェード＋「👆タップで横スクロール →」ピルで明示・タップで枠がオレンジに光り横スクロール可・外タップで解除)。はみ出さない表は素通り(innerのscrollW−clientW>4で判定・useEffectで計測)。app-06の全テーブルラッパー(overflowX:auto)21箇所を_HScrollBox化＝_elv2Table/_recTable/_ovPnlTbl/期間タブ表/週間サマリー/各分析表(時間帯/曜日/シグナル別/ヒートマップ等)。display:flexのナビ帯(銘柄フィルタ/タブ/サブナビ3本)は横スクロール自由のまま対象外。props.styleは外枠(余白)へ。, **_elWeeklyTargetSummaryV2＋定数_EL_DAY_TARGET_YEN=2500（週間サマリー 2026-06-26）**＝記録帳・期間タブ(view==="period")のIIFE結果の前にReact.Fragmentで連結＝全銘柄合算/各銘柄タブの両方に表示。日別損益を_elTotAccum(EP=plan/H1=holdPlanCap/H2=hold2・（）外＝○のみ・採用α基準・既に100株換算)で出し週(月〜金_elBucketKey/ラベル_elBucketLabel)に集約→実現損益(100株換算=損益÷株数×100・E成立分)/EP/H1/H2の4基準で「1取引日あたり平均損益(＝Σ損益÷取引日)＋5営業日換算/週(=×5)／達成率(達成日÷取引日)＋5日換算の達成日数/週(=達成率×5)」＋週別内訳表(各週の実額)。**2026-06-26b: 当初は週合計の単純平均だったが、休場で短い週があると下振れるバイアスのため日ベース(1取引日あたり/達成率)へ変更・週イメージは×5営業日換算で目安表示**。v2recs(上の期間フィルタ・銘柄・追加αに連動)を渡す。目標2500円は既存_elIdealAlphaV2 tgtAと一致。, _elPeriodStatsV2（recs({stock,signal,date})→記録系フル指標集計{n/osMed/osMean/reach/cnt/ok/ng/planAvg/planSum/h1Avg/h1Sum/h2Avg/h2Sum/h2Cnt/realAvg/realSum/realCnt/stop}・採用α基準・E成立分のEP/H1/H2/損切り＋実現損益(realizedPnl)の合計/平均/件数・cnt=osVal入力件数 2026-06-15→21に実現損益と各合計sumを追加→2026-06-21にH2損益(h2Avg/h2Sum/h2Cnt・_elHold2TotParts.main・後方互換の追加のみ)を追加）, _elDayStockBenchV2（日別ページ「📊比較データ」=指定銘柄の本日/今週(月〜金)/今月/全期間をdata.chartsから集め指標比較。指標=件数/OS値中央/推奨基本α(_elBaseAlphaPick)/E到達率/損切り率/E後勝率/EP損益/H1損益/H2損益/実現損益(損益は平均＋計を併記する内部pnlMT)。本日列を全期間比で↑↓色付け(↑赤=良い方向/↓緑=悪い方向・推奨基本αは▲▼で高低のみ・件数は無印・EPS閾値で≈は無印)＋💡読み取り(OS/推奨基本α/損切り率)。aiOf=_elAlphaInfo・recsAll空ならnull（2026-06-21=その日に取引記録が無くても過去データがあれば表示・本日列は「—」＝recsDay空でも非null・P.dayはnull可で全P.day参照をnull安全化）。DayView銘柄別記録タブ(活性銘柄)＋取引タブ本日の損益データ表末尾(銘柄別=_pbStks)に設置 2026-06-15→2026-06-21刷新(見出し「本日vs全期間」→「比較データ」・今月列追加・推奨基本α/実現損益追加・損益は平均＋合計併記)→2026-06-21「α比較・深掘り」化＝表にH2損益行＋常時表示の深掘り部(理想αEP/H1/H2=_elIdealAlphaV2・到達率別αa70/a80=_elOsPctlV2・推奨基本αの期間推移=_elBaseAlphaTrendV2を埋込)。取引本日(app-04:5230 _benchEl)＋DayView銘柄別(app-04:3986)に設置）**, _elStopOvershootSectionV2（**損切りの上振れ・損切り値シミュ 2026-06-18**＝実現結果が損切りのv2記録について、損切りライン(α＋cutLine)から最高値(_elHoldMaxHigh.all)がさらに何円上か=超過幅を1円ブロック(0/1/2/3/4/5+)で集計し、損切り損失(_epHoldLadder(cut).finalPnl)・保有なら(_epHoldLadder(BIG).finalPnl)・ベスト手仕舞い(.maxPnl)・改善額を併記＋個別一覧(超過幅小さい順=損切り早すぎ疑い順・日付/銘柄/シグナル/α/損切値/超過/各損益)＋読み取り(超過1円以下で保有利益化した割合・全保有時の利益化率・15〜20円で回避できた件数)。深掘りタブ先頭。実app-05関数でユニットテスト15/15）, _elHoldDepthSectionV2（**最適ホールド本数 2026-06-14**=_epHoldLadderを深さ別(EP/+1本H1/+2本H2…)に転置集計し平均損益/合計/ライン接触率(累積)/EP比改善率・★平均最大本数。深掘りタブ）, _elExpCalibSectionV2（**(A)は2026-06-21にα到達予想(reachPred◎○△×)→実OS1の的中に作り替え＝実カテゴリは_rpActual(OS1,baseAlphaVal)で ◎=基本α+_EL_REACH_GAP(5円)超/○=基本α前後/△=基本α未満/×=OS無し に分類し予想と一致率・予想超え/未満。旧difficulty版は廃止。** **期待度キャリブレーション 2026-06-14**=(A)予想OS度A〜E vs 実OS1帯の的中率・予想超え/未満 (B)H期待holdExp/hold2Exp ○△× vs 実H1/H2損益(_elDynHold/_elDynHold2)の的中。予想は当たりか過信か。深掘りタブ）, _elExecGapSectionV2（**計画EP vs 実エントリー乖離 2026-06-14**=実エントリー記録の計画EP高値 vs 実Entry-OS(entryOsVal)・tradeAlpha vs alphaValの規律遵守率・実Exit-OS。死蔵フィールド活用。深掘りタブ）, _elMemoPerfSectionV2（**メモ×成績 2026-06-14**=memo有無別(rationale/reflection/holdMemo/hold2Memo)の勝率/EP/H1/損切り/実現勝率＋負け記録の頻出キーワード_EL_MEMO_KW(簡易substring)。深掘りタブ）, _elStreakDDSectionV2（**連勝連敗・最大DD 2026-06-14**=実現損益の現ストリーク/最大連勝連敗＋実現/EP損益の最大ドローダウン(_ddOf)。集計タブ）, _elDowSectionV2（**曜日別成績 2026-06-14b**=月〜金(+土日)別の件数/OS中央値/E成立率/勝率/損切り率/平均EP・H1損益＋読み取り。時間帯別(_elTimeOfDaySectionV2)の曜日版・getDay()で分類。集計タブ＋groupPanel）, **_elMissSectionV2（未達記録の分析 2026-06-22）**＝記録帳のタブは現在「📊集計/📆期間」の2つ＋**銘柄を選んだ時のみ「❌未達」サブタブ(view==='miss'・全銘柄合算では非表示・タブ配列にconcat・badge=_missCnt・全銘柄ピルonClickでviewリセット)**。αに3本以内(OS1〜OS3)で届かずエントリーできなかった記録(_epResolve(s,α).judge==="miss"・×見送り除く)を分析: ①OS1〜3最高値(_elOscHighs[0..2]の非nullmax)の帯別分布(_EL_OSC_BANDS)＋一覧(惜しい順=α不足額の小さい順) ②α不足額(α−最高値)の分布(1円ブロック) ③α下げシミュ(Δ=1〜5円下げた時に未達のうち何件がmaxHigh≥α−Δで救えたか=short≤Δの累積) ④シグナル別未達率(タグ抽出は_sigGroupsと同ロジック・色は_elStopRateCell低い順)。recs=v2銘柄スコープ(v2recs)・aiOf=_elAlphaInfo・読み取り=_elInsightBoxV2(不足1/2円以下の割合・下落=基準線割れ件数・最未達シグナル)。実関数で機能テスト(miss判定/OS最高値/不足額/帯分類)pass＋new Functionパース2回OK。[[project_scalping_analysis_design]], **_elStopTabSectionV2（損切りの分析 2026-06-22）**＝記録帳の銘柄別タブ「🛑損切り」(view==='stop'・α値タブの右・全銘柄合算では非表示)。E成立(judge"ok")記録の損切りを分析: ①サマリー(損切り回数/率/損失合計/平均・_elStopStatsV2) ②損切り値別シミュ＝損切り値[5/8/10/12/15/20]を全記録に当てた損切り回数/率・EP/H1/H2損益(_elTotAccumのcut上書き)・★=H1損益最大の損切り値=意思決定表 ③損切りの上振れ・早すぎ検証(_elStopOvershootSectionV2を深掘りタブから移設) ④シグナル別損切り率(_isStop=_elIsStopFinal＝終値撤退方式・触れて且つ終値が損・損失=_epHoldLadder.finalPnl) ⑤理想損切り値(損切りした記録ごとに損切り値を最小いくつ広げれば回避できたか=cut+1〜40スイープでmin avoid cut・広げて保有なら損益=_epHoldLadder(avoid).finalPnl・増分[+1〜2/+3〜5/+6〜10/+11〜/回避不能]バケット別の実損失/保有なら/改善 2026-06-23) ⑥損切りvs×見送り比較(_epIsXSkipの×記録を_epAsTraded→_elDynPlannedで取引化しEP>0=機会損失/EP<0=損失回避・見送り正解率＝事前×判断の精度 2026-06-23)。recs=v2銘柄スコープ・aiOf=_elAlphaInfo・data。実関数で機能テスト(cut別stop判定/ladder finalPnl/miss除外)pass＋new Functionパース2回OK。深掘りタブからは損切りの上振れを削除(損切りタブへ移設)。集計タブ既定=今月に変更(2026-06-22)。, _elMedian（数値配列の中央値）, **_elMean（数値配列の平均・小数1位）, _elOsMMCell（OS系セル=中央値(主)＋平均(副)を1セル併記）, _elPnlMMCell（損益セル=平均＋合計。**損益は平均のみ＝中央値併記は取りやめ・平均の母数はE成立(エントリーできた)分のみ 2026-06-15b**）＝平均/中央値の片方しか出していなかった全分析表を両方併記に統一 2026-06-15（**2026-06-15b: 損益(EP/H1/H2/実損益/ホールド)は平均のみへ戻しOS値のみ中央/平均併記。さらに損益平均の母数を全表E成立分に統一＝時間帯別/曜日別/シグナル成功度が未達を0円で母数に含めていたのを除外修正。乖離(建玉ズレ/Exit-OS/α遵守ズレ)はOS水準系なので平均/中央併記を維持**）。グループ表の損益アグリゲータは sum/cnt に加え値配列(planArr/h1Arr/h2Arr等)を収集し _elPnlMMCell へ。OS列は既存osv/vals配列から両方算出。対象=_elOscAgg/_elOsChainSection(次OS・EP/H1)・_elEpPosSectionV2・_elTimeOfDaySectionV2・_elSignalSuccessTableV2・_elOsBandPerfV2・_elPeriodStatsV2(osMean/planMed/h1Med追加)→_elDayStockBenchV2・_elHoldDepthSectionV2・_elExpCalibSectionV2・_elExecGapSectionV2(ズレ/Exit-OS)・_elMemoPerfSectionV2・_elDowSectionV2＋app-04(本日/今週/銘柄別OS1)＋app-02(WeeklyPnlPanel)。見出しは「平均◯◯/◯◯中央値」→素の指標名(OS値/EP損益/H1損益)に統一しセル内ラベル中央/平均/計で識別（ラベルは略さず「中央」「平均」表記・_elDayStockBenchVは「本日 vs 全期間」見出し）。読み取り(insight)文は据え置き。休眠の_entryLogViewLegacy/EntryStatsSummary/_grpTableは対象外**, _elEwinCell（**E後の勝率セル 2026-06-14b**=エントリー(E成立)後にEP損益が利益だった割合＝ok/(ok+ng)・下に母数(E成立件数)・≥50%緑/未満橙・取引0は「—」。**全成績テーブルの「勝率」列はこれ**＝時間帯別/曜日別/EP位置別/OS帯別(_elOsSectionV2)/OS連鎖(_elOscAgg)/シグナル成功度/期間別合計表+期間KPI。共有なのでDayViewのEP位置別・OS連鎖カードにも反映。**左の列は「E成立率」→「E到達率」にリネーム(KPIカード含む・休眠_grpTableは除く)**。敗率・未達率は当初WLMで併記したがユーザー指摘で撤回(未達率=E到達率の裏返し・敗率不要・勝率はE後に限定)。miss集計(各mk・_ratesOf)はコードに残るが表示未使用）， **【2026-06-14b OS中央値統一】グループ比較表のOS列を平均→中央値に変更（OS値は右偏なので典型値は中央値・α到達確率と直結）。対象=_elDowSectionV2/_elTimeOfDaySectionV2/_elEpPosSectionV2(EP足高値)/_elExpCalibSectionV2(実OS)。各mk()のosSum/osCnt→osv配列に変更し_elMedian。損益(EP/H1)は期待値なので平均のまま（合計=平均×件数）。期間/月別/週別表は元から平均+中央値併記・休眠legacyは平均のまま**, _elv2Th/_elv2Td/_elv2Table/_elv2Card/_elv2CardRow/_elv2Avg/_elv2Rate（追加分析セクション共通小物 2026-06-14）, _elStopStatsV2（損切り回数集計: plan想定/h1/h2/any/rate/miss・採用α基準）, _elStopCellV2（損切り回数セル「n回(率%)＋内訳」）, _elOsDistBarV2（帯別積み上げミニバー）, _elOsPieV2（帯別円グラフSVGドーナツ）, _elOsBandLegendV2（帯色凡例）, _elInsightBoxV2（「💡 読み取り」欄＝分析の自動解釈ボックス・items配列+opts.title/note）, _elInsightEmV2（読み取り用強調スパン）, _elBestAlphaV2（記録群のα総当たりでH1/H2損益最大のαを返す {h1:{a,sum},h2:{a,sum},n}）, _elBestAlphaBadgeV2（早見表見出しの「現時点の最良α値」バッジ・H1/H2同値なら1つ表示。**2026-06-21にユーザー要望で早見表から撤去＝現在unused/死蔵。関数定義は残置（_elBestAlphaV2も連動で未使用）**）, _elEpPosStatsV2（EP位置別集計 ep0/ep1/ep2/miss/x・件数/EP損益/H1/H2/損切り＋勝敗(ok/ng)・EP足高値(平均OS値)）, _EL_EPPOS_DEFS（EP位置の定義・配色）, _elEpPosSectionV2（EP位置の積み上げバー+傾向表(件数/平均OS値/勝率/平均EP損益/平均H1/平均H2/損切り率)+読み取り。記録帳集計タブ＋DayView本日のデータ分析で使用）, _elTimeOfDaySectionV2（時間帯別の成績＝寄り足OSが出た時刻で分類(寄り〜9:15/9:16〜9:30/9:31〜10:00/10:01〜/時刻未記録/全体)し件数/平均OS値/E成立率/勝率/損切り率/平均EP・H1損益＋積み上げバー＋読み取り(〜9:15と〜9:30累計・早い寄り足OSの損切り率/OS値比較)。記録帳の集計タブ＋_groupPanel(シグナル別/銘柄別)で使用・採用α基準・2026-06-13）, _elSignalSuccessTableV2（シグナル別成功度ランキング＝「損失なし率(損切りもEP損益マイナスも無し・未達/×見送りは取引なし=損失なし)」と勝率で並べ替え＝どのシグナルが成功しやすいか。件数/損失なし率/勝率/E成立率/損切り率/平均EP・H1損益＋読み取り(最良/最悪シグナル)。期間タブの詳細展開で使用・採用α基準）, _elXSkipSectionV2（**期待度×（×見送り）分析 2026-06-14**＝×宣言後にα到達したが取引を見送った記録を_epAsTradedで「取引していたら」のEP/H1損益化し、見送り判断の精度を集計。取引EP<0=損失回避(正解)/>0=機会損失(逃した利益)。KPI5枚(×見送り件数/取引していたらEP合計/機会損失/損失回避/見送り正解率)+シグナル別内訳表+読み取り。集計タブ・_groupPanel・期間詳細_detailOfで使用・採用α基準。合計には一切算入しない（×は（）参考からも除外 2026-06-16）。△版＝_elTriangleHoldSectionV2）, _elTriangleHoldSectionV2（**△ホールド分析 2026-06-16**＝期待度△のH1/H2ホールドを本算入(（）外算入)していたらの損益＝△足実損益−1段下(H1△→EP損益/H2△→H1損益)の（）内寄与を集計。活きた(delta>0)/裏目(delta<0)/△保有正解率・シグナル別・採用α基準・E成立分。×見送り分析の△版で同じ3箇所(集計/_groupPanel/期間詳細)に並置）, _elLineChartV2（汎用SVG折れ線チャート series=[{label,color,pts}]・xTicks/ゼロ線/凡例。**Y軸はnice-number化＝生spanから_niceStep(1/2/2.5/5×10^k)を選びyMin↓/yMax↑をstep倍数へ丸める→目盛りが0/20,000/40,000…の切りのいい値に。ドメイン/グリッド/ラベルが同step共有 2026-06-17。記録帳の全折れ線(累積損益/α感応度/期間損益推移)が本関数経由なので中央1箇所でnice化**。**2026-06-17 opts拡張: targetTicks(目盛り目標本数・既定5・大で縦軸細かく)／height／xLabels(点ごとの日付)＋hoverIdx/onHover でホバー対応＝透明overlay rectのonMouseMove/onTouchで最近傍indexをonHover(idx)通知し、crosshair+各系列マーカー+ツールチップ(日付と各系列値)を描画。状態は呼出側が保持(関数のまま・hooks無し)**）, _elCumPnlSectionV2（累積損益チャート=EP損益/H1/H2/実現の累積・合計行と同一基準。**2026-06-17 ステートフル化(props{recs,aiOf}・React.createElementで描画・呼出側2箇所更新)＝useStateでhoverIdx/startDate保持。ホバーでその日の各損益ツールチップ／起算日セレクタ(選択日以降で累積を0から再計算・「最初から」/リセット)／グラフ拡大(height300)＋縦軸細かく(targetTicks10≒1万円おき)。xLabelsに日付を渡す**）, _elAlphaCurveSectionV2（α感応度カーブ=α0〜20円の合計推移+H1/H2最大αの読み取り）, **_EL_BASE_MIN_N（推奨基本αの最低件数=3・scN基準）/_EL_BASE_W_STOP（損切り回避の重み0.7）/_EL_BASE_W_H1（H1勝率の重み0.3）/_EL_BASE_SCORE_EPS（スコア僅差0.03）/_EL_BASE_ADD_MAX（追加α探索上限30・合計≤50）【再設計2026-06-22・旧_EL_BASE_ERATE廃止】, _elBaseAlphaEval（指定αを全recに一律適用＝entered(OS2でEP到達)/eRate＋新スコア用 scN(H1判定可能件数=母数)/stopH1N(EP〜H1損切り)/h1WinN(_elDynHold>0)/stopRate/h1win/score=0.7×(1−stopRate)+0.3×h1win。参考pnl/epPnl/stopN/ewin 2026-06-22）, _elPctCell（割合0〜1→%セル・70/50で色分け）, _elBaseAlphaPick（**【5回目 2026-06-22 現行・条件再設計】**＝推奨基本α(5〜20)を選定: 件数フロア(_EL_BASE_MIN_N=H1判定可能件数scN≥3)を満たすαから合成スコア(_EL_BASE_W_STOP 0.7×(1−損切り率)＋_EL_BASE_W_H1 0.3×H1勝率)が最大→僅差(_EL_BASE_SCORE_EPS 0.03)は到達率(件数)多い方→なお同点=最大α→フロア未満は件数最大αを参考'na'・entered皆無→'none'。到達率ゲート(_EL_BASE_ERATE)廃止→件数フロアへ降格・目的=損切り回避(0.7)＋H1勝率(0.3)の合成スコア。損切り率=EP〜H1で損切り割合(_elPlanIsStop∪_elHoldIsStop・H2は含めない)・H1勝率=_elDynHold>0割合(母数はscN)。_elBaseAlphaReach(到達率alias)削除・_elBaseAlphaPick引数からminERate削除・返り値{alpha,score,stopRate,h1win,eRate,entered,scN,status,sweep,minN}。追加α=_elAddAlphaReco(recs,aiOf,baseAlpha,baseScore)＝基本α+1〜+30(合計≤50)を同スコアで実データ総当たりし有意改善(>EPS)の最良加算幅{add,total,score,improved}(無改善は+0)・一括_elBaseAlphaA={pick,add}。表示=_elBaseAlphaSummary/_elBaseAlphaTrendBody(単一線+損切り率/H1勝率/スコア)/_elBaseAlphaTableV2(単一値+追加α)/フォーム_refBaseAlpha(各窓{alpha,ok}・自動入力はok のみ・到達率別α[OS分位]は撤去・_refAlphaAuxに追加α目安)。実ブラウザ(V8)で全app new Functionパース全OK＋スタブ機能テスト6/6 pass。ユーザー方針=損切りにならない＞H1利益＞到達率。**2026-06-22b 追加**＝`_elBaseAlphaDetailV2(recs,aiOf)`＝記録帳の新「📐 α値」タブ(view==='alpha'・L2746タブ配列に追加・銘柄スコープv2recs＝推奨基本α詳細+期間推移_elBaseAlphaTrendV2+α意思決定表+α感応度カーブを集約・α意思決定表/感応度は集計sumタブから移動)＋シグナル別パネル(_groupPanel)にも「🔬推奨基本α 詳細データ」として設置＝推奨値の根拠を全開示: 結論バー(推奨α/スコア/損切り率/H1勝率/母数/到達率/追加α)＋①α別の総当たり表(5〜20・到達率/件数/損切り率/H1勝率/スコア内訳[0.7×(1−stop)+0.3×win]/スコア・★採用・フロア未満淡色)＋②採用αでの全記録の内訳テーブル(日付/銘柄/EP到達足[OS1/OS2/OS3対象外/未到達]/EP損益/H1損益/判定[損切り/H1勝ち/H1負±0/対象外])＝損切り率・H1勝率の母数を1件ずつ可視化。実ブラウザ(V8)で実ソースの_elBaseAlphaDetailV2をeval実行しok/na/空3ケース描画パス(_elPnlColor/_elPnlFmtはapp-05)。[[project_scalping_analysis_design]]** _elBaseAlphaEvalにewin(=wOk/decided)/epStopN/wOk/wNg/wDr追加・返り値からminStop削除・statusはok/na/noneのみ。表示は_elBaseAlphaSummary(E後勝率カード＋sweep表を[基本α/E到達/E後勝率/EP損切/H1損益/EP損益]6列・gate外淡色・na時「該当なし」)/_elBaseAlphaTrendBody(損切り列→E後勝率列)/_elBaseAlphaTableV2(小書き「勝Z%」)/app-02・04・06のnote文を更新。**（旧経緯）方式A・到達率ベース 2026-06-21**＝①E到達率>=閾値thr(_EL_BASE_ERATE既定0.70)を満たすαに絞り→②損切り最少→③同点なら"最大"のα(＝確実に届く土台をなるべく高く)。**利益ゲート(EP/H1>0)は撤廃**＝利益は追加α＋ホールドで取る前提(pnl/epPnlは参考表示用に残す)。閾値を満たすα無し→到達率最大=最小α(low_entry)。entered=0(どのαも未到達)→none。返り{alpha,pnl,epPnl,stopN,eRate,minStop,status('ok'/'min_stop'/'low_entry'/'none'),sweep,thr}。**旧=単一α最適化(smallest profitable)はユーザー指摘「基本α＋追加αの2段構造と相性が悪い＝単一最適αは追加αで通り越す」で廃止。`_elBaseAlphaReach(recs,aiOf,target)`=thr指定エイリアス(a70/a80用)・`_elAddAlphaReco(recs,baseAlpha)`=◎記録のOS1−基本α中央値で追加α推奨(方式Bへの橋渡し)・`_elBaseAlphaA(recs,aiOf)`={a70,a80,add}。`_elBaseAlphaTableV2`は到達70%/80%両方＋◎追加α目安を表示(本日/今週/チャート/銘柄別)。a70/a80両方表示は全面適用済(Phase2 2026-06-21)＝比較データ(_elDayStockBenchV2のbase/base80 2行)・記録帳トレンド(_elBaseAlphaTrendBody=青70/橙80の2本線＋[期間/到達70%α/到達80%α/E到達/損切り/件数]表)・期間まとめ(_elBaseAlphaSummary=到達70%/80%/◎追加αカード)・フォーム参考(_refBaseAlphaが各窓{a70,a80}・α見出しとα到達の見立てに70%/80%併記・_defBaseAは.a70で自動入力)。80%は橙#B45309で色分け(チャート凡例と一致)。多角レビュー(workflow verify-method-a)で構文/ロジック/参照整合/表示一貫を検証=機能バグ無し**）, _EL_BASE_ALPHAS（推奨基本αの探索範囲5〜20円1刻み・内部理想α_EL_IDEAL_ALPHAS=0〜50とは別）, _elBucketKey/_elBucketLabel（日付→期間バケット month=YYYY-MM/week=その週の月曜・期間タブと共通の週ロジック）, _elBaseAlphaTrendBody（推奨基本αの期間別トレンド本体 gran=month/week＝期間ごとに_elBaseAlphaPick→折れ線(推奨基本α)＋表(期間/推奨基本α/損切り/合計損益/件数)＋最初vs直近の読み取り）, _elBaseAlphaSummary（「期間まとめ」＝推奨基本α1値＋α別(0〜20)の損切り件数/合計損益の早見表(★=推奨)＋読み取り）, _elBaseAlphaTrendV2（🎯推奨基本α値＝ステートフル(useState gran)で月別/週別/期間まとめを切替＝記録帳「銘柄別」**および「シグナル別」**タブの各パネル(α感応度カーブ直下)に表示・_groupPanelのstkKeyゲートは撤廃し常時描画＝銘柄別/シグナル別の両方で「この銘柄/このシグナルでは何円」が出る 2026-06-21）, _elBaseAlphaDayBlockV2（DayView「チャート」タブの早見表直下に出す「前日までの推奨基本α」ブロック 2026-06-24＝見出し(直近1か月→データ不足なら3か月→全期間にフォールバックした推奨α＋追加α)＋期間別表_elBaseAlphaPeriodTableV2。recs=_elCollectAllSignals→stock絞り・aiOf=_elAlphaInfo・refDate=その日(r.date<refDateで前日まで・当日除外)・履歴ゼロはnull。app-04:3947の早見表直下に設置・ChartSection内の同_elBaseAlphaPeriodTableV2とは両方残す。app-06に定義）, **_elBaseAlphaSimpleBoardV2（簡略版「本日の推奨基本α値」ボード 2026-06-25＝取引タブ「📊本日の損益データ」と「📝本日の総括」の間に新設(_simpleAlphaEl・app-04:5223の返却Fragmentで_pbMainElと_soukatsuElの間に挿入・ゲートは詳細版と同じ_pbHasAlpha&&_pbStks)。各銘柄(本日エントリーした_pbStks)を銘柄ごとのコンパクトカードで表示＝1か月をメイン(大きく)・1週/3か月/全期間はサブ行(小さく)。値は推奨基本α・推奨追加αの「再推奨＋次点」のみ(損切り率/勝率/想定損益は出さないシンプル版＝詳細は同じ_elBaseAlphaPeriodBlockV2)。期間窓は_elPeriodWindows(recs,refDate,false)=前日まで(当日除外)の移動窓・As=各窓_elBaseAlphaA。mainA=As[1](1か月)。1か月にデータ無しは「直近1か月：データ不足」、全窓無しは「推奨基本αのデータがまだありません」。recs=_elCollectAllSignals→stock絞り・aiOf=_elAlphaInfo。ユーザー選択=コンパクトカード/α値だけ/サブにも次点。**本日の取引記録が無くても表示 2026-06-25b＝app-04側で_alphaBoardStks=_pbStks.length?_pbStks:_elStocksWithV2Before(...)。早期return(!_pbStks.length)にも_simpleAlphaElを含め、取引0の日は黄色注記＋前日まで記録のある銘柄で推奨を出す。ゲートは_pbHasAlphaでなく_alphaBoardStks.lengthに変更。**本日の記録との乖離 2026-06-25c=各銘柄カードに当日の記録1件ごとの『採用基本α(_baseOf)・到達最高OS(_elOsMaxAll)』と推奨基本α(メイン1か月優先→3か月→全期間→1週=refA)の差(実際−推奨・_devNode +赤=深い/−緑=浅い)の明細表(devBlock・dayRecs=当日v2記録)。取引0の日は非表示。粒度=記録ごと・配置=簡略α欄統合(ユーザー決定)**）, _elStocksWithV2Before（前日まで(refDate未満)にv2かつ算入の記録がある銘柄一覧。orderHint(主要銘柄優先=_pbStkOrder)→件数多い順→名前順。簡略版ボードの本日取引0時のフォールバック銘柄に使用 2026-06-25b）, _elAddAlphaSectionV2（📐追加α値の分析 2026-06-24＝記録帳α値タブ(view==='alpha')・母数=追加α〇明示(_elAddAlphaYes)。①概況KPI(件数/全記録比/上乗せ幅中央/〇局面のE後勝率・損切り率) ②効果検証=反実仮想(各〇記録で採用α=基本+追加のH1損益 vs 基本αだけのH1損益を同値動き・同cutで比較・未達=取引なし=0円→「足して活きた率」/平均H1改善額/内訳[活きた・同じ・裏目]/損切り回避件数) ③上乗せ幅別の効果(_elBaseAlphaAの推奨追加α＋add別の件数/活きた率/平均H1改善) ④根拠別(addAlphaReasons別の件数/上乗せ中央/E後勝率/活きた率/損切り率・活きた率の高い順)。base=baseAlphaVal??(alphaVal−addAlphaVal)・_h1At=judge ok時のみ_elDynHold他は0。α値タブの推奨基本α期間推移の下に配線(app-06:3243)・〇は少数なので全銘柄合算で見やすい）, _elBaseAlphaTableV2（推奨基本α表＝groups[{label,recs}]・cutFn(r)→各グループの推奨基本α(_elBaseAlphaPick・5〜20)を1値表示＋E到達/損切り/H1合計の小書き。本日/今週/チャートの旧_elIdealAlphaTableV2を置換 2026-06-21）。【2026-06-21 3回目】①を「理想α分析」→「推奨基本α値」に再定義＝期間ごとに「損切り0(無理なら最少)かつ合計損益>0の最小α(0〜20)」を出す方式へ刷新・②(日次の基本α)は統合廃止。名称はアプリ全体で「理想α値→推奨基本α値」「一括α理想値→一括 推奨基本α」に統一(本日/今週/チャートのEP/H1/H2表ラベル・ボタンの表示のみ・関数名_elIdealAlpha*と内部0〜50計算は不変)** 【4回目 2026-06-21】推奨基本αの選定をv2化＝損益はΣH1損益(撤退floorなしはユーザー決定)・E到達率70%(_EL_BASE_ERATE)以上を条件に追加(高αで損切り回避できても“入れない”αを除外)・α別早見表とトレンド表にE到達率列を追加・_groupPanelのstkKeyゲート撤廃で銘柄別/シグナル別の両方に表示。背景=ユーザーの撤退ルール(H1<EPで撤退)とE未達(高αで建てられない)問題への対処**, EntryLogView（**出現タブ追加2026-06-18: タブ「📡 出現」=_apCollectAll(data)を期間(_elFilterPeriod)/銘柄(stockFil)で絞り、種別(apKindFil:signal/tech)・名前(apNameFil)でさらに絞ったエクセル風一覧(年月日/銘柄/時間/種別/名前/メモ)。** **タブ式記録帳 2026-06-12**: **【2026-06-22 銘柄別タブ化】トップ階層を『銘柄タブ』(主ナビ・記録のある銘柄をマスター登録順で固定=_tickerList・タップでstockFil設定→_selStockで解決・件数バッジ_cntByStockのみ期間連動・顔ぶれは全期間allRecsから固定)に変更し、下記の分析タブはその下のサブタブ＝全項目が選択銘柄スコープ(filtered/出現appearも_selStock)。冗長な「📈銘柄別」サブタブ(view==='stock')・_stkGroups・selStk stateは削除。シグナル別は残置(選択銘柄内のシグナル別分解)。** タブ=集計(**銘柄別タブのみ今月/全期間トグル2026-06-22＝_kpiBlockOf(rs)関数化＋_sumStockContent(rs)で同一構成を記録集合rsだけ差し替えて共用。今月=当月のみ(_stockAllV2→_sumMonthRecsで月抽出・top期間ドロップダウン非依存)・「←年月→データ早見」(_sumMonthNav/_shiftSumM/state sumMode・sumYM)で月移動・既定は全期間=v2recs。全銘柄合算は従来どおりトグル無し**／KPI+OS値の分析(_elOsSectionV2)+EP位置分析+累積損益+連勝連敗・最大DD(_elStreakDDSectionV2)+α意思決定表0〜20+α感応度カーブ+時間帯別成績+曜日別成績(_elDowSectionV2))/期間(粒度=日別/週別/月別/指定期間トグル・期間別合計損益表(EP/H1/H2/実現/勝率/損切り率)+損益推移折線(実現#2E7D32/EP#0369A1/H1#DC2626/H2#D97706/H2（）#DB2777の5本＝表の各列と値一致・2026-06-17にH1/H2/H2（）追加)・行タップで期間内詳細=シグナル成功度+時間帯傾向+EP位置・gran/cFrom/cTo/perExp state)/カレンダー(月グリッド・日付タップでその日の記録一覧=_recTable day・件数/実現損益バッジ)/シグナル別(各シグナルをサブタブ化→_groupPanelでリッチ分析=ミニKPI4列+OS円グラフ/統計+EP位置+累積+α曲線+記録一覧・複数タグは各タグ算入。2026-06-22に銘柄別サブタブは廃止＝銘柄は最上位タブへ移動)/OS連鎖(各足プロファイル_elOsLegsSectionV2(OS1〜OS5の中央値/下落率/分布)＋_elOsChainSection=OS帯ごとの次OS中央値分布・遷移ヒートマップ・ドリルダウン・成績・シグナル絞り込みセレクタ内蔵)/**深掘り(**損切りの上振れ・損切り値シミュ_elStopOvershootSectionV2 2026-06-18**+最適ホールド本数_elHoldDepthSectionV2+期待度キャリブレーション_elExpCalibSectionV2+計画EP vs 実エントリー乖離_elExecGapSectionV2+メモ×成績_elMemoPerfSectionV2・2026-06-14)**/一覧(旧記録含む全件・1行=1記録テーブル+さらに表示)。グループ行・記録行タップで明細(展開はfull列テーブル/EntryLogCard)。**集計はv2記録のみ**・旧記録は一覧のみ。絞込=期間+銘柄。内部部品=_recTable(day/fullモード)/_groupPanel(グループのリッチ分析パネル)/_subTabBar(シグナル・銘柄のサブタブ)。_grpTableはサブタブ化で休眠(2026-06-13)。**損益合計表示に×込み参考値（）併記 2026-06-14**＝局所ヘルパー_yenNR(v,cnt,ref,refCnt)で集計KPI/期間KPI/期間別合計表/groupPanel KPI/α意思決定表のEP/H1/H2合計に「期待度×でも取引していたらの合計」を_elHold2RefSuffixで（Ⓐ+9,900円）併記（実現損益は対象外）。期待度×専用分析は_elXSkipSectionV2を集計タブ・groupPanel・期間詳細に配置）, **_elIsEmaRefPeriod/_EL_PSEL_Y0/_elPSelAll/_elPSelThisMonth/_elPSelSet/_elPSelToggle/_elPSelMatch/_elPSelFilter/_elPSelSig/_elPSelOpts/_elPSelSummary/_ElPeriodPicker（年月週日カスケード期間選択 2026-07-20i・`_elBucketLabel`の直後）**＝記録帳の外側バーとシミュの対象期間を統一した絶対指定の期間ピッカー。選択値`{y,m,w,d}`で各階層`null`＝「全て」・全階層nullで全期間。**上位を変えたら下位はリセット**（`_elPSelSet`）。週は`_elBucketKey`/`_elBucketLabel`を再利用＝月曜始まり「7/13〜7/17」。`_elIsEmaRefPeriod`はEntryLogViewのローカル定義からトップレベルへ移動＝**2026年1〜4月を選択肢から隠す**足切りと既存の「※参考」表示が同じ単一源。既定は外側=全期間／シミュ=今月のみ。**⚠️新規に期間で絞るときは`_elPSelFilter`を使う＝`_elFilterPeriod`(app-05:5554)はJSTで窓が1日ずれる既知バグ持ちで呼び出し元ゼロ**, **multiTrade/effRows（🧮シミュ「複数回の取引 〇×」2026-07-20g）**＝既定×＝1回だけ建てる。`effRows = multiTrade ? rows : rows.slice(0,1)`が**計算・プレビュー・株数合計・行描画の単一の入口**（`rows`直参照を残すと×なのに第2取引が効く／effRowsはrowsの先頭からの部分列なので`_upd`等のindexはそのまま）。×に戻しても`rows`は消さず表示と計算から外すだけ＝〇で復活。自動配分も連動＝×は`for(_s1=totalN;...)`の1周＝単一取引25通り／〇は2分割1,225通り（**自動は2取引まで**・3取引は約33万通りで破綻するため手動専用）。〇の探索は×の上位集合（単一取引の配分も列挙）なので★が単一なら「分割不要」と読める, **_elKabuNoBuildReason/_kbStockTable/allStockモード（🧮全銘柄一括シミュ 2026-07-20f）**＝💰損益タブの3つ目のサブタブ「🧮 シミュ」（集計/期間の右・`_tabs`の`_isAllStock`分岐）。`view==="sim" && _isAllStock`の専用ブランチで`_elKabuLadderSimV2`に`{recs:_v2recsAll, baseRecs:allRecs, scopeStock:null, allStock:true}`を渡す＝**フォークせず既存コンポーネントを拡張**（複製すると今後のシミュ仕様変更が片方だけに入る）。⚠️**推奨α系（推奨α±X・推奨基本α値）は銘柄ごとに算出**＝`recoRef.current.fnByStock`/`pickByStock`（銘柄別に`_elKabuRecoBaseFn`）＋`_recoOfRec(r)`が単一の入口。**全銘柄を1本のrecoFnに混ぜてはいけない**（αは値幅の絶対値＝株価水準の違う銘柄をまたいで平均すると無意味・銘柄別シミュとも数字が合わなくなる）。⚠️**2026-07-20h訂正: 当初ここに書いた「全銘柄シミュ＝各銘柄シミュの合算」は誤り**＝αは銘柄ごとで正しいが母数の定義が3点で違う（①時間かぶり除外のスコープ ②銘柄別シミュの母数は選択中シグナルのみ ③floatModeで浮き足〇の除外が反転）。同一シグナル1本・被り無し・浮き足無しなら結果的に一致するが一般には一致しない（当初の実測が一致したのは合成データがその条件を満たしていたため）。`_recoOfRec`の呼び出し漏れに注意＝`_manTiersOf`/`_recoAs`/`_autoTiersOfFor`に加え**`_simCutOf`・`_simCutInfo`・マスター表の`recoA`**も通す（allStockでは`recoOf`がnullなので直呼びするとクラッシュ）。`_kbStockTable(calc,nShares)`＝銘柄別内訳（銘柄/対象/建った/**建たなかった理由**/シミュΣ/従来Σ/差額・シミュΣ降順・合計行はKPI通算損益と一致）。自動配分は`autoRunSig`＋`_autoSig`で**「総当たりを計算する」ボタン式**（全銘柄のみ・入力変更で再武装／銘柄別は従来どおり即時）。**検算＝採用α±0・100株・損切りオフセット空 で差額0円＝記録どおり再現**, **_EL_RN_T_MAX/_rnTierToggle/_elRnThrPool/_elRnThresholdBoardV2（RN加算閾値スイープ「RNは何円手前から〇にすべきか」2026-07-20e・`_elRnBoardV2`の直後）**＝📡シグナル総合→🔢RN加算→**4つ目のサブタブ「閾値」(`rnSub==="thr"`)**。③RN距離別(`_elRnBoardV2`)が`_elRnAdd`で分ける**実績の内訳**（母数＝〇にした記録だけ）なのに対し、こちらは**RN×も含む全記録の反実仮想**。`_EL_RN_T_MAX`=12（現行バンド=9・10以上は…40/…90を含む＝「現行バンド外」グレー表示＝40・90を外した2026-07-20の判断をこの表で再検証できる）。`_elRnThrPool(recs, aiOf, tier)`→`{pool, noLv, onRn, offTier, rnYesN}`＝**母数の仕分けの単一源**（ボード本体とサブタブの件数バッジが共用）・母数＝水準線値入り かつ RN前EPが…50/…00ちょうどでない記録（ちょうど＝どのTでも動かないので除外・件数は注記に表示）。`_elRnThresholdBoardV2(recs, aiOf, holiSet, tier)`＝①閾値スイープ表(T=0〜12・10列 閾値T/該当/到達率/E成立/頻度/損切り率/利確率/Σ最終損益/平均/**T=0比**)＋②距離別の限界寄与表(6列 距離/件数/またぐΣ/またがないΣ/差/**累計差**・最初にマイナスへ転じた距離に「符号反転」)。**⚠️検算不変条件: ②の累計差(距離d) ≡ ①のT=0比(T=d)**（距離dの記録はT=dでちょうど〇になるため数学的に恒等・ズレたら実装バグ。実マウントで検証済）。★はE成立≥`_EL_BASE_MIN_N`かつΣ黒字で平均最終損益 最大＝**①EP位置スイープと同じ軽いゲート**（推奨基本αの4条件フルゲートは使わない＝全T行が同じ母数・ほぼ同じ到達率/頻度でゲートが効かず「条件適合無し」しか出ないため）。`_rnTierToggle(tier,onSet)`＝全体/…50の段/…00の段（state `rnTier`・作法は`_ukiScopeToggle`に合わせる）。**RNの間隔は50円＝Tを1上げても該当は全体の約2%ずつしか増えず、母数の大半が不変でΣの差が埋もれる**→「該当」「T=0比」列と②の距離別で判断する設計
- **app-06.js シグナル軸再構成 2026-07-01（EntryLogView）**: 記録帳を「銘柄 → シグナル軸 → 分析タブ」の3階層化。**🎯シグナル別タブを廃止**し、シグナルを銘柄の下の常設ピル列に昇格（`_sigAxisGroups`=`_sigGroupsAll`＝トグル非依存の固定母数・既定=件数最多・件数<`_EL_BASE_MIN_N`は「参考」薄色）。選択シグナル=`_selSigKey`/`_selSigGrp`/`_selSigRecs`（EntryLogView top-levelで算出・`_missCnt`も選択シグナル母数に）。分析タブ順=集計/α値/損切り/未達/期間/深掘り。集計=旧シグナル別`_groupPanel(_selSigRecs)`を移設・α値/損切り/未達/深掘りは`_selSigRecs`でスコープ（α意思決定表は`_alphaTable`即時実行→`_alphaTableFn(_atRecs)`関数化して`_selSigRecs`で per-signal 化）。**期間タブのみ全シグナル合算(`v2recs`)据え置き**＋注記。追加α分析トグルは期間タブ限定に変更（`_secH`絞り込みバッジも`view==="period"`限定）。`_elStopTabSectionV2(recs,aiOf,data,hideSig)`/`_elMissSectionV2(recs,aiOf,hideSig)`に末尾引数`hideSig`追加＝単一シグナルで内蔵「④シグナル別」表を抑制（非破壊・既定=表示）。全銘柄合算(💰損益)は現行維持（集計/期間のみ・シグナル軸なし）。旧`view==="signal"`分岐/`_subTabBar`/`_sigGroups`/`_sumStockContent`/`_sumModeBar`は休眠。V8構文OK＋モックで銘柄→シグナル→全タブ巡回エラー0。
- **app-06.js 底抜け前足浮きサブタブ 2026-07-02（EntryLogView）**: 選択シグナルの記録を数値根拠（底抜け前足浮き＝`data.custom.addAlphaNumericReason`・`_elHasNumReason`）で二分する「内訳」サブタブをシグナルピルの直下に常設。ピル順＝`底抜け前足浮き`（左）/`その他`（右・既定）。state=`floatSub`("float"/"other"・銘柄/シグナル切替で"other"リセット)。`_selSigFloat`/`_selSigOther`/`_selSigRecsScoped`(=floatMode?float:other)をEntryLogView top-levelで算出し、**集計(`_groupPanel`)/α値/損切り/未達/深掘りの母数を`_selSigRecs`→`_selSigRecsScoped`にスコープ**（OS値分布・KPI・損切り率・未達率など全て内訳ごとに分かれる）。**基本α/追加αの推奨カード（KPI・α値①/detail）だけはシグナル共通=`_selSigRecs`で算出**（前足浮きは追加α〇で基本α母数に入らないため。base-α(その他)==base-α(全体)で一貫）。集計は`_groupPanel(_selSigRecsScoped, null, _selSigRecs)`（recs=内訳・fixedRecs=全体）。**前足浮きタブでは追加α母数トグル(osDistFil)を無効化**＝`_addFilOf`はidentity・`_addFilBar`は固定ノート・OS分布の赤/青マークは基本α＋追加α＋損切りの〇ロジック。α値②追加αは**前足浮きタブ=`_elFloatReasonSectionV2`（前足浮き値の何%を追加αにすべきか）**／その他タブ=通常の`_elAddAlphaSectionV2`＋期間表。`_elFloatReasonSectionV2`は`_groupPanel`(集計)から撤去しα値②追加αへ移設（数値根拠は根拠別分析からは従来どおり除外）。サブタブは全シグナルで常時表示（前足浮き0件のシグナルでは前足浮きタブが空・ユーザー選択）。期間タブは全シグナル合算のまま対象外。V8で全8ファイルPARSE_OK＋空配列ガード(_elOsAlphaPctlTableV2/_elAlphaCurveSectionV2/_elAddAlphaPeriodTableV2/_elAddAlphaSectionV2/_elFloatReasonSectionV2)＋合成データで底抜け水準線(10)→前足浮き(5)/その他(5)・集計件数その他3/前足浮き5・両サブタブ×全6タブ巡回エラー0を実証。
- **app-06.js 🧮株数ラダー・シミュレーション 2026-07-03（α値タブ④）**: 「基本α〇円で〇株・×円で×株から空売りしていたら通算何円だったか」を記録ごとの内訳つきで試算（[[project_scalping_kabu_ladder_sim]]の実装）。新トップレベル4定義＝**`_elKabuRecoBaseFn(baseRecs,aiOf)`**（日付時点の推奨基本α: `_elPeriodWindows`前日まで窓・直近50件→100件→全期間のok推奨優先→無ければna参考値・日付キャッシュ付きクロージャ）／**`_elKabuTierEval(s,a,cut)`**（1段=1建値の評価: `_epResolve`でOS1〜3到達判定・judge x=×見送りは建てない・損切りは`_elPlanIsStop`∪`_elHoldIsStop`・損益は`_elDynHold`＝H1手仕舞い(推奨基本α/追加α/%シミュと同一基準)・α<0は0クランプ・cut未設定は10既定=`_elDynHold`にnull cutを渡さない防御）／**`_elKabuLadderCalc(recs,aiOf,tiersOf)`**（ラダー一括適用: tiersOf(r)→[{a,add増し株数}]・sum/builtRecN建玉あり/stopRecN/indetRecN判定不可/xRecN/noBaseRecN・pnl=pnl100×add÷100）／**`_elKabuLadderSimV2(props)`**（UI: recs=内訳スコープ(_selSigRecsScoped)・baseRecs=シグナル全体(_selSigRecs=推奨α一貫母数)・floatMode。**手動ラダー**=基準モード[絶対値0円/推奨α±X(補正input+推奨−10/−5/−3/±0チップ)]＋段行エディタ{α(or推奨オフセット),累積株数}(α昇順ソート・増し=累積差分=積み増しスケールイン)＋実効ラダープレビュー＋通算/1記録平均/建玉あり/損切りカード＋_SNCollapse記録ごと内訳。**自動配分**=合計株数input(既定500・100株刻み)→段1α0〜20円×配分100株刻みを総当たり(段1=0株はα無意味なので1行に集約・段ごと評価はrecord×αでevCacheメモ化)・段2=日付時点推奨α(不明記録は段2建てない)・★最適カード+上位10ランキング表(行タップで記録ごと内訳展開)。推奨αの重い計算はuseRef+内容署名(recoSig=日付+α+〇×+cut)でスコープ実変更時のみ再構築(EntryLogViewは毎レンダー配列再生成のため参照同一性不可)。入力は全てtype=text+inputMode=numeric+全角→半角サニタイズ(_kbSan)**。α値タブに4番目ピル「④ 株数シミュ」(#0F766E teal)追加＝`_alphaSubs`+`_alSel==="kabu"`分岐。検証=V8全9ファイルPARSE_OK＋エンジン手計算11ケース一致(+2,600通算/損切り−1,500/×見送りskip/αクランプ/null cut防御)＋実マウントで手動+2,600円・自動★=α6円×500株+4,500円(手計算一致)・内訳展開・前足浮きサブタブ空状態・コンソールエラー0。※ランキング表の行タップは_HScrollBoxの仕様で表がはみ出す時1タップ目=横スクロール有効化・2タップ目=展開（全表共通UX）。**損益整合性監査 2026-07-03b**＝アプリの損益計算は2系統: **シミュ系**（生`_elDynHold`のΣ＝`_elBaseAlphaEval`/`_elSimPnlByDay`/%シミュ/感応度・期待度無視の反実仮想）と**合計行系**（`_elTotAccum`/`_elHold1TotParts`＝損益データ表/α意思決定表/損切り値別シミュ・H1○のみ本算入・×/△/未設定はEP額フォールバック・EP足損切りはEP額キャップhCap・EP△（）外0・（）refシステム）。株数シミュは**シミュ系と数値一致を実証**（記録ごと7パターン=○/×/△/未設定/EP足損切り/×見送り/未到達・通算・株数×3スケール・2段合成すべて一致／同一5記録でシミュ系2,100 vs 合計行（）外900・差1,200=×/未設定/△のEP額フォールバック分＋（）内ref=△差分400も検算一致＝設計どおりの別基準）。UI注記に「期待度は参照せず＝（）外合計とは基準が異なる」を明記。**2026-07-06 浮き足加算・追加α上乗せ**＝シミュの入力α/探索α＝基本α部分と再定義し、実効α＝入力＋浮き足加算（常時・`_elUkiAdd`）＋追加α上乗せ（〇記録のみ・`addOn`セレクタ 実追加α(既定)/推奨追加α/なし・`_simAddOf`）。`_elKabuRecoBaseFn`に`fn.addAt(date)`（同窓の推奨追加α）追加。
- **app-06.js 推奨基本α詳細の1円表示 2026-07-02（`_elBaseAlphaDetailV2`）**: 「🔬 推奨基本α 詳細データ」のα別総当たり表を**1円から表示**（従来5〜20円）。`_EL_BASE_ALPHAS`(5〜20)と`_elBaseAlphaPick`の推奨選定ロジックは不変＝**★・推奨対象は5〜20円のまま**、α=1〜4円は参考行として表示のみ追加。実装＝関数内で`_baseRecs`(=recsの×+未選択・`_elBaseAlphaPick`内部の〇除外に一致)を先頭で確定し`_lowSweep=[1..4]`を`_elBaseAlphaEval`で算出→`pick.sweep`(5〜20)へ前置(`_dispSweep`)。件数フロア`minN`は`pick.minN`(5〜20のmaxScN基準)不変＝1〜4を混ぜてもフロアは動かさない。ラベルを「1〜20円・★＝採用[推奨対象は5〜20円]」に更新。集計タブ`_groupPanel`とα値①基本αの両方に反映（同一関数）。V8で全8ファイルPARSE_OK＋合成データで1〜4円行が先頭に出ることを実証。
**【シグナル詳細スコープ＝セクション独立プルダウン 2026-07-08e（旧・詳細サブタブ2026-07-06を置換）】EntryLogViewのdetScopes state（_uDS・既定{}＝secKey→"all"(まとめて)/"__cmp__"(詳細ごと比較)/詳細名/"__none__"(未分類)のマップ）。旧・第3の「詳細」チップバーは撤去し、各分析セクションの見出しに小型プルダウン_detCtl(secKey,base)（まとめて(n件)/📊詳細ごと比較/各詳細(n件)/未分類(n件)・候補=_detNames＝custom.sigDetails∪sigDetails2∪記録実在名・候補が無いシグナルでは非表示＝従来と同一母数）。_detBody(secKey,base,renderFn)＝選択詳細で母数を絞ってrenderFn(rs,scopeVal)・"__cmp__"は全詳細（1件以上）を琥珀枠カードで縦積み比較。_detCtlRow＝見出しの無いブロック用の右寄せコントロール行。_secHに第3引数right（見出し右端コントロール）。設置: 集計_groupPanel（第4引数useDet＝シグナル別のみtrue・詳細タグ別モードは非表示）＝gp_kpi(KPI+OS値分布＝_kpiOs(rs,baRs)関数化・詳細選択時は推奨基本α/追加αカードやOS分布▲も詳細母数)/gp_ba(基本α詳細)/gp_baAdd(追加α詳細)/詳細分析collapse内7(gp_ep/gp_cum/gp_ac/gp_tod/gp_dow/gp_x/gp_tri)/gp_recs(記録一覧)、α値タブ(al_pctl/al_baD/al_addP/al_addA/al_float/al_tbl/al_curve)、損切りst・未達ms・シミュsim・深掘りdp_*7。_selSigRecsScoped＝内訳スコープのみ（詳細の一括絞りは廃止）。銘柄タブ/損益タブ/シグナルピル切替でsetDetScopes({})リセット（候補に無い選択は_detScopeOfが自動で「まとめて」へフォールバック）。

- **app-06.js シグナル総合タブ＋浮き足加算率の最適化 2026-07-12（EntryLogView）**: 記録帳の損益ピルの右に📡シグナル総合ピル（センチネル`_SIG_TOTAL`・`_isSigTotal`・全銘柄共通の分析）を新設。サブタブ`_SIG_TABS`=[浮き足%/時間帯/曜日/RN]（state=`sigSub`）。**浮き足%**＝`_elUkiPctBoardV2(_v2recsAll,_ai)`＝全銘柄の浮き足〇・浮き値>0記録で「採用α（基本＋追加＋RN）は実績のまま、浮き足加算だけをP=0〜100%(10刻み)で振り最終損益(H2)で評価」。新設ヘルパー: `_elH2EvalByFn(recs,aiOf,alphaOf)`＝`_elAlphaEvalByFn`の最終損益版（到達/E成立decided/損切り率/利確率(最終損益>0)/想定損益Σmain/平均/スコア=0.7×(1−損切)+0.3×利確）／`_elUkiPctSweep(pool,aiOf)`＝alphaOf=（alphaVal−floor(uv/2)）+floor(uv×P/100)でP=0〜100%スイープ・best=件数(decided)≥`_EL_BASE_MIN_N`&想定損益>0でスコア最大・**P=50%が現行(半額)＝実績と一致**／`_elUkiPctSweepNode(sweep)`＝表(浮き足%/到達率/件数/損切り率/利確率/平均最終/スコア/想定損益計・★・現行50・参考)。**時間帯/曜日**＝既存`_elTimeOfDaySectionV2`/`_elDowSectionV2`を全銘柄(_v2recsAll)で描画＝期間タブ末尾(旧`_timeScope`)から**移設**（期間タブからは削除）。**RN**＝近日プレースホルダ。既存シグナル別「浮き足」分析`_elFloatReasonSectionV2`の%別シミュも同`_elUkiPctSweep`に統一（旧「基本α＋浮き足%・ΣH1最大」を廃止・追加αを捨てない・最終損益基準・★スコア最大）＝Q4。配線=`_selStock`/`_isAllStock`に`!_isSigTotal`ガード追加・`filtered`/`_collScope`は全銘柄扱い・シグナル軸/内訳/追加αバーは`!_isSigTotal`で非表示・サブタブバーは`_isSigTotal?_SIG_TABS:_tabs`。検証=V8マウント＋合成データ+スタブでスイープ11段/P=50実績一致(到達13/20)/スコア/単調減/★/描画・EntryLogView実マウントでピル→4サブタブ→空メッセージ/RN/時間帯/損益復帰・console0。sw v97。[[project_scalping_uki_alpha]] [[project_scalping_analysis_design]]

- **app-05.js EPセクション新設＋浮き足を水準線側へ（EntryRecordForm）2026-07-12**: 新規エントリー記録フォームで浮き足加算を「α値でなく水準線値に加算」する見せ方に再設計（**UI表示のみ・保存データ/EP/損益/シグナル総合分析は完全に不変**＝alphaValは基本+浮き足+追加+RN畳み込みのまま・水準線生値のまま）。α値セクションの上に**EPセクション**を新設＝水準線値(fLevelPrice入力・OS見出し右と同一state＝自動同期・placeholder"—"の入力が2つに)＋浮き足加算(〇×+前足浮き値・α値セクションから移設・ラベル「＋浮き足加算」)＋**最終水準線**(=水準線+floor(浮き値/2)・自動)。α値セクションから浮き足行を除去し、合計αバーの見出し値を`_fAlpha`→`_fBaseA+_fAddA+_fRnAdd`(浮き足除外)にして内訳から浮き足を外し「→ 予定EP」(=水準線+_fAlpha=最終水準線+合計α・不変)を併記。α説明文・OS見出し右予定EPのtooltipも更新。**EPナビ(_EpnCalcForm app-04)は未変更**(別画面・浮き足はα側のまま＝将来揃える余地)。ユーザー決定(AskUserQuestion)=UI表示だけ/計算行は「最終水準線」命名/実EPはα下・OS見出し右。検証=実マウントでEP見出し→最終水準線→α値の順・**最終水準線=98(水準90+floor(17/2))**・水準線値2欄同期・console0。sw v98。[[project_scalping_uki_alpha]]

- **浮き足加算率を推奨%に（EPナビ・記録フォーム）2026-07-12b（app-04/05/06）**: 浮き足加算の自動計算を50%固定→**推奨%**に。app-06に`_elUkiRecoPcts(data,refDate)`新設＝全銘柄の浮き足〇記録(記録日前日まで)から`_elUkiPctSweep`で推奨(best.P)＋次点(runnerUp.P)を算出（`_elUkiPctSweep`に`runnerUp`＝best以外の有資格スコア最大を追加）。フォーム(app-05 EntryRecordForm)・EPナビ(app-04 _EpnCalcForm)の浮き足加算に**加算率セレクタ**（推奨%ピル/次点%ピル/手入力%）を追加＝選んだ%で`加算=floor(浮き値×%/100)`・既定=推奨(無ければ50%)。**データ**＝各記録に`signal.ukiPct`(使った%)を保存し`_elUkiAdd=floor(ukiVal×(ukiPct??50)/100)`（app-05・旧記録はukiPct無し→50%で従来と一致＝migration不要）。EPナビはitem.ukiPct保存(item.uki=加算額はそのまま＝`_epnRecalcBase`無改修)。分析`_elUkiPctSweepNode`の「現行50%」マーカーは★推奨＋次点表示に一本化（50%が普遍でなくなるため）。フォームは""=自動(推奨)・編集時はukiPct/旧uki〇は"50"で採用α保持。検証=V8マウントで推奨/次点top2・フォーム加算率セレクタ(推奨50%フォールバック→最終水準線108/手入力25%→104/推奨タップ復帰108)・EPナビ描画・sweepNode/board描画・console0。sw v99。[[project_scalping_uki_alpha]]

- **記録表・EPナビのα値欄から浮き足加算を除外 2026-07-12c（app-02/04/05/06）**: 浮き足を最終水準線側に移した再設計に合わせ、α値の表示から浮き足加算を除外（ユーザー指摘「α値欄にまだ浮き足がある」）。app-05に`_elAlphaShown(s,a)=a−_elUkiAdd(s)`新設＝表示用α（基本+追加+RN・浮き足抜き・浮なし記録はukiAdd=0で不変）。`_elAlphaBreakdownNode`の内訳から「＋浮uki」を除去（「（5＋浮14）」→「（5）」）。全記録表のα値欄**8箇所**（app-02:4684/5008・app-04:5103/5446・app-06:2619/4067/4528・app-05:8431 EntryLogカード）の数字を`_elAlphaShown(s, α)`に置換（採用α19→表示5）。EPナビ＝計算フォームの実効α（`effA−ukiAddV`・内訳から浮除去＋「最終水準線＝水準線＋浮」併記 app-04:3913）・早見カード（`bk`から浮除去＋起点に「＋浮N」併記・α表示`alphaSum−uki` app-04:4059/4120）。**保存alphaVal・EP・損益・分析は完全に不変＝表示のみ**（EP列`_epECell`は従来どおり採用α全体で計算＝正）。RN記録は内訳null（従来通り）＋数字はα−浮で整合。※浮き足専用分析(`_elFloatReasonSectionV2`の現実/推奨表)は浮きを見る目的なので対象外。検証=V8で`_elAlphaShown`(19→5/RN22→8/浮なし不変)・内訳「（5）」浮なし・EPナビ早見カード「起点1234.5＋浮14＋α5（基5）」・console0。sw v100。[[project_scalping_uki_alpha]]

- **app-06.js 🗓 期間指定（カレンダー）2026-07-12（EntryLogView）**: 上部の期間セレクトに新オプション `range`（🗓 期間指定）を追加。新state `rngFrom`/`rngTo`（"YYYY-MM-DD"・空=最古から/今日まで）。`_periodRecs` を `period==="range"` のとき日付範囲でフィルタ（それ以外は従来通り `_elFilterPeriod`）＝ v2recs→シグナル軸→全分析タブ（集計/📊OS値の分析/α値/損切り/未達/期間/深掘り/シミュ）の母数がその期間に追随。UIは `_rngBar`（月ピッカー`input[type=month]`＝選ぶと1日〜末日／開始日・終了日`input[type=date]`／プリセット 今月・先月・今日まで・クリア／右端に「開始〜終了・N件」）。※全銘柄合算の集計タブ「〇年〇月データ早見」は従来通り期間セレクト非依存（_stockAllV2ベース）。

## app-07.js (元 L31105-36524)
useModalBack, **_snNiCatHit（shvExtraCatsの突合。keep.cat/keep.subを先に見て旧cat/ni.subCatも併用 2026-08-03e）**, _shvIsStockNewsTag, _csCollectNewsForStock, _ntExtractStockFromTag, _aggregateBarsToDaily, ChartSectionDailyCandle, _parseDailyCsv, _bizDaysBetween, _dcVerifiedStocks, _dcSaveCsvToFb, _dcLoadCsvUploadedAt, _dcLoadCsvFromFb, _dcCacheLoad, _dcCacheSave, _calcEMA, _pickPriceStep, DailyCandleChart, _nhvUpdateNi, _nhvDeleteNi, _nhvCollectFlat, NewsHistoryView, _shvCollectFlat, _shvCollectSoukatsu, _shvTogPin, _shvAppendToToday, _SummaryCard, SummaryHistoryView, SI_DEFAULT_TAB_NAMES, SI_TEMPLATE_TAB_NAMES, _siFormatRelTime, _siGetTabs, _siSetTabs, _siGetTabContent, _siUpdateTabContent, _siAddTab, _siDelTab, _siRenameTab, _siReorderTab, _siCopyFromOtherStock, StockInfoTabsManagementModal, _renderUnsavedDialog, MemoEditableField, StockInfoSection, StockHistoryView
**2026-08-03e NewsHistoryView を保存済みライブラリに**
- `_nhvCollectFlat(trades)`＝カテゴリ引数を廃止し**全カテゴリを串刺し**で収集（クローンは同日1枚）。`NewsHistoryView`は`keep`のある記事だけを並べ、**カテゴリ（未分類を含む）／サブ／銘柄（銘柄なしを含む）**で絞り込む。分類は`keep.cat`/`keep.sub`/`keep.stocks`が正本。
- 記事★(`starred`)トグルと「★星のみ」絞込、サブの`<select>`、`_nhvAddNiToDate`＋「+今日に追加」は撤去（保存前の札は一覧に出ないのでボタンが成立しないため）。カードには保存分類バッジと「解除」（keepごと外す＝記事はその日のボードに残る）を追加。ピン留め(`ni.pinned`)は従来どおり。
- `_csCollectNewsForStock`（銘柄の関連ニュース）は`custom.stockSubCatRefs`の代わりに**記事の`keep.stocks`**で突合。StockInfoSection側の集計も同様。


## app-08.js (元 L36525-37896)
HomeEventFormModal, App

## app-09.js（新規 2026-08-05・分割前コードには対応部分なし）
`_dtsYmToIdx` / `_dtsIdxToYm` / `_dtsYmLbl` / `_dtsMonthCount` / `_dtsPickByYm` / `_dtsNumOrNull` / **`_dtsSimulate`（計算コア）** / `_DTS_SENS` / `_dtsSensitivity` / `_dtsFmtYen` / `_dtsFmtMan` / `_dtsFmtPct` / `_dtsUseTone` / `_dtsInitCfg` / `DtsNum` / `DtsYm` / `_dtsSec` / `_dtsRow` / `_dtsLbl` / `_dtsStepBaseNote` / **`DaytradeProjection`（UI本体）** / `_dtsHeader` / `_dtsSummaryCards` / `_dtsNiceMax` / `_dtsXLbl` / `_dtsSvgText` / **`_dtsChartAssets` / `_dtsChartPower`（自前SVGグラフ）** / `_dtsLegend` / `_dtsCharts` / `_dtsDelta` / **`_DTS_UP`/`_DTS_DOWN`/`_DTS_ZERO`（表の配色）/ `_dtsAlign` / `_dtsOut` / `_dtsRest` / `_DTS_W_DELTA`/`_DTS_W_TONE`** / `_dtsTable` / **`_dtsWarnBox`（効いていない前提の警告欄）** / `_dtsMarks` / `_dtsReachTarget` / `_dtsSensTable`（`_dtsSimulate` の `summary.stepOrigin` ＝段の実効起点も参照）

**📈 損益推移シミュレーター 2026-08-05** — 記録帳の💰損益タブ（全銘柄合算）、🧮シミュの右のタブ。前提を入れて資金・株数・生活口座・総資産の月次推移を出す。**実績の集計ではなく将来の見通し**。

- **タブ名と見出し 2026-08-05u**（ユーザー要望）: タブのラベルを「📈 損益推移」→**「📈 損益推移シミュレーター」**（`_tabs` の `_isAllStock` 分岐・app-06.js:5992）。合わせて本体の上にあった**青い説明ヘッダー（「損益推移シミュレーター ― 資金・株数・生活費の月次推移」＋前提の説明文＋オレンジの注記）を撤去**（app-06.js:7811付近）＝タブ名で分かるので見出しが二重だった。タブのピル列はラベルの絵文字を落として表示する（`kv[1].slice(indexOf(" ")+1)`）ので画面上は「損益推移シミュレーター」。ピル列は `overflowX:auto`＋`flexShrink:0` なので長いラベルでも折れずに横スクロールになる。

- **⚠️読み込み順**: `index.html` 末尾で **app-06.js の直後・app-07.js の前**に読む。番号順（app-08の後）に置くと、app-08.js の `render()` が即実行されるためタブを開いた瞬間に `DaytradeProjection` が未定義になる。グレード判定に app-05.js の `_profitGradeFromPnl` / `_elHoldGradeBadge` を使うので app-05.js より後であることも必要。`sw.js` の `APP_SHELL` にも要追加。
- **口座モデル＝A**（ユーザー決定）: 利益(税引後) → 生活費を先に引く → 余剰を生活口座へ積立（目標残高まで） → あふれた分が取引資金。モデルB（生活口座から生活費を出す）とは**総資産の推移が完全に一致**する（差は残高の内訳と「生活口座が枯れるのが見えるか」だけ）ので、足すなら月次ループ⑤⑥の2行の差し替えで済む。
- **収益の単位＝1日あたり円/100株換算**。アプリ既存のグレード（`_profitGradeFromPnl` S=2501〜/A=2001〜/B=1001〜/C=1〜）と同じ土俵なので、記録帳の実績をそのまま既定値にできる。月次は `1日額 × 営業日数` で内部換算する（月額で持つと実績と単位が食い違って自動入力できない）。オートフィル値は app-06.js の描画分岐（`view === "proj"`）で `_elHoldFinalParts(...).main` の合計 ÷ 記録のあった日数として作り `actual` propで渡す。**最終損益は値幅×100で既に100株換算なので株数で割り直さない**。実現損益ではなく最終損益を使うのは実現損益がほぼ未記録で母数にならないため。
- **月次ループ①〜⑥は順序を変えると結果が変わる**（仕様として固定）。①外部資金投入 ②株数決定 ③損益 ④支出 ⑤積立 ⑥確定。
- **株数ラダー**: `floor(max(0, 前月末capital − base) / stepAmount)` の**累積判定なので端数は自動で次段へ繰り越す**（別処理は不要）。判定は**前月末の資金**（当月の利益は含めない）。資金が減っても下げない（ラチェット）。**外部資金の投入月に `base`/`baseShares` を張り替える**＝これが無いと投入額を利益と誤認して株数が跳ねる。
  - **🔴⑧で株数を置き直したら段の基準株数もそこへ乗せ替える 2026-08-05C（挙動変更・ユーザー報告「上がるべきところで上がらない」）**。2026-08-05A で⑥の起点を生かした結果、ラダーが**②の基礎取引株数から数え直す**ようになり、⑧で増やした株数に計算が追いつくまで段が全部潰れていた。実測（起点210万・刻み20万・②600株・⑧で1,000株）＝**資金が218万→310万と4.5段ぶん増えても8ヶ月間1,000株のまま**。
    - 修正＝`baseShares = sa − floor(max(0, 投入直後の資金 − base)/刻み) × 刻み株数`。**base（⑥に入れた額）は動かさない**ので注記に出す起点はユーザーが打った数字のまま。
    - ⚠️**引き算が要**＝投入直後にすでに乗っている段を引かないと同じ段を二重に数えて跳ねる。実測: 起点210万・資金491万で1,800株の状態から2,500株を指定すると、引かなければ14段(+1,400株)が上乗せされる。引いた結果 12月＝2,900株＝資金+80万(4段)ぶんだけ増える正しい値。
    - 修正後: 11月1,000株 → **12月1,100株**（前月末235万＝1段越え）→ 以降きちんと段が進む。起点空欄の経路は不変（期末577.66万・株数推移とも一致）。
  - **🔴⑥の起点を明示している時は⑧が起点を上書きしない 2026-08-05A（挙動変更）**（ユーザー指摘）。2026-08-05z では「⑧が起点を張り替えるのは正しい挙動・注記が不足しているだけ」と判断したが、**それだと⑥に何を入れても結果が1ビットも変わらない**（実測: 起点210万／335万／500万／999万で株数推移が完全に同一）。ラベルが「月末の資金口座が◯万円になった次の月から」と言っている以上、入れた額が使われないのは仕様として通らないので**計算を変えた**。
    - `stepBase` が**明示されている → ⑧は `base`/`baseShares` を一切触らない**。起点は「口座がこの額になったら」という**絶対の水準**で、投入で勝手にズレてはいけない。
    - `stepBase` が**空欄 → 従来どおり⑧が両方張り替える**。空欄＝「開始時の資金が暗黙の起点」なので、張り替えないと投入額を利益と誤認して段が跳ねる（レビューの設定なら+70万＝3.5段＝+350株の幽霊）。**この経路の結果は変更前と完全一致**（回帰なし）。
    - ⚠️**⑧の「投入直後の株数」を `baseShares` に焼いてはいけない**＝すでに稼いだ段と**二重計上**になる（起点210万・資金300万で1,000株の時に投入して1,200株指定すると、8段ぶんが上乗せされて2,000株に飛ぶ）。代わりに②のラチェット `shares = max(want, prevShares)` が**下限**として受け止める。`sum.injFloor = {ym, shares}` に記録して注記で「◯株を超えるまではその値のまま」と説明する。
    - ⚠️**保存済みの `dtsCfg` で `stepBase` に値が入っている人は結果が変わる**（今まで無視されていた値が効き始めるため）。空欄の人は不変。
    - 検証: 起点210万で 2027/08 に1,100株へ進む＝**外部レビュー §4 が「注記どおりに読めばこうなるはず」と書いた予測と一致**。起点335万・500万では期間中1,000株のまま＝「その額に届くまで増やさない」がそのまま出る。
  - **⑧の起点リセットを注記に明示＋実効起点を表示 2026-08-05z**（外部レビュー `simulator-step-amount-review.md` §6・§9-A の指摘）。**⑧で外部資金を投入すると `base`/`baseShares` が投入直後の資金・株数へ張り替わり、⑥に入れた起点は捨てられる**。投入が期間の前半にあると⑥の起点が結果に一切効かなくなる（**実測: 起点を210万→1,000万にしても出力が1ビットも変わらない**＝実質デッドインプット）。挙動自体は正しい（張り替えないと投入額を利益と誤認して段が跳ねる。レビューの設定では+70万＝3.5段＝+350株の幽霊が乗る）ので**注記が実装に追いついていないだけ**と判断し、計算は一切変えずに表示だけ直した。
    - `_dtsSimulate` が `sum.stepOrigin = {ym, capital, shares, fromInjection}` を返す。⚠️**張り替えた本人（①の投入分岐）が記録する**＝投入直後の資金はその月までシミュを回さないと出ないので、入力欄側で再計算すると本体とズレる。`_dtsStepBaseNote(cfg, res)` はこれを読むだけで**絶対に自前計算しない**。
    - ⚠️**⑧由来の起点は円単位で出す**（`_dtsFmtYen`）。投入直後の資金は計算結果なので `2,182,488円` のような端数になり、「218.2万」と丸めると次の閾値を手で足せない（レビュー §6-3-2 の指摘そのもの）。⑥に手入力した起点は丸い数字なので万表記のまま。
    - 投入直後がすでに上限株数なら「次に段が上がるのは…」ではなく「すでに上限株数なのでこれ以上は増えません」。
    - 検証: レビューの16ヶ月設定で `stepOrigin.capital === 2182488`／1段目の閾値 `2382488` がレビューの手検算と一致。期末577.66万・株数推移とも**変更前と完全一致**（回帰なし）。
  - ⚠️**2段飛びは「余剰が刻みの2倍あるから」ではない**（レビュー §7-1 の原因説明は誤り）。段は `floor((前月末資金 − 起点) / 刻み)` の**累積**なので、**端数の繰り越しが境界を2本またぐと1段ぶんの増加でも2段上がる**。実測: 2027/03末→04月は生の段数 3.853→5.175（資金増加は26.44万＝**1.32段ぶん**）で 1,300→1,500株。刻み額をどう変えても防げず、止めたいなら「月あたり段数の上限」を別に持つしかない。
  - **⑥のラベルを「月末の資金口座が◯万円になった次の月から◯万円増えるごとに」へ 2026-08-05y**（ユーザー要望）。⚠️**表記だけの変更で計算は1行も変えていない**＝元から「前月末の資金で判定→当月に反映」なのでユーザーの言う規則と同一。実測で確認済み（起点210万・刻み25万・基礎600株で 8月末210万→9月末236万→**10月に700株**）。旧ラベル「資金口座◯万円から」はいつの資金で判定していつ反映されるかが読めなかった、というだけ。セクション副題からも「判定は前月末の取引資金」を落とした（ラベルが言っているので二重）。
  - **起点を明示できる `cfg.stepBase` 2026-08-05v**（ユーザー要望）＝⑥の先頭「資金口座◯万円から」。`base = (stepBase != null ? stepBase : capital)`＝**未入力(null)なら従来どおり開始時の取引資金**なので保存済み `dtsCfg` は挙動不変。単位は②取引資金に合わせて**万円**（`unit:"man"`）。**⑧の投入月の張り替えは stepBase より優先**（投入は利益ではないので基準点ごと更新する）。
  - ⚠️**stepBase は「その資金だった時が②の基礎取引株数」という後付けの基準点**＝今の資金が起点より多いと**初月からいきなり段が乗る**（130万・600株・刻み25万で起点100万なら初月700株）。驚かせないよう `_dtsStepBaseNote(cfg)` が⑥の下に初月株数を先出しする。**この注記は本体②と同じ式（floor→上限クランプ→ラチェット）で計算する**＝別式で書くと表と食い違う。開始月に⑧の投入があるとその月は②を通らない（①で指定株数へジャンプ）ので警告は出さない。
  - ⑥の**万円欄の▲▼刻みは 1万円**（`step:1`・2026-08-05v にユーザー要望で 5→1）。株数欄は 100 のまま。
- **社会保険料は生活費の期間別の行に足す**規約（別枠を持たない）。これで依頼メモ§8の期待値を完全再現できることを確認済み。
- **余力使用率で行を色分け**（〜70%緑／〜85%無地／〜95%黄「警戒」／95%超 赤「危険」／バッファ<0 は「保証金不足」で最優先の赤）。株価6,500円・保証金率30%だと100株あたり必要保証金195,000円なので、刻み250,000円では**1段ごとにバッファが55,000円しか増えず使用率が90%台で高止まりする**（検算で判明）。入力欄⑦にこの差額を出している。
- **入力欄の▲▼ 2026-08-05**（ユーザー要望）: `DtsNum` が既存の共通部品 **`_stepBtn`（app-05.js:6232）** を使う＝押しっぱなしで**350ms後に80ms間隔**の連続増減・`pointerup`/`pointercancel` で停止。app-05.js は app-09.js より先に読まれるのでそのまま呼べる。
  - **⚠️長押しリピートは pointerdown 時点のクロージャを `setInterval` で呼び続ける**ので、`props.value` を直接読むと初回の値のままになり**1段しか進まない**。`DtsNum` は `useRef` で最新値を保持して読む（app-04.js:4041 の推奨基本α欄と同じ理由・同じ対処）。`onChange` 側は `setCfg` の関数アップデータなので古いクロージャでも安全。実測: 8.1秒の長押しで +97（理論値98）。
  - **`props.step` は表示単位で渡す**（`unit:"man"` なら `step:1` が1万円）。内部で `×10000` して円に直す。**`step` 省略＝ボタンを出さない**＝「いじらない欄」の意思表示。現在ボタンが無いのは**税率**と**委託保証金率**の2つだけ。
  - 刻み（ユーザー指定）: ①月間営業日 `1` ／ ②取引資金・生活口座 `1`(万円)・基礎取引株数 `100` ／ ③1日あたり `100`(円) ／ ④生活費 `1`(万円) ／ ⑤積立 `1`・目標残高 `5`(万円) ／ ⑥刻み額 `5`(万円)・株数と上限 `100` ／ ⑦メイン株価 `100`(円) ／ ⑧投入額 `5`(万円)・直後の株数 `100`。
  - `min` 既定0（`min:1` は月間営業日だけ）。空欄（無制限）から▲を押すと0起点で1段ぶん入る。
- **🎯グレード感度表（`_dtsSensTable`）**: 1日あたり成績だけを `_DTS_SENS`（C=500 / B=1,500 / A=2,250 / S=3,000）に差し替えて同じ前提を回し直し、期末を比較する。
  - **「◯◯株に届く月」に到達月の差 2026-08-05b**（ユーザー要望③）＝実績の本(`self`)を基準に「**5ヶ月早い**（緑）／◯ヶ月遅い（赤）／同じ（灰）」を（）で併記。**実績自体が期間内に届かない場合は差を出せない**ので「（実績では届かない）」と添えて月だけ出す。
  - 目標株数は **`_dtsReachTarget(cfg)`**＝既定1,000株。ただし**基礎取引株数がすでに1,000株以上だと全部の本が初月到達になって列が死ぬ**ので、その時は次の500株刻みへ繰り上げる。見出しにも実際の目標株数を出す。
- **月次テーブル（`_dtsTable`）の配色と縦ぞろえ 2026-08-05w**（ユーザー要望）:
  - **配色は株式の慣習＝増える赤／減る・出ていく緑**（`_DTS_UP` #B91C1C・`_DTS_DOWN` #047857・`_DTS_ZERO` #9CA3AF）。⚠️**`_dtsDelta` は元が緑↑・赤↓で逆だったので反転した**。記録帳の比較データ（`_elDayStockBenchV2`）が「↑赤=良い方向」なので、この表だけ逆だと同じアプリで符号の読み方が食い違う。適用先＝株数（増えた月だけ赤・**旧「↑」記号は廃止**）／生活費・積立（`_dtsOut`＝「−◯万」緑・0はグレーの素の0）／残金（`_dtsRest`）／月末取引資金の（↑◯万）。
  - **「残金」列を生活口座の右に新設**＝`r.toCapital`（＝手取り−生活費−積立＝その月に取引資金へ残った額）。値は既に `_dtsSimulate` が持っていた（2026-08-05 に列として出したが caf8857 で（↑◯万）へ一本化した経緯あり）。合計行用に `sum.toCapital` を追加。⚠️**投入月だけ `capitalDelta` と食い違う**＝差が投入額そのもの（残金は自力で積んだ分だけ）。
  - **数値の縦ぞろえは `_dtsAlign(main, extra, exW)`**＝セルを［数値］［**固定幅の添え物枠**］に割る。**バッジの無い行にも同じ幅の空枠を置く**のが要で、これが無いと「（↑9.1万）」「警戒」の文字数ぶん数字の右端がずれる（td を `textAlign:right` にするだけでは揃わない）。`exW` は列内で最長の添え物に合わせる＝`_DTS_W_DELTA`(62)は合計行の「（↑396.7万）」、`_DTS_W_TONE`(56)は「保証金不足」。**開始時行・合計行にも同じ空枠を入れる**（入れ忘れるとその2行だけずれる）。列追加に伴い table の `minWidth` は 780→880。
  - 検証＝実マウントでRange計測。全10列×14行（開始時＋12ヶ月＋合計）で数値の右端が**列ごとに単一値**（例: 月末取引資金 803px×14・余力使用率 959px×14）。
- **全面バグ点検 2026-08-05B**（ユーザー依頼「不具合・バグ・不整合がないか徹底的に」）。app-09.js 全962行を読み、疑わしい箇所を**推測でなく実際に `_dtsSimulate` を叩いて**確認した結果の修正。
  - **⑧の「投入直後の株数」が今より少ないと株数が1ヶ月だけ落ちて翌月跳ね上がっていた**（実測 1,500→**700**→1,700）。⑥の「資金が減っても下げない」に反する。`shares = sa` の素の代入にラチェットを掛け、下回って無効化された時は `warnings` で知らせる。
  - **⑧の投入年月が期間外だと完全に黙殺されていた**（500万・3,000株を入れても影響ゼロ・警告なし）。`warnings` に出す。
  - **⑤「目標まで全額」＋目標残高が空欄＝取引資金が永久に増えない**（実測: 資金300万のまま6ヶ月・生活口座だけ55万→228万）。placeholder が「無制限」なので害が無さそうに見える罠。`warnings` に出す。
  - **`_dtsOut` が負の生活費を「0万」と表示していた**＝生活費に −10万 と打つと**表は0万なのに計算は−10万で回る**（残金が10万増える）＝表が計算に嘘をつく。負は「＋◯万」赤で出す。
  - **`_dtsFmtMan` の負値だけ半角ハイフン**で、`_dtsOut`/`_dtsRest` の全角「−」と同じ表の中で混在していた。全角に統一。
  - **注記の「次に段が上がるのは起点+1刻み」は1段でも進むと嘘**（段は起点からの累積なので2段目は+2刻み）。「最初に段が上がるのは…（2段目は◯円）」へ。
  - **グレード感度表の1行目が「実績 ◯円/日」**と出していたが、これは③に入っている**前提値**で実績ではない（保守的に実績より低く置くのが普通なので事実と違う数字を実績として提示していた）。「今の前提」へ。ツールチップと「実績では届かない」も同様。
  - **感度表の「今との差」の色が月次表と逆**（＋が緑）。2026-08-05w で決めた 増える赤／減る緑 に統一。
  - **`_dtsWarnBox`（新規）**＝「⚠️ 効いていない前提」欄。`sum.warnings` を出す。⚠️**節目(marks)とは分ける**＝marksは時系列の出来事、warningsは時点を持たない設定の不備。警告ゼロなら何も描かない。
  - 直さず許容と判断: 保証金不足の節目が月ごとに10行出る（うるさいが情報は正しい）／上限株数が現在株数より小さいとラチェットが勝って無視される（売却を強制しないので妥当）／税率に上限クランプ無し（200%でも落ちずに手取りマイナスになるだけ）／資金がマイナスの月は棒グラフが高さ0で消える（行は赤「保証金不足」で出るので気づける）。
- **前提の保存**は `data.custom.dtsCfg`（💾ボタン）。既存の `stSave`→`fbPut` 経路にそのまま乗るので専用の同期実装は無い。
- **回帰テスト**: 依頼メモ§8の12ヶ月・年間集計・境界ケース（2027-06 の 4.906→4段→1,400株）で **54 pass / 0 fail**。JScript（`cscript`）で `_dtsSimulate` を直接叩く方式。
- **グラフは自前SVG 2026-08-05**（ユーザー要望「視覚的にわかりやすいものもほしい」）。Chart.js/recharts は**使わない**＝ビルド工程なし・`file://` 運用・SWプリキャッシュのためCDNを増やせない。ダークモードは `html.sn-dark` のCSSフィルタ（invert+hue-rotate）が全体に掛かるので **light 前提の色で描く**（個別対応は不要）。
  - `_dtsChartAssets`（W720×**H300**）… 総資産の積み上げ棒（下=取引資金 `#93C5FD` / 上=生活口座 `#FCD34D`・左軸 万円）＋ **株数の階段線**（右軸・`#B45309`）。「資金が増える→株数が上がる」の連動を1枚で見せる。
    - **区切り線は100万円ごとに実線・50万円ごとに点線**（ユーザー指定 2026-08-05b）。旧 `_dtsNiceMax` の4分割はやめた。期間が長く総資産が伸びると本数が増えすぎるので `MAJ=[100万,200万,500万,…]` から**実線が12本以下**になる最小の刻みを選ぶ（点線は常に実線の半分）。`yTop` は刻みの倍数へ切り上げ＝上端の無駄な余白が出ない。
    - 右軸（株数）は左の区切り線とは**独立に4分割**。位置を揃えようとすると刻みが汚くなるので揃えない。
  - `_dtsChartPower`（W720×**H230**）… 余力使用率の折れ線。**70/85/95%の帯と破線**を敷き、点は `_dtsUseTone` と同じ色。目盛りは**25%ごと（左）**、警戒ラインのラベルは**右側**に置く＝左の目盛りと衝突しない。上端は25%刻みで切り上げ。
  - **⚠️軸タイトルは目盛りラベルと同じ x に置かない**（旧実装で「総資産」と「1,000万」、「株数」と「2,000」が重なっていた）。上端の余白 `y=13` に、左タイトルは `x=4` 左寄せ・右タイトルは `x=W-4` 右寄せで逃がす＝目盛りとは列がずれるので絶対に重ならない。検証は `getBBox()` の総当たり交差判定（現在0件）。
  - X軸ラベルは `Math.ceil(n/12)` 件おきに間引く（最長120ヶ月でも潰れない）。`viewBox` + `width:100%` + 親を `overflow-x:auto` ＝横スクロールで縮まない。
- **月次テーブルの列 2026-08-05**（ユーザー指示で3回に分けて調整）。現在の**10列**＝`年月 / 株数 / 税引前 / 手取り / 生活費 / 積立 / 生活口座 / 月末取引資金 / 余力使用率 / 損益分岐`。各列の見出しに `title`（ホバー説明）付き。
  - **出さない列**: 「税」（税額合計はサマリーカード「手取り合計」の下に残存）／「総資産」（＝月末取引資金＋生活口座で両方すでに列にある＝冗長。**サマリーカードとグラフ①には残す**）／「バッファ」（余力使用率と同じことを額で言っているだけ。**保証金不足は節目の警告として残る**）。
  - **「月末取引資金」に先月比** = `capitalDelta` = `capital − 先月末capital`（`_dtsDelta` で「（↑〇万）」）。
  - **旧「残額」列（`toCapital`）は廃止＝先月比に一本化した**（2026-08-05b ユーザー決定）。`toCapital` = `手取り − 生活費 − 積立` は**投入月以外は `capitalDelta` と必ず同値**（差は投入額そのもの）で、2列並べる意味が投入月にしか無かったため。投入は節目に「外部資金◯万を投入」と出るので情報は失われない。**`toCapital` は行データには残してある**ので、復活させたければ列を足すだけ。
  - **合計行（`<tfoot>`）2026-08-05b**（ユーザー要望④）。他の集計表には合計行があるのにここだけ無かった。⚠️**フロー（期間の足し算に意味がある＝税引前/手取り/生活費/積立）とストック（残高＝生活口座/月末取引資金）が混在する**ので、ストック側は合計ではなく**期末の残高**を「期末 ◯◯万」と明記して出す（合算すると無意味な数字になる）。検算: `手取り − 生活費 − 積立 = 取引資金の増加` と `開始生活口座 + 積立合計 = 期末生活口座` の2本が実描画で一致することを確認済み。
  - **表の先頭に「開始時」行**（薄いグレー）＝ `initialShares / initialLiving / initialCapital`。これが無いと1行目の「（↑〇万）」の起点が画面に無い。
  - `_dtsDelta(v, {lowerIsBetter, fmt})` … （）内の差分バッジ。**「増える方が良い」欄にだけ既定で使う**。余力使用率のように下がる方が良い欄は `lowerIsBetter:true` で色が反転する。
  - **サマリーカードは全カードに開始時からの増減を出す**（旧: 期末取引資金だけ「＋◯◯万」で不揃いだった）。カード上に「2026年8月 〜 2027年7月末（12ヶ月）の見通し」の1行。

## 変更ログ

### 2026-08-05y 実現損益の下段を「100株換算の1日平均＋グレードバッジ」に（sw v362→v363）
- ユーザー要望「実現損益は、損益額の下を1日平均額（100株換算）にしてグレードバッジを付けて」＋確認3点（上段は据置で**バッジ2つ並べる**／株数未入力は**共通部を触らず**警告のみ／🏷銘柄別の実現損益列は**実額のまま**）。
- **`totOf`/`totExOf` を `real:` → `realPair:` 経路へ**。`_elTotAccum`（app-05 L4550）が **`t.real`＝100株換算合計／`t.realRaw`＝実額合計／`t.realHasShares`** を同時に埋める。
  換算は**記録ごとに行ってから積算**なので、株数が違う記録が混ざっても正しい（合計を株数で割る誤りを構造的に回避済み）。**新しい計算は1行も書いていない**。
  - ⚠ **`t.real` の意味が「実額」→「100株換算」に変わる**。実額が要る所は `t.realRaw`。
  - ⚠ `_elRealPnlPair` は `item.pnl` を `signal.realizedPnl` より優先する＝両方あって違う記録では実額が動く（取引テーブル側と揃う方向）。
- **`realCell` を2段構成に**: 上段＝実額＋**実額スケール(10倍)**のバッジ（従来と同じ）／下段＝100株換算÷営業日数＋**通常スケール**のバッジ。
  **1000株ちょうどなら2つのランクは一致し、ズレたら「株数を張ったから金額が出ただけ」と読める**のが狙い。`days<=1` はラベルを「1日平均」→「100株」にして冗長さを回避。
- **株数未入力**: `_elTotAccum` は換算できない記録の実額をそのまま100株換算側に足す（＝下段が過大）。共通部は触らない方針なので、`_noShareN`（`totOf` と同じ除外条件）で数えて「株数未入力N件を含む」を琥珀で小書き。**除外条件を変えるときは両方直すこと**（コメント済み）。
- **🏷銘柄別の損益割合**（`totOf` 共有）は `a.tot.real` → `a.tot.realRaw` に差し替えて**見た目不変**。
- 検証: 全8ファイル構文OK・console 0。`EntryLogView` を実マウントし4ケース—
  **①500株**（実額+30,000/3営業日）: 上段 `C +30,000円`／下段 `B 1日平均+2,000`＝**ランクがズレる**（100株換算6,000÷3=2,000）。
  **②1000株ちょうど**: 上段 `C`／下段 `C`＝**一致**。
  **③株数未入力1件混在**: 下段が `S 1日平均+4,000` と過大になり **「株数未入力1件を含む」** が出る＝警告が歪みと連動している。
  **④日別(days=1)**: ラベルが `100株+2,000`＝「1日平均」にならない。🏷銘柄別の実現損益は `+30,000円`＝実額のまま。

### 2026-08-05x 最大DDに1日平均を併記＋DD期間の空欄バグ修正（sw v361→v362）
- ユーザー要望「DDについては1日平均も下に表示して」。`_elStreakDDSectionV2` に **第3引数 `data`** を追加し、
  `_ddDays = _elBizDaysOf(v2, data)`（この母数のスパンの営業日数）で割った1日平均をカード小書きの2行目に出す。
  **割る数は全体損益の`_bizDaysIn`・KPI「1日あたり損益」と同じ**＝同じ画面で分母が食い違わない。色は`_EL_SUBNOTE_COL`。
- **ついでに直した①: DD期間が空になるバグ**。`_ddOf` の `peakDate` 初期値が `null` だったため、**初手が負けで累積が一度も0を超えない**と
  peak=0(初期値)のままpeakDateがnull → **金額は出るのに from〜to だけ空**。読み取り文が「−8,400円**（）**」と空カッコになり、
  カード小書きも「実現損益の記録待ち」＝記録があるのに無いように見えていた。累積0のピークは「最初の記録の時点」なので
  `peakDate = seq[0].date` を初期値にした。ユーザーの画面（最大DD(実現) −8,400円 なのに小書きが「実現損益の記録待ち」）が実例。
- **ついでに直した②: DD=0のときの文言**。`from`がnullなだけで「実現損益の記録待ち」と出ていた。`realSeq`が空のときだけその文言にし、
  記録はあるがDD=0なら「ドローダウンなし」に。読み取り文もカッコが空なら丸ごと出さないようにした。
- 検証: 全8ファイル構文OK・console 0。`_elStreakDDSectionV2`を単体で実描画し3ケース—
  **①初手負け(-5000,-3400,+2000,+1500)**: 「−8,400円 / **07-06〜07-07** / **1日平均−420円**」＝-8400÷20営業日(7/6〜7/31)で一致、読み取り文の空カッコも解消。
  **②全勝**: 「0円 / **ドローダウンなし**」。**③実現損益なし**: 「— / 実現損益の記録待ち」＝この文言が正しい場合だけ出る。

### 2026-08-05w グレード凡例の直下に「※グレードはすべて1日換算」（sw v360→v361）
- ユーザー要望「右上のグレード表示のすぐ下に、『※グレードはすべて1日換算』と赤字で注記」＋質問「実現損益のグレードも1日あたり換算だっけ？」→ **答え: はい**。
- **根拠**: `_yenN`（app-06 L6037）の `var _gv = (days && days > 0) ? Math.round(v / days) : v;` は `isReal` の外側にあり、
  `isReal` は **しきい値表を `_profitGradeFromPnlReal`(10倍スケール)へ切り替えるだけ**。日数で割る処理は最終損益と共通。
  `realCell` も `_yenN(v, cnt, days, true)` と days を渡している。（）内の参考値も app-05 `_elHold2RefSuffix` で同じ規約。
  **＝この表のグレードバッジは全列とも「合計÷営業日数」判定**。実測: 実現+30,000円は1日判定なら`S`だが27日で割ると+1,111＝`C`（割っている証拠）。
- 実装: `_ovGradeLegend` を **column ラッパ**（`flexDirection:"column"` / `alignItems:"flex-end"`）で包み、凡例の下に赤注記を置いた。
  右端寄せ `marginLeft:"auto"` は凡例本体の opts からラッパへ移動（凡例と注記が同じ右端で揃う）。色 `#C0392B`・9px・700・nowrap。
  `title` にホバー説明（「表示額は合計なので合計額をグレード表に当てても一致しない」）。
- 検証: 全8ファイル構文OK・console 0。実マウントで注記の色 `rgb(192,57,43)`・凡例の**直下**・**右端差0px**（凡例454px / 注記112px が同じ right）を確認。

### 2026-08-05v 明細行の小バッジだけ「指値同値」に戻した（sw v359→v360）
- ユーザー判断「小バッジは従来通りでいいや」。2026-08-05uで一緒に改名した `_elFillRiskNode`（app-05）の表示文字だけ旧称へ復帰。
- **住み分けが確定**: 列見出し・`🎯`セクション見出し・KPI副文言・説明文の4系統＝**同値除外損益**／1記録に付く小バッジ＝**指値同値**（損益そのものではなく印なので短い旧称のまま）。定義箇所のコメントにこの対応を明記。
- 検証: `app-05.js`構文OK・`_elFillRiskNode`健在・実マウントで列見出しは `…｜最終損益｜**同値除外損益**｜実現損益` のまま・console 0。

### 2026-08-05u 補助行の文字色を濃く（1日平均が薄い）／「指値同値」→「同値除外損益」（sw v358→v359）
- **① 1日平均が薄い**（ユーザー指摘「アプリ全体の話として、1日平均に関する文字が薄いのが気になる」）。
  - 旧 `#94A3B8`（slate-400）は白背景でコントラスト比 **約2.9:1**。9pxの小さい字には足りていなかった。
  - **`_EL_SUBNOTE_COL = "#475569"`（slate-600・約7.4:1）を app-05 の `_GRADE_STYLE` の直前に新設＝単一源**。薄く戻すなら `#64748B`（slate-500・約4.8:1）へ。**ここ1か所で全箇所が変わる**。
  - 置換したのは**8か所**: app-06 `avgDayLine`／`avgCntLine`／`reachAvg2`／`_avgCntLine2`／期間集計表の到達・最終損益・実現損益の3つ、app-04 `_wkStack` のラベル列。
  - **app-04 の `_wkStack` ラベル列は「1日/平均」と「100株」の共用セル**。1日平均だけ濃くすると同じ列に濃淡が混ざるので両方まとめて濃くした。
  - app-04 は app-05 より**先に**読まれるが、`_wkStack` が走るのは描画時＝定数は既に定義済み。同ファイルは `_GRADE_STYLE`/`_elHoldGradeBadge` でも既に app-05 に依存しているので新しい依存ではない。
  - **対象外**: 到達列の「%」（1日平均ではない）・KPIカードのラベル(`#9A9186`＝全カード共通のタイトル色)。
- **② 「指値同値」→「同値除外損益」**（ユーザー指示）。**画面に出る文字だけ**を6か所置換:
  app-05 明細行バッジ、app-06 の α理想値の説明文・シミュの注記・全体損益（期間別）の列見出し・KPI「件数」カードの副文言・`🎯 同値除外損益（OS値＝α値）` セクション見出し。
  - **内部識別子は据え置き**（`_elFillRisk` / `_elFillRiskRec` / `_elFillRiskNode` / `_elFillRiskCountRecs` / `fillEqZero` / `_friskN` / `riskOpen`）＝リネームの巻き添えで挙動が変わる箇所を作らない。コード内の旧称コメント（app-05:5件・app-06:35件）は履歴として残置。定義箇所（app-05 `===== 同値除外損益 =====`）に対応表のコメントを追加。
- 検証: 全8ファイル構文OK・console 0。実マウントで列見出しが `週｜日数｜件数｜到達｜利確｜同値｜損切り/損失｜最終損益｜**同値除外損益**｜実現損益`、描画物から旧称「指値同値」が消えたこと、1日平均spanの実効色が `rgb(71,85,105)`＝#475569 になったこと、`🎯 同値除外損益` 見出しの表示を確認。

### 2026-08-05t 分析母数トグル「全期間／6/29以降のみ」を新設（sw v357→v358）
- ユーザー要望「シグナル分析についても6月29日以降のみで見れる機能もつけたい」＋設計確認3点（範囲＝分析タブだけ／既定＝OFF＝全期間／推奨αも追従）＋「シミュもそろえる」。
- **配線は実質1か所**: `_v2recsAllData`（app-06 L5948付近＝分析の根）に `_sinceCut` を挟むだけで、
  📡シグナル総合の全サブタブ・銘柄別の集計/α値/損切り/未達/深掘り/株価帯別・**シグナルピルの件数**・`_missCnt`・
  銘柄タブのシミュ（`_selSigRecs`経由）・**推奨α**まで一括で追随する。
- 追加物: state `sinceOnly`（既定`false`）＋ヘルパー `_sinceCut(rs)`＋トグルUI＋ON時の注記。
  - **`_elSinceRecs` は使えない**（定義がL6128付近＝`_v2recsAllData`より後ろの `var` 代入＝実行時undefined）。
    トップレベル関数宣言で巻き上げ済みの **`_elIsOldRule`** を`_sinceCut`内で直接使って定義順の問題を回避。境界の正本は`_EL_RULE_SINCE`のまま。
- **シミュ2種の扱い**: 銘柄タブのシミュは`_selSigRecs`経由で自動追随。**🧮全銘柄一括シミュだけは`_v2recsAll`（計算算入系）由来で根を通らない**ので明示的に`_sinceCut`。
  どちらも `baseRecs`（銘柄ごとの推奨α算出元＝`allRecs`直参照）も絞る＝「母数は6/29以降なのに推奨αは全期間」というちぐはぐを防ぐ。
- **トグルの表示条件は `_isSigTotal || !_isAllStock || view === "sim"`**。💰損益タブでは`_v2recsAllData`を使わない＝押しても何も変わらないので出さない。ただし全銘柄一括シミュは💰損益タブにあって絞る対象なのでシミュ時だけ出す。
- **対象外（意図的）**: 全体損益（期間別）・KPI早見・累積損益・銘柄ピルの件数・一覧・カレンダー。特に全体損益は旧ルール行を薄く残して比較できる仕様なので、根ごと落とすと競合する。ON時は注記でこの食い違いを明示。
- 検証: 全8ファイル構文OK・console 0。合成データ（旧ルール4件／新ルール3件）で実マウントし—
  **銘柄タブ**: OFF=底抜け(5)/反発(2) → ON=底抜け(2)/反発(1) → **OFFに戻すと(5)/(2)へ完全復元**（＝既定OFFなら従来と1件も変わらない）。銘柄ピルは(7)のまま＝設計どおり。
  **🧮全銘柄一括シミュ**: 母数 OFF=7件 → ON=3件 → 戻して7件。
  **トグル表示**: 💰損益(集計)=出ない／銘柄タブ=出る／📡シグナル総合=出る／💰損益+シミュ=出る。

### 2026-08-05s 銘柄別・グレード別の母数も6/29境界へ統一＝境界を1本に（sw v356→v357）
- ユーザー指示「揃えて」。**🏷銘柄別の損益割合**と**🎯グレード別の件数（月別）**だけが独自の月単位境界 `_EL_SINCE = "2026-07"` を持っていて、**6/29〜6/30の記録が丸ごと落ちていた**（ユーザー実データで実証: 全体損益の合計100件に対しこの2つは92件＝差の8件が6/29〜）。
- `_EL_SINCE` を廃止し、`_elSinceRecs` を **`!_elIsOldRule(r.date)`（＝`_EL_RULE_SINCE`＝2026-06-29・日単位）** に差し替え。これで**アプリ内の集計境界は`_EL_RULE_SINCE`の1本だけ**になった（旧 `_EL_SINCE` の参照は0）。
- ラベルも定数から自動生成に: `_EL_SINCE_LBL` は「2026年6月29日以降」＝バッジが「📅 2026年6月29日以降のみ」になる。
- **部分月の明示**: グレード別（月別）は境界をまたぐ月だけ行ラベルを **「2026/06（6/29〜）」** にした（`_elSinceYmLbl`）。ただの「2026/06」だと6月まるごとに見えてしまうため。全体損益（期間別）の月別分割と同じ書式。
- 見出しの説明文から「上の『全体損益（期間別）』は全期間なので**合計行は一致しません**」を削除＝**母数が同じ期間になったので嘘になった**。
- 検証: 合成データ（5/12・6/26＝旧／6/29・6/30・7/15・8/3＝新）で実マウント。**銘柄別=4件・グレード別の合計=4件・全体損益の合計=4件で三者一致**、グレード別の行は「2026/08 / 2026/07 / 2026/06（6/29〜）」＝旧ルール側（5/12・6/26）は入らず、部分月ラベルも出た。②日単位表も同じ並び。console 0。

### 2026-08-05r ×宣言の判定を次足期待度の正本へ統一＝E判定の食い違いを修正（sw v355→v356）
- **症状**: フォームが「E：×（∵次足期待度×）＝見送り」と表示している記録が、集計側では**E成立として到達に算入**されていた。ユーザーが「金額不明」の記録を調べていて発覚。
- **原因**: `_epResolve`(app-05 L3527)の×宣言判定だけが **レガシーの `os1Exp`/`os2Exp`（=`legs[j].exp`）** を見ていた。
  - `os1Exp`/`os2Exp` はフォーム保存時に **`_useOs2`/`_useOs3`＝保存時点のEP位置** でゲートされる（app-05 L7636/7641）。保存時EPがOS1/OS2だと手前足の×は `nextExpN` にしか残らない。
  - その後αが変わってEPが後ろへ動くと、EPより手前になった×を `_epResolve` だけが見落とす。**フォームのEバッジ(L7021)は `fNxs`＝次足期待度を見ている**ので、両者が食い違った。
  - 他の消費者（手じまい判定・TotParts系）は全て `_epNextExpAt` 経由に移行済みで、**`_epResolve` だけが2026-07-06の統一から取り残されていた**。
- **修正**: `legs[j].exp === "×"` → `_epNextExpAt(s, j) === "×"`。`_epNextExpAt` は ①`nextExp(j+1)` ②レガシー導出（待ち足は`legs[j].exp`）の順で読むので、**`nextExpN`を持たない旧記録は従来と完全に同値＝後方互換**。
- **影響**: E成立母数（到達）が動く＝利確率・損切り率・損益の集計が変わり得る。取り残されていた記録が正しく「×見送り＝ノートレード」へ移る方向。
- 検証: `app-05.js`構文OK。4パターンで判定を確認—
  「nextExp2=×・os2Exp未保存」＝**修正前judge:ok→修正後judge:x（到達から除外）**／「旧記録(nextExpN無し・os2Exp=×)」＝judge:x のまま／「×無し」＝judge:ok のまま／全パターンで `_epAsTraded`（×を無視した参考損益の経路）は judge:ok を維持＝壊れていない。`EntryLogView`実マウントも描画OK・console 0。

### 2026-08-05q 「旧ルール」バッジを撤去（sw v354→v355）
- ユーザー指示「旧ルールという表記は不要」。2026-08-05oで付けた行頭バッジ（`_old ? …「旧ルール」`）を削除。
- **薄さ（opacity .42）と合計除外はそのまま**＝挙動は不変。理由の説明は見出しの説明文と合計行の「※6/29〜のみ」（title付き）に残るので、情報の行き先は確保されている。
- 検証: 実マウントで行の薄さ0.42・合計「※6/29〜のみ 13日 4件」が不変、描画物から「旧ルール」の文字列が消えたことを確認（console 0）。

### 2026-08-05p 累積損益・連勝連敗DDの母数も6/29境界へ統一（sw v353→v354）
- ユーザー指示「揃えて」＝2026-08-05oで残していた既知の非一致（期間別表の合計は6/29境界・累積損益と連勝連敗DDは4月のみ除外）を解消。
- **境界をトップレベルの単一源へ**: `_EL_RULE_SINCE = "2026-06-29"` と `_elIsOldRule(ds)` を app-06 の `_elIsEmaRefPeriod` の隣に新設。
  `_ovPnlTbl` のローカル `_RULE_SINCE`/`_isOldRec` はこれを指すだけにした＝**境界を動かすなら1か所**。
- `_v2recsNonRef`（4月のみ除外）→ **`_v2recsNewRule`（`!_elIsOldRule(r.date)`）にリネームして中身も差し替え**。名前が「NonRef＝参考期間以外」のままだと意味とズレるため改名（参照5か所とも更新済み・旧名の残りは0）。
  - これで `_v2recsNewRule` ＝ `_ovPnlTbl` の `rsInc`（合計行の母数）と**定義上ぴったり一致**する。
- 見出しを戻した/直した: 📈累積損益は「**上の期間別表の合計行と同一基準**（6/29より前＝旧ルール期間は除外）」＝2026-08-05oで外した記述が再び正しくなった。📉連勝連敗も「4月＝参考期間は除外」→「6/29より前＝旧ルール期間は除外」。
- 検証: `app-06.js`構文OK・旧名`_v2recsNonRef`の残存0。合成データで実マウントし、**合計行「※6/29〜のみ 13日 4件」に対し累積損益の起算日候補が 2026-06-29 / 06-30 / 07-15 / 08-03 の4件**＝母数一致を確認（5月・6月前半が消えている）。連勝連敗DDも描画OK・console 0。

### 2026-08-05o 旧ルール期間（〜6/28）を薄く＋合計から除外／期間集計の小書きも撤去（sw v352→v353）
- ユーザー指示「揃えて」＋「4月～6月は薄くして、合計行にも算入しないで。日別・週別についても同様。※以上の件について、6/29～は除く」。
- **① 揃えた**: 📆期間集計（app-06 `_thPSub`）の小書きも撤去＝2026-08-05nの全体損益（期間別）と同じ「見出しだけ」に統一。`_thPSub`は定義ごと削除。
- **② 旧ルール期間の扱い（`_ovPnlTbl`のみ）**: 境界 `_RULE_SINCE = "2026-06-29"` を新設。**これより前の記録は行を薄く（opacity .42）＋「旧ルール」バッジ＋合計・平均から除外**。
  - **除外判定の正本は記録の日付（`_isOldRec`）**。`_RULE_SINCE`が**月曜**なので日別・週別は境界がそのまま行の切れ目になる（週キーは月曜日付）。
  - **月別だけ2026年6月が境界をまたぐ**ので、キーに `#1`(〜6/28) / `#2`(6/29〜) を付けて**1か月を2行に割る**（ユーザー選択）。`#1<#2` にしてあるので降順ソート後に「6/29〜」が上・「〜6/28」が下＝新しい方が上、が崩れない。
  - `_bizDaysIn` は割った側の営業日だけ数える＝**20日＋2日=22日**で元の6月と一致。`labelOf`は「2026/06（〜6/28）」「2026/06（6/29〜）」を生成（境界の定数から自動生成＝日付を変えれば追随）。
  - **分割キーは`_ovPnlTbl`専用**。共用の `_granKeyOf` / `_granLabelOf` は触っていない（指値同値など他の表に波及させないため）。
  - 旧 `_isRefKey`（4月＝`_elIsEmaRefPeriod`）を置換。**新境界は旧境界を内包する**ので4月・5月も従来どおり除外される。
- **注意（既知の非一致）**: 📈累積損益・📉連勝連敗DDの母数 `_v2recsNonRef` は**従来どおり4月のみ除外**＝期間別表の合計と母数が違う。指示の対象が期間別表だけだったのでデータは触らず、見出しの「**合計行と同一基準**」という記述だけ外した（そのままだと嘘になるため）。揃えるなら `_v2recsNonRef` を `_RULE_SINCE` 基準にする。
- **前提**: 2026年4月は `_anaRecs`(app-06 L5930) で**記録帳全体から既に除外済み**＝そもそも4月の行は出ない。今回の「薄く」が実際に効くのは5月〜6/28。
- 検証: `app-06.js`構文OK。`EntryLogView`を合成データ（5/12・5/13・6/10・6/26・6/29・6/30・7/15・8/3）で実マウントし、
  **週別**＝6/29〜7/3は通常・6/22〜6/26と6/8〜6/12と5/11〜5/15はopacity 0.42＋「旧ルール」、合計「※6/29〜のみ 13日 4件」。
  **月別**＝2026/06が「（6/29〜）2日2件・通常」と「（〜6/28）20日2件・薄い」の2行に分離、合計「28日 4件」＝**週別と件数が一致**。
  ドリルダウンも確認（「（6/29〜）」→6/29〜7/3のみ／「（〜6/28）」→6/22・6/8の薄い週）。console 0。

### 2026-08-05n 全体損益（期間別）のヘッダ小書きを全撤去（sw v351→v352）
- ユーザー指示「到達や利確などの直下の注記はすべて不要」（対象は💰全体損益（期間別）＝`_ovPnlTbl`の`_headTr`）。
- **`_othSub`（ヘッダ小書きのspan生成）と、その6か所の呼び出しを削除**＝到達／利確／同値／損切り・損失／最終損益／指値同値は見出しの1行だけになった。
  - 消えた文言: 「EPに乗った＝E成立」「利益で手じまい」「±0で手じまい」「上=ライン起因／下=その他」「○途切れで手じまい ()=△」「上=件数／下=除外後」。
  - **説明は各列の`title`（ホバー）に残っているので情報は失われない**。ただし「損切り/損失」だけは上下段の対応が小書きにしか無かったので、`title`側に「（上段）」「（下段）」を書き足した。
- **同じ小書きは📆期間集計（`_thPSub`・app-06 L7590付近）にも残っている**＝今回の指示は添付画像の全体損益（期間別）が対象なので手を付けていない。揃えたくなったらそちらも同じ手順で消せる。
- 2026-07-30bの「小書きが列幅を押し広げる」問題は、これで元から断たれた（小書きが無い＝`maxWidth:74`の折り返し調整も不要）。
- 検証: `app-06.js`をブラウザで`new Function(src)`＝構文OK（720,943バイト）、SW/キャッシュを消して実マウントしconsoleエラー0。残る`_othSub`はコメント2件のみ＝コード参照なし。

### 2026-08-05m 凡例のラベル短縮と列の縦ぞろえ（sw v350→v351）
- ユーザー指示「実現損益の（10倍）という表記は不要」「最終・指値同値についても『最終損益』で足りる」「きれいに縦ぞろえして。今ずれてるでしょ」。
- ラベルを **「最終損益」「実現損益」** に短縮（指値同値列は最終損益と同じスケールなので上段に含まれる）。
- **`_gradeLegendNode` → `_gradeLegendGrid(rows, opts)` に作り直した**。旧実装は1段＝1つのflexで、段ごとに独立して並べていたため、**桁数の違い（`2501~` と `25001~`）でA以降のバッジが右へずれていく**。「ラベル＋9段」を**1つのgrid**（`gridTemplateColumns: "auto repeat(9, auto)"`）に入れると、各段が同じ列に入り列幅は広い方（実現損益）に自動で合う。
- **注意**: `display:inline-grid` を指定しても、`_granSeg`の`display:flex`の子になるとblockify されて computed は `grid` になる（挙動は同じ）。DOM検査するときに `inline-grid` で探すと見つからない。
- 検証: 全8ファイル構文OK。`EntryLogView`を実描画し、**上段/下段の全10列のleftが完全一致**（0 / 46 / 98 / 150 / 201.9 / 233.9 / 260.9 / 296 / 351.1 / 406.2px）、バッジ位置も一致。凡例454×28px。

### 2026-08-05l 全体損益（期間別）の右上にグレード凡例（sw v349→v350）
- ユーザー要望「ここにもグレードバッジの基準が表示されるようにして。右上らへんが空いてるでしょ」。
- **新設: `_gradeLegendNode(real, opts)`（app-05・グローバル）**。既存の凡例は app-02 の `_renderGradeLegend2Row`（ローカル・他ファイルから使えない）＋しきい値のベタ書きだったので、**`_GRADE_DESC` / `_GRADE_DESC_REAL` から生成する**方式にした＝**境界を変えてもこの凡例は自動追随する**（ベタ書き6か所への追随は従来どおり必要）。
  - `opts.lowerOnly`＝「2001~2500円」「2501円~」どちらの書式でも下限だけ残して幅を詰める（`円`は落とす）。狭い場所向け。レンジ全文は`title`に入るのでホバーで読める。
  - `opts.label`＝行頭のラベル、`opts.style`＝コンテナへの追加スタイル。
- **`_granSeg(cur, setFn, keyPfx, trailing)` に第4引数を追加**。未指定なら従来どおりなので他への影響なし（実際の呼び出しは全体損益の1か所だけ。「指値同値と共用」というコメントは2026-07-20cの統合で実態と合わなくなっていた）。右端寄せは`marginLeft:auto`。
- **この表は列でスケールが違う**（最終損益・指値同値＝損益スケール／実現損益＝10倍）ため、**2段で併記**する。片方だけ出すと実現損益列の読み違いを招く。
- 検証: 全8ファイル構文OK。`EntryLogView`を実描画し、セグメントが0..166px・凡例が560..1038pxで右端に寄り、行高34px（2段）で折り返し無しを確認。

### 2026-08-05k 利益側A/B/Cを1段ずつ薄く（sw v348→v349）
- ユーザー指示「ABCももう少しづつ薄くできる？Aが少し濃すぎるかも」。明るさ **A=39→50／B=67→74／C=86→89**（`#F8A5A5`／`#FFD5D5`／`#FFEFEF`。枠も追随）。
- **これが薄くできる限界**。もう一段薄い案（A=58/B=78/C=91）も試したが、**Cが札の地色`#f8f7f4`との色差4.6まで落ちて実質見えなくなる**（今回の案でも5.8）。C側の余白が無いので、A/Bだけ薄くするとA/BのΔEが14まで縮む。3段まとめて等間隔に薄くするのが最善だった。
- 結果: 隣接ΔE **S/A=53.5・A/B=21.8・B/C=12.1・C/D=8.9**（最小8.9）。**文字コントラストはむしろ改善**（A 5.1→6.3・B 5.1→5.5・C 5.0→5.1）＝背景が明るくなり暗い文字との差が開いたため。
- **限界の目安として残す**: 淡い側は「隣接ΔE」だけでなく**札の地色との色差**も見ること。バッジ同士が区別できても、地に埋もれたら意味がない。

### 2026-08-05j Sの金色を薄く調整（sw v347→v348）
- ユーザー指示「Sはもう少し薄く」。`#F5C518`→**`#FADD78`**（明るさ59→73／文字`#5A3F00`・枠`#D2A421`）。
- 薄くしすぎると2026-08-05i以前の`#FBEBB5`（明るさ83）に戻り「金色に見えない・C/Dと近づく」ので、**明るさ73で止めた**。この時点でC/Dとの背景ΔEは50以上あり混同しない。
- 他8段は変更なし。**隣接ΔEの最小9.4・文字コントラスト比の最小4.6は維持**（Sのコントラストは7.3）。

### 2026-08-05i グレードバッジの配色を総入れ替え（sw v346→v347）
- ユーザー指摘「もっときれいな金色がいいな」「AとBの色の違いが分かりづらい」「ほかにもわかりづらいところはない？」。
- **旧配色を実測したら、指摘以外にも潰れている組み合わせがあった**（ΔE=CIE76。**2.3未満は人間には判別不能**）:

  | 組 | 旧背景ΔE | 判定 |
  |---|---|---|
  | A / B | **2.8** | ほぼ同色（ユーザー指摘どおり） |
  | E / F | **2.9** | 同じ問題が損失側にもあった |
  | B / C | **3.9** | かなり近い |
  | D / Z | **2.1** | 「±0」と「取引なし」が同じ見た目 |

  文字コントラスト比も C=3.9・E=3.8 で **WCAG AA(4.5)未達**だった。
- **原因**: 利益側3段・損失側2段がどれも同じくらいの淡さで、背景の明るさが単調に並んでいなかった。個々の色はきれいでも隣同士が潰れる。
- **対処**: `C→B→A`・`E→F→G-→G+` が段階的に濃くなる並びへ作り直し。Sは彩度の高い金(`#F5C518`)にして色相ごと別格に。D(±0)は少し濃い灰にしてZ(取引なし)から離した。
- **実測（新）**: 隣接背景ΔEの最小 **2.8→9.4**／文字コントラスト比の最小 **3.8→4.6**（全段AA達成）／D vs Z **2.1→7.6**。背景の明るさは S=59・A=39・B=67・C=86・D=74・E=87・F=71・G-=52・G+=35 で、利益側も損失側も単調に濃くなる。
- **⚠️ 色を変えるときは必ず「隣接ΔE」と「文字コントラスト比」の両方を測ること。** 淡い色を並べると個々はきれいでも隣が潰れる（旧配色がまさにこれ）。`_GRADE_STYLE`（app-05）の1か所を直せば全画面に効く。

### 2026-08-05h 最上位を A+→S（金色）・A-→A に改名（sw v345→v346）
- ユーザー指示。**しきい値は2026-08-05gのまま**で、呼び名と色だけ変えた（S=2501円〜/25001円〜、A=2001〜2500/20001〜25000）。
- **Sは金色**: `bg:#FBEBB5 / color:#6B4E00 / border:#C9971B`。他は赤系（利益）・緑系（損失）なので、最上位だけ色相を変えて別格にする狙い。
- **`A-`は`_GRADE_STYLE`から削除した**。下の方にあった legacy な`A`キーと**配色が完全に一致**していたため、キーを消して`A`に寄せるだけで済んだ（新規追加は`S`のみ）。
- **9段の並びは S / A / B / C / D / E / F / G- / G+**。±が付くのはG-/G+だけになり上下非対称だが、Sを別格として際立たせる意図なのでこれで正しい。
- **グレード文字列は永続化されていない**（毎回 pnl から計算）ので、**データ移行は不要**。改名で触るのは表示側だけ。
- 変更箇所（2026-08-05gの「6か所ベタ書き」に**並び順の配列2つ**が加わる）: 判定2関数と`_GRADE_STYLE`/`_GRADE_DESC`/`_GRADE_DESC_REAL`（app-05）・凡例2つ（app-02）・`_trLegendPairs`と`var grades`（app-04:5909/6850）・グレード基準表2行（app-04:6866/6867）・`_EL_GRADES`（app-06:6230）・グレード別件数の説明文（app-06:7320）。
- 検証: 全8ファイル構文OK。境界16点で2500→A／2501→S／25000→A／25001→Sを確認、**実現が通常のちょうど10倍**もズレ0。9段すべてに固有スタイルがある（Zフォールバックに落ちていない）ことと、旧キー`A+`/`A-`が消えたことを確認。DayViewを実描画し、Sバッジが金色（`rgb(251,235,181)`）で出ることを確認。

### 2026-08-05g グレード境界を1円ずらす＋実現損益スケールの食い違いを解消（sw v344→v345）
- **(1) 境界を1円ずらした**（ユーザー指示「A+は2501または25001円〜にして、それに合わせて全部ずらす」）。キリのいい額（2500/2000/1000とその負数）が**下のグレードに入る**形へ統一し、0を挟んでプラス側とマイナス側が対称になるようにした（A+が2501〜ならG+は-2501〜）。

  | | 通常スケール | 実現スケール(10倍) |
  |---|---|---|
  | A+ | 2501円〜 | 25001円〜 |
  | A- | 2001〜2500 | 20001〜25000 |
  | B | 1001〜2000 | 10001〜20000 |
  | C | 1〜1000 | 1〜10000 |
  | D | 0 | 0 |
  | E | -1〜-1000 | -1〜-10000 |
  | F | -1001〜-2000 | -10001〜-20000 |
  | G- | -2001〜-2500 | -20001〜-25000 |
  | G+ | -2501円〜 | -25001円〜 |

- **判定本体は app-05 の`_profitGradeFromPnl`/`_profitGradeFromPnlReal`の2つだけ**だが、**しきい値の文字列が別に6か所ベタ書き**されているので同時に直した: `_GRADE_DESC`/`_GRADE_DESC_REAL`（app-05）・凡例2つ（app-02:4499実現/4547通常）・`_trLegendPairs`（app-04:5909）・グレード基準表の2行（app-04:6866実現/6867通常）・グレード別件数の説明文（app-06:7316）。**しきい値を動かすときはこの6か所も必ず追随させること**（コード側だけ直すと凡例と食い違う）。
- **(2) 実現損益スケールの食い違いを解消**。同じ -6,000円 が今週の損益データでは`E`（実額スケール）なのに全体損益（期間別）では`G+`（通常スケール）になっていた。原因は app-06 の`_yenN`が最終損益と実現損益で共用されていて、通常スケール固定だったこと。**`_yenN(v, cnt, days, isReal)`** に引数を足し、実現損益の呼び出し5か所（`realCell`／`_td(t.real…)`／KPIカード2つ／📆期間集計）に`true`を渡す。最終損益側（`_yenNR`経由）は通常スケールのまま。記録1件ごとの実現損益（app-06:3691/6548）は元から実額スケールだったので、集計側がそちらに揃った形。
- **目標額2500は据置＝ユーザー確定事項（2026-08-05）**: `_elIdealAlphaV2`の目標額（app-05・既定2500）と`_EL_DAY_TARGET_YEN`（app-06・2500）は**グレード境界ではなく目標利益の定数**。境界を2501にずらした際に「揃えますか」と確認し、**「目標は2500のままでいい」と明示的に回答をもらっている**。
  - つまり「目標達成(2500円)＝A+」ではなくなり、ちょうど2500円だと**目標は達成だがバッジはA-**になる。これは**不整合ではなく仕様**。
  - 動かすと推奨αの値と過去の達成率が変わるため、**気を利かせて2501に揃えないこと**。変更が要るなら必ずユーザーに確認する。
- 検証: 全8ファイル構文OK。境界24点を総当たり（2500→A-／2501→A+／2000→B／2001→A-／1000→C／1001→B／0→D／-1000→E／-1001→F／-2000→F／-2001→G-／-2500→G-／-2501→G+）。**実現スケールが通常のちょうど10倍であることをズレ0で確認**。件数0→Zも維持。`EntryLogView`を実描画し、実現損益-6,000円が`E`になったことを確認。

### 2026-08-05f 日別の行から「1日平均」を消した（sw v343→v344）
- ユーザー指摘「日別での1日平均額の表示は不要では？」。日別は各行＝1日なので**1日平均＝その行の額**で、同じ数字が2つ縦に並んでいた。
- **粒度(`g==="day"`)ではなく日数(`days<=1`)で判定**したのが要点。これで3つが同時に成立する: ①日別の各行は消える ②休場続きで1営業日しかない週/月の行も消える ③**合計行は日別表示中でも残る**（`_ovTotDays`＝全期間の営業日数なので平均が意味を持つ）。粒度で切ると③まで消えてしまう。
- 対象2か所（どちらも日別/週別/月別トグルを持つ表）:
  - app-06 `_ovPnlTbl`（**💰全体損益（期間別）**）の`avgDayLine`＝最終損益列・指値同値の除外後列・実現損益列に効く。
  - app-06 **📆期間集計**の表（7613/7614行）＝`(dn && …)`→`(dn > 1 && …)`。同じ表の件数側(`_avgCntLine2`)と到達側は2026-07-19/07-24から既に日別で非表示にしており、金額側だけ取り残されていた。
- 既存の`avgCntLine`（件数の「（1日平均〇件）」）は`(gg||g)==="day"`のままにした＝挙動を変えないため。**金額側だけ日数判定**という不揃いは残るが、件数側は合計行でも非表示という既存仕様なので触っていない。
- 検証: 全8ファイル構文OK。`EntryLogView`を合成データ（`scheme:3`でv2判定を通す＋`_elHoldFinalParts`差し替え）で実描画し3モードを確認。**日別**＝各行に1日平均なし・合計行(4日)は`1日平均+2,850`あり。**週別**＝`3日 … 1日平均+3,367`あり。**月別**＝`3日 … 1日平均+3,367`あり。

### 2026-08-05e （）も含めて縦ぞろえ＝括弧を独立した列に（sw v342→v343）
- ユーザー要望「（）内の金額などもきれいに縦ぞろえがいいな。（）自体の位置なども」。
- **原因**: （）は`_elHold2RefSuffix`が作る1つのノードで、それを額セルの**後ろに直付け**していた。額の桁数が違うと（の開始位置がずれる（例: `+10,100円 （…` と `+3,367円 （…`）。
- **対処**: 括弧・参考バッジ・参考額・閉じ括弧を**それぞれ独立したgrid列**にした。列 = `ラベル / バッジ / 額 / （ / 参考バッジ / 参考額 / ）`。**額と参考額は`justifySelf:"end"`で右寄せ**＝桁数が違っても右端が揃う。参考額を持つ行が1つも無い場合は3列に落として余分な余白を作らない。
- **対象2か所**: app-04 `_wkStack`（最終損益・実現損益）と **app-05 `_elDetailPnlStackNode`（詳細損益のEP/H1/最終の3行）**。後者は今週の損益データ・本日の損益データ・単一銘柄パネルの**4呼び出し全部**に効く。
- `_wkStack`のrowは`ref`(ノード)を捨てて**`refAmount`(素の数値)**を持つ形に変更。`_wkRefNode`は不要になったので削除。`_elHold2RefSuffix`は他所（取引テーブル等）でまだ使うので残置。
- **H１行の上下罫線が途切れないよう`columnGap:0`**にし、余白はセル内のpaddingで作った（gapだと罫線に隙間が空く）。
- 検証: 全8ファイルを`new Function()`で構文OK。DayViewを実描画し取引タブまで到達。**最終損益**＝バッジ左548・額右端618・（左618・参考バッジ左631・参考額右端688・）左688が2行とも一致。**詳細損益**＝EP/H1/最終の3行とも額右端110・（左110・参考額右端179・）左179が一致（参考なしのEP行も列を崩さない）。

### 2026-08-05d 【緊急修正】app-04.jsの構文エラーで日別ページが真っ白（sw v341→v342）
- 症状: 日別ページで「画面の描画でエラーが発生しました / **DayView is not defined**」。app-04.js全体がパースに失敗し、同ファイルの全関数が未定義になっていた。
- 原因: 2026-08-05cで最終損益セルのタグチップを撤去した際、`var _pnlNode = (function() {` の**開きだけ残して閉じ（`})();`＋後続のreturn）を消した**。タグチップの後処理が無くなり内側IIFEは不要になっていたので、開きごと削除するのが正解だった。
- **見落とした理由（次回の教訓）**: 検証をホーム画面のconsoleだけで済ませた。ホームは`DayView`を描画しないので**エラーが出ない**。しかもパースエラーはerrorsのみのフィルタに乗らず「console 0」に見えていた。
  - 対策1: 構文チェックは `fetch('/app-0N.js').then(t=>new Function(t))` を全8ファイルに回す（node不要でパースエラーを確実に検出）。
  - 対策2: 触ったコンポーネントは必ず**実際に描画して**確認する（DayViewは`{date,data,save,onBack,onSelectDate,cfg,initialTab,onOpenEntryLog}`の8propsだけで単独描画できる）。
  - 対策3: 金額が要る経路は`window._elHoldFinalParts`を一時差し替えすれば、v2シグナルを合成せずに通せる。
- 併せて実表で検証済み: 週合計＝`A+ +10,100円（A+ +9,400円）`／`1日 平均 A+ +3,367円（A+ +3,133円）`、日別行に平均行なし、ヘッダ`確損`/`損切`・タグ列なし、**バッジ左端549px・額左端573pxが2行とも一致**。

### 2026-08-05c 損益表の整理（1日平均の並び・タグ列撤去・列名短縮）＋ニュース「＋」枠の高さ（sw v340→v341）
- ユーザー要望6件をまとめて対応。
- **最終損益欄の「ノーシグナル／有効シグナルなし」チップを撤去**（app-04 `_wkRow`）。`_wkRow`の第6引数`tradeTags`と、集約元の`_wdTradeTags`（`chartShapeTags`から「取引:」を拾う処理）ごと削除。
- **補助行を`[ラベル][バッジ][額]`の3列gridへ**（app-04 `_wkStack`／app-02 `_wkPnlCell`）。旧は`[バッジ][ラベル][額]`のinline-flexで、行ごとにラベル幅が違うためバッジも額も揃っていなかった。gridにして**バッジ列・額列が縦ぞろい**（実測: バッジ左端31px・額左端55pxが2行とも一致）。「1日平均」は`_wkAvgLabel`で**「1日」「平均」の2行**（`<br>`）。
- **1日平均にも（）の参考額を追加**（ユーザー指摘「1日平均についても（）の金額は表示されないの？△の記録の件」）。本行の（）は`_elHold2RefSuffix`＝`main+ref`の**合計**を出すので平均行には流用できず、`_wkRefNode(Math.round((_h2Tot+_h2Ref)/_wkRowDays))`を新設した。
- **実現損益欄も同様**。`_wkPnlCell(grade, sum, sub, avg, avgGrade)`に引数追加＝`100株`行に加えて`1日平均`行を出す。この列のバッジも`_profitGradeFromPnlReal(総額÷_wkRowDays,…)`＝**1日平均で判定**しているので、最終損益と同じ理由で平均額を見せる。
- **タグ列を撤去**（app-04 `_wkRow`＋header／app-02 `_sumRow`＋header）。`_wkTags`は両ファイルとも呼び出し元が無くなったので削除。ついでに未使用化した`_wkAmt`も両方削除。**本日の損益データ(app-04 `_pbTh`)のタグ列は据置**（週/月の表だけという指示のため）。
- **列名を短縮**: 「確定損」→**「確損」**・「損切り」→**「損切」**（元の語はtitle属性に残す）。対象＝app-04（今週:件数列／本日の損益データ:件数列／取引テーブル`_trTh`／明細`_rTh`）・app-02（今週:件数列／`_esTh`／`_rTh`）・app-06（記録一覧`_th`）。**`損切り率`や`推奨損切り`など別の意味の列と、説明文・tooltipの本文は変更していない**。
- **月間データについて**: タグ列を持つ表を全ファイルで探したが、該当は今週の損益データ2つと本日の損益データだけで、**月間の表にタグ列は存在しなかった**（app-06の「全体損益（期間別）」の月別にもタグ列なし）＝月間側は変更なし。
- **ニュース「＋」枠の高さ**（app-03）: グリッドが`alignItems:"start"`なので＋枠だけ短かった。`alignSelf:"stretch"`＋内側`alignItems:"stretch"`で**同じ行の一番高い札に合わせて伸びる**（実測 120px→298px＝隣の札と一致）。＋が単独で行を占める場合は従来どおり`minHeight:120`。
- 検証: console 0。グリッドの縦ぞろいは同一CSSを再現して実測。＋枠は`NewsTab`を合成データで単独描画して高さ一致を確認。**今週の損益データ本体は合成データでの再現が重い（signalスキーマ依存）ため未描画＝実データでの確認が必要**。

### 2026-08-05b ニュース札の「タグ付け／記事を保存」を上段の帯へ移動（sw v339→v340）
- ユーザー要望「タグ付けボタンと記事を保存ボタンを上の三本線ボタンなどの行内に移動して。元の行に合うようにサイズも小さく」。
- **帯をflex1行に組み直した（app-03 `NewsTab`のカード・旧2026-08-03qの絶対配置を置換）**: 旧は☰/📅/🔗/✕をそれぞれ`position:absolute`（left4／right30／right56／right4）で置いていた。間の空きに可変幅のボタンを差すと**🔗クローンバッジ（件数で幅が変わる）と重なる**ため、`absolute`の親1つ＋中身flexへ変更し重なりを構造的に無くした。並びは`☰ / 🏷️タグ付け / 🔖記事を保存 / 🔗 / 📅 / ✕`（🏷️と🔖が`flex:1`、他は`flexShrink:0`）。カード側の`paddingTop:28`は据置。
- **サイズ**: 帯の22pxに合わせ`height:22 / fontSize:11 / padding:"0 6px"`。旧は`fontSize:12 / minHeight:IS_TOUCH?38:30`でカード下端に大きく2つ並んでいた。長い保存ラベルは`overflow:hidden`＋`textOverflow:ellipsis`で省略（全文はtitle属性）。
- **TagPickerに3つのoptional propを追加（app-02）**: `hideTrigger`＝開くボタン(と`trailing`)を出さない／`tagOpen`・`onTagOpenChange`＝開閉を親に持たせる。**3つとも未指定なら従来どおり内部stateだけで動く**ので他3か所の呼び出し（app-02:6110／app-07:1864／app-07:5051）は不変。
- **なぜ分離したか**: TagPickerは「選択済みタグのチップ」も描くが、チップは可変高で22pxの帯に入らない。そこでTagPicker本体は本文側に残してチップ表示とシート本体だけ担当させ、**開くボタンだけを帯に置いた**。開閉は`NewsTab`の新state`tagSheetNiId`（開いている札のid）で制御。旧2026-08-03iの`trailing`で横並びにする方式は廃止。
- 検証: `NewsTab`を合成データ（通常札＋保存済み札）で単独描画。帯は`☰ / 🏷️タグ付け / 🔖記事を保存 / 📅 / ✕`で**全項目22px・はみ出しなし・横スクロールなし**（カード幅242pxの狭い条件でも）。タグシートは開閉とも動作し**同時に開くのは1枚だけ**、保存シートも従来どおり起動。console 0。

### 2026-08-05 📅日替わりタブの2段表示＋週合計に「1日平均」（sw v337→v339）
- ユーザー要望2件。
- **(1) 日替わりタブ（app-03 `StockTabs`）**: 銘柄を指定するとラベルが「📅 日替わり」から「📅 SUMCO」へ丸ごと置き換わり、どのタブか分からなくなっていた。見出しを残したまま銘柄名を2段目に出す**新prop`rotDayStock`**を追加（app-03:1237-1268／受け渡しapp-04:5630）。`rotLabel`は常に「📅 日替わり」を渡す方式へ変更＝銘柄名は`rotDayStock`に分離。空/未定義なら従来の1行にフォールバック。**文字サイズは両段13pxで同一**（初版は見出し10pxにしたがユーザー指示で揃えた）。2段時はpadding上下8→2pxで**他タブと同じ37px**。prop名は既存`rotStocks`（候補プール・複数形）と紛らわしいため`rotDayStock`。
- **(2) 週合計の1日平均（app-04 `_wkRow`の最終損益セル）**: この表のグレードバッジは元々`_profitGradeFromPnl(総額÷_wkRowDays, 件数)`＝**1日平均で判定**しているのに表示は総額だけで、根拠が見えなかった（例: 総額+9,400円にA+だが判定の実体は3営業日で+3,133円）。最終損益額の下に`[バッジ] 1日平均 +3,133円`を追加。割る数はバッジ判定と必ず同じ`_wkRowDays`を使う。
- **`_wkRowDays`＝`_elBizDaysOf`＝記録の初日〜最終日の営業日数**（休場除く。取引が無かった中日も数える＝「記録のある日数」ではない）。ツールチップにも明記した。
- **出す条件**: `isTotal && _h2Cnt>0 && _wkRowDays>1`。日別行と単日週は平均＝総額で同じ数字が2行並ぶだけなので出さない。
- 検証: ローカルHTTPサーバでconsole 0。タブは未選択/未定義/選択中を実測（37/37/37px・両段13px）。グレードは画面の実データと突合し一致（月+5,600→A+／火+2,800→A+／水+1,000→B／週の平均3,133→A+）。

### 2026-08-03 🏷 銘柄別の損益割合を新設（全銘柄合算「📊集計」・sw v312→v313）
- ユーザー要望「エントリー記録帳で、銘柄別での損益割合分析も欲しい（JX金属／フジクラ／SBG／日替わり銘柄＝まとめて一つ）」→ 構想を詰めてから実装。
- **既存概念に乗せた**: 調べると日替わりは既に一級概念だった（`custom.rotatingStocks`＝候補プール・設定モーダルapp-04:2568／日別ページは「📅日替わり」タブ1つ／`data.dailyStock[日付]`＝その日の指定銘柄）。固定銘柄も`custom.epnStocks`（EPナビ⚙表示銘柄・最大3）がある。**銘柄名はハードコードせずこの2設定から導出**＝設定を変えれば分析も追従する（2026-07-23に古河電工を日替わりへ足したような変更が実際に起きている）。
- **日経平均株価は固定バケツの導出から必ず除外**: `epnStocks`未設定時の既定は「マスターの先頭3」（app-04:4508）だが、マスターの先頭が日経平均株価なので外さないと固定3枠が1つ潰れてSBG等が押し出される（app-04側の既定計算はこの除外を持っていない＝日替わり列の方だけが`s !== "日経平均株価"`を持っていた）。ただし母数に日経の記録が残っていたら「その他」で数える＝**どの銘柄も必ずどこかのバケツに入る**。
- **母数は`_v2recsAmt`＝上の「全体損益（期間別）」と同一**。そのため**合計行が必ず一致する**（実測でも純損益+1,000円・実現損益+1,900円が期間別表の合計と一致）。行は固定順（損益順に並べ替えない＝毎回同じ位置で読める）。
- **「割合」は利益と損失を分ける**（ユーザー決定）: 純損益の構成比にすると、マイナス銘柄があるとき負の%や100%超が出て積み上げ棒にできず読めない（例: +50,000/+30,000/−20,000/+10,000 → 71%/43%/−29%/14%）。そこで**トレード単位で符号を分け**「全体の総利益に占める割合」「全体の総損失に占める割合」を別々に出す＝どちらも0〜100%で閉じ、横積み棒2本がそのまま描ける。「利益の6割はJX・損失の6割はSBG」という銘柄を絞る判断に直結する読み方になる。
- **集計の単一源化**: `totOf`/`totExOf`/`stopsOf`/`winTakeOf` を `_ovPnlTbl` の中から EntryLogView スコープへ**引き上げ**（中身は不変・位置だけ移動）。銘柄別が同じクロージャを呼ぶので、期間別表と銘柄別表で除外条件がずれようがない。`keyOf`/`labelOf`/`_holiSet`/`_bizDaysIn` は `g` 依存なので `_ovPnlTbl` の中に残置。
- 新規トップレベル相当（EntryLogViewスコープ）: `_EL_IDX_STOCK`／`_STK_SHARE_FROM`／`_STK_SHARE_FROM_LBL`／`_stkShareRecsOf(rs)`／`_stkShareBuckets()`／`_stkShareAgg(x)`／`_STK_SHARE_COLORS`／`_stkShareSection(rs)`。列＝銘柄/件数/到達/勝率/総利益(内訳)/総損失(内訳)/純損益(PF)/1件平均/実現損益。総利益・総損失は`totOf`と**同じ除外規約**（時間かぶり除外・×見送り・main非null）で符号分け＝ Σ利益＋Σ損失＝純損益(hold2) が必ず成立。件数系は`stopsOf`/`winTakeOf`をそのまま使用＝期間別表の同名列と同じ数字。日替わり行は`_SNCollapse`で銘柄別内訳を展開。
- **母数を2026年7月以降に限定**（2026-08-03b ユーザー要望「今年の7月以降のみのデータにして」／08-03c に🎯グレード別の件数と共用するため `_EL_SINCE` / `_EL_SINCE_LBL` / `_elSinceRecs(rs)` / `_elSinceBadge()` へ改名・EntryLogViewスコープ先頭へ引き上げ）: `_EL_SINCE="2026-07"` で `_elSinceRecs` が `r.date.slice(0,7) >= ` で絞る。理由＝7月に集計ルール自体が変わっている（07-09 EP/H1損益を廃し「最終損益」1列へ集約・07-27 損切りの平均額を理想値→終値撤退の実額へ）ため、6月以前を混ぜると別基準の記録の寄せ集めになり割合として読めない。境界は`_elIsEmaRefPeriod`（4月＝EMA参考期間を外す）と同じく**定数で固定**（`今年`を動的に出すと年明けに母数が勝手に消える）。**この絞り込みでⓘ下記検証①の「合計行＝期間別表と一致」は意図的に成立しなくなった**（上の「全体損益（期間別）」は全期間のまま）。見出し右に`_secH`の第3引数でバッジ「📅 2026年7月以降のみ」を出して明示。呼び出し側の出し分けも`_sinceRecs.length`（絞り込み後）で判定＝7月以降が0件ならセクションごと出ない。
- 検証: 合成11記録（JX3/フジクラ2/SBG3/古河電工1/INPEX1/IHI1・`epnStocks`=JX,フジクラ,SBG・`rotatingStocks`=古河電工,INPEX）をlocalStorageへ注入し実ブラウザで実測。**①合計行＝期間別表と一致**（純損益+1,000円・実現損益+1,900円）**②Σ利益(+1,500/3件)＋Σ損失(−500/5件)＝純損益(+1,000/8件)** ③バケツ分けが設計どおり（IHI＝その他・古河電工/INPEX＝日替わり）④日替わり内訳の展開が動作 ⑤PF表示（0.00／5.00／∞）⑥console errors 0。※金額8件に対し件数11件なのは合成データの時刻を全件09:20にして時間かぶり除外が効いたため＝**件数系は除外前・金額系は除外後**という既存の期間別表と同じ挙動（この表だけの仕様ではない）。

### 2026-08-03c 🎯 グレード別の件数（月別）を新設（app-06.js・エントリー記録帳「損益→📊集計」・全銘柄時のみ）

ユーザー要望「1か月あたりの各グレードの件数が知りたい。A+何件、A-何件みたいな感じ」。🏷銘柄別の損益割合の直下に追加。

- 新規トップレベル相当（EntryLogViewスコープ）: `_EL_GRADES`（A+〜G+の9段・表示順）／`_gradeMonSection(rs)`。共通小物として `_elFinalPnlOf(r)` を新設。
- **`_elFinalPnlOf(r)` が除外規約の単一源**: 「時間かぶり除外(`_elCollExcluded`)・×見送り(`_epIsXSkip`)・`_elHoldFinalParts().main`非null」を1箇所に集約し、`_stkShareAgg` もこれを呼ぶよう置き換え（**判定内容は不変・位置だけの移動**）。おかげで「🏷銘柄別の 総利益件数＋総損失件数＋D(同値)」と「🎯グレード別の合計件数」が食い違いようがない。
- **表は2つ**（ユーザー選択「両方」＝「良いトレードが何件あったか」と「良い日が何日あったか」は別の話）:
  - **①トレード単位** … 記録1件の最終損益を `_profitGradeFromPnl`（**損益スケール** A+=2500円〜／D=0円／G+=−2500円〜）で判定 → 月別に件数。
  - **②日単位** … その日の最終損益**合計**を同スケールで判定 → 月に何日がA+だったか。単日は days=1 なので合計そのまま＝既存の「複数日は1日平均・単日は合計」規約（app-06の`pnlCell`・app-05:4811）と同じ。母数は**記録があった日だけ**（ノーシグナル日・全除外日は数えない）＝合計＝その月の稼働日数。
- **実現損益スケール（`_profitGradeFromPnlReal`＝カレンダーの日付バッジ `gradeByDate` app-05:99）は使わない**（ユーザー選択）: `realizedPnl` がほぼ未記録（全期間202件中フジクラの+1,200円のみ）で分布が作れないため。**カレンダーのバッジとは基準もスケール（10倍）も違うので数字は一致しない**＝見出し説明に明記済み。
- 表の形: 行=月（新しい月が上＝「全体損益（期間別）」と同じ並び）、列=A+〜G+の9列＋合計。セルは件数＋構成比%。セル背景に `_GRADE_STYLE[g].bg`（A側=赤系／G側=緑系）を敷き、**0件は白のまま**残すので分布の形がそのまま見える。ヘッダは `_elHoldGradeBadge` の丸バッジ＋`_GRADE_DESC` のしきい値＝凡例なしで読める。
- 母数は🏷と共通の `_elSinceRecs`（2026年7月以降）＝見出し右に同じ `_elSinceBadge()`。
- 検証: ①`_profitGradeFromPnl` の**しきい値17ケース**（3000/2500/2499/2000/1999/1000/999/1/0/−1/−999/−1000/−1999/−2000/−2499/−2500/−9999）が全て期待どおり、かつ**全出力が`_EL_GRADES`に含まれる**（Z は enteredCount=0 のときだけ＝本セクションは常に1以上を渡すので発生しない＝列漏れなし）。②`_gradeMonSection` の集計部を**同一コードのまま**合成9記録（6月1件＋7月5件＋8月3件）に適用し、`recM`/`dayM` が手計算と完全一致（7月=A+1/B1/C1/D1/F1・日別=A+1/F1/D1、8月=A+1/G+2・日別=D1/G+1）。**6月が期間フィルタで落ちる・新しい月が上・記録合計=母数8・日合計=実日数5・dayMの月⊆recMの月**の5不変条件が全てtrue。③console errors 0。
- ユーザー質問「KPIカードは最新の状態が反映されてる？」→ **数字の土俵はすべて追従していた**が、たどる過程で**別件の配線漏れ**を発見。
- **追従の確認（変更なし）**: 件数＝`_addFilOf`の母数／E到達数＝`_epResolve`のjudge／**一番引っ張った損益＝`_elTotAccum`→`_elHoldFinalParts`**（app-05:4537・2026-08-02nの終値撤退修正がそのまま入る）／**損切り件数＝`_elStopStatsV2`→`_elIsStopFinal`**（app-06:336・2026-07-25eで配線済み）／**推奨基本α・推奨応用α＝`_elBaseAlphaA`→`_elBaseAlphaPick`/`_elSpecialAlphaPick`**（Σ最大・オフセット0が自動反映）。集計タブの「最終損益」KPI早見も同じ`_elTotAccum`経由。KPIカードは独自計算を持たず全部が単一源を通るので取り残しは無い。
- **バグ（2026-07-22j以来の配線漏れ）**: 株価帯別モードでは詳細表にだけ帯基準の頻度分母(`bandSpan`=`_pbBandBizDays`)を渡していて、**KPIカード側の`_elBaseAlphaA(baRs, _ai)`は第3引数なし**＝記録全体のスパンで頻度ゲートを判定していた（分子も`_elEnteredDays`⇔`_elEnteredCells`で単位違い）。頻度ゲート`< _EL_FREQ_MAX`の通り方が変わるため、**同じ画面でKPIカードの「推奨基本α 8円」と詳細表の★が別の円**になり得た。`bandSpan`は`_groupPanel`の引数で同じスコープに居たのに未使用だった。
- **修正**: `_elBaseAlphaA(baRs, _ai, bandSpan)` の1行。`_elBaseAlphaA`は第3引数を`_elBaseAlphaPick`と`_elSpecialAlphaPick`の両方へ流すので、KPIカードの推奨基本α・推奨応用αが同時に詳細表と同じ土俵になる。銘柄別/詳細タグ別モードは`bandSpan`未指定＝`undefined`で、受け側が`spanOverride != null`で分岐するため**従来と完全に同一**。
- 検証: SW解除＋caches削除のうえ実ブラウザで、`_elBaseAlphaPick`/`_elSpecialAlphaPick`をスタブして`_elBaseAlphaA`の引数伝播を実測。**帯基準42を渡すと両pickへ42が伝播／未指定ならundefined（従来の記録スパン経路）**。arity=3・mounted・console errors 0。

### 2026-08-02n 最終損益の3段目以降の損切りを終値撤退方式へ追従（sw v310→v311）
- ユーザー質問「ここでの負け平均などの損失額分析は、最新の損切り概念に従ってる？」→ **1か所だけ従っていなかった**。
- **前提の確認（ユーザー要請「損切り件数は損切り値に達した件数という解釈？そこをまず確認して」）**: 違う。`_elIsStopFinal`＝`_elRideVals().stoppedLoss`＝**①手じまい足までに損切り値に達した ②かつその足の終値も損側** のAND（2026-07-27 終値撤退方式）。実測＝高値25でライン到達でも終値が建値より益側に戻っていれば**損切りに数えない**／ライン未達も当然数えない／2026-07-25dの修正で**期待度×で降りた後の足での接触は無視**／終値が無い記録だけ「達した事実」を優先して損切り側に倒す。app-05/06の26か所すべてがこの単一源を見ている。
- **バグ**: 金額側の正本`_elHoldFinalParts`の**3段目以降のループだけ旧規約**（`if (この足で損切り) break;`＝×と同一視して1段下の値のまま確定）が残っていた。ヘルパー`_elDynHoldAt`は2026-07-27に終値撤退へ直っていたのに、それを呼ぶ側のbreakが追従漏れ。結果、**「損切（H3）」と表示され損切り件数にも計上される記録の最終損益が前の足の値**になり、負け平均から抜けて勝ち平均に入っていた（実測: 損切りなのに+400円）。
- **修正**: breakの位置を変え、**その足も保有した扱いで`_elDynHoldAt(s,α,cut,d)`（＝その足の終値ベース）を採ってからループを抜ける**。main/refの振り分けは非損切りの足と同一規約のまま＝`d<=2`側(`_elHold2TotPartsFixed`→`_elHold1TotParts`)の挙動に完全一致させた。**△でチェーンが切れている記録は（）外が1段下のままなのも`d<=2`と同じ**（△規約は今回の対象外＝据え置き）。
- 併せて**勝ち/負け平均の母数の注記を実態へ**（ユーザー決定）: 2026-08-02eの同値0円算入以降、同値は`takeN`/`lossN`に入らない（`_elH2EvalByFn`が早期return）ので、**平均/中央/Σの母数(h2Cnt)より勝ち/負け平均の母数(takeN+lossN)がfillEqN件少ない**。計算は妥当なので変えず、基本α表・応用α表のnote2か所と`_elH2EvalByFn`のコメントに明記。
- 検証: SW解除＋caches削除のうえ実ブラウザで合成v2記録4本を実測（EP=OS1・α5・前提損切り15・4本目で判定）。**A**ライン到達＋終値損側→損切り計上・金額**−100円**（修正前+400）／**B**ライン到達だが終値益側→損切りに数えず・撤退はその足で**+300円**（修正前+400）／**C**ライン未達→最後の足まで伸びて**+500円**（修正前と同値＝回帰なし）／**D**△でチェーン切れ＋損切り→mainは1段下+400・refに−500の差（△規約どおり）。console errors 0。

### 2026-08-02m α詳細表の列整理＝「未達」撤去・利確/損切りを「件数＋率」の横並びへ（sw v309→v310）
- ユーザー要望（応用α詳細表のスクショを見ながら）「一番右の未達欄は不要」「利確率欄の名前を『利確』にして、利確件数・利確率が横並びで表示されるように」「損切り率欄も同様に」。**適用範囲＝α詳細表3つすべて**（ユーザー決定＝応用α・基本α・全記録の一律α。コードが「列構成は基本α詳細表と同一」を意図的に維持しているため、片方だけ変えると並べたときにバグに見える）。
- **新規トップレベル `_elCntRateCell(n, rateNode)`（app-06.js・共通セル節の先頭）**: 件数を9px薄字で左に、既存の率セル（`_elPctCell`/`_elStopRateCell`の色分けはそのまま）を右に置く`inline-flex`。**率セルの実装には一切触らない**＝色の閾値や"—"の扱いが2表で食い違う余地を作らない。件数は`_elH2EvalByFn`が元から返している`takeN`/`stopN`（新規集計なし・率と同じ分母＝E成立）。
- ヘッダ: 「利確率(最終)」→**「利確」**／「損切り率(最終)」→**「損切り」**／「未達」列は**削除**。基本α表側の先頭列は2026-08-02lの`poolAll`で「基本α/一律α」が切り替わるのを維持。
- **「未達」の情報は捨てずに行のtitle（ホバーのツールチップ）へ移設**: `_ng`（落ちたゲートの配列）は淡色判定`pass`に元から使っているので算出はそのまま、`<tr title="未達: 到達率が下限70%未満 / …">`として出す。読み取り欄の文言も「未達列＝…」→「淡色の行は…どの条件で落ちたかは行にカーソルを合わせると出る」＋「利確・損切りの各列は『件数 率』の並び（率の分母＝E成立）」へ追従（基本α表・応用α表の2か所）。
- `_elGateBadges`は**未配線として残置**（呼出0件・定義とコメントのみ）。列を戻すなら各表の`_cells`末尾に`_elv2Td(_elGateBadges(_ng))`とヘッダ"未達"を足すだけ、と関数の直上に明記した（`_elAddAlphaDetailV2`と同じ残置の作法）。
- 検証: SW解除＋caches削除のうえ実ブラウザで、`_elBaseAlphaPick`/`_elSpecialAlphaPick`をスタブして両表を実描画し**thead/tbodyを直接読み出して実測**。①ヘッダ＝`[…,"利確","損切り","最終損益(平均/中央/Σ)","勝ち/負け平均","スコア"]`で**2表とも完全一致・"未達"なし** ②応用α表の1行目セル＝**「8件 72%」「2件 18%」**（件数＋率の横並び・E成立11件と整合）③行title＝「未達: 頻度が5営業日/回以上（低頻度すぎ）」④console errors 0。

### 2026-08-02l 分類「全記録」の詳細データ表を“基本・応用まとめ”へ（sw v308→v309）
- ユーザー指摘「分類を全記録にした際の詳細データ表、違ってない？分類を全記録にしている以上、基本・応用関係なくまとめた表にすべきでは？」→**そのとおりだった**。分類トグル(`osDistFil`)は3択（全記録/基本α/応用α）で、KPI・OS分布・損切り・未達・記録一覧は`_addFilOf`で母数が切り替わるのに、**詳細データ表だけは`_baRecs`（トグル非適用の固定母数）＋表の内部で`_elIsBaseAlphaPoolRec`（応用〇・浮き足〇・RN〇を除外）**が掛かっていた。基本α→◯／応用α→応用α表が出る◯／**全記録→基本α表しか出ない✕**＝3択で全記録だけ母数がズレる唯一の分岐だった。
- 方針＝**基本α表はトグル連動にしない**: `_baRecs`固定は「推奨基本αはフォーム/EPナビ/シミュへ流れる値だからタブや母数トグルでブレさせない」という設計意図（app-06:6745のコメント）で、これ自体は正しい。連動させると**フォームに流れる推奨がトグル操作で動く**。そこで**全記録のときだけ“一律α表”へ差し替える**（ユーザー決定＝差し替え／★は出す／母数は文字どおり全記録）。応用α分岐が基本α表を畳んで応用α表を出すのと対称。
- 実装は**表を二重定義せず、母数フィルタを外すフラグ1本**: `_elBaseAlphaPick(recs, aiOf, spanOverride, poolAll)`＝`poolAll`で`_elIsBaseAlphaPoolRec`の絞り込みをスキップ。`_elBaseAlphaDetailV2(..., spanOverride, poolAll)`＝`_baseRecs`も同様に無絞り込み。`_elBaseAlphaA`（応用αの併記）と`_elBaseAlphaPickScore`（旧基準チップ）は**呼ばない**＝素の記録が母数の値と土俵が違い、並べると別物同士の比較に見えるため。列・ゲート・★選定ロジックは基本α表と完全に同一（母数だけが違う）。
- 表示差分（poolAll時のみ）: 結論バー「推奨基本α」→**「全記録の一律α」＋“参考・この母数だけ”バッジ**／表の先頭列「基本α」→**「一律α」**／読み取り「採用α」→「★の一律α」／noteに「加算あり(浮き足〇/RN〇)の記録も一律αに置き換わる＝**加算ルールを使わず一律αだけで通していたら**の答え。★はこの母数だけの参考値で、フォーム/EPナビ/シミュへ流れる推奨基本αは分類『基本α』の値＝ここでは変わらない」を明記。
- **死にコードの再生**: `_exAddN/_exUkiN/_exRnN`（2026-07-13「⑤透明化」で母数内訳を出すために作られたが、どこにも描画されないまま残っていた）を使い、全記録モードで**「母数の内訳: 全記録 N件（うち応用〇 x件・浮き足〇 y件・RN〇 z件）」**を表の上に出す。何が混ざっているかが読み取りの前提になるため。
- 配線: 集計パネル(`_groupPanel`)の分岐に`osDistFil === "all"`を追加（`!_floatMode`条件は応用α分岐と同じ＝前足浮きタブは従来どおり基本α表）。α値タブ(app-06:7195)は基本α/応用αのゾーン構成で「全記録」の概念が無いため対象外。
- 検証: **SWを解除しcachesを削除してから**実ブラウザ実測（前タスクでsw v308に上げた直後だったため、そのままでは旧app-06.jsが配信され arity が古いままだった＝**版数を上げた後に編集を続けたら再検証前にSW解除**）。①arity 4/7 ②`_elH2EvalByFn`を差し替えて母数実測＝**通常2件（素の記録のみ）/ poolAll 5件（全記録）** ③`_elBaseAlphaPick`をスタブして全描画パスを通し、見出し「全記録の一律α」・参考バッジ・内訳行「全記録 5件（うち応用〇1件・浮き足〇1件・RN〇1件）」・「★の一律α」を実測 ④**従来モードの回帰なし**（見出し「推奨基本α」・一律α表記/バッジ/内訳行なし・「採用α」）⑤console errors 0。

### 2026-08-02k 理想αを「累計重視」へ＋指値同値マージン(推奨＝理想−1)を撤廃（sw v307→v308）
- ユーザー「指値同値を排除した以上、推奨値を理想値からー１するのはもう不要かな？」＋「理想値の基準を変えたい。これまでより累計額を重視したい」。**選択＝オフセット0／Σ最大（同点は平均）／全スイープ統一**（AskUserQuestionで3点確認）。
- **Ⓐ`_EL_ALPHA_OFFSET` 1→0（app-06.js）**: 2026-08-02eで同値を**全掃引の評価に0円算入**した時点で「刺さりにくさ」は各αの平均/Σに価格として入っており、そこへ一律−1円を引くのは**二重計上**。さらにオフセット>0だと**「最良と判定したα(理想)」と「実際に置くα(推奨)」がズレる**（`_finish`は`recA=理想−off`の統計を返す）＝最適だと判定した値を使っていない状態だった。0で推奨＝理想。**計算経路（`max(0,理想−_EL_ALPHA_OFFSET)`）は温存＝定数を戻せば復活**。
- **Ⓑ理想αの選定基準を平均最大→Σ最終損益（累計）最大へ（`_elBordaBest`本体1行）**: 比較順を`(平均→Σ→α小)`から**`(Σ→平均→α小)`**へ入れ替え。ゲート（黒字・到達率≥下限・損切り率(最終)≤上限・E成立≥`_EL_BASE_MIN_N`・頻度<`_EL_FREQ_MAX`）は不変＝**候補集合は同じで順位付けだけ変更**。Σと平均は連動しない（**未到達は`h2Cnt`から落ちる**＝高αほど件数が減る）ので、Σ主軸の理想αは平均主軸より**低め＝件数を取る方向**に出る。旧＝平均最大(2026-07-15f)／その前＝順位和(2026-07-15e)。
- **適用範囲＝全スイープ統一（ユーザー決定）**: `_elBordaBest`を共有する**基本α・応用α・追加α・浮き足%・浮き足円版**に加え、独自のインライン★選定を持っていた**RN加算ボード(`_elRnBoardV2`)・RN閾値スイープ(`_elRnThresholdBoardV2`)**の`_gated.sort`も同じ並び（Σ→平均）に揃えた＝どのボードでも★の意味が「累計最大」で一致。
- **新規トップレベル `_elAlphaOffTxt(unit)`（app-06.js・`_EL_ALPHA_OFFSET`直後）**: 「推奨＝理想−N円」注記の断片を返す純関数。**オフセット0なら空文字**＝「−0円」という無意味表示を出さずに済み、1以上に戻せば全文言が自動で復活する。表示文言5か所（🎯到達率ピル説明・応用α表note/ラベル・期間別note・α詳細表ラベル）に配線。`(ideal !== a)`でガード済みの「（理想 8円 −1）」バッジ3か所は**0で自動的に消える**ので無改修。
- 文言の追従（app-06.js 14か所＋app-04.js 1か所）: 「平均最終損益（1件あたり）が最大」→「Σ最終損益（累計）が最大」。KPIカードのバッジ`"平均最終損益 最大"`→`"Σ最終損益 最大"`。応用αの結論インサイトに**Σ最終損益を追記**（選定基準の値が画面に出ていなかったため／平均も併記のまま）。app-04.jsはフォーム/EPナビ側の推奨α説明。
- 検証: ローカルHTTP(3457)＋実ブラウザ。**console errors 0・mounted OK**。実関数`_elBordaBest`に合成候補`[{α3,Σ1000,平均50},{α8,Σ900,平均90},{α5,Σ1000,平均70},{α0,Σ1000,平均70}]`→**α0を返す**（Σ最大3件→同点は平均大(70)→さらに同点はα小）＝Σ主／平均タイブレーク／α小の3段が期待どおり。`_EL_ALPHA_OFFSET===0`・`_elAlphaOffTxt("円")===""`も実測。※この端末のlocalStorageに記録が無いため、理想αが実データで何円動くかはユーザー環境で確認。

### 2026-08-02j 🧮シミュの母数を「株価帯」でも絞れるように（sw v306→v307）
- ユーザー「ここ（シミュの対象）を株価帯・銘柄から選べるようにして」。既存は`対象銘柄:`ピルだけだったので、**`対象株価帯:`ピル行を新設**（app-06 `_elKabuLadderSimV2`）。
- **銘柄フィルタとは独立のAND**にした（軸の排他切替にしなかった理由＝ANDなら「銘柄=全て＋帯を選ぶ」で軸切替と同じ使い方ができる上位互換で、かつ既存UI（分類/除外/RN自動加算の行が縦に積まれる形）に素直に収まるため）。両方「全て」なら従来と完全に同じ母数。
- 実装: 新state`bandFil`（既定`__all__`）。帯判定は`_pbDayBandOf(data, stock, date)`＝💴株価帯別タブ・浮き足%の帯ピルと**同一源**（手動選択＞前日終値の自動）。キーは`b0..bN`/`mat`(⚡固有材料日)/`unk`(帯不明)で`_pbSplitByBand`と同じ規約。(stock|date)でメモ化。
- 絞り込みは**既存の`_stkRecs`に乗せた**＝下流（pool・_stkN・_origPnl・_kbStockTable・時間かぶり除外・自動配分）に一律で効く。`_autoSig`（自動配分メモの署名）にも`bandFil`を追加＝帯を切り替えたら再計算される。
- UIは**帯が2種類以上あるときだけ表示**（1種類なら絞る意味がない）。**銘柄別モードでも出す**＝同じ銘柄でも日によって帯が変わるため。ピルには件数を併記。
- 検証: 合成24記録（JX金属14＋フジクラ10・帯を`dayPriceBand`で明示＋⚡材料あり日2件）で実ブラウザ実測。①帯ピルの件数＝〜4000(8)/4001〜5000(9)/5001〜(5)/⚡材料あり(2)＝**投入分布と一致**（計24）。②絞り込み＝帯〜4000→8件・帯4001〜5000→9件。③**銘柄とのAND**＝〜4000×JX金属→5件・4001〜5000×フジクラ→5件。④銘柄別モード（JX金属タブ）でも帯行が出て 5/4/3/2 の内訳・5001〜→3件。⑤⚡材料あり×フジクラ＝**0件**の組み合わせも既存の「この母数に該当する記録がありません」で受け止め、ピルは選択可能なまま＝復帰できる。console 0。

### 2026-08-02i α値欄に「RN補正」バッジ（応用αの根拠がRN補正の記録／sw v305→v306）
- ユーザー「こういう表のα値欄、応用αの根拠がRN補正の場合はα値の下に『RN補正』と表示されるようにして」。
- **app-05に`_EL_RN_ADJ_REASON="RN補正"`と`_elIsRnAdjReason(s)`を新設**（`_DEF_SPECIAL_REASONS`の直後）。判定＝`_elSpecialUsed(s)`かつ`s.specialReasons`（ユーザー定義の根拠名・配列/null）に該当名を含む。**応用α〇の記録だけが対象**＝基本α・浮き足〇には出さない。
- 描画は**`_elAlphaTypeCell`（app-05.js）1箇所**に追加＝この関数はapp-02(2表)/app-04(2表)/app-06(3箇所)の**計6〜7の記録表で共用**なので、全表に一度で入る。位置は値（＋RN内訳）の下・「併存」バッジの上。色はRN内訳と同じ青系（#1D4ED8/#EFF6FF）で系統を揃えた。
- **RN加算(`_elRnAdd`)とは別概念**＝加算そのものではなく「そのαにした理由がRN補正だった」ことの表示。両方ある記録は「計8円（応5 ＋RN3）」の下に「RN補正」が付く。
- ⚠️ **既知の弱点**: 根拠マスターで「RN補正」を改名すると記録側の`specialReasons`は追従する（app-04 `_rsnRename`）が**この定数は追従しない**ので、改名時はここも直す必要がある。コメントに明記済み。
- 検証: 合成5記録（①応用α×RN補正 ②応用α×指標線支え ③基本α ④応用α×RN補正＋RN加算 ⑤浮き足〇×RN補正）で実ブラウザ実測。記録一覧のα値セルが ①「応用/8円/**RN補正**」②「応用/8円」③「基本/8円」④「計/8円/（応5 ＋RN3）/**RN補正**」。⑤は`_elIsRnAdjReason`=false＝**浮き足〇には出ない**ことを述語直呼びで確認。console 0。

### 2026-08-02h 浮き足%・RN加算ボードにも指値同値を配線（適用範囲を全スイープ系へ拡大／sw v304→v305）
- ユーザー「揃えたい」＝08-02e〜gで α詳細表2つだけに入れた同値の扱いを、**浮き足%スイープ／浮き足α値スイープ／RN加算ボード／RN閾値ボード**にも広げる。モデルは同一（同値＝そのαなら取引していない→到達/E成立/損切り/利確から外し、損益だけ0円で母数に残す）。
- **配線（すべて`_elH2EvalByFn`の第4引数true＋`_elEnteredDays`の第3引数aiOf）**:
  - RN加算ボード①EP位置スイープ・③RN距離別／RN閾値ボード①閾値スイープ・②距離別の限界寄与（**両側そろえる＝相互検算が崩れない**）。
  - RN②寄与の内訳は`_elH2EvalByFn`を通らない専用ヘルパー`_pnlAt`なので、そこに`_elFillEqAt`→**nullを返す**分岐を追加＝呼び出し側が未到達/未決着と同じく「そのαでは建っていない」として cfOnly/realOnly に振り分け、差額は`(pr||0)-(pc||0)`で0円算入になる（既存のnull規約にそのまま乗る）。
  - 浮き足%スイープ`_elUkiPctSweep`・浮き足α値スイープ`_elUkiValSweep`。**★推奨（best/runnerUp）もこの評価から選ぶので、フォーム/EPナビへ流れる推奨浮き足加算率(`_elUkiRecoPcts`)も同値を踏まえた値になる**。
- **「同値」列を4つの表すべてに追加**（RN①EP位置＝E成立の右／RN閾値①＝E成立の右／浮き足%・円版＝到達率の右）。描画コアは`_elUkiSweepNodeCore`が%版と円版で共通なので1箇所の追加で両方に出る。
- **今回も対象外**: `_elAddAlphaDetailV2`（未配線のデッドコード・app-06.js:2592のコメント参照）と集計KPI早見の頻度（app-06.js:6392）。KPI早見は実績表示で、頻度だけ同値を外すと同じカード群のE到達数と食い違うため据置。
- 検証: 合成34記録（RN〇16件＋浮き足〇18件）で実ブラウザ実測。
  - **RN①EP位置スイープ**: 「RNちょうど（現行）」が**同値5件で最多**（投入した osMax=10 × 5件＝採用α10と一致）・E成立8件/到達率50%、★が RN−1（同値1件）へ。**RNはEPをキリ番ちょうどに置く運用なので同値が集中する**という現象がそのまま列に出た。
  - **浮き足%スイープ**: 実効α=floor(6×P/100)=0〜6 に対し **P=50/60(α=3)→4件・P=90(α=5)→3件・P=100(α=6)→2件・P=40(α=2)→1件・P=70/80(α=4)→1件** ＝投入分布と完全一致。頻度も0.8→1.1→1.6→2と連動。
  - ※途中、浮き足%表の同値が全行「—」になったが**実装ではなくテストデータ側の問題**だった: 浮き足〇記録は`migrateData`で`alphaVal=浮き足加算+RN`（土台α=0・app-05.js:3231の仕様）に正規化されるため実効αが0〜6円になり、OS値を8〜15で作っていた初版データでは同値が発生しえなかった。OS値を0〜6帯に作り直して確認。
  - 表幅: 列追加後の自然幅は EP位置584px／閾値583px／浮き足%538px／浮き足α値532px（表示枠1042px）で全表とも余裕あり・横スクロール0。console 0。

### 2026-08-02g スコア列を最終基準へ（旧H1基準を撤去・これでα詳細表の全列が同じ土俵に／sw v303→v304）
- ユーザー「スコア列の、旧H1基準とは？」→説明のうえ「直して」。**旧スコアは`_elBaseAlphaEval`（=`_elAlphaEvalByFn`の固定α版）の`score`**で、式は同じ`0.7×(1−損切り率)+0.3×勝率`ながら中身の定義が2つとも古かった:
  1. **見る範囲がEP足とH1足まで**（`_elDynHold`/`_elPlanIsStop`/`_elHoldIsStop`）。他の列は手じまいまで（○が続く限りOS5まで）。
  2. **ラインに高値が触れたら損切り**（`_elHoldIsStop`＝`(h−α)>=cutLine`）。他の列は終値撤退方式の「損で撤退したか」（`_elIsStopFinal`→`_elRideVals().stoppedLoss`・2026-07-27移行）。
- **修正**: 基本α表は`e.score`→`_h2r.score`、応用α表は`_h1.score`→`e.score`（応用表は主スイープ自体が`_elH2EvalByFn`）。どちらも`_elH2EvalByFn`の`score`＝`0.7×(1−損切り率(最終))+0.3×利確率(最終)`。**★選定はスコアを使っていない**（★＝全条件を満たすαのうち平均最終損益が最大）ので**推奨α・理想αは動かない**＝行の優劣の見え方だけが正しくなる。
- 応用α表の`_h1ByA`（`_elBaseAlphaEval`を全α分だけ並走させていた）は**唯一の利用者が消えたので撤去**＝α総当たり1本ぶんの計算が丸ごと減る。`_aiAna`は頻度列の同値除外（08-02f）で引き続き使うので残す。
- **これでα詳細表2つの全列が最終損益（手じまい・終値撤退）基準で揃った**。08-02b以来の「スコアだけ旧基準」は解消。
- 検証: 合成46記録（素28件＋応用〇18件）で実ブラウザ実測。①基本α表の全17行で表示スコア＝`0.7×(1−損切り率(最終))+0.3×利確率(最終)`と一致（1円行だけ検算が71・表示72とズレたが、これは**検算側が表示済みの丸め%を使ったため**で、生値0.7179→72が正しいことを評価関数を直接叩いて確認）。②応用α表は全行で`_elH2EvalByFn(...,true).score`と完全一致（不一致0件）。③E成立0件の行はスコア「—」。console 0。

### 2026-08-02f 頻度列・頻度ゲートからも指値同値を外す（08-02eの積み残し／sw v302→v303）
- ユーザー「頻度列らへんも修正して」。08-02eで到達/E成立/損益からは同値を外したが、**頻度の分子（到達実日数）だけ同値を除いていなかった**＝同値だけの日も「到達した日」に数えて頻度が高頻度側に出ていた。
- **`_elEnteredDays(recs, alphaOf, fillEqAiOf)` / `_elEnteredCells(...)` に第3引数を追加**（opt-in・未指定＝従来どおり）。渡すと`_elFillEqAt`で同値記録を到達から外す。**同値だけの日はどの記録も到達しないので日付キーが立たない＝分子から正しく落ちる**。
- **渡すのは4箇所だけ**: `_elBaseAlphaPick._freqOk`／`_elSpecialAlphaPick._freqOk`（★の頻度ゲート）／基本α詳細表の`_ed`／応用α詳細表の`_ed`（頻度列の表示）。**RN加算ボード(1708/1865)・`_elAddAlphaDetailV2`(2234/2694)・浮き足%スイープ(3335/3404)・集計KPI早見(6379)は未指定＝従来どおり**（適用範囲はα詳細表2つ・08-02eと同じ線引き）。
- これで**表の頻度列と★の頻度ゲートが同じ値**になり、E成立・到達率・損益・頻度がすべて「同値＝取引していない」で揃った。残る旧基準はスコア列のみ（08-02b以来ユーザー決定で据置）。
- 検証: 「同値だけの日」を意図的に作った合成15記録（1日1記録・α=10で同値のみの日×5＋通常到達日×10）で実測。①`_elEnteredDays`/`_elEnteredCells`とも α=8: 15→15（同値0件）／**α=10: 15→10**（同値5件ぶん減）／**α=15: 10→0**（全10日が同値）＝`fillEqN`と過不足なく一致。②表の頻度列も α≤9行=0.7・**α=10行=1.1**（活動11営業日÷到達10日）・α=15行=「—」。③α=15行の未達バッジが「到件頻損赤」＝**頻ゲートも正しく落ちる**。console 0。

### 2026-08-02e 指値同値を損益・理想・推奨にも配線（0円算入・α詳細表2つ／sw v301→v302）
- ユーザー「ここでは指値同値を踏まえた損益額・スコア・理想・推奨になってる？」→ **なっていなかった**（08-02bは表示専用の独立列にとどめた方針）。AskUserQuestionで**「0円で算入して繋ぐ」＋「到達・E成立には数えない」**を確定。
- **モデル**: 同値＝「そのαなら指値が刺さらず取引していない」。**到達(entered)・E成立(decided)・損切り率・利確率からは外し、最終損益だけ0円で母数(h2Cnt/h2Vals)に残す**＝🔁応用α換算の「無エントリー0円算入」と同規約。結果、平均最終損益＝**「そのαを置いた1機会あたりの期待値（刺さらなかった分も含む）」**になり、これが理想α選定の土俵になる。
- **実装**: `_elFillEqCountAt`（08-02bの件数関数）を**単一記録の述語`_elFillEqAt(s,item,a,adoptA)`へ置換**＝評価と表示の単一源。`_elH2EvalByFn`に**第4引数`fillEqZero`**を追加し、trueのとき同値記録を`fillEqN++; h2Cnt++; h2Vals.push(0); return;`で早期returnする。返り値に`fillEqN`を追加＝**同値列は評価が数えた件数をそのまま表示**（二重計算しない）。
- **trueを渡すのは3箇所だけ**: `_elBaseAlphaPick`のfull（★選定・基本α）／`_elSpecialAlphaPick`のsweep（★選定＋応用α表の表示・同一オブジェクト）／基本α詳細表の`_h2ByA`（表示）。**浮き足%スイープ・RN加算ボード・`_elAddAlphaDetailV2`は未指定＝従来どおり**（適用範囲はα詳細表2つ・ユーザー決定）。
- 基本α表の**到達率列を`e.eRate`（H1評価）→`_h2r.eRate`へ**＝★ゲートと同じ評価源に統一。配線前は両者一致していたので、この変更単体では見え方は変わらない。
- **既知の未対応**: 頻度列と頻度ゲートの分母（`_elEnteredDays`/`_elEnteredCells`）は同値を除いていない＝同値だけの日もまだ到達側に数える。`_elEnteredDays`は他所でも使う共有関数なので今回は触らず。スコア列は08-02bから引き続き旧H1基準のまま（ユーザー決定）。
- 検証: 合成46記録（素28件＋応用〇18件・OS最大を意図的に偏らせた分布）で実ブラウザ実測。
  - **評価関数の直接比較**（同一プールでfillEqZero false/true）: α=8/10/12/14 の各点で ①`fillEqN`＝手計算の同値件数と一致（6/6/4/2）②`entered`・`decided`が同値件数ぶんちょうど減る（24→18/16→10/9→5/4→2）③`h2Sum`が同値記録の実損益ぶんちょうど減る（−4,200/−4,200/−2,800/−1,400）④**`h2Cnt`は不変**＝0円で母数に残っている、を全点で確認。
  - **表の描画**: 基本α表のE成立・到達率・同値・平均/Σが上の評価値と完全一致。E成立0件/同値1件の行（16円）も0円表示で破綻なし。応用α表も同値列が投入分布と一致（9円5件・11円3件・13円3件・7円2件）。
  - **★の移動**: 同じ母数で理想αを新旧両方式で計算 → **旧8円（到達86%・平均+429円）→ 新7円**。8円は同値6件を外して到達率64%＝下限70%割れで脱落。推奨は理想−1で7円→6円。応用αも理想8円・推奨7円。console 0。

### 2026-08-02d 記録帳のヘッダーに「📊 銘柄別記録」導線を新設（sw v300→v301）
- ユーザー「記録帳から入れるようにして」＝08-02cでホームのボタンを撤去し入口がニュース一覧1本になったので、記録帳側に入口を作る。
- **app-06 `EntryLogView`で`onSelectStock`を受け取るようにした**（`_ref_elv2.onSelectStock`・従来はapp-08から渡っていたのに未destructureで死にpropだった）。ヘッダー行の右端（＋新規の左）に青系ボタンを追加。
- **銘柄タブを選んでいるときだけ表示**（`onSelectStock && !_isAllStock && !_isSigTotal && _selStock`）＝💰損益／📡シグナル総合では銘柄が定まらないので非表示。色はホームにあった旧ボタンと同じ`#EFF6FF`/`#BFDBFE`/`#1565C0`＝同じ行き先だと分かるように。
- 動線: 押す→app-08の`onSelectStock(銘柄)`が`sn_shv_stock_v1`に銘柄を控えて`setShowEntryLog(false)`＋`setShowStockHistory(true)`→`StockHistoryView`が**mount時のuseState初期化子**（app-07.js:3992）でそのキーを読んで選択銘柄に合わせる。
- **既知の制限（未対応）**: app-07.js:3992は`allStocks.indexOf(v) >= 0`で弾くので、**マスター(custom.stocks)に無い銘柄**（記録帳のticker listは記録側にしか無い銘柄も`extras`として拾う）でジャンプすると`allStocks[0]`にフォールバックする。通常運用の銘柄はマスターにあるので実害は想定薄。
- 検証: 実ブラウザ（合成16記録・JX金属8件/フジクラ8件）。①💰損益タブと📡シグナル総合ではボタン非表示・銘柄タブでのみ表示を確認。②フジクラ→押す→`StockHistoryView`が開き`sn_shv_stock_v1`="フジクラ"。③JX金属で再実行→ビューの銘柄`<select>`の値が"JX金属"＝**選択銘柄が引き継がれる**。④ブラウザの戻るで記録帳へ復帰。console 0。

### 2026-08-02c ホームの「📊 銘柄別記録」ボタンを撤去（sw v299→v300）
- ユーザー「ここの銘柄別記録ボタンは不要」。app-08 `App` のホーム見出し行から該当ボタン（`setShowStockHistory(true)`）だけを削除。state・`StockHistoryView`本体・履歴トークン`stockhistory`はすべて残す。
- **撤去後の入口は「ニュース一覧の記事内・銘柄チップ」1本だけ**（app-07 `NewsHistoryView` の`onJumpToStock`→app-08.js:1199で`setShowStockHistory(true)`）。
  - **`onSelectStock`（app-08.js:1162でEntryLogViewへ渡している）は死にprop**＝app-06のEntryLogView側が一度も読んでいない（grep済み）ので入口にならない。「記録帳の銘柄タップから入れる」は誤り。
  - 履歴トークン`"stockhistory"`は`applyHistoryToken`（pushStateのstate駆動）で、URLハッシュからは開けない＝戻る/進むでの復元専用。
  - app-04.js:5475の`onJumpToStock`は日別ページ内の銘柄チャートタブ切替で別物。
- 検証: 実ブラウザでホームのボタン列が［今日／📖エントリー記録帳／📰ニュース一覧／📝メモ・アイディア一覧／＋予定／🔍／⚙️］になり銘柄別記録が消えたことを確認。console 0。

### 2026-08-02b α詳細表に「同値」列（指値同値）を新設＋推奨αマージンを 理想−2 → 理想−1（sw v298→v299）
- ユーザー「こういうデータについて、指値同値を考慮するようにしたい」「あと、損切りの概念も以前変えたから、そこについても考慮したい」。AskUserQuestionで4点確定。
- **損切りの棚卸し（結論＝スコア列以外は移行済み・今回は触らない）**: 損切り率(最終)・利確率(最終)・最終損益・★/理想の判定は`_elH2EvalByFn`→`_elIsStopFinal`→`_elRideVals().stoppedLoss`＝**終値撤退方式（2026-07-27）に移行済み**。**スコア列だけ`_elBaseAlphaEval`（H1まで・ライン接触で負け＝旧概念）**のままで同じ行の他の列と土俵が違う。実測: 損切り6%/利確71%の行は最終基準なら87だが表示88、損切り17%/利確67%の行は最終基準78に対し表示71＝**行の優劣（3円と5円）まで入れ替わる**。`_elH2EvalByFn`は最終基準scoreを既に返している（app-06.js:1633）が表は`e.score`（app-06.js:2554）を出している＝直すなら参照の付け替えだけ。**ユーザー決定＝今回はスコア列に触らない**（★選定にスコアは不使用なので実害は表示のみ）。→ 直す場合の未処理項目としてここに記録。
- **指値同値（新規）**: 判定の正本`_elFillRisk`(app-05)は「その記録の採用α」基準で、α総当たり系には意図的に非配線（同関数のコメントで明文化）。掃引αごとに判定が変わるので**新関数`_elFillEqCountAt(recs, aiOf, alphaOf)`＋セル`_elFillEqCell(n)`をapp-06に新設**（`_elH2EvalByFn`の直後）。判定＝`_elOsMaxAll(s)`（OS1〜3高値の最大・アウトカム盲目）が掃引αとちょうど一致＝予定EPに触れただけで一度も上抜けていない。株数シミュの`_fillEqN`と同規約で「採用αと掃引αが一致し、かつ実約定済み(`_elIsEntered`)」は約定の証拠につき数えない。
- **配線＝到達率の右に独立列「同値」**（ユーザー決定＝「同値除く」で既存の数字を書き換えるのではなく素の件数を出す）。基本α詳細表`_elBaseAlphaDetailV2`・応用α詳細表`_elTotalAlphaSectionV2`の2つ（列構成が同一なので両方）。**到達率・E成立・損益・★選定には非配線＝推奨αのロジックは不変**。0件は薄いダッシュ・1件以上は指値同値バッジ(`_elFillRiskNode`)と同じ緑。※浮き足%スイープ・RN加算ボードは同じ列立てだが今回の適用範囲外（ユーザー決定）＝列構成がここだけ1列多くなる。
- **`_EL_ALPHA_OFFSET` 2→1**（ユーザー決定）: 推奨α＝理想α−1。同値リスクをαごとに数字で読めるようになったので一律マージンを薄くする。※このオフセットは**αを下げてフィルしやすくする**マージンなので、値を小さくすると推奨αは理想αに近づく＝**同値リスクは増える方向（保守化ではない）**。この向きを確認したうえでのユーザー決定。表示文言はすべて`_EL_ALPHA_OFFSET`を参照しているので「（理想 8円 −1）」等は自動追従。推奨αはフォーム/EPナビ/本日採用α/シミュへ流れるため、実際に置く指値が1円上がる。
- 検証: 合成40記録（素24件＋応用〇16件・OS最大を意図的に偏らせた分布）で実ブラウザ実測。基本α表の同値列＝5円1/6円2/7円1/8円5/9円2/10円4/11円1/12円3/13円1/14円2/15円1/16円1件＝**投入分布と完全一致**（0〜4円は該当なしで「—」）。応用α表も6円1/7円2/8円1/9円4/10円1/11円2/12円1/13円3/14円1件で一致。推奨表示は「推奨基本α 7円（理想 8円 −1）」「推奨応用α 8円（理想 9円 −1）」＝オフセット1が効いている。**理想行の同値が最多（基本8円=5件・応用9円=4件）で推奨行は1件**＝オフセットの狙いが列で可視化できている。表幅＝自然幅649px（表示枠1042px）で余裕あり・ページの横スクロール0。console 0。

### 2026-08-02 📡シグナル総合に各シグナルのタブを新設（RN加算の右／sw v297→v298）
- ユーザー要望「シグナル総合タブの中、RN加算より右に各シグナルタブも欲しい。各シグナルの中で株価帯別でこれまでと同様の分析内容が見れる感じ」。AskUserQuestionで4点確定＝**①フラット＋区切り線 ②帯ピル先頭に「すべて」 ③全シグナルを件数降順 ④選択帯(bandSel)は💴株価帯別タブと共有**。
- **タブ生成（app-06 EntryLogView）**: `_SIG_TAB_DIV`（センチネル`"__sigdiv__"`）と`_sigTotTabs`を`_sigAxisGroups`直後に新設＝`_SIG_TABS`＋区切り＋`_sigGroupsAll.map(g => ["sig:"+g.key, g.label, g.recs.length, true])`（件数降順は`_buildSigGroups`の並びのまま）。タブバーの`_isSigTotal?_SIG_TABS:_tabs`を`_isSigTotal?_sigTotTabs:_tabs`へ。区切りは幅1pxの`<div>`（`alignSelf:stretch`）で、mapの先頭で早期returnして描く。4要素目`true`＝シグナルタブの印で、**絵文字剥がし（先頭スペース区切りのslice）を通さない**＝シグナル名にスペースが入っても切れない。件数バッジ付き・`_EL_BASE_MIN_N`(10)未満は「参考」＋淡色（銘柄別のシグナルピルと同じ扱い）。
- **中身**: `sigSub.indexOf("sig:")===0`分岐で`_bandAxisBody(_stGrp.recs, true, {withAll:true, sigLabel:_stGrp.label})`＝💴株価帯別と**同じ関数を再利用**（帯ピル→`_groupPanel`）＝分析内容が二重管理にならない。シグナルが見つからない（改名/削除で消えた）ときは空メッセージ。
- **`_bandAxisBody(pool, cross, opts)`に第3引数optsを追加**: `withAll`＝帯ピルの先頭に`{key:"all", label:"すべて", recs:pool}`を足す／`sigLabel`＝グループ見出しに`🎯 シグナル名 ・ `を前置。「すべて」は必ず最大件数になるので**bandSel=null時の「件数最多へフォールバック」がそのまま既定として効く**＝分岐の追加が要らない。帯基準頻度`_bandSpanSel`は`/^b\d+$/`判定なので"all"では自動的にundefined（記録スパン）＝正しい。空メッセージも"all"のときだけ「記録がありません」に切替（「この帯の」は不適当なため）。
- 母数は`_sigGroupsAll`＝`_v2recsAllData`（データ算入・全銘柄・期間ドロップダウン準拠）由来で💴株価帯別と同一。**複数タグの記録は各タグに算入**＝タブの件数を足すと総件数を超えうる（`_buildSigGroups`の既存仕様どおり）。
- 検証: ローカルHTTP＋合成36記録（4シグナル×3帯×3銘柄）で実ブラウザ確認。タブ行＝[株価帯別/損切り/浮き足%/RN加算/|1px区切り/底つきライン(14)/指標線底抜け(10)/寄り足下値(9)参考/レアシグナル(3)参考]。シグナルタブ→帯ピル[すべて(14)/💴〜4000(5)/💴4001〜5000(4)/💴5001〜(5)]・見出し「🎯 底つきライン ・ すべて（14件・銘柄3種・銘柄横断）」・分析セクション（推奨基本α詳細/記録一覧等）描画。**帯共有**＝4001〜5000を選択→別シグナルタブ→💴株価帯別タブ→さらに別シグナルタブまで保持を実測。データ0件時もエラー無し（区切り・シグナルタブとも非表示）。console 0。

### 2026-08-04 各シグナルタブに「① 底抜けの選択肢別」ピルを追加（帯ピルの下・sw v329→v330）
- ユーザー質問「シグナル総合の各シグナルで、底抜けの選択肢ごとのデータは見れる？」→**見られなかった**。各シグナルタブの中身は`_bandAxisBody`＝株価帯でしか割れず、`_groupPanel`も`useDet=false`呼びなので「詳細:」プルダウンすら出ない。底抜けで割れるのは①銘柄別タブの🏷詳細タグ別（銘柄内・全シグナル横断）②シグナル別集計の「詳細:」プルダウン（**名前ベースでb/k/fを区別しない**）だけで、「全銘柄横断×シグナル別×底抜け選択肢別」は存在しなかった。AskUserQuestionで**帯ピルの下に底抜けピルを足しAND絞込**に決定（切替トグル／置き換えは不採用）。
- **実装（app-06 EntryLogView）**: `_bandAxisBody(pool, cross, opts)`のoptsに**`bSigKey`**を追加。渡したときだけ帯ピルの下に`_subTabBar`をもう1段描く。`sigBSel`state（`"__all__"`/`"d:名前"`/`"__none__"`）を新設。読み取りは正本の`_elSigDetailSec(r.signal, bSigKey).b`（app-05）。
  - **選択肢の作り方**: マスター`custom.sigDetails2[シグナル].b` ∪ そのシグナルの全記録に実在する`.b`（設定から消した過去の選択肢も拾う）。**pool全体から作るので帯を切り替えてもピルの並びが動かない**（件数だけ変わる・0件も出す＝その帯に無いこと自体が情報）。`.b`が空の記録が1件でもあれば末尾に「未選択」。
  - **底抜けは単一選択**（`_EL_SIG_SECS`の`b`は`multi:false`）＝1記録は1バケットにしか入らず、**件数の合計が母数を超えない**（🏷詳細タグ別の重複算入とは違う）。
  - シグナルを切替えて選択中の底抜けがそのシグナルに無ければ`"__all__"`へ自動フォールバック。見出しは`🎯 シグナル ・ 帯 ・ ① 底抜け名`、銘柄種数も絞込後で数え直す。**頻度の分母`_bandSpanSel`は帯のまま据え置き**＝「この帯で、この底抜けは何営業日に1回か」を見たいため。
  - `bSigKey`未指定なら従来どおり＝**💴株価帯別タブは不変**（底抜けピルも出ない＝実測で確認）。
- 検証: ローカルHTTP＋合成4記録（底抜けA×2／底抜けB×1／未選択×1）で実ブラウザ確認。ピル行＝[すべて(4)/わかりやすい底抜け(2)/だましの底抜け(1)/未選択(1)]＝**2+1+1=4で重複なし**。「わかりやすい底抜け」を押す→見出し「🎯 底つきライン ・ すべて ・ ① わかりやすい底抜け（2件・銘柄1種・銘柄横断）」＝母数が2件に絞られる。💴株価帯別タブに底抜けピルが出ないことも確認。console 0。

### 2026-08-05b 保有足の数え方＋実現損益欄の表示整理（sw v336→v337）
- **保有時間の off-by-one 修正**（ユーザー指摘「OS1で撤退したら1分、OS2で撤退したら2分では？」）。`_elRideVals.exitD`は**EP足からの“距離”**（EP足で手じまい＝0）なので、`_elHoldMinNode`がそのまま出すと1本ぶん少なかった。**表示だけ`exitD + 1`**にして「建てた足も1本と数える」に。⚠️`exitD`自体は時間かぶり判定など他のロジックが使うので触っていない。
  - 数え方は**建てた足から数える**（EP=OS2・H1で手じまい→OS2とOS3の2本＝2分）。「撤退したOSの番号＝分数」ではない＝建玉より前の足は保有していないため。EP=OS1のときは両者一致する。
- **採用α値(`tradeAlpha`)を実現損益欄→α値欄へ移設**。旧: 実現損益の左に小さな「α13」チップ(`_tradeAlphaChip`)。新: α値欄の既存2行表記（種別ラベル＋値）の下に破線区切りで**「採用 / 〇円」**を同じ書式で追加（`_elAlphaTypeCell` app-05）。計画αと実際に建てたαが同じ欄に縦に並ぶ。チップの呼び出しは表4箇所（app-02 EntrySignalSection/WeeklyPnlPanel明細・app-04 取引テーブル/本日の損益データ・app-06 記録一覧）から撤去。`_tradeAlphaChip`本体とEntryLogCard(app-05:9280)での使用は残置。
- **100株換算の下段を「通常の文字」に＋グレードバッジ追加**。旧: 9px・#94A3B8の薄い1行。新: 金額は主表示と同じサイズ・`_elPnlColor`の色、単位の「100株」だけ9pxグレー。**バッジは通常スケール`_profitGradeFromPnl`（A+=2500円）**で判定＝実額側の`_profitGradeFromPnlReal`（10倍）とは別物なので、同じ取引でも上下で文字が変わる（例 実額-8,400円=Ⓔ／100株-1,400円=Ⓕ）。これは画面の2本の凡例どおりで正しい。`_elRPnlDispW`(app-05)・`_wkPnlCell`(app-02/app-04)・`_amtCell`(app-02)・実エントリー記録フッター(app-04)に反映。
- **既存バッジの検算**（ユーザー「2枚目の表のグレードバッジはすべてあってるか確認して」）: EP/H1/H2＋合計＋実現損益の全13箇所を凡例と突き合わせて**全部正しい**ことを確認（EP+700=Ⓒ／H1 1,600=Ⓑ／H2 0=Ⓓ／-900=Ⓔ／2,100=Ⓐ-／合計H2 2,800=Ⓐ+／実現-8,400=Ⓔ・+2,400=Ⓒ・合計-6,000=Ⓔ）。修正なし。
- 検証: 合成2記録で実ブラウザ確認。α値欄=`基本13円 採用14円`／`基本13円 採用0円`・実現損益欄=`Ⓔ -8,400円 / Ⓕ 100株 -1,400円`（αチップは消滅）・フッター=`損益合計 -6,000円 Ⓔ / Ⓕ 100株 -1,000円`・保有=`exitD:1`に対し表示`2分`（+1が効いている）。console 0。

### 2026-08-05 実エントリー欄のE-OSを「入(Entry足)／出(Exit足)」の2段に（sw v335→v336）
- ユーザー要望「撤退足がOS何かも入力できるようにしたい。OS1〜5からEntry足・Exit足をそれぞれ選択する形」。UI4案（A:2段ボタン／B:1本のバーで範囲選択／C:入＋保有本数／D:足ブロック側で指定）から**A**を選択。
- **実装（app-05 EntryRecordForm）**: 従来の`E-OS [1][2][3]`（`entryOsNo`）を **`入 [1][2][3][4][5]` / `出 [1][2][3][4][5]` の2段**へ。**入も1〜3→1〜5に拡張**（OS4=H1・OS5=H2。実際の建玉が計算上のEPより後ろになることがあるため）。新フィールド **`signal.exitOsNo`**（1〜5・未選択null）。
  - **出は入より前を選べない**（`disabled`＋淡色）。入を後ろへ動かして出が前になったら**出を自動で外す**。
  - `<label>`ではなく`<div>`で組んでいる＝labelの中のbuttonがクリックを二重に拾うのを避けるため。
- ⚠️ **保存するだけで、まだどこにも使っていない**。保有時間(`_elHoldMinNode`←`_elRideVals`)と「うち損切り」(`_elHoldIsStop`)は**従来どおり計算上の手じまい位置**から出している。実データ基準への切替は別途（切り替えると過去記録＝`exitOsNo`未入力の扱いを決める必要がある）。
- 検証: 実ブラウザで新規フォームを開き `E-OS | 入 1 2 3 4 5 | 出 1 2 3 4 5` の2段描画を確認。操作テスト＝**入=3→出の1,2が無効／出=4を選択→青枠で選択／入=5に変更→出1〜4が無効になり選択済みの出=4が自動解除**。console 0。
  - なお保存の往復（`exitOsNo`が書かれて読み戻る）は**フォームの必須入力バリデーションに阻まれ端から端までは通していない**。読み書きの行は直上の`entryOsNo`と同じ式で、`entryOsNo`は種データから「実E」マーカーが描画されることを実測して確認済み。

### 2026-08-04e 利確/同値/損失の件数を「実現損益の符号」で数えるよう修正（sw v334→v335）
- ユーザー指摘「明らかに損失1件なのに0とされているのはなぜ？ 実現損益では次足期待度関係ないよ」。**そのとおりで判定基準の取り違えだった**。
- **原因**: 2026-08-04cで入れた件数は`_elWinBucket`基準＝**計算上の手じまい位置**で判定していた。`_elWinBucket`はEP足から`_epNextExpAt`（次足期待度）を見て「×/損切り済/未設定＝ここで降りる」と辿るため、**実現損益が-8,400円でも計算上は利が乗った所で降りた扱い→「利確」**になっていた。損益合計（実現損益）と数え方の基準が食い違っていた。
- **修正**: 利確=実現損益>0／同値=実現損益===0／損失=実現損益<0。**実現損益が未記録の記録はどれにも数えない**（損益合計にも入っていないため）。集計は`_elRealPnlPair`(app-05)の`real`を使う＝損益合計と同じ単一源。
  - **「うち損切り」は`_elHoldIsStop`**で判定＝「値が損切り値ぶん逆行したか」という**価格の事実だけ**を見る（`_epResolve`の到達位置と損切り値の比較で、期待度は一切見ない＝ユーザーの指摘に反しない）。損失の**内数**なので、表示側の`_cL`は`cLoss`そのまま（旧`cLoss+cStop`は二重計上だったので同時に修正）。
  - `cTri`（△バケット）は実現損益ベースでは意味が無いので削除。
- ⚠️ 週間表・記録帳の「利確/△/確定損/損切り」列は**従来どおり`_elWinBucket`（計算上）のまま**＝EP/H1/H2損益と同じ土俵で比べる列なので変えていない。**実現損益ベースなのは実エントリー記録フッターのこの1行だけ**。
- 検証: 合成3記録（-8,400円／+2,400円／0円）で実ブラウザ確認。**「利確 1件 / 同値 1件 / 損失 1件」**（修正前は「利確 2件 / 損失 0件」）・損益合計-6,000円と整合。`_elHoldIsStop`はEP足が採用αから15円逆行したケースでのみtrueを返すことも直接評価で確認。console 0。

### 2026-08-04d 選外銘柄を独立カードから本日の損益データの表の中へ（sw v333→v334）
- ユーザー「選外銘柄、本日の損益データと別枠に見えすぎ。いい案ない？」→ 3案（A:同じ表に差し込む／B:カードだけ1枚に／C:折りたたみ）から**A**を選択。
- そもそも**主表と選外表は`_pbHeadTr()`（列見出し）と`_pbRow()`（行）を既に共有**していた＝別枠に見えていたのはカード・見出し・注記・列ヘッダーを二重に出していたから。
- **実装（app-04 DayView）**: `_pbOutEl`（独立Card）を廃止し、`_pbMainEl`のtbody末尾へ **区切り行`__outsep__`（colSpan13・上罫線`2px #C7D2FE`＋地色`#F8FAFF`＋注記）→ 参考合計 → 選外銘柄行** を差し込む。`_pbOutStatOf`/`_pbRealRawOf`/`_pbEntRawOf`/`_pbOutRowOf`の定義を`_pbMainEl`の**前**へ移動（中身は不変）。行キー（`__out__`+銘柄／`__outtotal__`）も従来どおりなので**明細展開は主表と連動しない**。
  - 合計との混同対策は区切り行に集約＝「上の合計には足さないでください」を太字で残し、行ラベル色は従来の藍色`#4338CA`のまま。
  - 主表が0件のときのメッセージは「（下の「選外銘柄」の行を参照）」に微修正。
- 結果: **カード1枚・列ヘッダー1組**になり、選外の数字が主表と同じ列の下に縦に揃う。見出し/注記/列ヘッダーの重複ぶん縦も詰まる。
- 検証: 合成3記録（フジクラ=本日の取引銘柄／IHI・川崎重工=候補プールだが未選択・`includeInTotal:false`）で実ブラウザ確認。**選外を含む表は1つだけ**・thead 13列が1組・tbody行順＝`フジクラ → [区切り行] → 参考合計 → IHI → 川崎重工`。旧カード見出し`📅 選外銘柄`は消滅。console 0。

### 2026-08-04c 実エントリー記録フッターの損益合計を実額へ＋勝敗表記を件数内訳に（sw v332→v333）
- **バグ修正（ユーザー指摘「損益合計の計算違ってない？」）**: すぐ上の合計行は実額（-6,000円）なのにフッターの損益合計は**100株換算（-1,000円）**を出していた。2026-08-04の実額化で`_trEntryAgg.realSum`（app-04:5207で`÷株数×100`していた）を直し漏れていた。
  - `_trEntryAgg`に**`realSumRaw`（実額）/`realHasSh`**を追加し、集計は`_elRealPnlPair`(app-05)に委譲＝表の合計行と同じ単一源に。フッターは**実額を主表示＋下に小さく「100株 −1,000円」**、ランク`_profitGradeFromPnlReal`にも実額を渡す（実額スケール＝A+25000円と整合）。
- **「成功/失敗 N勝N敗」→「利確N件 / 同値N件 / 損失N件（うち損切りN件）」**（ユーザー指定の表記）。**同値は0件なら出さない・損切りも0件なら括弧を出さない**。位置は損益合計の**下**へ（フッターを横並び→縦積みに変更）。
  - 判定は**`_elWinBucket`基準**＝週間表の「利確/△/確定損/損切り」列と同じ（利確=win+tri／同値=even／損失=loss+stop／うち損切り=stop）。**実現損益の符号ではなく計算上の損益（値幅・損切りルール適用後）で数える**点に注意＝損益合計（実現損益）とは別基準。旧「N勝N敗」も`_elDynResult`＝計算上基準だったので、そこは変えていない。
  - `_trSuccessCount`/`_trFailCount`は撤去（`_trEntryAgg`の`ok`/`ng`は残置）。
- 検証: 合成2記録（600株で-8,400円／600株で+2,400円）で実ブラウザ確認。フッター**損益合計 -6,000円／100株 -1,000円**が表の合計行と一致（修正前は-1,000円と食い違い）。件数は「利確 2件 / 損失 0件」＝**同値0・損切り0は非表示**。表示式の分岐は`利確2/同値2/損失4（うち損切り1）`・`利確0/損失4（うち損切り2）`も直接評価して確認。console 0。

### 2026-08-04b 実エントリー欄のOS自動入力＋取引タブの欄の整理（sw v331→v332）
- **Entry-OS値/Exit-OS値を自動入力に**（app-05 EntryRecordForm）。ユーザーは一度「OS欄は不要」と言ったあと「やっぱり不要ではない。自動入力にできる？」に変更＝**撤去はせず自動化**。`useEffect([fPriceIn, fPriceOut, fLevelPrice, fEntered])`で**OS＝価格−水準線値**（符号は水準線比＝上なら↑/下なら↓/同値は符号なし）。実現損益の`⚡自動計算`(価格×株数)と同じ作り・同じ位置に置いた。
  - 手入力の`<input>`＋符号トグルは廃止し、**読み取り専用の表示**（`⚡自動`バッジ付き）に。**価格か水準線が空のときは何もしない**＝手入力時代の保存値がそのまま出る（消さない）。保存は従来どおり`entryOsVal`/`entryOsSign`/`exitOsVal`/`exitOsSign`なので、app-06の規律遵守分析（建玉ズレ／実Exit-OS）は**むしろ入力漏れが減って母数が増える**。
  - 価格（入り）・価格（出）は**横並び＋各132px**に縮小し、それぞれの右隣にOSを置いた（入り→Entry-OS／出→Exit-OS）。
- **取引タブ（app-04 DayView）の整理**（すべてユーザー指示）:
  - 見出し「エントリー記録」→**「実エントリー記録」**（app-04:5679。※銘柄別記録タブ側の「🎯 エントリー記録」(app-02:4452)は別物なので**変えていない**）。
  - **「α値シミュレーション」欄を撤去**（`VirtualAlphaCalc`の呼び出しと`_trVirtByStk`の組み立てごと削除）。部品(app-05 `VirtualAlphaCalc`)は残置。
  - **「🎯 本日の推奨基本α値（簡略・銘柄別）」欄を撤去**（`_simpleAlphaEl`と`_alphaBoardStks`等の組み立てごと削除＝毎レンダーの計算も走らない）。部品`_elBaseAlphaSimpleBoardV2`(app-06)は残置＝記録帳側は不変。
  - **並び替え**: `_pbMainEl(本日の損益データ) → _pbOutEl(選外) → _wkMainEl(今週の損益データ) → _soukatsuEl(本日の総括)`。今週を本日の直後へ、総括を一番下へ。選外は本日の損益データの続きの表なので本日側に付けたまま（＝今週は「本日の一式の真下」）。記録0件時の早期returnも`_wkMainEl, _soukatsuEl`へ揃えた。
- 検証: 実ブラウザ。フォーム＝水準線4487・価格4501/4515で **Entry-OS `⚡自動 ↑14`／Exit-OS `⚡自動 ↑28`**（元の手入力値と一致）・実現損益−8,400円・価格2欄は同じ行(y一致)で各132px。取引タブ＝`実エントリー記録`あり／`α値シミュレーション`と`本日の推奨基本α値（簡略`は**本文から消滅(indexOf=-1)**／並びは本日の損益データ→今週の損益データ→本日の総括の順を実測。console 0。
- ⚠️ また Service Worker のキャッシュで旧app-04.jsが出続けた。`unregister()`＋`caches.delete`のあと**クエリ付きURLで開き直す**まで反映されなかった（`location.reload()`だけでは足りない場面がある）。

### 2026-08-04 実現損益を「実額」主・「100株換算」を下段へ（ランクのスケール不一致も修正／sw v330→v331）
- ユーザー「実際では100株以外での取引が多く、実際の損益額も見たい。100株換算はこれまで通りほしい」。データは既に全部あり（`signal.realizedPnl`+`realizedPnlSign`／取引紐付けなら`item.pnl`・株数`signal.shares`）で、**表示のときに`÷株数×100`していただけ**だった＝入力の追加は不要。
- **バグだった点**: 実現損益のランクは専用の`_profitGradeFromPnlReal`（**実額スケール＝通常グレードの10倍・A+=25000円/E=-1〜-9999円**・画面の凡例もこれ）なのに、**エントリー記録・取引テーブル・今週の損益データは100株換算値を渡していた**＝凡例と1桁ずれたランクが出ていた（例: 実額-7000/500株→表示-1400にEが付く）。一方で上部の「💰実現損益」バッジ・本日の損益データ・記録帳一覧は実額を渡しており、**同じ列名で基準が混在**していた。実額主に統一してこれを解消。
- **共通部品（app-05）**: `_elPer100Of(v, s)`＝100株換算（株数未入力は**null**＝換算しようがないので下段を出さない＝換算済みのフリをしない）。`_elRealPnlPair(s, item)`→`{real, per100, shares}`。`_elRPnlDispW(v, grade, valW, showZ, sub)`に**第5引数sub**を追加（下段に小さく「100株 -1,400円」・null/未指定なら従来どおり1段＝実現損益以外の欄は不変）。`_elTotAccum`のgetに**`realPair`**を追加＝`t.realRaw`(実額合計)/`t.realHasShares`も埋まる（従来の`t.real`=100株換算合計はそのまま）。`_elCalcStats`に**`sumPnlRaw`/`realHasShares`**を追加（`sumPnl`=100株換算は不変＝既存の読者に影響なし）。
- **直した表（同じ列を持つもの全部）**: エントリー記録`EntrySignalSection`の行と合計（app-02）・今週の損益データ`WeeklyPnlPanel`の日別/週合計`_sumRow`と明細フッター`_detailTotRowFor`（app-02）・取引テーブルの行と合計（app-04）・本日の損益データの行/合計/銘柄合計（app-04・元から実額なので下段の追加のみ）・記録帳一覧（app-06）。`_wkPnlCell`/`_amtCell`にもsub引数を追加。
- **あえて変えていない**: 週間サマリー（app-06 `_elWeeklySummary`系の`_get.real`）はEP/H1/H2（100株ベースの動的値）と目標額を横並びで比べる表なので、**実現損益も100株換算のまま**にしている（実額を混ぜると比較が壊れる）。
- 検証: 合成3記録（500株で-7,000円／200株で+800円／株数なしで+300円）で実ブラウザ確認。行＝`E -7,000円/100株 -1,400円`・`C +800円/100株 +400円`・`C +300円`（**株数なしは下段なし**）。合計＝`E -5,900円/100株 -700円`が **エントリー記録・今週の損益データ（週合計/日別/明細フッター）・取引タブ・本日の損益データで全部一致**。console 0。
- ⚠️ 検証中の落とし穴: Service Workerが**途中版のapp-05.jsを掴んだまま**で、`_elCalcStats`の新フィールドだけ古いという状態になった（行の下段は出るのに合計だけ旧値）。`getRegistrations().unregister()`＋`caches.delete`してから再読込で解消。sw版数は最後に上げる運用のままで正しい。

### 2026-07-30d 他の表も横幅を点検＆詰め（期間集計表・本日の損益データ・記録一覧／sw v296→v297）
- ユーザー「他にも同様にすべき表がないか、アプリ内をチェックして。冗長な余白があるところ」。実ブラウザ（viewport 1100・合成84記録）で**全画面の`<table>`を自然幅（`width:max-content`）で実測**し、表示枠と突き合わせて棚卸し。
- **直した3つ**:
  - **期間集計表（記録帳📆期間タブ／銘柄タブ→期間）**＝全体損益と同じ長い小書きが残っていた → `_thPSub`（折り返し＋maxWidth 74px）＋文言短縮＋padding 5→4px。**761→636px（−125px）**。
  - **本日の損益データ（日別ビュー→取引・app-04）**＝最終損益列の小書き「○途切れで手じまい・( )=△含む」がnowrapで列を押し広げていた → 折り返し可＋「○途切れで手じまい ()=△」へ。**889→868px**（残りは`_pbTh`の明示width＝銘柄120/詳細損益150/タグ120で決まっており余白ではない）。
  - **記録一覧テーブル `_recTable`（アプリ内で最も横長）**＝14列前後あるのに左右padding 6px → 4pxへ。**925→854px（−71px）**。
- **問題なしと確認**: 株価帯別(603)・EP位置スイープ(559)・曜日別(556)・週間サマリー(587)・シグナル総合の各ボードは表示枠1022pxに対し十分な余裕。ホーム/日別ビューの他タブは表なし。
- **既知（無害と確認・未変更）**: 明細カード内のH1/H2ミニ表（`_elHoldStackInner`）は`tableLayout:fixed; width:241px`に対し内容が251px＝10px分だけセル内で溢れるが、祖先にスクロールは発生せず（実測 over 0）。桁揃えのための固定幅なので触らない。
- 検証: 上記の実測値はすべて修正前後で同一データ・同一viewportで測定。console 0。

### 2026-07-30c 全体損益（期間別）の横スクロール解消＝表幅を約4割圧縮（sw v295→v296）
- ユーザー指摘「横スクロールしないで済むように幅を詰めれるのでは？」。**幅を決めていたのは中身ではなくヘッダの小書き**だった: `oth`が`whiteSpace:nowrap`なので「○が途切れた所で手じまい・( )=△含む」等が1行のまま列幅を押し広げていた（実測: 到達109/利確125/同値121/損切り156/最終損益168/指値同値120px）。
- **`_othSub(txt)` を新設**（app-06 `_ovPnlTbl`内）＝小書き用の共通span: `whiteSpace:normal` ＋ `maxWidth:74px` ＋ `margin:0 auto` で**2行に折り返す**（thのnowrapは主見出しにだけ効かせる）。
- **小書きを短縮**（詳細は各列のtitleツールチップに残す・利確にもtitleを新設）: EPに乗った件数＝E成立→「EPに乗った＝E成立」／利益で手じまい・E成立母数→「利益で手じまい」／±0で手じまい・E成立母数→「±0で手じまい」／上=ライン起因／下=それ以外の負け→「上=ライン起因／下=その他」／○が途切れた所で手じまい・( )=△含む→「○途切れで手じまい ()=△」／上=件数／下=除外後の損益→「上=件数／下=除外後」。
- **左右paddingを6→4px**（`oth`/`otd`）。列の削除・値の丸め・計算式の変更は一切なし＝**表示情報は不変**。ドリルダウンの入れ子テーブルも同じ`_headTr`を使うので同時に細くなる。
- 検証: 実ブラウザで同一データの自然幅（`width:max-content`）を測定＝**941px → 593px（−348px・−37%）**。ユーザー実データ相当の長い金額（「（A+ +153,600円）」「C +1,206,000円」「差額-19,900」等）を流し込んでも**635px**。viewport 820px（箱742px）でも横スクロール無しを確認・console 0。`_HScrollBox`は狭い画面用にそのまま残置。

### 2026-07-30b 全体損益（期間別）の行展開をドリルダウン（マトリョーシカ）化（sw v294→v295）
- ユーザー要望「週別で週をタップ→まず日別、そこから日をタップ→その日の記録。月別なら 月→週→日→記録」。従来はどの粒度でも1タップで取引記録に飛んでいた。
- **`_lvlRows(gg, xs, exs, pfx)` 再帰**（app-06 `_ovPnlTbl`内）: 段の粒度`gg`で記録をグループ化し行を作る→展開時、次の段があれば**同じ列構成の入れ子テーブル**（`_NEXT_GRAN` = month→week→day）を出し、最小段（日）で従来の取引記録テーブル`_recTable`を出す。日別トグルは日→記録で従来と同じ。
- **段の粒度を渡す形に**: `_bizDaysIn(k, gg)` / `avgCntLine(cnt, days, gg)` / `reachAvg2(rn, days, gg)` / `cntCell(..., gg)` / `reachCell(..., gg)`（省略時は表の粒度＝従来の呼び出しと互換）。ヘッダ行は `_headTr(gg)` に切り出して外側と入れ子で共用（先頭列の見出しだけ日/週/月で変わる）。
- **展開状態 `ovExp` を単一キー→パスのマップへ**（`"2026-07"` / `"2026-07>2026-07-27"` / `"…>2026-07-30"`）＝親を開いたまま子・孫を開ける。`_recTable`のkeyPfxもパス単位にして衝突を防止。
- **不変**: 合計行（tfoot）・母数・各セルの計算式・`_aggKeys`由来の日数/※参考除外。入れ子は表示だけの話で数字の出所は同じ関数。
- 検証: 実ブラウザで合成6記録（7/30×2・7/28・7/22×2・6/15）— **月別**: 2026/07→週別(7/27〜7/31・7/20〜7/24)→7/27〜7/31→日別(07-30・07-28)→07-30→取引記録2件、**週別**: 週→日別→記録、**日別**: 日→記録（従来どおり）。合計行は6件/4日で不変・console 0。

### 2026-07-30a EPナビ早見カードもRN加算を自動判定（採用α切替などでEPを組み直したとき）（sw v293→v294）
- ユーザー質問「EPナビ、早見でも自動でRN加算してくれる？たとえば早見で基本αを応用αに切り替えたときなど」→ **していなかった**。早見カードのインライン編集は `rn` を据え置いたままEPを組み直すため、切替後の予定EPが…41〜49／…91〜99でもRNが乗らず（逆に対象外になっても乗ったまま）、計算フォームの自動判定と食い違っていた。
- **新helper `_epnApplyRnAuto(item)`（app-04・`_epnComputeEp`の直後）**: RN“前”EP（水準線＋base-levelα＋浮き足）で `_elRnAutoFrom`(app-05・記録フォーム/計算フォームと同じ単一源)を引き、rn/rnUsed/epを差し替えて返す。`rnAuto === false`（手動）・水準線≤0・判定不可(null)・変化なしは元のitemをそのまま返す＝無駄な保存をしない。
- **適用先＝αが動く早見カードのインライン編集**: 採用α切替(`onSetSpecialUsed`)・基本α値(`onSetBase`)・応用α値(`onSetSpecial`)・③起点(`onEditK`)・④その他(`onToggleF`)・ライン併存(`onSetLineCoexist`)。
- **手動規約を計算フォームと統一**: カードで〇×/数値に触ると `rnAuto:false`（自動停止）。RN欄の右のラベルが「自動」⇄「↺自動」に切り替わり、**どちらもタップで即再判定**（「自動」タップは本変更より前に作った既存カードを乗せ直す用）。
- **計算フォームの読込も `e.rnAuto === true` → `!== false`**＝rnAuto未設定の旧カードを✎で開くと自動判定が効く（エントリー記録の`_migRnAutoOn`と同じ「未設定＝自動」）。
- **開いただけでは書き換えない**（描画時の自動保存はしない）＝既存カードは編集か「自動」タップで乗る。
- 検証: 実ブラウザで実カード操作（水準線5201・基本α5・本日の採用応用α値40）— ①基本α→応用α切替で**RN9が自動で乗りEP5241→5250** ②基本αへ戻すとRNが自動で外れEP5206 ③RN〇を手動で押すとrnAuto:false・rn5・ラベル「↺自動」 ④手動のままα切替してもrn5据え置き（EP5246）⑤「↺自動」でrn9・EP5250へ復帰 ⑥rnAuto未設定の旧カードを✎で読込→計算フォームが「自動判定中」＋9円 ⑦「自動」ラベルのタップで旧カードが5241→5250。console 0。

### 2026-07-29f 記録帳📆期間タブの期間集計表にも「同値」列（sw v292→v293）
- ユーザー「よろしく」＝2026-07-29eで見送った期間集計表にも同じ列を入れる。**利確の右**に同値（件数＋対E成立%・グレー、金額不明があれば小書き）＝全体損益（期間別）と同じ位置・同じ中身。
- **`_ratesOf` に even / unk を追加**（`stopsOf` の evenN/unkN と**同じ判定・同じ優先順**＝2表で必ず同じ数になる）: even＝decided かつ 非損切り かつ `main===0`／unk＝decided かつ 非損切り かつ `main==null`。既存の take/stop/soft/win は不変。展開行の `colSpan: 9→10`。
- 検証: 同じ合成5記録（利確/損切/損失(見切り)/同値/金額不明）で**両表が同一値**（利確1件・20%／同値1件・20%＋金額不明1件／損切り率20%／見切り率20%）・列数10・展開行colSpan=10・console 0。

### 2026-07-29e 全体損益（期間別）に「同値」列を新設＝到達と件数が閉じるように（sw v291→v292）
- ユーザー指摘「到達件数と、利確・損切り/損失件数の合計が合わなくない？」。調査結果＝**第4のバケツ「同値（最終損益ちょうど±0）」がどの列にも出ていなかった**（`stopsOf`のコメント自身が「E成立母数＝利確＋損切＋損失＋同値」と書いていたが表示先が無かった）。ユーザー提示画面で 7月54−(44+3+5)=**2件**／6月45−44=**1件**／5月35−34=**1件**／合計134−130=**4件** が全て同値。2026-07-29cで到達＝E成立を表に出したことで初めて可視化された差。
- **利確の右に「同値」列を新設**（ユーザー指定の位置・app-06 `evenCell`）: 件数＋率（対E成立・グレー）。0件は「—」。
- **`stopsOf` に evenN / unkN を追加**: evenN＝`main===0`／unkN＝**損切り以外で撤退足の終値が未入力＝金額を出せない記録**（従来どこにも出ず、これも差の一因になり得た）。unkN>0のときだけ同値セルに「金額不明◯件」を小書き（tooltipで「終値を入れれば該当バケツに入る」と明記）。これで **到達＝利確＋損切＋損失＋同値＋金額不明** が必ず閉じる。
- 展開行の `colSpan: 9→10`。ヘッダtooltipにも4バケツの関係を明記。記録帳📆期間タブの期間集計表は列構成が率主体（損切り率/見切り率）で件数の足し算が見えないため対象外。
- 検証: 実ブラウザに5記録（利確+900／損切−1,000／損失−400／同値0／金額不明＝EP足のみで後続足なし）を投入 → 到達5件に対し **利確1・同値1（金額不明1件）・損切1・損失1** で一致・合計行も同じ・展開行 colSpan=10・console 0。同値と金額不明の判定は実ロジック（`_elHoldFinalParts`/`_elIsStopFinal`）で個別に実測して確認。

### 2026-07-29d 記録フォームの損益計算を終値撤退方式へ追従（EP損益・想定値幅・内部勝敗／sw v290→v291）
- ユーザー指摘「新規エントリー記録画面の損益計算が最新じゃないかも。損切り額の概念」。同じ画面でEPチップは **-900円**（終値基準・2026-07-29に修正済み）なのに、下の**EP損益（100株換算）欄は -1,600円**（旧式 -(高値−α)×100＝ラインで約定する前提）を出していた＝保存後の表・集計（撤退足の終値ベース）とも食い違い。
- **原因は3つの取り残し（すべてEntryRecordForm・app-05）**:
  1. 予定損益/想定値幅effect（v2分岐）が `高値−α ≥ 損切り値` で旧式に分岐 → **終値(epConf)基準に一本化**。終値未入力のときだけ旧式を保守的フォールバックとして残す（EPチップ`_epPnlChip`・`_elRideVals`と同じ倒し方）。
  2. 旧記録用の「OS1高値＋想定値幅→予定損益」effectが**v2フォームでも走って**いた → `if (isV2Form) return;`。①EP=OS1で旧式の額に上書き（＝今回の-1,600円の直接原因・②のeffectが後勝ち）②EP=OS2/3ではOS1高値<αのため予定損益を0で上書き、の二重の壊し方をしていた。
  3. 内部勝敗fResultのeffectが**ラインに触れた時点で無条件ng** → `_elDynResult`(app-05:3682)と同じ判定順（終値優先・終値不明のときだけライン接触でng）へ。
- **EP損益欄の条件も `epIdx>0`→`epIdx>=0`**＝v2は常にEP足から直接計算（EP=OS1だけがfPlan経由＝effectの実行順に依存していた）。
- 影響: 表示だけでなく**保存値 plannedPnl も正しい額になる**（`_elDynPlanned`は終値がある限り保存値を使わないので既存記録の集計は元から正・フォームの見た目だけがズレていた）。旧記録（非v2）の計算は`_elDynPlanned`の旧記録パスが旧式のままなので**あえて変更なし**（正本と揃える）。
- 検証: 実ブラウザで新規フォームに指摘のケース（水準線5201・α5・損切り15・OS1高値21・確定値14）を投入 → チップとEP損益欄が**どちらも-900円**で一致（修正前は欄だけ-1,600円）。派生ケースも実測 = ①終値0（触れたが利益側）→**+500円・「（損切り）」ラベルなし** ②EP=OS2（OS1未達・OS2高値21/確定値14）→-900円 ③終値未入力→-1,600円（保守的フォールバック）。console 0。

### 2026-07-29c 期間表の「到達」を【EPに乗った件数＝E成立母数】へ（sw v289→v290）
- ユーザー確認「EPに乗った分でいい」。同日の 2026-07-29「有効な到達」（③到達足の次足期待度が×/損切り済/未設定を除く）を**撤回**＝乗ってはいる記録なので数える。7月＝46件→**54件**（＝画面に併記していたE成立と同数）。
- **全体損益（期間別）`_ovPnlTbl`（app-06）**: 専用集計 `reachOf` を廃止し、到達セルへ **`stopsOf(x).wn`（＝右隣の利確/損切り/損失の分母 E成立母数）をそのまま渡す**＝分母が構造的に食い違わない。`reachCell` の第5引数 `eN`（「E成立◯件」小書き）は常に同数＝冗長になったので撤去。
- **記録帳📆期間タブ 期間集計表（app-06）**: `_reach` を `_elIsValidReach` フィルタ → **`rr.ok + rr.ng + rr.draw`**（同じ行の利確率・損切り率・見切り率が使う `_ratesOf` の母数そのもの）。
- **`_elIsValidReach`（app-05・2026-07-29に新設）は削除**＝利用者が0になったため。単純到達 `_epReachedAt`（×見送りも数える・成立率/時間帯別など9箇所）は従来どおり不変。
- ヘッダ小書き「有効なEP到達件数」→ **「EPに乗った件数＝E成立」**、titleも①②のみの定義へ（両表）。到達率の分母は従来どおり件数（全記録）。
- 検証: 実ブラウザに合成4記録を投入（①乗って勝ち ②×見送り後に到達 ③未達 ④乗ったが到達足の次足期待度×）→ エンジンで judge/decided を実測（②③のみ除外）し、**両表とも到達＝2件**（旧・有効到達なら1件／単純到達なら3件）・E成立小書きは消滅・KPI「E成立2件」と一致・console 0。

### 2026-07-29a 既存記録もRN加算を自動判定に＋「RNまたぎ加算」→「RN加算」へ改名（sw v288→v289）
- ユーザー指示「既存のエントリー記録でRN加算が自動になっていないものは自動に」「名前はRN加算へ」。
- **移行 `_migRnAutoOn`（app-01 migrateData末尾・returnの直前）**: 全記録 `charts[*].signals` の `signal.rnAuto` を **true** に。一回性フラグで**この移行より後に手動へ倒した記録（rnAuto:false）は二度と踏まない**。フラグ後も **rnAuto未設定**（2026-07-20b以前の記録が旧端末から後から同期された分）は毎回true補完＝条件ベースで冪等。**rnUsed/rnVal/alphaVal/ep は書き換えない**＝開いて保存し直すまで過去の損益・分析は不変。
- **記録フォーム（app-05 `_useStateRNA`）**: 初期値を `isEdit ? (rnAuto===true) : (rnAuto!==false)` → **`initSig.rnAuto !== false`（未設定＝自動）** に一本化。2026-07-20bの「既存記録は自動を止める」決定は撤回。明示的なfalseだけ手動を維持（従来どおり〇×/数値に触ると自動OFF・「↺自動」で復帰）。
- **改名**: app-04/05/06 の「RNまたぎ加算」「RNまたぎ」→ **「RN加算」**（UIラベル・ツールチップ・📡シグナル総合のサブタブ「🔢 RN加算」・候補一覧見出し・コメント計49箇所）。FILEMAP索引も追随（変更ログの過去記述は履歴なので据え置き）。関数名（`_elRnAdd`/`_elRnAutoAt`等）とデータキー（rnUsed/rnVal/rnAuto）は不変。
- **対象外**: EPナビ早見カード（app-04 `e.rnAuto === true`）は従来どおり保存済みカードを開いても自動が効かない＝指示は「エントリー記録」のため据え置き。
- 検証: 実ブラウザ（127.0.0.1:3457・SW解除して最新ファイルを読ませた上で）で**migrateData実測** — 未設定/false/true の3記録が全てtrue・rnUsed/rnVal不変・再実行で「移行後の手動false」は維持し「未設定」だけ補完・空データでも例外なし・実マウントconsole 0。

### 2026-07-23o 1日平均グレードの分母を「営業日」に統一（取引実日数→_elBizDaysOf・sw v254→v255）
- ユーザー「営業日ベースに寄せて」。2026-07-23nの分母 `_elActiveDays`（取引実日数）を **`_elBizDaysOf(recs, data)`（記録スパンの営業日数＝`_elBizSpanDays`・全体損益`_bizDaysIn`の営業日ベースと統一）** に差し替え。`_elActiveDays`(app-01)は削除。
- **全9呼び出し更新**: app-02（`_totDays`/`_rowDays`）・app-04（`_pbTotDays`/`_wkRowDays`）・app-06（`_frDays`/`_pkDays`/分析パネル`_osFilRecs`／集計KPIは旧`_entDays`→`_elBizDaysOf(rs,data)`）。holiSetは`_buildHolidayDateSet(data.trades,(data.custom||{}).eventCategories)`で内製。**単日=1で不変・スパン0時は合計にフォールバック**（判定側の`days>0?…:合計`）。
- 検証: app-01/02/04/06 V8 parse OK＋`_elBizDaysOf`単体（月〜金=**5**・単日=1・空=0・木〜翌木=**6**[土日除外]）＋`_elActiveDays`残存0＋実マウント（console 0）。実データの見た目はユーザー実機。

### 2026-07-23n 複数日合計のグレードを全表で「1日平均」に統一（記録帳KPI・今週の週合計・共有関数）（sw v253→v254）
- ユーザー「エントリー記録帳などアプリ全体でもチェックして」。監査（専任エージェント＋精査）でper-day目盛りのグレードを複数日合計に当てている箇所を**17＋共有関数1**特定→全て1日平均化。
- **新helper `_elActiveDays(recs)`（app-01・`stripHtml`直後）**＝集計対象記録の異なる取引日数（単日/1記録は1→不変で安全）。これを除数に `Math.round(合計/日数)` でグレード判定（**表示は合計のまま**）。
- **共有 `_elDetailPnlStackNode`（app-05）**に`days`引数追加（詳細損益EP/H1/H2の合計グレードの元・内部の`_elHold2RefSuffix`へもdays伝播）。
- **今週の損益データ 週合計**（app-02 `WeeklyPnlPanel`の`_sumRow`/`_detailTotRowFor._amtCell`／app-04 `_wkRow`＋`_pnlDetailTableEl` totRow[`expRecs`基準]）＝最終/実現/詳細損益を`_elActiveDays`で1日平均化（**単日行・本日の株価帯単日展開はdays=1で不変**）。
- **記録帳KPI（app-06 EntryLogView）**＝集計タブ(既存`_entDays`)・期間タブ(`_pkDays`)・指値同値(`_frDays`)・分析パネル(`_elActiveDays(_osFilRecs)`)の最終/実現損益カードに`days`を渡す。
- **不変**: 全体損益(期間別・2026-07-23m済)・本日/取引/カレンダーの単日・1記録・死コード`_grpTable`(未配線)。
- 検証: 5ファイルV8 parse OK＋`_elActiveDays`単体（空/null→0・重複→1・3日→3）＋`_elDetailPnlStackNode`6引数＋実マウント（console 0）＋差分レビュー（`expRecs`はL5955 `var expRecs=(records||[]).slice()...`で実在＝記録配列と確認）。分母は「取引実日数」（全体損益の営業日ベースとは僅差・粗いグレードでは実害なし・厳密統一も可）。実データの見た目はユーザー実機。

### 2026-07-23m 全体損益（期間別）の複数日合計行はグレードを「1日平均」で判定（sw v252→v253）
- ユーザー指摘「A〜G+は1日あたりの目盛りなので、複数日の合計にそのまま当てると常にA+化して無意味」。AskUserQuestionで「1日平均で判定」を選択。
- **days（除数）を引数化**（後方互換＝未指定なら従来の合計判定）: app-05 `_elHold2RefSuffix(mainSum,refSum,refCnt,days)`／app-06 `_yenN(v,cnt,days)`・`_yenNR(v,cnt,ref,refCnt,days)`。days>0のとき **`Math.round(合計/days)`（＝1日平均）をグレード判定に使う（表示は合計額のまま・（）内参考も同基準）**。
- **配線（app-06 `_ovPnlTbl`＋別の期間別テーブル）**: `pnlCell`(最終損益)・`realCell`(実現損益)・`friskCell`(指値同値除外後) が持つ `days`（=`_bizDaysIn`／`avgDayLine`と同じ営業日数）を `_yenN`/`_yenNR` へ。別テーブル(6904/6905)は `dn`。**日別行は days=1 で従来と同値**。**KPIカード・per-signal行（1日平均を出さない箇所）は対象外＝合計判定のまま**（1記録の実現損益グレード6006も不変）。
- 検証: app-05/06 V8 parse OK＋days配線8箇所を実ソースで確認＋グレード妥当性（合計+128,200/56日＝+2,289→**A−**／2026-07=A+／2026-05=B・旧合計額ならA+）＋実マウント（console 0・`_elHold2RefSuffix`4引数）。実データの見た目はユーザー実機。

### 2026-07-23l グレードを9段(A+〜G+)に細分化＋Q→Z統合＋ノーシグナル=DNF/有効シグナルなし=Z（sw v251→v252）
- **9段グレード（ユーザー設計）**: 損益/実現損益のグレードを A〜G の7段から **A+/A−/B/C/D/E/F/G−/G+ の9段**へ（D=0中心に対称・両端A/Gのみ±分割）。`_profitGradeFromPnl`（損益: A+≥2500 / A−2000〜2499 / B1000〜1999 / C1〜999 / D0 / E−1〜−999 / F−1000〜−1999 / G−−2000〜−2499 / G+≤−2500）と`_profitGradeFromPnlReal`（実現損益＝10倍スケール: A+≥25000…G+≤−25000）を書換（app-05）。`_GRADE_STYLE`/`_GRADE_DESC`/`_GRADE_DESC_REAL`にA+/A−/G−/G+追加（旧A/Gはエイリアス保持）＝赤(利益)→グレー(0)→緑(損失)ランプ。
- **Q→Z統合**: 「E基準未達で損益なし(Q)」を **Z（取引なし）に統合**。`_qZeroCell`＝「Q ー円」→「Z ー円」、app-04 `_pbBadge("Q")`→`_pbBadge("Z")`。全凡例からQ削除（app-02×2 `_renderGradeLegend2Row`／app-04 `_trLegendPairs`＋`grades`配列＋mkRow）。
- **ノーシグナル=DNF / 有効シグナルなし=Z**: 早見表(app-04:3299)の最終損益欄のタグ日を、旧タグ名バッジ→**`_elTradeTagMarker(c)`**（ノーシグナル→グレー「DNF」ピル`_dnfBadge`／それ以外の取引:タグ→Z丸バッジ・タグ名はtooltip）。共用関数`_dnfBadge`/`_elTradeTagMarker`はapp-05（`_qZeroCell`直後・hoistでapp-04から呼出）。**今週の損益データ（per-day集約）はタグ名維持**（複数銘柄混在のため今回は据置）。**別系統の手動評価セレクタ`GRADES`(app-01:80)は不変**。
- 検証: app-02/04/05 V8 parse OK（SW unregister＋caches消し＋fetch reload）＋**グレード2関数の全境界スモーク全pass**（A+〜G+/Z・損益/実現損益 各18ケース）＋実マウント（root描画・console 0・`_GRADE_STYLE["A+"]/["DNF"]`存在・`_elGradeBadge18("A+")`描画）。**DNFは丸凡例に3文字入らないためピル専用＝凡例には非掲載（セルのtooltipで表現）**。実データの見た目はユーザー実機。

### 2026-07-24 全体損益（期間別）/期間集計の到達列に「1日平均〇件」を2行で追加（sw v260→v261）
ユーザー要望「到達欄にも1日平均を（欄が狭いので『1日平均』『〇件』の2行で）」。**app-06.js の2つの期間集計表**。①**`_ovPnlTbl`「💰全体損益（期間別）」の`reachCell`**＝引数に`days`追加＋新ヘルパー`reachAvg2(rn,days)`（到達÷営業日数・`Math.round(rn/days*10)/10`＝件数列`avgCntLine`と同丸め・整数は整数表示・日別`g==="day"`と0件は非表示）を到達件数/率の下に2行（「1日平均」「〇件」）で追加。呼出2箇所に`days`を渡す（日別行=`dn`・合計行=`_ovTotDays`＝旧3引数目のstyleを4引数目へずらす）。②**「📆期間集計」表(6903)**も同型2行を到達セルにインライン追加（`dn`/`gran`/`_reach`）。件数列は元々1日平均あり＝到達列も揃えた。検証=V8 parse OK＋表示式実測（46/17→2.7・64/22→2.9・39/18→2.2・合計149/57→2.6・60/20→"3"整数・日別/0件→非表示）。

### 2026-07-24 今週の損益データ（日ごと集約）の取引タグは「全銘柄が取引なしの日」だけ表示（sw v259→v260）
ユーザー指摘「最終損益欄のノーシグナル等は全銘柄がそうである場合のみ表示すべき（集約表なのに1銘柄でも取引があると誤解）」。**app-04.js `_wkMainEl` の日別行のみ**（v246で追加した`_wdTradeTags`集約・[[2026-07-23f]]）。`_wdTradeTags`の収集を **`if (_wkEntCnt(_wkByDay[_wd]) === 0)` でゲート**＝その日エントリーした銘柄が1つも無い（＝全銘柄が取引なし）ときだけ「取引:」タグを集約表示、1銘柄でもエントリー有りなら非表示（旧＝いずれかの銘柄にタグがあれば表示）。表示は全銘柄のタグの和集合（例「有効シグナルなし・ノーシグナル」）。`_wkEntCnt`=`_elIsEntered`ベース＝ノーシグナル銘柄はrecs無し(0)・有効シグナルなし等の未エントリー記録も0。週合計行は元々tradeTags非渡し（不変）。バッジtitleも更新。ユーザー選択＝AskUserQuestion 3案（全銘柄取引なしの日だけ[採用]／全銘柄同一タグの日だけ／集約表からは撤去）。検証=V8 parse OK＋`_elIsEntered`実測（entered:true→true/false→false）＋ゲート再現（記録なし→0=表示・エントリー混在→1=非表示・全未エントリー→0=表示）。

### 2026-07-24 本日の取引銘柄チップを銘柄コード昇順で自動整列（sw v258→v259）
ユーザー要望「左から銘柄コードが早い順に（SUMCO→古河電工→安川電機）」。**app-04 `_rotPickerBar`（本日の取引銘柄バー・app-04:5238付近）のみ**。`rotStocks.map` を **`_rotSorted.map` に変更**＝`_rotCodeOf(stk)`=`custom.stockCodes[銘柄名]` を数値化（`parseInt(String(c).replace(/\D/g,""),10)`・未登録は`Infinity`）→`rotStocks.slice().sort()` で**コード昇順・同/無コードは名前順**。表示順のみ整列（rotStocks本体・_rotView/dailyStock等のロジックは不変）。**コードは設定→銘柄管理で登録した`custom.stockCodes`（名前キー）が前提＝未登録銘柄は末尾**。EPナビ側の日替わり`<select>`(app-04:4719・別rotStocks)は今回対象外。検証=V8 parse OK＋比較関数をユーザー銘柄コード(3436/5801/6506)で実行＝任意入力順→[SUMCO,古河電工,安川電機]・コード無しは末尾。

### 2026-07-23k 不算入行の文字濃度を0.65に＋本日の取引銘柄チップにノーシグナル銘柄（0）表示（sw v250→v251）
- **不算入行の濃さ（app-05 `_elNotInclRowStyle`）**: 不算入(includeInTotal===false)行の opacity 0.62→**0.65**（α詳細表の参考行と同じ・ユーザー要望「詳細データ表と同じ」）。スルーは0.6据置。全記録テーブル共通の行スタイルなので銘柄別記録/取引テーブル等すべてに反映。
- **本日の取引銘柄チップ（app-04 `_rotPickerBar`）**: `_rotHasTradeTag(stk)`＝`charts[銘柄_日付].chartShapeTags`に「取引:」カテゴリ（ノーシグナル/有効シグナルなし等）があるか。記録0件でもこのタグが付いた銘柄はチップに**（0）**表示（記録ありは従来どおり（件数））。
- **実現損益0円のグレード表示は「Zのまま（現状維持）」＝変更なし**（AskUserQuestionでユーザー選択。審議/見送り/不算入は無エントリー＝Z＝取引なし）。
- 検証: app-04/05 V8 parse OK（fetch cache:reload）＋opacity0.65/スルー0.6据置＋チップ（0）分岐を実ファイルで確認。実データの見た目はユーザー実機。
- **未了（相談中）**: ノーシグナル「取引:」バッジを損益データ表（📊本日の損益データ/銘柄ページ/記録帳💰損益）へ展開＝これらは記録のある銘柄しか行に出さない（`_pbStks=Object.keys(_pbByStk)`）ため、ノーシグナル銘柄を行として追加する必要があり別途方針確認。

### 2026-07-23j 候補（日替わり）銘柄は「本日の取引銘柄」未指定日でも既定でデータのみ（合計除外）に（sw v249→v250）
- ユーザー報告「古河電工が日替わり銘柄になったのに合計算入が既定ON」。原因＝2026-07-22iで入れた**未指定日カーブアウト**（その日にdailyStockを指定していないと候補も既定ON）。AskUserQuestionで「候補は常にデータのみ＋集計も揃える（B案）」を選択。
- **撤回した2箇所（app-05）**: `_isDataOnly`（集計側・3140付近）の `if (!_ds) return false;` と `_indDataOnlyCand`（記録フォーム既定・6516付近）の `if (!_dsInd) return false;` を削除。以後どちらも `stock !== dailyStock[date]`（未指定日は dailyStock=="" なので候補は全てデータのみ＝合計除外）。**指定銘柄本人のみ算入**。手動override(totalOverride in/out)＞明示includeInTotal＞自動判定 の優先順位は不変。
- **影響**: グランド/銘柄横断の合計（`_elInclTotalAmt`経由＝カレンダー日次・ホーム月次・損益ダッシュボード・今週・被り母数等）で、**未指定日の候補記録（includeInTotal未設定の旧記録も含む）が合計から外れる**＝合計損益が変わりうる（ユーザー選択B）。分析母数（`_elInclData`/`_elInclTotal`）は不変＝α等の分析には残る。残る不変条件は『候補プール空なら現行一致』のみ（『未指定日一致』は意図的に撤回）。
- 検証: app-05 V8 parse OK＋新版ソースから`_isDataOnly`を抽出し9シナリオ単体テスト全pass（非候補=算入／候補・未指定日=データのみ[新]／候補=指定銘柄本人=算入／候補・別指定=データのみ／明示includeInTotal=ON=算入／override in・out／空プール=不変）。実データの合計はユーザー実機。

### 2026-07-23i α詳細表の「条件を満たさない参考行」の薄字を濃く（opacity 0.4/0.45→0.65・sw v248→v249）
- ユーザー指摘「前提条件を満たさない行の文字が薄すぎて見づらい・もう少し濃く」。α詳細表の総当たり行は前提条件（到達率/E成立/頻度/損切り率/黒字）を満たさない行を`opacity: pass ? 1 : X`で淡色表示していたが、X=0.4（一部0.45）は薄すぎた。show_widgetで0.4/0.55/0.65/0.75を並べて提示→**0.65**を採用（★行=opacity 1との区別は残す・未達バッジで「どの条件で落ちたか」は元々分かるので濃くしても意味は崩れない）。
- **変更（app-06.js・3箇所＝表示中のα詳細表すべて）**: `_elBaseAlphaDetailV2`(2615・基本α)／`_elTotalAlphaSectionV2`(2195・応用α)／`_elBaseAlphaSummary`(2499・🎯推奨基本α値の期間まとめ)の `opacity: pass ? 1 : 0.4`（2499は0.45）→ **0.65**。共有テーブルなので株価帯別/銘柄別/シグナル別の全モーダル（EPナビ・記録フォーム・記録帳・app-04 `_PbDayBandReco`帯別）に一括反映。**未配線の`_elAddAlphaDetailV2`(2695/2713)は画面に出ないため据置**（0.4のまま）。
- 検証: app-06 V8 parse OK（**SW unregister＋caches全消し＋fetch cache:reload**で確実に新版取得＝preview初回のSWがstale-while-revalidateで旧版を返すため）＋opacity 0.65が3件・0.45残0・誤変換0.655なしを実ファイルで確認。実データ（参考行だらけの表）の見た目はユーザー実機。

### 2026-07-23h 記録フォームのα値📊詳細表に「株価帯別／銘柄別」トグルを追加（sw v247→v248）
- ユーザー指摘「この詳細表は株価帯別とその銘柄だけの両方がトグルで見れるのでは？」。記録フォーム(EntryRecordForm・app-05)のα値セクションの📊詳細表（基本α/応用α共通モーダル`_modalEl`）は従来「全体／この記録のシグナル」の2択のみ＝どちらも**この銘柄だけ**で、株価帯別が無かった（帯別トグルは「本日の採用α値」の`表を参照`=`_ElDayAlphaPair`だけに存在＝2026-07-22i）。AskUserQuestionで「帯別/銘柄別トグル＋銘柄別の中に既存の全体/シグナルのサブトグルを残す」案を採用。
- **実装（app-05のみ）**: 新state `_alTblPool`（"band"=株価帯別/"stock"=銘柄別・既定band・📊ボタン(`_tblBtn`/応用αインライン)を開くたび`_setAlTblPool("band")`でリセット）。モーダルの母数行に最上段トグル`_alPoolToggle`（💴 株価帯別（帯ラベル）／🏷 銘柄別（銘柄名））を追加＋`_scopeToggle`（全体/この記録のシグナル）は`!_alUseBand`＝銘柄別のときだけ表示。株価帯別プールは`表を参照`と同一ロジック＝`_pbDayBandOf(data,fStock,fDate)`→`_pbBandPoolFor(data,idx,fDate)`（銘柄横断・前日まで・データ算入・材料日除外）。頻度分母は`_pbBandBizDays`を`_elBaseAlphaDetailV2`/`_elTotalAlphaSectionV2`の第6引数spanOverrideへ渡す（銘柄別時はundefined＝記録スパン）。帯不明/材料日/帯0件は株価帯別ボタンdisabled＋自動で銘柄別へフォールバック（注記「株価帯が未判定/材料日…」等）。**共有モーダルなので基本α・応用αの両方の詳細表に反映**。閲覧のみ（行タップ取込なし＝onPick/curSel引数は渡さない）。**app-05からapp-04のトップレベル`_pb*`ヘルパーをグローバル参照**（連結・load順で解決）。
- 検証: 全8ファイルV8 parse OK（fetch cache:reload＝SW回避）＋実マウント（root描画44KB・console 0・依存グローバル`_pbDayBandOf`/`_pbBandPoolFor`/`_pbBandBizDays`/`_pbBandLabel`/`IS_TOUCH`/detail関数を解決）＋帯計算スモーク（前日終値なし=idx null→銘柄別フォールバック／手動帯idx=1→ラベル「4001〜5000」・プール/頻度算出・例外なし）。実データ（帯をまたぐ実記録の見た目・件数）はユーザー実機。

### 2026-07-23g 古河電工を日替わり銘柄(rotatingStocks)に組み込む＋EPナビ固定表示の古河電工ハードコード除外を一般化（sw v246→v247）
ユーザー指示「コードで自動組み込み」。**①app-01.js `migrateData` に一回性移行 `_migFurukawaRotating`**＝`custom.stocks`に古河電工が実在すれば`custom.rotatingStocks`へ追加＋`custom.epnStocks`(EPナビ固定表示)から除去・フラグ`d._migFurukawaRotating`で冪等（ユーザーが後で設定UIから外せる）・in-place。**②app-04.js `EpNaviPanel` の epnStocks既定(4491-4495)の`s!=="古河電工"`ハードコード除外を`rotatingStocks`参照(`_epnRot.indexOf(s)<0`)に一般化**＝日替わり銘柄は固定表示の既定から自動除外（`rotStocks`の日替わり列へ回る）。日替わり銘柄は日別ページで個別タブを作らず「📅日替わり」タブに集約（既存2026-07-22・設定→📊データ・銘柄→📅日替わり銘柄でトグル`custom.rotatingStocks`）。検証=app-01/04 V8 parse OK＋`migrateData`合成でrotatingStocks追加/epnStocks除去/冪等/未実在はスキップ/既存は重複追加せず。

### 2026-07-23f 予定EP計算もその日の採用α値ベースに＋早見表・今週の損益データに「取引」タグ（ノーシグナル/有効シグナルなし）バッジ（sw v245→v246）
2つの続き実装（ユーザー指示）。**①予定EP計算も その日の採用α値ベースに**（app-05.js `EntryRecordForm`）＝`_fBaseAInput`(6598)/`_fSpecialA`(6599) の推奨α`_autoBaseA`/`_refSpecialA`フォールバックを廃止＝新規は その日の採用α値のみ・無ければ基本α0／応用αは基本α入力＝入力欄デフォルトと一致（v245で入力欄だけ変えたのを予定EPにも波及）。**②早見表＋今週の損益データの最終損益欄に「取引」カテゴリタグ（ノーシグナル/有効シグナルなし等）を淡いグレーバッジ表示**（app-04.js）。判定＝`chartShapeTags` の `取引:` プレフィックス（v244で「ノーシグナル」を`取引`カテゴリへ移動済み・カテゴリ全体対応で将来のタグも自動）。**早見表 `StockQuickRefTable`(3298)** ＝銘柄別（`c2.chartShapeTags`）を最終損益欄冒頭で判定→バッジ。**今週 `_wkMainEl`** ＝日別行で全銘柄集約（`_wdTradeTags`・**ノーシグナルの銘柄はsignals無しで`_wkByDay`(recs)に含まれないため`_pbCharts`直参照**）→`_wkRow`に`tradeTags`引数追加＋最終損益ノードを`_pnlNode`に wrap してバッジ併記（`_pbMainEl`の同型セルと2箇所マッチするので`_wkRecsM`詳細損益をcontextに含めて限定）。検証=app-04/05 V8 parse OK＋`StockQuickRefTable`実マウント(ノーシグナル/有効シグナルなし各2＝最終損益欄＋既存タグ欄)＋`DayView`実マウント(今週の損益データ日別行 各1)。

### 2026-07-23e 記録フォームのα入力欄デフォルトを「その日の採用α値」のみに＋推奨α値の右にその日の採用α値を表示（sw v244→v245）
ユーザー指示。**app-05.js `EntryRecordForm` の基本α/応用α**。①`_baDefault`/`_spDefault` を `epNaviDayAlpha`/`epNaviDaySpecialAlpha`（＝その日の採用α値・`_epnDayAlphaGet`/`_epnDaySpecialAlphaGet`）**のみ**に（推奨α`_autoBaseA`/`_refSpecialA`フォールバックを廃止）＝入力欄デフォルトはその日の採用α値のみ・未設定なら空（placeholder「推奨基本α/応用α」→「ー」）②基本α/応用α入力欄の value の推奨αフォールバックを空文字に③推奨基本α値/推奨応用α値サマリーの右に「その日の採用α値 N円 or ー」バッジを追加（青#E0F2FE/茶#FFEDD5）。**予定EP計算 `_fBaseAInput`/`_fSpecialA` は既存維持**（欄が空のとき従来どおり推奨α/基本αで値を出す＝EPプレビューは変えない）。自動入力useEffectは`_baDefault`経由で連動＝その日の採用α値が無ければ自動入力なし（欄空・手入力）。検証=`EntryRecordForm`実マウントで その日の採用α値3→基本α入力欄value="3"・「その日の採用α値 3円」表示／未設定→「その日の採用α値 ー」（対称の応用αも同コード）。

### 2026-07-23d チャート形状タグ「ノーシグナル」を「チャート形状」→「取引」カテゴリへ移動＋既存記録を一括移行（sw v243→v244）
ユーザー指示。**app-01.js `migrateData` に一回性移行 `_migShapeNoSignalToTrade` を追加**（`return d` 直前）＝①マスター `custom.chartShapeCats` の「チャート形状」配列から「ノーシグナル」を除去し「取引」へ追加 ②全記録 `charts[ck].chartShapeTags` の `"チャート形状:ノーシグナル"→"取引:ノーシグナル"` を付け替え。タグは「カテゴリ:名前」形式・区切りは半角`:`（`stripCat`＝`/^[^:]+:/`と同じ）。**条件ベース冪等**（「チャート形状」に「ノーシグナル」が無ければ no-op）・「取引」カテゴリが無ければ作成・in-place mutate（`_migLineCoexist`と同型）。タグ管理は `makeShapeTagPoolHandlers`(app-02)＝`chartShapeCats`{cat:[names]}＋loose `chartShapeTags`。検証=`migrateData` 合成データで カテゴリ移動／記録付替（他タグ不変）／冪等／取引カテゴリ新規作成 を確認。※早見表・今週の損益データへの「ノーシグナル」バッジ表示は別途（前回決定=淡いバッジ・早見表＋今週）。

### 2026-07-23c DayView（取引テーブル）の今週の損益データ欄より下＝「α比較・深掘り」＋「本日のデータ分析」を削除（sw v242→v243）
ユーザー要望「取引テーブルの今週の損益データ欄より下の、深堀り欄以下のセクションはすべて不要」。**app-04.js `DayView` から2セクションを削除**＝①「📊 α比較・深掘り」（`_benchEl`＝定義＋Fragmentから除去）②「📊 本日のデータ分析」IIFE（📍EP位置別／📈EP→OS5ホールド検証／📊銘柄別OS値分析／🔗OS連鎖分析）。今週の損益データ欄（`_wkMainEl`）までは残す＝Fragmentは `_pbMainEl, _simpleAlphaEl, _soukatsuEl, _wkMainEl` に。取引フォームのモーダル（`EntryRecordForm`・showForm時）は不変。IIFE（約120行）はPythonで行削除（CRLF保持・境界assert）、`_benchEl` はEditで処理。未使用化する `_elDayStockBenchV2`/`_elEpPosSectionV2`/`_elOsChainSection` 等は他ファイル定義なので無害。検証=app-04.js V8 parse OK（442KB→430KB）＋`DayView`(initialTab:"trades")実マウントで 今週=残存・深掘り/本日のデータ分析/ホールド検証/OS連鎖/α比較=全消滅・クラッシュ無し。

### 2026-07-23b 早見カード（保存済みEP）の基本α/応用α選択時のデフォルト値を「本日の採用α値」優先に（sw v241→v242）
ユーザー要望「早見カードで基本α・応用αを選択したときのデフォ数値を本日の採用α値にして」。**app-04.js `EpNaviPanel` の `onSetSpecialUsed`（早見カードの採用αセグメント切替ハンドラ・4546）を修正**＝応用α選択時は `_epnDaySpecialAlphaGet`（本日の採用応用α値）を最優先→無ければ推奨応用α→基本α／基本α選択時は `_epnDayAlphaGet`（本日の採用α値）を最優先→無ければ既存の基本α値、で `special`/`base`＋EPを再計算。計算フォーム `_EpnCalcForm` は元々本日の採用α値ベース（dayAlpha/daySpecialAlpha優先）＝早見カードをそれに揃えた。本日の採用α値が未設定なら従来フォールバック（後方互換）。検証=実マウントで epNaviDayAlpha=1/epNaviDaySpecialAlpha=4 を設定→応用αボタン click→special=4/ep=1004・基本αボタン click→base=1/ep=1001（本日の採用α値がセット）を確認。

### 2026-07-23 EPナビ（計算フォーム＋早見カード）から ③底抜け・詳細（③起点/④特徴）・応用αの根拠欄 を削除（sw v240→v241）
ユーザー要望「EPナビの応用α根拠欄は不要（早見も同様）／②底抜け・詳細（特徴含む）も不要」。**app-04.js の `_EpnCalcForm`（計算フォーム）と早見カード `_renderCard`＋`_EpnAddSection` のレンダーのみ削除**＝state/保存（`doSave` の b/k/f/specialReasons）/推奨カスケード（`autoPick` は `det` 空でも `sig→stk` へフォールバック）は無改修＝過去記録データも保持。
- **計算フォーム**: ②シグナル直下の③底抜け欄・「▷詳細を入力（③起点〜⑤）」トグル＋③起点/④その他チップ・推奨基本αの「詳細別」行・応用α入力下の「根拠（選ぶと…）」見出し＋根拠チップ を削除。推奨応用α値の表示は残置（根拠なし＝銘柄全体推奨へ自動フォールバック）。案内文言「シグナル・詳細を選ぶと…」→「シグナルを選ぶと…」。
- **早見カード**: EP価格右の③底抜け値・詳細要約(底:/起:/特:)・インライン編集の起点select/特徴チップ・`_EpnAddSection` 内の応用α根拠チップ を削除＝カードは EP値・α内訳・(推奨元)・採用α(基本/応用)・RN・済/編集のみ。
- 未使用化した定義（nSelB/nSelK/nSelF/detOpen/cands/_detAdd系/onEditK/onToggleF/_candK/_candF）は無害につき残置。検証=app-04.js V8 parse OK＋`EpNaviPanel` 合成データ実マウントで KEEP(推奨基本α/水準線/採用α/RN/予定EP/早見EP)=全live・DEL(底抜け名/詳細を入力/その他複数可/根拠選ぶと/起点名/特徴名)=全消滅・クラッシュ無し。

### 2026-07-24 ③底抜け欄「だけ」を復活（計算フォーム＋早見カード・sw v255→v256）
ユーザー要望「EPナビ、底抜けライン欄だけ復活させて。早見表でも」。上記2026-07-23削除の逆適用のうち**③底抜けのみ**を戻す（詳細トグル/③起点/④特徴/応用α根拠/推奨基本αの「詳細別」行/早見カードの起点select・特徴チップ・detTxt要約は復活させない）。残置していた state/ヘルパー（nSelB/setNSelB/cands.b/_detAdd/_detRename/_detDelete/_detReorder）と loadForEdit(e.b)/doSave(b) がそのまま生きているためレンダー2行の復元のみ。
- **計算フォーム**（`_EpnCalcForm`・②シグナル `_lrow` の直下）: `nTag ? _lrow(_mgmtHead("③","底抜け"), _EpnChipMgr{items:cands.b,…}) : null` を復元。
- **早見カード**（`EpNaviPanel` の EP価格行）: `e.b ? <span 16px #475569>e.b</span> : null` を EP価格の右に復元。
- 検証=node --check OK＋`EpNaviPanel` 合成データ実マウント（早見カードに「EP 1234円→前日安値」／②シグナル選択後に③底抜け候補チップ「前日終値」が出現）＝pass。

### 2026-07-24 早見カードの配色を2チャンネル化＝面色は分足・EP数字色は採用α（sw v256→v257）
ユーザー要望「1分足はオレンジ・5分足は黄緑、基本αはEP文字を青・応用αは赤」（show_widgetモック提示→承認）。**app-04.js `EpNaviPanel._renderCard`（4652付近）のみ**。①**5分パレット `C`（has5）を緑→黄緑(lime)** に（bd #86EFAC→#A3E635・bg #F0FDF4→#F7FEE7・bbg #DCFCE7→#ECFCCB・bc #166534→#3F6212・sub #16A34A→#65A30D・main #15803D→#4D7C0F）。1分オレンジ/済グレーは不変。②**EP数字色 `epColor` を採用αで決定**＝`isDone ? C.main : (_applied ? "#B91C1C"(赤) : "#1D4ED8"(青))`。`_applied` = 浮き足カードは `e.ukiSpecial===true`（浮応）・通常カードは `e.specialUsed===true`。旧は「応用αのみ赤・他は分足色」。③**起点サブ行 `subColor` は分足色のまま**（`C.sub`固定・旧の応用α時#DC2626を撤去）＝色でαを示すのはEP数字だけ（ユーザー選択）。面色（枠/背景/分足バッジ）は従来どおり分足駆動。検証=V8 parse OK＋実マウントで computed color 実測（1分=bg#FFF7ED/5分=bg#F7FEE7・基本α=EP rgb(29,78,216)青/応用α=rgb(185,28,28)赤/済=rgb(148,163,184)灰）全一致。

### 2026-07-22k 監査の残課題をユーザー決定で解消（既定サブタブ/帯頻度再計算/死コード削除/残stale文言）（sw v238→v239）
- 2026-07-22jの「未修正（ユーザー判断待ち）」3点をAskUserQuestionで確定し実装。
- **①シグナル総合の既定サブタブ "uki"→"band"**（app-06 `sigSub` useState）＝移設先の株価帯別を前面に（ユーザー決定）。実マウントで📡シグナル総合クリック→帯本体が既定表示を確認。
- **②帯コンテキストの頻度を帯基準に再計算**（ユーザー決定＝注記でなく再計算）: `_elBaseAlphaPick`/`_elSpecialAlphaPick`/`_elBaseAlphaA`/`_elBaseAlphaDetailV2`/`_elTotalAlphaSectionV2` に**任意引数 `spanOverride`** を追加＝頻度ゲート(`_fspan`)＋表示頻度列(`_baseSpan`/`_span`)の分母を外部指定に。`_groupPanel` に `bandSpan` 引数を追加し `_bandAxisBody` が実帯グループ("bN")で `_pbBandBizDays`（その帯だった営業日）を計算して渡す。app-04の `_PbDayBandReco`（`_elBaseAlphaA(pool,aiOf,span)`＋モーダル）と `_ElDayAlphaPair` 帯モーダル（`_bandSpan=_pbBandBizDays`）も渡す。**帯ピルとα詳細表の頻度が同一分母に統一**（旧＝ピル帯基準/表は記録スパンで乖離）。**後方互換＝spanOverride未指定は`_elBizSpanDays`（従来）で非帯呼び出しはバイト等価**（arity pick3/A3/detail6/total6/special4＋source確認・V8＋実マウントconsole0）。⚠️新たに帯コンテキストでα詳細表を出す時は`spanOverride`/`bandSpan`を渡す。**【同日修正 sw v240】頻度の分母も帯基準に統一**＝初版は分子(span)だけ`_pbBandBizDays`（銘柄×営業日セル）にして分母が`_elEnteredDays`（distinct日付）のままで**単位不一致**だった（ユーザー指摘「頻度の解釈あってる？」）。**`_elEnteredCells`（銘柄×日付セル・ピルの`_entSeen`と同一(stock\|date)キー）を新設**し、`spanOverride`時は`_freqOk`ゲート（基本/応用pick）＋表示頻度列（詳細表/合計α表）の**分母もセル基準に切替**＝ピルと同単位（セル÷セル）。単体テスト＝同日別銘柄2件で`_elEnteredCells`=2/`_elEnteredDays`=1を確認。
- **③死コード削除（ユーザー選択）**: app-06 `_sigGroups`(死変数)/`_sumStockContent`(未使用関数)削除。app-04 `_EpnDayAlphaField`(未マウント関数・_ElDayAlphaPairへ差替済)削除／`_spModal`(応用α詳細ポップアップ)・`_tableModal`(表参照ポップアップ)のIIFE本体を削除しnull化＋`showSpTable`/`tableStock` state撤去（いずれもsetX(true/stock)呼出無し＝到達不能を確認済）。**表参照は_ElDayAlphaPairに集約**。EpNaviPanel実マウントで無破綻を確認。**EPナビ日替わり列の「表示」ラベルはユーザー選択せず＝見送り**。
- **③残stale文言（ユーザー選択）**: app-06 合成スコア→平均最終損益（2731/3921）・追加α損切り率20/30%→40%＋na=頻度ゲート解除（2240-2241/2666）のコメント修正。**app-05:8595の日付タグ(g→h)は\uエスケープ保存でEdit不能につき見送り（非表示コメント）**。

### 2026-07-22j 当日変更(v228〜v237)の横断整合性監査＝5観点並列＋実コード検証で確定バグ/不整合/stale文言を修正（sw v237→v238）
- **監査方法**: 観点別5エージェント（A記録帳UI/B算入母数/C定数文言/D日替わり改名/E帯推奨α）並列＋各指摘を実コードReadで検証＋V8構文＋実マウント再テスト。**確定分のみ修正**（設計判断はユーザーへ別途）。
- **[BUG・今日の回帰] `setDetTagMode(false)`×4 → `"sig"`**（app-06:6985/6989/6995/7015）: v229でdetTagModeをboolean→3値文字列化した際、リセット4箇所が旧`false`残存＝銘柄タップ等で切替後に**銘柄別「集計」の分析軸トグル(🎯/🏷/💴)が全部無ハイライト**（中身はsig・選択状態と乖離）。実マウントで🎯シグナル別が`rgb(154,52,18)`ハイライト復活を確認。
- **[BUG・データ消失の穴・今日拡大] 銘柄改名の重複ガード**（app-03:1325）: `stocks.indexOf(nn)`は固定タブ銘柄のみ照合＝**日替わり候補(rotStocks・今日v231でタブから除外)/非表示(hiddenStocks)と同名に改名すると素通り**→`custom.stocks`重複＋`charts`同名日付キー上書き（データ消失）。`(stocks∪rotStocks∪hiddenStocks)`照合に修正（両propはStockTabsに既存app-03:1107/1114）。
- **[不整合] `_indDataOnlyCand`（app-05:6518）に未指定日カーブアウト追加**: `_isDataOnly`(app-05:3140 `if(!_ds)return false`)と非対称＝取引銘柄**未指定日**でも候補の新規記録が既定【合計算入OFF】になり、同条件の旧記録(includeInTotal未設定→_isDataOnlyで算入)と算入が逆転。`if(!_dsInd)return false`追加で対称化（v232不変条件「未指定日は現行一致」に整合）。
- **[不整合・ユーザー表示] α選定条件の説明文が実ゲートと乖離（今日の定数変更で拡大）→定数補間化**: app-06:2624「理想−2円…1円下げた」矛盾（offset1→2の残骸）→「少し下げた」／app-06:2917・2955（ChartSection＋DayViewの推奨α説明）「到達率50%以上・損切り率20%以下・損切り30%緩和・応用到達50%/損切り20%」（実=70%/40%・フォールバック到達率50%）→`_EL_ANA_REACH_DEF/_EL_BASE_MAX_STOPRATE/_EL_ANA_REACH_FLOOR2`等へ補間／app-06:2483 noteSubの到達率が誤定数`_EL_BASE_MIN_ERATE`(50%)→`pick.reachFloor`(70%)＋フォールバック文言修正／app-04:6486 週次表の説明が「合成スコア・到達率50%」（実=平均最終損益・70%）→補間で全面更新。実マウントで「到達率70%」表示・旧「50%以上」消滅を確認。
- **[stale文言修正] コメント**: app-06:2078「理想−1」→`_EL_ALPHA_OFFSET`／5669 detTagMode「全銘柄側は銘柄横断」→「銘柄別タブ専用＝横断はシグナル総合へ」／5672 sigSubにband追記／2058「na緩和全廃」→到達率50%フォールバックは復活済(2065)と整合。**v237のコメント日付タグ`2026-07-22g`→`2026-07-22i`**（g=到達率60→70の別変更・app-04/06計8箇所）。
- **[v237 UX] `_ElDayAlphaPair`**: インライン推奨に「（銘柄別）」明記（同画面の`_PbDayBandReco`帯推奨と混同回避）／空帯フォールバック文言を「帯未判定/材料日」と「帯判定済み・記録0件」で分離。
- **未修正（ユーザー判断待ち・別途提示）**: ①sigSub既定を"uki"→"band"にするか（最左bandだが既定uki）②帯コンテキストの頻度不一致＝帯ピル(新`_pbBandBizDays`)とα詳細表の頻度列(旧`_elBizSpanDays`＝帯外日含む)が同ラベルで乖離（app-04:3708 vs app-06:2568）③EPナビ▽と📅●の絵文字被り＝タッチで紛らわしい。**低優先の残stale文言**（app-06:2240-2241/2666/2731/3921 損切り率/合成スコアのコメント・app-05:8595 date tag[\u escape で編集困難]）。**死コード**（`_EpnDayAlphaField`/`_spModal`/`_tableModal`/`_sigGroups`(6229)/`_sumStockContent`(6149)）。

### 2026-07-22i 株価帯別をシグナル総合へ移設＋EPナビ日替わり列＋α詳細表の株価帯別トグル（sw v236→v237）
- ユーザー要望3件（AskUserQuestion＋show_widgetモックで確定）。**A: 株価帯別分析をシグナル総合の先頭サブタブへ**（app-06）＝`_SIG_TABS`に`["band","💴 株価帯別"]`を浮き足%の左（先頭）へ追加＋`sigSub==="band"`分岐で`_bandAxisBody(_v2recsAllData,true)`（全銘柄横断＝帯共通αの検証）。**全銘柄「集計」の分析軸トグル（💰全体/💴株価帯別＝`_allAxisToggle`）は撤去**＝損益ダッシュボード専念（**銘柄別タブの株価帯別軸`_axisBtns`は存続**・detTagMode共用）。sigSub既定はuki据置（左に足すだけ）。
- **B: EPナビ右端に日替わり列**（app-04 `EpNaviPanel`）＝`custom.rotatingStocks`（実在・日経・固定表示銘柄を除外＝二重表示回避）を候補プールに、見出しの`<select>▽`で1銘柄を選び固定3銘柄の右に1列追加（本日採用α値/早見カード/計算フォームが固定銘柄と同じに動く）。**表示のみ＝指定dailyStock/合計算入は変えない**（ユーザー確定）。既定表示＝その日の指定銘柄（`_dailyStockGet`）→無ければ候補先頭。選択は端末ローカル`localStorage scalping_epn_rot_v1`（per-date・Firebase非同期）＝**新関数`_epnRotGet`/`_epnRotSet`（app-04・_dailyStockSet直後）**。grid列数`_colN=epnStocks.length+(_hasRot?1:0)`・`_rotSel`は失効時_rotDefaultにフォールバック・列key`epncrot_/epnfrot_`は固定列と別prefix（衝突回避）・stock変化でkeyが変わり_EpnCalcFormはリマウントされてフォームstateがリセットされる。
- **C: 「本日の採用α値」表を参照に`[💴株価帯別|🏷銘柄別]`トグル**（app-04 `_ElDayAlphaPair`・**既定=株価帯別**）＝共有部品なのでEPナビ・日別ページ(銘柄タブ)・記録帳の取引/銘柄別記録の**4箇所すべて**に反映。株価帯別＝この銘柄の本日の帯と同じ帯だった全記録（`_pbDayBandOf`→`_pbBandPoolFor`・銘柄横断・前日まで・データ算入＝`_PbDayBandReco`と同じプール）。帯不明/材料日は帯プール空→**銘柄別へ自動フォールバック**（`💴株価帯別`ボタンdisabled＋注記「株価帯が未判定/材料日のため銘柄別で表示」）。取込先(onPick)は常にこの銘柄の本日採用α値（`_epnDayAlphaSet`/`_epnDaySpecialAlphaSet`）。state`tblScope`（既定"band"）。
- 検証: 全9ファイルV8構文OK（`new Function`）＋実マウント（別ポート3468別オリジン・空データ→合成データ）。A=EntryLogViewでシグナル総合クリック→サブタブ順`["株価帯別","浮き足%","RNまたぎ"]`・帯本体描画（「帯＝日×銘柄で判定」）・全銘柄集計に💴株価帯別トグル無し（分析軸ラベルも消滅）。B=EpNaviPanelで▽既定＝指定銘柄(安川電機)・候補2件（件数付き「安川電機（1）」）・列描画。C=_ElDayAlphaPairで表を参照→既定株価帯別で「同じ帯だった全銘柄・3件」（帯プール=銘柄横断・SUMCO/JX金属）・帯不明時は銘柄別フォールバック注記。console 0。実データ/見た目はユーザー実機。

### 2026-07-22h スルー選択で算入チェックを自動オフ（sw v235→v236）
- ユーザー要望。記録フォームの実エントリー4択（あり/見送り/スルー/要審議・app-05:8593 onClick）で**スルー選択→「合計算入」「データ算入」を自動でOFF**（`setFIncl(false)`/`setFInclData(false)`）＝スルーは合計・分析とも常に不算入（`_elInclTotal`/`_elInclData`がpassThroughでfalse）なので見た目のチェックも外す。**スルーから他状態へ戻すときは既定へ復帰**（`else if (fThru)`＝候補なら合計OFF/データON、それ以外はON/ON）＝あり記録を誤って除外したまま保存しないため。実マウントで initial=✓✓ / スルー=外れる / あり=✓✓に復帰 を確認。⚠️検証時のstaleキャッシュ注意（app-05だけHTTPキャッシュで旧版配信＝`fetch(cache:'reload')`で全app-*.jsをprime後にreloadが確実・reference_scalping_verify参照）。

### 2026-07-22g α推奨の到達率下限 60%→70%（sw v234→v235）
- ユーザー要望「推奨条件に到達率70%以上を加えて」。`_EL_ANA_REACH_DEF` 60→70（app-06:1485）＝基本α★(_elBaseAlphaPick)・応用α★(_elSpecialAlphaPick)の全条件ゲート`_confAt(reachFloor)`が到達率≥70%を要求（両方が共有`reachFloor`を使用）。ステッパー(`_ElAnaReachCtl`/custom.anaReachFloor・10刻み)の既定も70%。フォールバック`_EL_ANA_REACH_FLOOR2`(50%・目標未達時に参考=青★を出す安全網)は据置＝70%を満たすαが無ければ従来通り50%まで下げて参考表示。⚠️ステッパーを過去に手動調整して`custom.anaReachFloor`を保存済みならその値が優先（過去の60/50/60変更は定数だったので通常は未保存＝70%が効く）。

### 2026-07-22f 計算/データ算入の分離＋第2弾（株価帯の推奨α表示・帯カスケード・頻度再定義）（sw v232→v233）
- **計算/データ算入の分離（Req1）**: 記録フォーム最下部の「計算・データ算入」1チェック→「**計算算入（合計損益に算入＝_elInclTotal）／データ算入（分析に算入＝_elInclData）**」の2チェックに分割。`_elInclData(s)`（app-05・_elInclTotal直後）＝passThrough→false／includeInData明示があればそれ／未設定は includeInTotal に追従（**移行不要**＝既存チェックの入切をそのまま2軸に引き継ぐ）。`_elFilterData` も追加。
  - **分析母数を_elInclDataへ（約35箇所）**: 推奨α全経路（_epnCascade/_saByStock/_elStockRecsBefore/最良αbadge/期間窓_okR）・OS分析・今週α表_wkGroups・シグナル別勝率_sigStats・浮き足/RN/追加α pool・詳細タグ母数・ホームダッシュボード。**合計損益（計算算入）は_elInclTotalのまま据置**。
  - **記録帳(app-06)の共有母数を分割**: `_v2recsAllData = filtered.filter(_epIsV2 && _elInclData)` を新設し、**分析パネルを再ルート**（6228 `_sigGroupsAll`→銘柄別軸[集計/α値/損切り/未達/深掘り/詳細タグ別/株価帯別]＋_selSigRecs(Scoped)が一括／浮き足RNボード／band）。**合計損益ダッシュボード（_ovPnlTbl/KPI早見_sumMonthRecs2/期間タブ_perRecs/累積/連勝連敗/指値同値）は_v2recsAll/v2recs系のまま**。設計＝「分析パネルに同居する金額（一番引っ張った損益等）はデータ側、合計ダッシュボードに同居する率（到達率等）は合計側」の主目的振り分け（関数内二重化は回避）。
  - **不変条件（検証済）**: includeInData未設定なら `_elInclData ≡ _elInclTotal` → `_v2recsAllData ≡ _v2recsAll` → 分割前と一致。V8で全ケースpass。
  - **データ算入の既定（ユーザー要望）**: 「本日の取引銘柄」に選ばれていない候補銘柄（rotatingStocksに属しdailyStock[日]でない＝データのみ）の記録は、計算算入の状態に関わらず**データ算入を既定ON**（分析には残す）。フォーム`fInclData`の初期化で判定（`_indDataOnlyCand`・fStock/fDate基準）。
- **株価帯の推奨α（第2弾/Req2＋Req4）**: 
  - **helpers（app-04・_dailyStockSet直後）**: `_pbBandPoolFor(data,bandIdx,beforeDate)`＝同じ株価帯だった全記録（銘柄横断・前日まで・データ算入・材料日除外・各(stock,date)の帯は_pbDayBandOfで動的判定・chartsごとにメモ化）／`_pbBandBizDays(data,bandIdx,pool,holiSet)`＝頻度の分母＝プール各銘柄のプール日付範囲の営業日のうち「その帯だった日数」の合計（(銘柄×営業日)セル数）。
  - **本日の推奨α表示（Req2）**: `_PbDayBandReco`（app-04・`_PbDayBandBar`内の固有材料の下にマウント）＝その銘柄の本日の株価帯と同じ帯の推奨**基本α/応用α**（_elBaseAlphaAを帯プールに適用）＋**頻度**（Req3＝帯営業日/EP到達日で「N日に1回」）＋📊帯別α詳細表ボタン（_elBaseAlphaDetailV2/_elTotalAlphaSectionV2を帯プールで開く）。材料日/帯不明は非表示。
  - **帯カスケード（Req4）**: `_epnCascade`に**帯フォールバックleg**を追加＝銘柄全体が確信推奨を出せない（!stk.ok）ときだけ帯プールから推奨基本αを計算（perf: 通常は計算しない・lazy）。カスケードを**詳細→シグナル→銘柄→帯**の順に（3つの梯子builder `_epnRecalcBase`/`_epnBaseLevelKey`/autoPick`_autoBase`＋`_bp`キーマップ）。**帯は純フォールバック＝既存推奨は不変**（V8で stkWinsOverBand/bandPicked 検証済）。
- **頻度の再定義（Req3）**: 「その株価帯であった営業日の中でEP到達が何日に1回か」＝分母 `_pbBandBizDays`（その帯だった(銘柄×営業日)セル数）÷分子（EP到達した(銘柄×日)セル数）。**株価帯コンテキスト（本日の推奨α表示）でのみ適用**。記録帳の株価帯別パネルは`_kpiOs`（頻度カード無し）・非帯の頻度（シグナル総合RN/浮き足・_kpiBlockOf）は従来どおり`_elBizSpanDays`（記録スパン）＝帯スコープでないため不変。
- **フォームUI確定（ユーザー確定）**: 記録フォームの算入UIを「**合計算入／データ算入**」の2チェック横並びに統一。**②の「合計算入(本日の取引銘柄): 自動/入れる/外す」セレクタは廃止**（fTotOv/totalOverride保存も削除）。
- **候補銘柄の既定（ユーザー確定 2026-07-22f）**: 「本日の取引銘柄」でない候補銘柄の**新規記録は既定で【合計算入OFF・データ算入ON】**＝合計から外れ分析に残るのが見た目で分かる（`_indDataOnlyCand`をfIncl/fInclData両方の初期化に使用・fIncl既定を`候補?false:true`に）。**`_isDataOnly`を整合化**＝`signal.includeInTotal`が明示設定済み(true/false)ならチェックが制御（自動判定は上書きしない）＝候補でも合計算入ONにすれば合計に入る／未設定(null)の旧記録だけ動的自動判定にフォールバック（遡及・後方互換）。V8で候補OFF=除外/候補ON=算入/未設定=動的除外/空プール≡不変 全pass。
- **敵対レビュー(find→verify 2エージェント)修正3件**: (1)🐛`_pbBandPoolFor`のcacheが`priceBandBounds`（境界）変更でstale＝chartsのみkey化だった→boundsシグネチャをcacheキーに追加。(2)🐛頻度の分母(銘柄×営業日セル)と分子(旧`_elEnteredDays`＝日付のみ)の単位不一致→分子を(銘柄×日)セルに統一。(3)銘柄別🧮シミュが分析根(`_v2recsAllData`)由来で損益what-ifに計算off/データon記録が混入→SIM母数を`_elInclTotal`（money）でフィルタ。**コア（不変条件/分類/配線/カスケード安全性）は全PASS**。

### 2026-07-22e ②データのみ除外（候補銘柄でその日の指定でない記録をグランド合計から除外・分析は残す・sw v231→v232）
- **コア（app-05・_elInclTotal直後）**: `_isDataOnly(data,r)`＝手動override（`signal.totalOverride` "in"=常に算入/"out"=常に除外）＞自動（`r.stock`が候補プール`custom.rotatingStocks`所属 かつ その日`data.dailyStock[date]`が**指定済み** かつ `r.stock!==指定銘柄`）。**ガード＝指定日のみ発動**（未指定日は候補も算入＝不変条件「プール空 or dailyStock未指定なら現行と一致」を満たす・ユーザー確定）。dailyStook参照はインライン（app-04依存を避ける）。`_elInclTotalAmt(data,r)=_elInclTotal(r.signal)&&!_isDataOnly(data,r)`＝**グランド/銘柄横断の合計消費側だけ**で使う金額版。
- **方針**: 分類監査で全`_elInclTotal`（6ファイル）を (G)グランド合計=`_elInclTotalAmt`へ差替／(S)銘柄別の自行・自タブ=`_elInclTotal`据置／(P)分析母数=据置 に分けた。**分析母数（α/OS/推奨α/浮き足/RN/株価帯別プール・`_v2recsAll`）は一切緩めない**＝データのみ記録もα分析には残る。
- **(G)差替え箇所**: app-04＝`_enteredSigsT`（検索日カード）・`_trEntryAgg`（取引集計 realSum/ok/ng/totRecords）・`_pnlDetailTableEl`（展開P&Lの`_inclTpb`/`_pbAllMiss`を新引数**`amtScope`**でグランド時のみ除外）・`_wkRow`（今週の週合計/日別行＝全銘柄横断）・`_pbAllRecsT`＋`_pbAllReal`/`_pbAllEnt`（本日「合計」行＝per-recordで再計算・銘柄別スカラー`_pbRealByStk`は据置）・最終損益合計バッジ（`rowKey==="__total__"`時のみ）。app-05:111（カレンダー日次）。app-08:1015（ホーム月次）。app-06＝全体タブ集計（`_ovPnlTbl`/`_fillRiskSection`/KPI月`_sumMonthRecs2`/累積・連勝連敗`_v2recsNonRef`は`_v2recsAmt`から）＋期間タブ（`_perRecs=_isAllStock?データのみ除外:v2recs`＝全体タブのみ除外・銘柄別タブは据置）。
- **`amtScope`（app-04 `_pnlDetailTableEl` 第8引数）**: 既定falsy＝銘柄別展開は据置。グランド展開（`_pbExpRow("__total__")`）と今週展開（`_wkExpRow`）でのみtrue＝共有関数を壊さず両立。
- **(S)/(P)据置（変更なし）**: app-04 `_skT`（銘柄別行）・本日データ分析/OS別/`_epnCascade`（推奨α）。app-02 ChartSection全て（`_recsForTot`/`_detailTotRowFor`/`_sumRow`/α表・per-stock）。app-05 `_hdRecentRecords`/被り除外母数/早見表stats共有関数/詳細タグ母数。app-06 `_v2recsAll`/`_sigGroups`/分析プール。app-01 `_saByStock`（推奨α）。
- **記録フォーム（app-05 EntryRecordForm）**: 「計算・データ算入」欄の下に「**合計算入（本日の取引銘柄）: 自動／入れる／外す**」セグメント新設（`fTotOv`/`setFTotOv`・既定auto・保存`totalOverride`=auto時null）。分析母数は不変の注記つき。
- **敵対レビュー修正2件（find→verify 2エージェント）**: (1)**`handleRenameStock`（app-04:4976）が`dailyStock`を改名追従していなかった**＝`dailyStock[date]`は値が銘柄名なので、指定銘柄を改名すると記録が「候補で未指定」＝データのみ扱いに落ちて全グランド合計から静かに消える（指定●も外れる）→`rotatingStocks`追従の直後に値remap（oldName→newName）を追加。**⚠️銘柄名を値/キーに持つ永続データを新設したら`handleRenameStock`に追加**（signalTags等と同方針）。(2)**全銘柄横断（scope=null）の時間かぶり母数`_elCollisionExcludedSet`（app-05:4218）はデータのみを外す**＝参考トレードは実トレード枠を占有しない→指定/固定銘柄の記録を被りで落とさない。母数gateを`scopeStock?_elInclTotal:_elInclTotalAmt`に・memoキーに`dailyStock`/`rotatingStocks`identity追加（銘柄別scopeは_elInclTotalのまま＝自タブは自記録で被り判定）。
- **検証**: V8で`_isDataOnly`/`_elInclTotalAmt`を直接11ケース単体テスト全pass（指定日=データのみ/未指定日=ガードで算入/override in・out/固定銘柄/プール空でAmt≡Incl＝不変条件）＋改名migration再現テスト（rename後に指定銘柄が算入に戻る）＋衝突集合no-throw。実マウント無エラー。push済(dd9e6ab)。

### 2026-07-22d 日替わりを「本日の取引銘柄」per-day化＋タブを外国市場の右に固定（①・sw v230→v231）
- **概念変更**: 「日替わり銘柄」＝1日1つ実際に取引する銘柄を指定（`data.dailyStock[日付]`・trades/foreignMarkets同型のtop-levelマップ＝汎用マージで同期）。`custom.rotatingStocks` は「候補プール」に役割変更。以前の集約チップ切替（v229）を置換。
- **helpers（app-04・_PbDayBandBar直前）**: `_dailyStockGet(data,date)`／`_dailyStockSet(save,date,stock)`。
- **タブ配置（app-03 StockTabs）**: 📅日替わりタブを**外国市場ボタンの右に固定**（旧位置＝銘柄マップの後から移動）。`onRotSelect` があれば常時表示。ラベルは`rotLabel`（常に「📅 日替わり」）＋**`rotDayStock`（2026-08-05追加）**。`rotDayStock`が非空なら**2段表示**（1段目=見出し「📅 日替わり」・2段目=銘柄名。**文字サイズは両段とも13pxで同一**＝2026-08-05のユーザー指示。太さのみ600/700で差をつける）、空なら`rotLabel`の1行のみ。旧仕様は`rotLabel`自体を「📅 銘柄名」に差し替えていたため見出しが消え、どのタブか分からなくなっていた（app-03:1237-1268 / 受け渡しはapp-04:5630）。2段時はpadding上下を8→2pxに詰めて**他タブと同じ37px**に揃う（border1px×2込みで実測調整）。
- **DayView（app-04）**: `rotTabActive`（fmActiveと並列の明示state）＋`rotViewStock`（タブ内の表示銘柄・指定とは別）。`dayStock=_dailyStockGet`、`dispStock=rotTabActive?_rotView:activeStock`（フル表示に渡す銘柄）。`activeStock` は候補プールを除いた固定銘柄優先に。旧`_rotChipBar`→**`_rotPickerBar`**（「📅 本日の取引銘柄」バー＝候補を「銘柄名（件数）」で並べ、名前タップ=表示/記録切替 setRotViewStock、各チップの**指定●**=dailyStock[date]に指定（赤●・合計算入）、＋でその場追加）。
- **中身**: rotTabActive時は`_rotPickerBar`＋**固定銘柄と同じフル表示**（_PbDayBandBar/早見表/日足/ChartSection/bench を`dispStock`で描画）。StockTabsの`active`は(fmActive||rotTabActive)で空、onSelectでrotTabActive解除。
- **記録件数**: `_rotRecCount(stk)=charts[stk_date].signals.length`。候補チップに（件数）表示。
- **注意**: 損益/分析の母数は**未変更**（②データのみ除外は別コミット予定）。指定＝表示ではない（安川電機を表示・記録しても指定＝合計算入にはならない＝②で「未指定候補はデータのみ」に）。実マウント検証済み（タブ固定位置・件数・指定●・表示≠指定・フル表示）。

### 2026-07-22c 株価帯バーの前日終値を早見表と一致（バグ修正）＋材料あり→固有材料〇×＋材料タグ管理（sw v229→v230）
- **前日終値バグ修正（app-04 `_pbPrevClose`）**: 旧実装は日足CSV（sn_dc_csv_v1_*）を独立に読み、早見表(StockQuickRefTable)と食い違った（JX金属で前日終値4,653と誤表示・正は早見表7/21終値3,665）。真因＝早見表は `charts[銘柄_日付].dayClose`（CAツールの当日1分足終値・開くたび自動取得）を読むのに株価帯バーだけ別フィードのCSVを読んでいた。→ `_pbPrevClose(data,stock,date)` を **dayClose走査**（date未満の最大日付のdayClose・`Math.round`＝早見表と同一源同一丸め）に置換。`_pbBarsOf`/`_pbCsvCache` 削除。`_pbDayBandOf` は `_pbPrevClose(data,...)` を呼ぶよう変更。移行不要・早見表が新鮮化されれば即追従。CAツール未取得日は前日終値null＝「未設定」（早見表が「—」の日と整合）。
- **固有材料UI（app-04 `_PbDayBandBar` 縦2段化）**: 「⚡材料あり」トグルを廃し、バーを上段（本日の株価帯）＋下段（固有材料）に分離。下段＝「⚡固有材料」ラベル＋〇×セグメント（既定×＝`dayMaterial` false）。〇のとき材料タグ選択UI（`_EpnChipMgr` 流用＝タップで選択・✎編集で改名/削除・ドラッグ並替・＋追加）。マスター＝`custom.materialTags`、選択＝`charts[ck].dayMaterialTags`（名前ベース）。ハンドラは `_PbDayBandBar` 内インライン（`_matAdd`/`_matDelete`/`_matReorder`/`_matToggle`）＋改名cascade `_matRenameData`（トップレベル）。
- **改名cascade `_matRenameData`（app-04）**: `custom.materialTags` の改名＋`charts[*].dayMaterialTags` の旧名も追従（重複排除・既存名への統合可）。**孤児（マスター削除済みだが過去日に選択残存）の改名も効くよう、chartsカスケードは無条件・マスター更新は `oi>=0` 時のみ**（`_elSignalRenameData` と同型・敵対レビュー指摘の修正）。削除は候補からのみ（過去の選択は残す＝signalTags同方針）。
- **同期（app-01 `_LOCAL_WINS_KEYS`）**: `materialTags` を追加（マスター配列の端末間LWWマージ保護＝signalTags等と同列）。`charts[ck].dayMaterialTags` はcharts差分同期に乗るため追加不要。
- 保存する新データ: `charts[ck].dayMaterialTags`（選択タグ名配列）／`custom.materialTags`（材料タグマスター）。`dayMaterial`(bool)は〇×として流用（株価帯別分析の「⚡材料あり」グループは従来どおり `dayMaterial` bool でグループ化）。

### 2026-07-22b 「本日の株価帯」＋記録帳「株価帯別」分析軸＋📅日替わり集約タブを新設（sw v228→v229）
- **株価帯エンジン（app-04・_epnDaySpecialAlphaSet直後）**: `_PB_DEF_BOUNDS`[4000,5000]／`_pbBoundsOf(custom)`＝custom.priceBandBounds正規化（昇順・重複除去・空なら既定）／`_pbBandIdx(price,bounds)`／`_pbBandLabel(idx,bounds)`（〜4000/4001〜5000/5001〜）／`_pbBarsOf(code)`＝sn_dc_csv_v1_*を_parseDailyCsvしdates/closes化（3秒メモリキャッシュ＋uploadedAt変化で無効化）／`_pbPrevClose(custom,stock,date)`＝対象日より前の直近close（二分探索・証券コードは_caGetStockInfo）／`_pbDayBandOf(data,stock,date)`＝**日×銘柄の帯解決の単一源**（手動charts[ck].dayPriceBand＞前日終値自動＞null。{idx,src,prevClose,material,bounds}を返す）／`_pbDayBandSet`／`_pbDayMaterialSet`（_epnDayAlphaSetと同じsaveパターン）。
- **DayView（app-04）**: `_PbDayBandBar`＝銘柄タブ直下の常時バー（帯チップ＝タップで手動保存・手動中の同帯再タップ/↺で自動へ・自動時は「前日終値N円」注記・⚡材料ありトグル＝charts[ck].dayMaterial）。chartsタブの銘柄コンテンツFragment先頭にマウント。
- **設定「📊データ・銘柄」（app-04）**: 「💴 株価帯の境界」＝カンマ区切りFastInput→custom.priceBandBounds（動的判定なので過去分も即追従・現在の帯プレビュー付き）＋「📅 日替わり銘柄」＝銘柄チップでcustom.rotatingStocksをトグル。チャート分析ツール連携とシグナル管理の間に2セクション追加。
- **記録帳（app-06）**: detTagModeをboolean→**文字列3値**（"sig"/"det"/"band"）化・bandSel state追加（選択帯キー・nullは件数最多帯へ）。銘柄側の分析軸トグルを常時表示化＋「💴 株価帯別」追加（詳細タグ別ボタンだけ_hasDetTags条件）、全銘柄側（💰損益・集計）にも「💰全体/💴株価帯別」トグル新設（state共用・band以外は全体扱い）。`_bandAxisBody(pool,cross)`＝帯グルーピング（(stock,date)メモ化・⚡材料あり日は帯から外し独立グループ・判定不能は「帯不明」・帯は0件でも表示）→_subTabBarピル→`_groupPanel(recs,null,recs)`＝詳細タグ別と同型（KPI/OS分布/推奨基本α・応用α/α詳細表が既存パネルのまま帯プール＝銘柄横断で出る）。文脈カード非表示条件も3値対応。
- **📅日替わり集約タブ（app-03/app-04）**: StockTabsに追加プロップ`rotStocks/rotActive/onRotSelect/rotHasData`（＋ボタンの前に「📅 日替わり（N）」インディゴタブ・rotStocks空なら非表示＝従来と同一）。DayView側: rotStocks＝custom.rotatingStocksの実在フィルタ・常設タブ列から日替わり分を除外・onReorderは**渡していない銘柄（日経平均/日替わり）を末尾保持**しマスター消失防止・`_rotChipBar`＝タブ内銘柄チップ列（本日データあり赤点・選択はscalping_rot_cs_v1に記憶・「＋」でマスター＋日替わり属性へ同時追加）・handleRenameStockでrotatingStocksも改名追従。
- 保存する新データ: charts[ck].dayPriceBand（帯idx・手動上書きのみ）／charts[ck].dayMaterial／custom.priceBandBounds／custom.rotatingStocks。帯の自動判定結果は保存しない（動的計算）。

### 2026-07-21d 日別ページに「本日の採用α」欄＋記録フォームα既定のdayAlpha化＋EPナビ応用α対称化＋浮き足応用%の下限（sw v223→v224）
ユーザー要望3件＋敵対的レビュー修正。(1)要望1: 取引タブ「エントリー記録」見出し直下に既存部品 _ElDayAlphaPair(基本α+応用α本日採用値・stock=activeStock) を配置・📖記録帳ボタン削除(他タブ5135/5215に導線残る・app-04:5250/5243)。(2)要望2: 記録フォーム(EntryRecordForm)の基本α/応用α既定を「本日の採用α値(charts[stock_date].epNaviDayAlpha/epNaviDaySpecialAlpha)→無ければ従来推奨」に(_baDefault/_spDefault・新規のみ!isEditゲート・見出しの推奨表示は_autoBaseAのまま=履歴推奨・dayAlpha採用時は欄に出所ラベル)。(3)要望3: 浮き足応用の推奨%を必ず基本%より大きく(同値不可・10%刻み→基本+10以上へクランプ・_elUkiPctPickScopedのspecialでbasicを再帰取得しfloor=basic+10・runnerUp重複はnull・app-06:3494)。
- **レビュー修正(Workflow4観点→10 confirmed・誤検出1棄却)**: A[回帰]dayAlpha時に基本α母数スコープ切替(_applyBaScope)が自動useEffectで巻き戻りデッドコントロール化→ _baTouchedRef.current=(_dayBaseA!=null) で手動上書き扱い(app-05:6427)。B[要望連携の核心]＋追加の初期銘柄がallStocks[0](日経=指数)で見出し下α欄(activeStock)と食い違う→ activeStock優先(app-04:6497)。E[副作用]編集時にdayAlphaが既定に混入し得る→ _fBaseAInput/_fSpecialA/入力value を!isEditゲート(app-05)。C[非対称→対称化]EPナビ計算フォームが応用day-alphaを無視していた→ daySpecialAlpha配線(app-04:4396)＋_EpnCalcForm内daySpecialAlpha(3851直後)＋specialVフォールバックに手入力＞本日応用α＞推奨＞baseVで優先(3862)。D[二重表示]EPナビ版(各銘柄カラム)と見出し下版(activeStock)のα欄同居=値連動・意図通り・据置。F[UX]見出し履歴推奨と欄dayAlphaの食い違い→ dayAlpha採用時に「本日の採用α値/応用α値」出所ラベル(app-05・青/茶)。
- データ: charts[stock_date].epNaviDayAlpha(基本)/epNaviDaySpecialAlpha(応用)・get/set=_epnDayAlphaGet/Set・_epnDaySpecialAlphaGet/Set(app-04:3381-3407)。共通部品_ElDayAlphaPair(app-04:3410)。記録フォームはdayAlphaを読むだけ(編集は_ElDayAlphaPairのみ)。
- 検証: 全ファイル構文OK・EntryRecordForm新規で基本α17/応用α22＋出所ラベル・_elUkiPctPickScoped特殊クランプ(基本30→応用生20が40・生70は据置・runnerUp重複null)を実マウント/関数で確認。A/B/C(EPナビ応用α)は構文＋コード＋mount(実データはユーザー実機)。sw v223→**v224**。

### 2026-07-21b 浮き足加算の入力を「浮き値の直接入力」→「底抜け前足−底抜けライン＝差額を自動計算」へ（sw v221→v222）
ユーザー要望「浮き足加算の数値を、浮き値ではなく『底抜け前足』『底抜けライン』の2価格を入力して差額を自動計算、そこから加算値も自動に」。AskUserQuestionで確定＝**①底抜けライン＝既存の水準線を流用（相互反映・浮き足欄でも上書き可）②％は既存の加算率のまま（差額×率＝加算値／新しい乖離率指標は作らない）③レイアウト案B（前足・ライン2欄＋「浮きN円」差額バッジ）**。
- **データ（下流無改修）**: 浮き値`signal.ukiVal`は差額の保持役として存続＝`差額=max(0, 前足−ライン)`をuseEffectで`ukiVal`へ書込み。加算(`_elUkiAdd`)・EP・損益・分析・シミュは`ukiVal`駆動で不変。前足価格は内訳として新フィールド`signal.ukiPrevBar`に保存。**過去記録は移行せず**、前足が空なら保存済み`ukiVal`を据え置き（前足を入れ直せば再計算）。前足がライン以下なら浮き0。
- **記録フォーム（app-05 `EntryRecordForm`）**: `fUkiPrev` state新設＋差額useEffect（依存`[fUkiPrev,fLevelPrice,fUkiUsed]`）。ライン欄＝`_lvPriceBox`に第2引数`_label`を追加して流用（`_lvPriceBox("lv_uki","底抜けライン")`＝水準線`fLevelPrice`と同一state＝分足右/OS右の水準線欄と相互反映）。UI＝浮き値input廃止→底抜け前足input（緑・小数±1）＋「−」＋ラインボックス＋差額バッジ。doSaveに`ukiPrevBar`追加。
- **EPナビ（app-04 `_EpnCalcForm`）**: `nUkiPrev` state新設＋差額useEffect＋浮き足欄のライン欄＝`nLevel`流用（上の水準線欄と相互反映）。ラベル「浮き足加算（前足−ライン）」。`_resetForm`/`doSave`/`loadForEdit`に`nUkiPrev`/`ukiPrevBar`追加。
- **早見カード**: 保存済み実効加算`e.uki`の表示のみ（インライン編集は追加α根拠/RN/ライン併存だけ）＝浮き足の前足入力は対象外・無改修。
- 検証: node無し→preview(:3457)でSW/旧キャッシュ削除→リロード→`new Function`パース全8ファイルOK・console 0。両フォーム隔離レンダー＝`_EpnCalcForm`/`EntryRecordForm`で浮き足〇→前足4250・ライン4230→差額「浮き20円」・加算「＋10円」(floor(20×50%))・前足<ライン(4220)→「浮き0円」(0クランプ)・浮き足欄のライン入力がOS欄の水準線欄へ相互反映(4230)を確認。実データ確認はユーザー。sw.js APP_CACHE v221→**v222**。
- **2026-07-21c 追補**: EPナビ(app-04 `_EpnCalcForm`)の浮き足欄ラベルを記録フォームに合わせ「前足／線」→「底抜け前足／底抜けライン」にフル表記統一（早見サイドバーは列が狭いがflexWrapで折返し）。計算・保存は不変。sw v222→**v223**。

### 2026-07-21a シミュのRN自動加算を「乗せる」トグル化＝既定ONで復活（sw v220→v221）
2026-07-20kで「絶対値方式だけRN自動加算を常時OFF」にしたが、ユーザーが「やっぱりRN自動加算はありにしたい（既定ON）」と判断。**トグル化して撤回**した。

- **`rnAuto` state（既定true）＋UIトグル「RN自動加算: ☑乗せる」**（除外行の下）: 全方式一律に効く。ONで予定EP下二桁41-49/91-99を…50/…00へ自動加算（記録フォーム/EPナビと同じキリ番調整）／OFFで入力αがそのまま効く（下二桁に依らず判定が安定＝07-20kの「絶対値OFF」と同じ効果をトグルで得られる）。
- **配線**: `_manTiersOf` の `_rnD = rnAuto ? _simRnAt(r, _pre) : 0`（07-20kの `(rw.method === "abs") ? 0 :` を撤回）／自動配分 `_candEffAlpha` の `pre + (rnAuto ? _simRnAt(r, pre) : 0)`／`_autoSig` に `rnAuto` を追加＝トグルで全銘柄自動配分のゲートが再武装。
- **採用α±X系は対象外**＝RNが採用αに内包済み（`_manTiersOf`/`_candEffAlpha` とも adopt は early return で別処理）。
- **水準線未入力バッジ**は `rnAuto` OFF 時は非表示（RN自体を乗せないため無意味）。
- **建たない理由チップ（07-20k）は維持**＝ON時に下二桁で未到達になる記録は「未到達」チップで見える。
- **検証**: 実UIで既定=☑乗せる／水準線1043・OS高値5・絶対値3円で **ON=建玉あり1件(未到達2)・OFF=建玉あり3件** に切り替わる（実効7円⇔3円）／全銘柄自動配分で複数回の取引〇＋RNトグル切替でゲートが再武装／全タブ巡回で consoleエラー0。Firebase同期なし・投入データ削除済み。

### 2026-07-20k シミュ検算：絶対値方式のRN自動加算を撤去＋建たない理由を記録ごとに表示＋除外チェックを浮き足だけに（sw v219→v220）
ユーザー報告「オプション通りならエントリーできているはずの記録が建たないことになり、従来欄との差額が出ている」を検算。**4つの原因を実コードで再現**し、うち1つを修正・3つを可視化した。

- **🐛【原因①・修正】絶対値方式にRNまたぎ自動加算が乗っていた**: `_manTiersOf`(app-06.js:5041)が実効α＝`基底α＋浮き足＋_simRnAt(掃引α)`で組み立てるため、「絶対値3円」と入力しても予定EPの下二桁が41-49/91-99なら**最大+9円**が乗る。**実測: 水準線1043・OS高値5円・指定3円 → 予定EP1046（下二桁46）→ RN+4 → 実効7円 → 未到達で建たない**。同条件でも水準線1010なら建つ＝**OS高値が同じでも水準線の下二桁だけで結果が変わっていた**。→ `(rw.method === "abs") ? 0 : _simRnAt(...)` で絶対値のみ除外。自動配分側も`_candEffAlpha`(5085)で`cand.kind === "abs"`を同様に除外＝手動と挙動を揃えた。推奨α系(reco/recobase)は実運用の建値を模すので**従来どおり再判定する**（設計は変えない）。
- **【原因②・可視化】×フリップ**: αを上げるとEP足が後ろにずれ、`xBefore`(app-05.js:3457-3458)が手前の足の×を拾って`judge:"x"`＝見送りに反転する。**実測: legs=[OS1 高値5・次足×][OS2 高値9][OS3 高値12] で α=3→EP=OS1・ok・建つ／α=7→EP=OS2・judge=x・建たない**。仕様どおりだが画面から追えなかった。
- **【原因③・仕様】未到達は「0」ではなく「まるごとマイナス」**: 従来列`_kbConvSums`は建たなかった記録も全件加算するため、採用α5で従来800円の記録が掃引α7で未到達になると**差額が800円まるごと悪化**する。1件あたり従来額ぶん一方的にシミュ不利へ振れる。
- **【原因④・片側計上】採用α未入力**: 「採用α値±X」は`_adoptOf`(app-06.js:5006)が`s.alphaVal`直読みでnull→建てないが、従来列は`_elAlphaInfo`が`_gradeAlpha`（難度既定10円）へフォールバックして**金額を出す**（実測1300円）。※監査が挙げた「非v2の旧記録でも同じ片側計上」は母数が`_v2recsAll`由来で`_epIsV2`済みのため**この経路では発生しない**＝誤りとして除外。
- **【表示】建たなかった理由を記録ごとに**: `_mtNoBuildChip`＋`mrecs`に`row.noBuild`を追加し、マスター表のシミュ列を「—」から**未到達／×見送り／α不明／取引なし**のチップへ。銘柄別内訳(`_kbStockTable`)と同じ`_elKabuNoBuildReason`の分類を記録単位でも見せる。α列は元から「（浮N+RN M込）」の内訳を出しているので、修正後の絶対値方式では素の値だけが並ぶ。
- **【縮小】除外チェックを「浮き足〇」だけに**（ユーザー決定）: **RN〇の除外は意味を失っていた**＝保存値`rnUsed`の記録を母数から外すだけで動的再判定は止まらず、RNを使っていない記録にも加算が乗るので誤解を招くだけだった。**ライン併存**は概念として使わなくなったため撤去。`exFlags`は`{uki:true}`のみ・`_autoSig`からも該当項目を削除。
- **検証**: 実関数で 絶対値3円→旧実効7円「建たない」／新実効3円「**建つ**」を確認。実UIで 除外チェックが「☑ 浮き足〇」1つだけ・浮き足〇1件を除外して対象4件／**絶対値3円で4件中4件すべて建つ**（修正前は水準線1043の記録が未到達）／絶対値7円で建玉1件・注記「EP未到達2件・×見送りのみ1件」・マスター表に理由チップ・銘柄別内訳「未到達2 ×見送り1」で 1+2+1=4 が整合／列順は従来Σ→シミュΣ→差額／全タブ巡回（自動配分の総当たり含む）で**consoleエラー0**。Firebase同期なし・投入データ削除済み。

### 2026-07-20i 期間の指定を「年→月→週→日」のカスケード複数選択へ全面置換＋「今週」の金曜落ちバグを解消（sw v217→v218）
「直近1ヶ月」のようなローリング窓をやめ、**◯年◯月◯週◯日を選ぶ絶対指定**にした（ユーザー要望）。記録帳の外側バーとシミュの対象期間の**両方**を同じ部品に統一。

- **エンジン（app-06.js・`_elBucketLabel`の直後・全てトップレベル）**: 選択値 `sel = {y,m,w,d}`（各階層 `null`＝「全て」／配列＝明示選択・**全階層nullなら全期間**）。`_elPSelAll`／`_elPSelThisMonth`／`_elPSelSet`（**上位を変えたら下位をリセット**）／`_elPSelToggle`（全て状態から1つ選ぶ＝そのキーだけ・空になったら全てへ戻す）／`_elPSelMatch`／`_elPSelFilter`／`_elPSelSig`（`_autoSig`用）／`_elPSelOpts`（選択肢＋件数の列挙）／`_elPSelSummary`／`_ElPeriodPicker`（UI・案B＝折りたたみサマリー＋変更▾）。
- **週は`_elBucketKey`/`_elBucketLabel`を再利用**＝月曜始まり・「7/13〜7/17」（月〜金）。週の所属判定を自前で書かない。
- **`_elIsEmaRefPeriod`をEntryLogView内のローカル定義からトップレベルへ移動**＝ピッカーの選択肢足切りと既存の「※参考」表示が同じ単一源を見る。これにより**2026年1〜4月は選択肢に出ない**（年のハードコード不要・境界2026-05-01を1箇所で管理）。
- **選択肢のルール**: 年＝2026〜今年／月＝1月〜今月（未来は出さない）／週＝選択中の月に1日でも重なる月曜週（月をまたぐ週は両方に出す）／日＝選択中の週（無ければ月）の**平日のみ・今日まで**（土日は市場休みで記録が存在しないため出さない）。**空の期間も件数付きで並べ、0件は薄字**（ユーザー選択）。
- **既定はスコープで違える**（ユーザー選択）: 外側バー＝**全階層「全て」＝全期間**（開いた時の見え方を従来から変えない）／シミュ＝**今月のみ**（週日は全て）。
- **撤去**: 外側の`<select>`（全期間/今週/1ヶ月/3ヶ月/6ヶ月/1年）・`🗓期間指定`バー（`_rngISty`/`_rngBtn`/`_rngBar`と`rngFrom`/`rngTo`）・シミュの対象期間ピル（本日/1週間/…）と今日起点ローリングの`_periodRecs` IIFE。自由レンジは無くなるが年月週日の複数選択でカバーする。
- **🐛【実バグ解消】「今週」で金曜が落ちていた**: `_elFilterPeriod`（app-05.js:5554）がローカル深夜0時のDateに`toISOString()`をかけるため、JST(UTC+9)では窓が丸ごと1日前へずれる。**実測（2026-07-20 月・JST）で境界が 月7/20〜金7/24 のつもりで 日7/19〜木7/23 になり、金曜の記録が全タブから消えていた**（日曜は誤って混入）。`"1m"`以降は`cutoff`に深夜0時でなく「現在時刻」を入れているため、朝9時前に開くと境界が1日手前へずれる（開いた時刻で母数が変わる）。新経路は`_elBucketKey`（ローカルgetter）だけを使うので同じ穴を踏まない。
- **`_elFilterPeriod`/`_elPnlPeriodFilter`は呼び出し元ゼロになった**（後者は元から0）。削除はせず**使用禁止＋バグ内容を明記したコメント**を付けた（再利用を防ぐため）。新規に期間で絞るときは`_elPSelFilter`を使う。
- **ラベル**: ピッカーのリセットは「全期間」ではなく**「クリア」**（すぐ隣の母数トグルに「全期間」があり同じ文言が2つ並ぶため）。
- **検証**: esprima全ファイルOK／実関数で16項目のエンジンテスト全パス（**7/20週に金曜7/24が入ることを含む**・4月と未来月が選択肢に出ない・週ラベル形式・矛盾する選択は空・カスケードのリセット・空選択で全てへ復帰）／実UIで 外側の既定=全期間14件→7月6件→クリアで14件復帰・月2つ選択で9件・週で3件・日チップの件数(7/15,7/16が0件)・上位変更で下位リセット・シミュ既定=今月6件で外側と独立・「全期間」ボタンの重複解消／全タブ巡回（集計/期間/シミュ/α値/損切り/未達/深掘り/シグナル総合/RNまたぎ/閾値/日別週別月別）で**consoleエラー0**。Firebase同期なし・投入データ削除済み。

### 2026-07-20h 当日3コミット（e/f/g）の全体監査 → 実バグ8件を修正（sw v216→v217）
4系統の並列監査（RN閾値／全銘柄シミュ／複数回の取引／横断整合）＋自作の実機検証で出た指摘を、**実コードで再現できたものだけ**修正。損益計算のロジックは1つも変えていない（不変条件で確認）。

- **【実バグ】💰損益ピルが🧮シミュへ戻れない**（app-06.js:6758）: `if (view !== "sum" && view !== "period") setView("sum")`。07-20fで`_tabs`に`sim`を足した際にこのガードを更新し忘れ、銘柄タブでシミュを開いてから💰損益を押すと📊集計へ飛ばされ、新タブに入れなかった。→ `&& view !== "sim"` を追加。
- **【実バグ】手動モードで「推奨α不明」が「取引なし」と誤表示**（app-06.js:4875）: `_manTiersOf` の `.filter(t => t != null && t.a != null)` がα不明のtierを`_elKabuTierEval`へ渡す前に捨てていたため、`skip:"noalpha"`→`anyNoBase`が**構造的に立たず**、①注記「推奨α不明N件」が手動では一度も出ず、②銘柄別内訳の理由が「取引なし（株数0・方式未選択）」という誤ったツールチップ付きで出ていた（自動配分は捨てないので手動だけの非対称）。→ `t.a != null` を撤回（消費側`_kbMasterTable`の`_sa`/`_tMax`/`_perTx`は全て`c.t.a != null`でガード済＝自動と同じ形なので安全）。nullは末尾ソート。**合計・損益は不変**（skipのtierは積算されない）＝採用α±0で差額0円を再確認。
- **【誤った表示】「銘柄別タブのシミュを合算した値と一致します」は偽**（app-06.js:6681）: 母数の定義が3点で違う＝①時間かぶり除外のスコープ（全銘柄=null＝銘柄横断／銘柄別=同一銘柄内）②銘柄別シミュの母数は**選択中シグナルのみ**③floatModeで浮き足〇の除外が反転。→ 画面文言とコメントを訂正。**同一シグナル1本・被り無し・浮き足無しなら結果的に一致するが一般には一致しない**（当初の実測が一致したのは合成データがその条件を満たしていたため）。
- **【実バグ】〇の再クリックで消したはずの第2取引が復活**（app-06.js:5400）: トグルに同値ガードが無く、🗑で第2取引を消した直後に〇を押すと空行が復活し、開いていた展開（autoExp/mtExp）まで畳まれた。→ `if (multiTrade === kv[0]) return;`。
- **【不整合】閾値タブのバッジが段別トグルを無視**（app-06.js:6312）: `"all"`固定で、「…50の段」選択時にバッジ48件／ボード24件と食い違い。→ `rnTier`を渡す。あわせて`rnSub === "thr"`の時だけ算出（他サブタブでは捨てるだけの重い`aiOf`全走査だった）＝他タブでは「—」表示。
- **【誤誘導】閾値ボードの空状態**（app-06.js:1689）: 段別トグルで0件になっても「水準線値を入れてください」と表示し、既に入力済みのユーザーを誤誘導。→ `offTier>0`なら「この段に該当する記録がありません（他の段にN件）」に出し分け。`_tierName`を注記と共用。
- **【矛盾】①★（平均基準）と②脚注「1つ手前が最適T」（Σ基準）が別のTを指す**: Tを上げるほどEPが深くなり到達が減る＝残った少数の強い記録で平均が上がる（選抜バイアス）ため★は深いTへ寄る。→ ②脚注に両者の違いと使い分けを明記。
- **【データ不整合の除外】採用α < RN加算 の記録**（app-06.js:1671）: ボードは実効αを`Math.max(0,…)`でクランプする一方、距離は未クランプEPから測るため、残すと「クランプ後のαではキリ番に乗らない」矛盾した反実仮想を出す。→ `_elRnThrPool`で除外し件数を注記に表示（`badPre`）。あわせて`_epIsV2`ガードとNaN判定も追加。
- **【軽微】** ×（単一取引）の自動配分は25通り＝〇の約1/49なのにゲートを挟んでいた → 即時実行に戻す／`_kbStockTable`のヘッダが`_elv2Th(h,i)`でkeyを渡せずReact key警告 → 直接`createElement`／`_elKabuLadderSimV2`のprops説明が`data`/`scopeStock`/`allStock`を欠いていた → 追記。
- **修正しなかった指摘（既存・別件）**: `autoRes`が毎レンダー再計算（ゲートは初回だけ抑止＝ランキング展開のたびに再探索。07-20f以前からの挙動）／`recoSig`が毎レンダーO(allRecs)で再構築／取引ごと追加αのUI（`_addSel`）が組み立てられるが未マウント＝2026-07-13の応用α化で`_addA: null`固定になった名残／`_elRnBoardV2`③と新②の提示上の重複／CLAUDE.mdの「コメントは除去済み」「連結でバイト一致」が実態と乖離。
- **検証**: esprima全ファイルOK／実UIで①💰損益→シミュ復帰②誤文言の消失③手動で「推奨α不明32件」が初表示・理由が「α不明」に是正（対象20＝建った8＋未到達2＋α不明10 の算術一致）④**採用α±0・100株で差額0円＝損益ロジック不変**⑤銘柄別内訳の行合計＝フッター＝KPI⑥〇連打・削除後再クリックで行が増えない⑦バッジが段別追従・他サブタブは「—」⑧空状態の出し分け⑨×はゲート無し25通り／〇はゲート有り⑩新規タブで全タブ巡回し**consoleエラー0・警告0**。

### 2026-07-20g シミュに「複数回の取引 〇×」を新設＝分割するかを最初に選ぶ（sw v215→v216）
株数シミュの入口に **「複数回の取引 〇×」トグル**（既定×＝1回だけ建てる）を追加。〇にすると第1取引・第2取引…と増やせる。手動/自動の両モードに効く大前提なので、手動⇄自動トグルの**上**に置く（全銘柄タブ・銘柄別タブ共通）。

- **state**: `multiTrade`（既定false）／`effRows = multiTrade ? rows : rows.slice(0,1)`。**計算・プレビュー・株数合計・行描画は全部effRowsを見る**（`rows`直参照を残すと×なのに第2取引が効く）。effRowsは`rows`の**先頭からの部分列**なので`_upd`/`_stepCum`/`_stepOff`のindexはそのまま通用する。
- **×に戻しても第2取引以降は消さない**（ユーザー選択）＝`rows`は保持し表示と計算から外すだけ。〇に戻すとそのまま復活。隠れ行に**中身がある時だけ**「第2取引以降は保持中」バッジ（空行を保持しているだけの初期状態で出すと意味が無いため）。
- **初期`rows`を2行→1行に変更**（既定×で1枚表示のため）。〇に切り替えた時に`rows.length < 2`なら空行を1つ足す＝〇にすると従来と同じ2枚になる。
- **自動配分も連動**（ユーザー選択）: ×＝`for (_s1 = totalN; ...)`の1周だけ＝**全株を1方式に寄せる25通り**（`_s2=0`で`_c2s=[null]`）／〇＝従来の100株刻み2分割＝1,225通り。`_autoSig`に`multiTrade`を追加＝〇×を切り替えるとゲートが再武装。**自動配分は2取引まで**（3取引は約33万通りで破綻するため手動ラダー専用）＝説明文に明記。
- **〇の探索は×の上位集合**（第1取引に全株を寄せた配分＝単一取引も列挙される）。だから〇にしておけば★が単一取引になることで「分割不要」と読める＝×側に「分割した方が良かった」ヒントは出さない（ユーザー選択）。実測でも×の★と〇の★が同じ「採用α+3で500株」で一致した。
- **表記**: 行見出しは`multiTrade ? "第N取引" : "取引"`／🗑は〇かつ2行以上／「＋取引を追加」は〇のみ／プレビューの「＝合計N株」は2取引以上のときだけ／自動ランキングの列見出しは`配分（α方式×株数）`⇄`α方式（全株）`／ゲートの注記も切替。
- **検証**: esprima OK／3銘柄66件で実UI操作。①既定×＝行見出し「取引」1枚・＋追加/🗑/バッジ無し②×で採用α±0・100株・損切りオフセット空→**差額0円**（不変条件維持）③〇で第1/第2の2枚に増え「＋取引を追加」「🗑」出現④第2取引を設定すると合計500株・+111,300円⑤×に戻すと**計算が単一の+25,300円/差額0に戻り**「保持中」バッジ表示⑥〇に戻すと+111,300円が完全復活⑦自動配分は×=25通り「α方式（全株）」・〇=1,225通り「配分（α方式×株数）」でゲートも再武装⑧銘柄別タブもトグルは効くが**ゲート無しの即時計算のまま**（挙動不変）⑨新規タブで全タブ巡回しconsoleエラー0。Firebase同期なし・投入データ削除済み。
- ⚠️**検証時の教訓**: SWキャッシュを消さずにリロードして旧コードを掴み「バッジが消えない」と誤診しかけた（`getRegistrations()→unregister()`＋`caches.delete`＋`fetch(cache:"reload")`が必須）。また入力欄を`innerText`の近傍テキストで探すと行をまたいで誤爆する→**行カードのコンテナを特定してからその中のinput/selectを探す**こと。

### 2026-07-20f 💰損益タブに「🧮 シミュ」＝全銘柄一括の株数シミュを新設（sw v214→v215）
記録帳の💰損益タブ（全銘柄）に、期間の右へ3つ目のサブタブ「🧮 シミュ」を追加。銘柄を問わず全記録に同じラダーを当てる。設計はユーザー選択（推奨αは銘柄ごと／株数は全銘柄同じ／銘柄別内訳あり／自動配分はボタン式／建たなかった理由は銘柄別に出す）。

- **フォークせず`_elKabuLadderSimV2`を拡張**（`allStock` prop）＝前セッションの整合修正4件のような「片方だけ直る」事故を防ぐため。最新仕様（最終損益基準・時間かぶり除外・RN動的再判定・採用α±X）が自動的に効く。
- **⚠️推奨αは銘柄ごと**: `recoRef.current.fnByStock`（銘柄別`_elKabuRecoBaseFn`）＋`pickByStock`（本日時点の表示用）。入口は`_recoOfRec(r)`に一本化。全銘柄を1本のrecoFnに混ぜるとαの意味が壊れる（株価水準の違う銘柄をまたいだ平均になる）。絶対値・採用α±Xは元から銘柄非依存。
- **`_recoOfRec`への差し替え漏れが事故になる**: `_manTiersOf`/`_recoAs`/`_autoTiersOfFor`のほか**`_simCutOf`（損切りオフセット）・`_simCutInfo`・マスター表の`recoA`**も対象。allStockでは`recoOf`がnullなので直呼びすると即クラッシュ（実際に最初の実装で3箇所取りこぼし→検出して修正）。
- **銘柄別内訳`_kbStockTable`**: 銘柄/対象/建った/建たなかった理由/シミュΣ/従来Σ/差額。合計行はKPI「通算損益」と一致。同じ「16/21件」でもEP未到達なら“αが高すぎる＝下げれば拾える”、推奨α不明なら“記録が薄いだけ＝ラダーの問題ではない”と**打ち手が正反対**になるため理由を出す。
- **EP未到達をシミュ全体で可視化**（銘柄別シミュにも効く既存の穴を修正）: `_elKabuAccumTiers`に`anyUnreached`・`_elKabuLadderCalc`に`unreachedRecN`・`_notesLine`の先頭に追加。従来はフラグ自体が無く、**「対象−建玉あり」の一番大きい塊が注記に出ていなかった**。理由は`_elKabuNoBuildReason`で1記録1つに排他化（×見送り＞α不明＞未到達）＝銘柄別内訳が 対象＝建った＋各理由 でぴったり合う。
- **自動配分はボタン式**（全銘柄のみ）: `autoRunSig`／`_autoSig`。株数・期間・除外・分類を変えると再武装。銘柄別タブは従来どおり即時（挙動を変えない）。
- **時間かぶり除外は銘柄横断**（`scopeStock:null`）＝💰損益タブの他の集計と同じ線引き。
- **検証**: esprima OK／5銘柄90件の合成データを投入し実UI操作。**①採用α±0・100株・損切りオフセット空で差額0円＝記録どおり再現**（全銘柄・銘柄別の全行でdiff=0）②銘柄別内訳の各列が合計行と一致・合計＝KPI通算損益③理由の件数＝対象−建った（全銘柄でEP未到達7件＝90−83）④**推奨αが銘柄ごとに違う値**（STK-A 9円/B 17円/C 5円/D 11円・注入した値幅スケール1.0/1.8/0.6/1.2に比例。混ぜていれば全部同じ値になる）⑤記録2件のSTK-Eは「データ不足」で推奨α系を建てない⑥**全銘柄の銘柄別行＝各銘柄タブ単独シミュと5銘柄すべて完全一致**（10,900+1,900+8,700+5,200+0＝26,700＝全銘柄通算）⑦自動配分1,225通り×90件が215ms・ゲートが株数/除外変更で再武装⑧新規タブで全タブ巡回してconsoleエラー0（銘柄別シミュ・閾値タブの回帰込み）。Firebase同期は発生していないことをネットワークで確認・投入データは削除済み。

### 2026-07-20e RNまたぎ「何円手前から〇にすべきか」閾値スイープ分析を新設（sw v213→v214）
📡シグナル総合 → 🔢RNまたぎ に**4つ目のサブタブ「閾値」**を追加。既存の③RN距離別(`_elRnBoardV2`)は`_elRnAdd`で分ける**実績の内訳**で母数が「実際に〇にした記録」に偏るため、**RN×も含む全記録の反実仮想**として「RNまでの距離≤T円なら〇」のTをスイープする別分析を新設した。設計はユーザー選択（T範囲=0〜12／段は合算主＋トグル／4つ目のサブタブ／主表＋距離別の2表）。

- **エンジン（app-05・`_elRnAutoOfRec`の直後）**: `_elRnPreEpOfRec`（RN加算“前”EP＝水準線＋(採用α−`_elRnAdd`)）／`_elRnDistAt`（直近キリ番までの距離1〜49・0=ちょうど・null=判定不可）／`_elRnTierAt`（"50"/"00"）／`_elRnAddAtT(epPre,T)`（閾値Tでの加算額）。**`_EL_RN_BANDS`と運用の自動判定は一切変更なし**＝この4関数は分析専用で記録フォーム/EPナビ/シミュの挙動に影響しない。
- **既存の重複を解消**: `_elRnAutoOfRec`を`_elRnAutoAt(_elRnPreEpOfRec(s,alpha))`へ委譲＝「RN前EP」の算出式を単一源化（結果は従来と同一・**旧実装との差分テスト936通りでpass**）。
- **ボード（app-06・`_elRnBoardV2`の直後）**: `_EL_RN_T_MAX`(=12)／`_rnTierToggle`／`_elRnThrPool`（母数の仕分け＝ボードとタブ件数バッジの単一源）／`_elRnThresholdBoardV2`。①閾値スイープ表(T=0〜12・10列)＋②距離別の限界寄与表(6列)。
- **設計上の要点**: RNの間隔は50円＝**Tを1上げても該当は全体の約2%ずつしか増えず**、母数の大半が不変なのでΣ最終損益の差が埋もれる。そのため主表に「該当」「**T=0比**」列を、下に距離別の限界寄与表（**符号が反転した距離**をマーク）を置き、そこで判断させる。
- **検算不変条件**: ②の「累計差(距離d)」≡ ①の「T=0比(T=d)」。距離dの記録はT=dでちょうど〇になるため数学的に恒等＝**ズレていたら実装バグ**。
- **★の選定**: ①EP位置スイープと同じ軽いゲート（E成立≥`_EL_BASE_MIN_N`かつΣ黒字で平均最終損益 最大）。推奨基本αの4条件フルゲートは**使わない**＝全T行が同じ母数・ほぼ同じ到達率/頻度になりゲートが効かず「条件適合無し」しか出ないため。T=0が★なら「そもそもまたぎ不要」のサイン。
- **T≥10は「現行バンド外」グレー表示**＝…40/…90を外した2026-07-20cの判断を、この表の実データで再検証できるようにするため。
- **母数**: 水準線値入り かつ RN前EPが…50/…00ちょうどでない記録。除外は理由別に件数表示（水準線未入力N件／ちょうどN件／他の段N件）＝薄いときにユーザーが判断を保留できる。`levelPrice`は2026-06-22再導入の任意欄なので**当面は母数が薄い可能性あり**。
- **検証**: esprima全8ファイルOK →実ブラウザで実関数テスト（T=9≡現行バンド 1000通り／`_elRnAutoOfRec`差分936通り／距離・段・加算額の恒等式 1300通り）→合成84記録をReactDOMで実マウントし**検算不変条件0違反**→localStorageに52記録を投入して**実UI経路**（記録帳→シグナル総合→RNまたぎ→閾値）で13行/12行・検算0違反・段別トグル48→24/24を確認→**新規タブでconsoleエラー0**。Firebaseへの同期は発生していないことをネットワークで確認済み・投入データは削除済み。

### 2026-07-20d シミュ整合監査の未対応②③④を消化（sw v212→v213）
2026-07-20bの監査で残していた未対応4件のうち、ユーザー選択で②③④を実施（①4月「※参考」バッジは引き続き保留）。
- **②（唯一の実害）`_simBaseLevel` の null 伝播**: 候補αが無い（未選択／絶対値の空欄／その日の推奨α不明）ときでも、**浮き足〇は0・応用〇は応用αを返して建ってしまっていた**＝「未選択は建てない」の不変条件違反。先頭に `if (candidateBase == null) return null;` を追加。**同じ族の残り2件も同時に修正**＝`_ladderPrev`（絶対値でα欄が空の行を「α0円で100株」と嘘表示していた）と `_manTot`（同じ行を合計株数に数えるため従来列の按分株数だけ膨らみ差額が食い違っていた）から当該行を除外。
- **③ floatModeの見出し**: 「浮き足加算（**浮き値÷2切捨て**）を上乗せ」＝加算率可変化(2026-07-12)で**古い**うえ「候補α＋浮き足」と読めて誤解を招いた。→「浮き足〇は土台α無し＝実効α＝浮き足加算（採用加算率）＋RN自動加算のみ。**絶対値／推奨α±X／推奨基本α値を選んでも実効αは変わりません**（土台αが常に0のため）。αを掃引したいときは『採用α±X』方式を」に書き換え＋琥珀の注意ボックス化。
- **④ 指値同値とα掃引**: `_elFillRisk`(app-05)は「α総当たり系には配線しない」方針が明記済みなので**損益からは除外せず、件数だけ可視化**。`_fillEqN(calc)` 新設（`_notesLine`内）＝**掃引αがその記録の`_elOsMaxAll`とちょうど一致**した記録を数える（＝`高値 >= α`で「刺さった」扱いになる楽観側の可視化）。ただし「採用αと一致しかつ実際に約定済み(`_elIsEntered`)」は約定の証拠があるので除外。注記行に「指値同値N件（…損益からは除外していません）」を追加。
- 検証: preview(自前:3464)＋**SWキャッシュを明示クリア**（unregister＋caches.delete＋`fetch(cache:"reload")`→リロード→新文字列で鮮度確認。※1回目のリロードは stale-while-revalidate で旧コードが返り「修正が効いていない」と誤診しかけた）。②＝浮き足〇の記録が方式abs・α欄空で**シミュα「—」＝建てない**（修正前は α=0+浮き足10 で建っていた）・プレビュー「有効な取引がありません」・KPI「通算÷**0株**」で`_manTot`も整合。④＝シミュα18円＝`_elOsMaxAll`18とちょうど一致する記録で「**指値同値1件**」の注記、浮き足〇の10円は非該当。③＝floatMode実マウントで新しい注意文を確認。console例外0。sw v212→**v213**。

### 2026-07-20c RNまたぎ自動判定（エンジン＋記録フォーム＋EPナビ＋シミュ）＋分足欄の右に水準線値欄（sw v211→v212）
ユーザー構想「予定EPの下二桁が範囲内なら自動でRNまたぎ〇にし50/00になるよう加算・外れたら×」を全面実装。設計は事前に全項目確定（範囲/計算順序/シミュ動的/フォーム自動＋手動上書き/過去記録は移行しない/水準線未入力の扱い）。
- **A. 判定エンジン（app-05・`_elRnAdd`直後／新規トップレベル）**: `_EL_RN_BANDS`＝`[{from:41,to:49,target:50},{from:91,to:99,target:100}]`（**40・90は含めない**＝距離10はバンド内で最も高い授業料なのに得るものは同じ「RNちょうど」1個・…40/…90自体が10円刻みのキリ番＝費用対効果が最悪。ユーザー決定。後から③RN距離別データで調整できるよう定数外出し）／`_elRnAutoAt(epPre)`＝予定EP価格→加算額（**null=判定不可／0=対象外(自動×)／>0=加算額**）／`_elRnAutoFrom(levelPrice, preAlpha)`／`_elRnAutoOfRec(s, alpha)`（採用αからRN分を引き戻して「RN前α」にするのでRN〇/×どちらの記録でも使える）。
- **⚠️計算順序（循環回避の唯一の読み方）**: 予定EP＝水準線＋採用α で**採用αにRNが含まれる**ため、判定は必ず**RN加算“前”のEP**（水準線＋基底α＋浮き足加算）に対して行う。RN込みEPで判定すると循環する。
- **既存の重複を解消**: 2026-07-19に入った「🎯RNまたぎ候補の記録一覧」(app-06 `_rnCandRecs`)が**自前の下2桁判定（40〜49/90〜99）を持っていた**→`_elRnAutoOfRec`へ寄せて**単一源化＋範囲を41-49/91-99へ統一**（見出し文も更新）。ユーザー選択＝両方を41-49/91-99に統一。
- **B. 記録フォーム（app-05）**: `fRnAuto` state（**新規=true／既存記録(isEdit)=`initSig.rnAuto===true`＝既定false**＝過去記録を開いただけでRNが書き換わらない＝「過去記録は移行しない」の保護）。`_fRnPre`/`_fRnAutoAdd`＋useEffectで自動セット（`_fBaseLevel`/`_fUkiAdd`の定義後に置くこと＝前に置くとundefinedを掴む）。〇×/数値/▲▼に触ると`_offAuto()`で自動停止→「**↺ 自動に戻す（手動中）**」ボタンで復帰。自動中はバッジ（水準線未入力は「水準線値を入力すると効きます」）。doSaveに`rnAuto`。
- **C. EPナビ（app-04 `_EpnCalcForm`）**: 同じ挙動＝`nRnAuto` state・`_nRnPre`/`_nRnAutoAdd`＋useEffect・○×/入力/▲▼で`setNRnAuto(false)`・「↺ 自動に戻す」・doSaveに`rnAuto`・`_resetForm`で`setNRnAuto(true)`・保存カードを開くときは`e.rnAuto===true`（既定false＝過去の値を自動で書き換えない）。
- **D. シミュ（app-06）＝αごとに動的再判定**: `_simRnAt(r, preAlpha)`＝掃引αでのRN加算額（**記録の保存値`_elRnAdd`は使わない**＝αが動けばEPも下二桁も動くため）。`_manTiersOf`は`_pre=_bl+_uki`→`_rnD=_simRnAt(r,_pre)`→`a=_pre+_rnD`。`_candEffAlpha`も非adopt系は同じ流れ。**adopt系（採用α±X）はRN再判定しない**＝採用αがRN込みの実績値なので二重加算になり、かつ「採用α±0＝記録どおりを再現（差額0円）」という検証用の不変条件を壊すため。水準線未入力＝**RN無し(0)で建てる**＋`_noLvN`件数バッジ（ユーザー指示「未入力の記録が分かるように」）。注記も更新。
- **E. 分足欄の右に水準線値欄（app-05・ユーザー要望）**: 既存のOS見出し右の欄と**完全に同じもの**を共通部品`_lvPriceBox(_k)`に括り出し、「分足」行の右とOS見出しの右の2箇所で呼ぶ。**同じstate(`fLevelPrice`)を見るので相互に自動反映**（同期処理は不要）。RN自動判定は水準線が要るので早い段階で入力できるのが狙い。
- 検証: preview(自前:3463)でparse＋App mount(root 42932・console例外0)＋**エンジン単体13ケース全PASS**（40→0/41→+9/49→+1/50→0/90→0/91→+9/99→+1/00→0・水準線未入力→null）＋**シミュ実マウント**＝水準線3936の記録で 基底α3→3円(下二桁39=対象外)／基底α5→**14円「（RN9込）」**／基底α7→**14円「（RN7込）」**＝**どちらもEP3950へ収束**＝またぎの意図どおり・水準線なしの記録は5円/7円のまま・「水準線未入力 1件」バッジ表示 ＋**記録フォーム実マウント**＝水準線値欄が**2つ**・**双方向で相互反映**（片方に3936→両方3936／もう片方に3949→両方3949）・水準線3949で**RN+1→暫定EP3950**「自動判定中」・RN行の×を手動クリック→「↺ 自動に戻す（手動中）」に変わり**水準線3941へ変えても×のまま**（自動に上書きされない）・↺で復帰し**RN+9→EP3950**。sw v211→**v212**。
- ⚠️検証メモ: フォームには「×不要」ボタンが**2つ**ある（浮き足行とRN行）。`innerText`で最初の1つを掴むと別行を押してしまい「手動上書きが効かない」と誤診する。**RN行のコンテナを特定してからその中のbuttonを探すこと**。

### 2026-07-20b 🧮シミュを現行の損益基準・母数ルールに合わせる整合修正4件（sw v210→v211）
ユーザー指示「そもそも現行のシミュレーションが、最新の取引記録や損益計算方法と合致しているかチェックして」→3並列エージェント監査＋一次コード検証。**シミュはH1基準の孤島になっていた**（アプリ全体は2026-07-09〜07-13で最終損益へ移行済み）。ユーザー選択＝①最終損益へ全面切替／②時間かぶり除外を配線／④RN加算を実効αに（③4月※参考バッジは今回見送り）。
- **① 損益基準 H1→最終損益（H2）**: `_elKabuTierEval`(app-06:4553)と`_kbConvParts`(app-06:4844)の`_elHold1TotParts`→**`_elHold2TotParts`**。これで「従来」列＝記録表の「最終損益」列と**同じ記録・同じαで同じ数字**になる。推奨α選定(`_elBaseAlphaPick`=`_elH2EvalByFn`)も最終損益基準なので、**αを最終損益で選んでH1で採点していた構造的ねじれ**を解消。★最適配分も最終損益で最大化されるようになる。`_elHold2TotParts`は内部で`_epResolve(s,alpha)`を引くのでα可変でも正しい（`_elH2EvalByFn`と同じ使い方）。
- **② 時間かぶり除外を配線**: 呼び出し元(app-06:6349)に`data`/`scopeStock`(=`_collScope`)を渡し、コンポーネント先頭で`_simData`/`_simCollScope`を受けて**pool を直接絞る**（`_elCollExcluded`）。poolを絞るのでシミュ損益・従来列・`_origPnl`・対象取引リストに一律で効く。除外件数バッジ`_collN`（グレー・常時ONの明示）。旧＝`_elKabuLadderCalc`に`excluded`フックが無く`data`も渡っていないため構造的に未適用＝**被り記録を二重計上**していた。
- **④ RN加算を実効αに**: `_simAddOf`＝`_elUkiAdd(s)`→**`_elUkiAdd(s) + _elRnAdd(s)`**（採用α＝base-levelα＋浮き足＋RN・app-05:3141と成分一致）。`_manTiersOf`も`_uki`に加え`_rn`を持ち`_bl + _uki + _rn`。tierに`_rn`を添付し、マスター表の内訳注記を「（浮N+RN M込）」に（`_tMax`ヘルパーで`_uki`/`_rn`の最大を取る・採用α±X方式のtierは両方0＝内包済み）。旧＝RN〇を母数に入れると実効α＝**採用α−RN値**になり、EP位置も損切り判定もズレていた（既定除外のため普段は不可視）。
- **⚠️バグ修正（2026-07-20で入れた`_adoptOf`の欠陥）**: `aiOf(r).alpha`は`_elAlphaInfo`(app-05:3318)が`alphaVal`未入力時に**`_gradeAlpha`（難度既定10）へフォールバック**するため決してnullにならず、「採用α未入力の記録は建てない」が**実アプリで効いていなかった**（前回の検証はaiOfを自前スタブにしていたため通ってしまった＝スタブを検証していた）。`_adoptOf`を**`signal.alphaVal`直読み**に修正＝仕様どおり建てなくなった。
- 注記の是正: シミュ脚注(app-06:5098)の「手仕舞い＝実際のH1」「従来列＝損益データ欄と同じ…」→最終損益基準の記述＋時間かぶり除外の明記。`_mtSimCell`のtooltip「（）内＝…H1まで保有した場合の参考額」→期待度ベースの表現に。`_elKabuTierEval`の先頭コメントも更新（cut既定10が他所の15と違う点も注記＝実際には到達しない）。
- 検証: preview(自前:3462)でparse＋App mount(root 42932・console例外0)＋**実`aiOf`(`_elAlphaInfo`)で合成3記録を実マウント**＝①通常記録の従来が **H1 300円→最終損益 200円** に変わりシミュとも一致 ②RN〇記録(基3+RN8=採用α11)のシミュαが **3円→11円**（旧は`_elKabuTierEval(B,3,15)`＝**損切り-1,500円**、新は`(B,11,15)`＝**+600円**＝損切り判定まで歪んでいた実証）③採用α未入力の記録がシミュα「—」＝**実際に建てない**（`aiOf`は10を返すのに建てない＝修正が効いている）④**採用α±0で差額0円が通常記録・RN記録の両方で成立**＝α再現とP&L基準の相互検証 ⑤自動配分も全1,225通り・★採用α+5で 11→16円/7→12円・未入力は候補から除外。sw v210→**v211**。
- **未対応（監査で判明・要判断）**: (a)③4月「※参考」＝シミュは除外しない（母数トグル「5月〜」で回避可・警告バッジ無し。`_elIsEmaRefPeriod`が`EntryLogView`内のvarなのでモジュールスコープへ出す必要あり） (b)`_simBaseLevel`が候補null時も浮き足〇/応用〇では0/応用αを返し**建ってしまう**（「未選択は建てない」の不変条件と不整合・`_manTot`は数えないので従来列とのスケールがズレる） (c)floatModeでは絶対値/推奨α系の掃引が全記録で無効（α＝浮き足加算固定）なのに見出しは「候補＋浮き足」と読める＝採用α±Xだけが掃引可能である旨の明記が要る (d)指値同値(`_elFillRisk`)はα総当たり系に付けない方針が明記済みだが、シミュの到達判定は`h >= alpha`＝掃引αがOS高値ちょうどでも「刺さった」扱い。

### 2026-07-20 🧮シミュに「採用α値±X」方式を追加（手動ラダー＋自動配分の候補・sw v209→v210）
ユーザー要望「シミュのオプションを増やしたい。各取引の採用α値±X を導入する」。AskUserQuestion3問で確定＝**①起点＝実際の採用α（合計α・浮き足/RN込み）②手動＋自動配分の候補にも追加③採用α未入力の記録は建てない**。
- **意味**: 他方式（絶対値／推奨α±X／推奨基本α値）は「基底α部分」を差し替えて `実効α＝基底α＋浮き足加算` と積み上げるが、**採用α±Xだけは例外で `実効α＝記録のalphaVal＋X` をそのまま**使う（浮き足を重ねない＝二重加算回避）。副産物として、`_simBaseLevel` が基底αを固定してしまう **浮き足〇（基底0）・応用〇（応用α固定）の記録もこの方式なら初めてα掃引の対象になる**。
- **新規（すべて `_elKabuLadderSimV2` 内＝トップレベル定義の追加なし）**: `_adoptOf(r)`＝`aiOf(r).alpha`（採用α・未入力はnull）。`_candEffAlpha(cand, r, rb, ex, ad)`＝**候補方式→実効αの単一源**（adopt系は`ad+x`を0クランプ／abs・reco系は従来どおり`_simBaseLevel`＋浮き足加算・null＝建てない）。ランキング`_rankEvalOf`とマスター表`_autoTiersOfFor`が**この1関数を共有＝両者の一致を構造的に保証**（旧: 各自で`_candBaseFromReco`＋`_simBaseLevel`を組み立て）。
- **手動ラダー**: `_METHODS`に`adopt`（short「採用α±X」）。`_manTiersOf`はadoptを先頭で分岐＝`_simBaseLevel`も`_uki`も通さず`{a: max(0, 採用α+off), _uki: 0}`を返す。`_ladderPrev`／`_rowInputs`（ラベル「採用α」＋注記）／`_manTot`／行の入力表示条件にadoptを追加。
- **自動配分**: `_AUTO_CANDS`に採用α±0〜±5の11種を追加＝**14→25種**（`a0`/`a±1`〜`a±5`・`_candLabel`「採用α」「採用α+2」等）。総当たり数は500株で **378→1,225通り**。`_adoptAs`（記録ごとの採用α配列）と`_noAdoptN`（採用α未入力件数の注記）を追加。
- **表示の追随**: マスター表の`_exU`（シミュα列の「（浮N込）」注記）を`_elUkiAdd(s)`直読みから**tierの`_uki`最大値**に変更＝採用α方式のtier（`_uki:0`）では「浮N込」を出さない。`_autoTiersOfFor`もtierに`_uki`を付与。`_addAs`のelse分岐は`_exU`と切り離し（採用α方式で浮き足を「追N」に誤表示しない）。`_mtTierNodes`の`skip:"noalpha"`文言を「推奨α不明（取引対象外）」→「**α不明（取引対象外）**」（推奨α不明／採用α未入力の両方を指すため）。注記4か所を更新。
- 検証: preview(自前:3461)でparse＋App mount(root 42932・console例外0)＋**合成3記録で実マウント**＝①方式セレクタに「採用α±X」4択目 ②**採用α+2 → 通常(採用α7)=9円／浮き足〇(採用α13・浮き足加算10)=15円**（旧経路なら10円・二重加算なら25円＝どちらでもない）③**採用α±0 → シミュ＝従来で差額0円**（浮き足〇の記録も一致＝二重加算も土台取り違えも無い決定的証拠）④絶対値5円に戻すと従来どおり 通常5円／浮き足〇10円「（浮10込）」＝既存挙動不変 ⑤採用α未入力の記録はシミュα「—」＝建てない ⑥自動配分＝全1,225通り・★「採用α+5で500株」・マスター表 13+5=18円/7+5=12円・**ランキング★合計 +11,000円＝マスター表合計 +11,000円で一致**。sw v209→**v210**。

### 2026-07-24 記録表「ライン」列に「水準」（EPの上）と「実現到達」（損切の下）を追加（sw v257→v258）
ユーザー要望「EP行の上に水準・損切行の下に実現到達を縦積みで（EPと同じ体裁）」。**`_elLineInner`(app-05:4992・共通ヘルパー)のみ**＝4行(EP/額/損切/額)→**6行(水準/額・EP/額・損切/額・実現到達/額)**。①**水準**＝`s.levelPrice`(水準線値・色#64748B薄スレート)を先頭に。②**実現到達**＝`levelPrice + _elRideVals(s,α,cut).mx`(mx=決済サマリーの「最高↑○」＝EP足〜手じまい足の最高値・水準線比↑正)＝**手じまいまでに到達した最高値の実価格**(色#475569スレート・案C)。OS値ではなく実価格（水準線比の最高値を水準線に足して円換算）。E成立(v2)以外・α未達・水準線未入力は「—」。EPラベルにmarginTop:1を追加(水準との間隔)。6記録表すべて(`_elLineInner`/`_elLineCell`経由・app-02/04/06)に自動反映。**「最高値」は決済サマリーの最高(`_elRideVals.mx`)＝ユーザー定義に厳密一致(OS欄の`_elOsMaxCapped`とは通常同値だが手じまいがH1/H2まで伸びた時に差得る・AskUserQuestionで前者選択)**。検証=V8 parse OK＋実マウント（level3727/α2/cut15/mx6→「水準3727 EP3729 損切3744 実現到達3733」・水準線未入力＋非v2→全て「—」）。

### 2026-07-18h RNまたぎ〇の記録はライン列EP行を「EP（RN）〇」表記（紫・sw v194→v195）
ユーザー要望「RNまたぎ〇の記録のライン列は『EP（RN）〇』と表記」（EPがRN＝キリ番に乗っている明示）。`_elLineInner`(app-05・EP行ラベル)を `_elRnYes(s) ? "EP（RN）〇" : "EP"` に分岐＝RN〇(`signal.rnUsed`)は紫#7C3AED・whiteSpace:nowrap。値(予定EP額)・損切行ラベルは不変。6記録表すべて(共通ヘルパー)に自動反映。
- 検証: preview→app-05.js `new Function`パースOK・合成レンダー＝{rnUsed:true,levelPrice5940,α10,損切15}→「EP（RN）〇5950 損切5965」(ラベル紫rgb(124,58,237))・RN無し→「EP5950 損切5965」・例外0。sw v194→**v195**。

### 2026-07-18g 要審議記録を合計・累積に算入（見送りと同じ）＋記録一覧に「要審議のみ」絞り込み（sw v193→v194）
ユーザー要望「要審議記録はデータに算入されるように（合計算入なども）＋各銘柄で要審議記録の一覧を表示・直接編集」。要審議(`signal.review`)は2026-07-14cで「分析算入・合計不算入」だったが、合計にも算入（＝見送りと同じ無エントリー扱い＝×宣言が無ければ仮想損益で（）内）へ変更。審バッジ・ピンク・審議メモは維持。
- **合計除外の撤回（9箇所の`!_elIsReview`/`if(_elIsReview)return`を削除）**: `_elTotAccum`(app-05:4236)・`_elCalcChartGrades`(app-05:5291)・`_elCumPnlSectionV2`(app-06:1200)・手集計ループ`_recsForTot`(app-02:3972)/`_recsM`(app-02:5114)/`_inclTpb`(app-04:5627)/`_wkRecsM`(app-04:5912)/`_pbRecsM`(app-04:6103)/最終損益バッジ`_recs`(app-04:6221)。`_elIsReview`自体は残す（審バッジ表示app-02:4655/4698・app-04:5319・app-06:5355＝現状維持）。
- **フォーム説明更新（app-05）**: `_elIsReview`定義コメント(3166)・4択コメント(8378)・要審議ボタンtitle(8390)・要審議選択時の説明文(8417)を「合計損益・データ分析ともに算入（見送りと同じ・無エントリーなので仮想損益）」へ。
- **記録一覧に「要審議のみ」絞り込みトグル（app-06 `_groupPanel`記録一覧）**: state`reviewOnly`(5082)新設。「被り除外のみ」ボタンの隣に「要審議のみ（N件）」ボタン（ピンク#DB2777・要審議0件なら非表示）。ONで`_fr.filter(_elIsReview)`＝その銘柄・そのビューの要審議だけ表示、行タップで明細→編集(既存`_recTable`/`EntryLogCard`のonEdit)。collOnlyと独立トグル・両方AND可。集計/KPIは不変（表示のみ）。
- 検証: node無し→preview(:3457)でSW/旧キャッシュ削除→リロード→app-02/04/05/06 `new Function`パースOK・App/Calendar/EntryLogView定義・カレンダー正常表示・`_elTotAccum`/`_elCalcChartGrades`/`_elCumPnlSectionV2`のソースに`_elIsReview(`呼び出し無し(除外撤回)・`_elIsReview(`残数=app-02:2/app-04:1/app-06:3(審バッジ+トグルのみ)・合成recsで要審議フィルタ1件。※リロード一過性CalendarクラッシュはSWレースで無関係(最終描画正常)。実データはユーザー実機。sw v193→**v194**。

### 2026-07-18f 記録表「ライン」列を実価格に修正（EP水準比→予定EP額/予定損切りライン額・解釈違いの訂正・sw v192→v193）
ユーザー指摘「EPは実際の予定EP額でしょ。損切もそう。損切りラインが何円か（損切り値とは違う）」。2026-07-18dで追加したライン列(`_elLineInner`・app-05:4872)がEP足高値の水準線比(↑11/↑26)を出していた＝解釈違い。EPナビ/フォームの「予定EP」「予定損切りライン」欄(app-04:3506正本式/4124・app-05:8088/8099)と同じ実価格（円）へ修正。
- **`_elLineInner`(app-05)**: EP行＝予定EP額`round((levelPrice+合計α)*100)/100`（app-04:3506の正本式と同一）／損切行＝予定損切りライン額`予定EP+損切り値`（向きは常にEP＋・2026-07-18d確定）。水準線値(`signal.levelPrice`)未入力 or α未確定は「—」。旧の符号↑↓＋絶対値表示は廃止＝実価格そのまま。6記録表すべて(共通ヘルパー`_elLineInner`/`_elLineCell`経由)に自動反映。
- **`_elEpHighVal`(app-05:4857)**: ライン列が唯一の利用先だったため未使用化（コメントに明記・関数は残置＝EP水準値が再び要れば流用可）。
- 検証: node無し→preview(:3457)でSW/旧キャッシュ削除→リロード→app-05.js `new Function`パースOK(len514283)・`_elLineInner`合成レンダー＝levelPrice5750/α3/損切15→「EP5753 損切5768」(画像の予定EP/予定損切りラインと一致)・水準線orα未入力→「EP— 損切—」・小数保持(5753.5/5768.5)。※リロード時にCalendar未定義の一過性クラッシュがconsoleに出るが最終描画は正常(`typeof Calendar==="function"`・カレンダー表示確認)＝SW stale-while-revalidateのレースで本変更と無関係。sw v192→**v193**。

### 2026-07-18e 浮き足分析に「浮き足α値（円）」の詳細表を追加＝%表と併記（基本α詳細表と同形式・sw v191→v192）
ユーザー要望「浮き足を%でなく『浮き足α値＝何円』として、基本α値の詳細データ表と同じ形式で見たい」。既存の浮き足加算率(%)スイープ表(`_elUkiPctSweepNode`)はそのまま残し、その下に円版を併記（AskUserQuestion＝「円版を追加（両方表示）」「%表がある全箇所に」）。
- **描画コア共通化（app-06）**: `_elUkiPctSweepNode`の中身を汎用`_elUkiSweepNodeCore(sweep,cfg)`へ分離（`cfg.keyOf`=行→P/X・`unit`="%"/"円"・`head`=第1列見出し・`recoWord`=推奨バー語・`noneTail`=推奨無し時の末尾）。%版`_elUkiPctSweepNode`は薄いラッパに（列/スタイル/ロジックは完全等価＝見た目不変）。
- **円版スイープ`_elUkiValSweep(pool,aiOf,holiSet)`（app-06）**: 加算率%でなく浮き足α値=固定X円(0〜20・1円刻み＝基本α詳細表と同レンジ)を全記録に上乗せ＝実効α=`(採用α−_elUkiAdd(s))+X`（%版`_elUkiPctSweep`の`uv*P/100`をXへ置換）。母数/評価(`_elH2EvalByFn`)/★選定(`_elBordaBest`=平均最終損益最大・qual=E成立≥`_EL_BASE_MIN_N`&Σ黒字)は%版と同一。円版ノード`_elUkiValSweepNode`＋併記ブロック`_elUkiValBoardBlock(pool,aiOf,holiSet)`（見出し「⚡浮き足α値（円）で見る＝基本αと同じ土俵」＋緑説明＋表）。
- **呼出3関数＝4画面に反映**: `_elUkiPctBoardV2`（📡シグナル総合タブ）・`_elUkiPctBoardScoped`（記録フォーム📊 app-05:7567／EPナビ📊 app-04:4076）・`_elFloatReasonSectionV2`（各シグナルの浮き足分析欄）の%表ノード直後に`_elUkiValBoardBlock(pool,…)`を追加＝%と同じpoolで母数一致。app-04/05は`_elUkiPctBoardScoped`経由で自動反映（両ファイル無改修＝編集はapp-06.js＋sw.jsのみ）。
- **検証**: node無し→preview(:3457別オリジン)でSW/旧キャッシュ削除→リロード→`new Function`パースOK(len581396)・新関数5つ`typeof=function`・`_elUkiValSweep`合成poolでrows21(X=0〜20)・`_elUkiValBoardBlock`を`ReactDOM.render`例外0(本文に「浮き足α値」「円」)・console 0。実データの数値/見た目はユーザー実機。sw v191→**v192**。

### 2026-07-18d 記録表に「ライン」列を追加（損切り列の右・EP水準値／損切ライン=EP＋損切り値／sw v189→v190）
ユーザー要望「損切り欄の右に『ライン』列を追加。EP（円不要）と損切（EPに損切り値を足した数値）の計4行構成（EP／値／損切／値）」。損切ラインの向きは「常に EP＋損切り値」（ロング/ショートで符号は変えない）で確定。数値の左右余白は最小（2px）。
- **共有ヘルパー新設（app-05・`_elCutValNode`直後）**: `_elEpHighVal(s,alpha,cutLine)`＝記録のEP水準値（高値＝OS・損益詳細の「EP ↑○」の○。v2は`_epResolve(s,alpha).ep.h`／旧記録は`Number(s.osVal)`。α未達(`_elDynResult==="miss"` or v2 judge==="miss")・EP未成立はnull）。`_elLineInner(s,alpha,cutLine)`＝4行縦積みノード（`EP`／符号つき`↑/↓`値／`損切`／`EP＋損切り値`。EP未成立は「—」・EP色#334155/損切色#B45309）。`_elLineCell(s,alpha,cutLine,border)`＝`_elLineInner`をtd(padding "1px 2px"・borderBottom/borderRight=border)で包む。
- **適用＝同レイアウト（EP/OS・損益詳細を持つ）記録表6つすべて**（`_elDetailFlowStack`描画表）: ①ChartSection銘柄別シグナル表`_esTh`(app-02・画像の表・`_elLineCell(s,_avH,_cutLH,...)`)②WeeklyPnlPanel週次明細`_detailTableFor`(app-02)③DayView取引表`_trTh`(app-04)④DayView銘柄別サブ表`_rTh`(app-04・α/損切りシミュ表)⑤OS分析クリック明細`_elOsTradeMini`(app-06)⑥記録帳一覧`_recTable` full(app-06・`_elLineInner`を`_td`で包む)。各表で「損切り」ヘッダーthの右にライン列thを追加＋本体の`_elCutValNode`セルの右に挿入。
- **colSpan整合**: 各表の合計行の「合計」or空セルのcolSpanを+1／行展開カード(EntryLogCard)のcolSpanを+1／`_recTable`の`colN`を13→14。EntryLogCard自体（app-05:8721=縦カード・列表でない）は対象外。
- 検証: nodeなし→preview_start scalping＋SW古キャッシュ削除後リロードで`_elEpHighVal/_elLineInner/_elLineCell`定義確認＋合成記録でEP確立→`EP ↑20/損切 ↑35`（20+15）・α未達→`EP —/損切 —`を確認。表描画（実データ）はユーザー実機。

### 2026-07-18c 銘柄別記録タブのα詳細表を「その日に取引記録が無くても」既定表示（sw v184→v185）
ユーザー要望「銘柄別記録テーブルのα値の詳細表などは、その日に取引記録がなくてもデフォルトで表示して。現状表示されていない」。
- **原因**: ChartSection（app-02・DayViewの「銘柄別記録」タブ`tab==="charts"`で描画）の「α 推奨α値（{stock}）」ブロック（`_elBaseAlphaPeriodBlockV2`＝本日の採用α値`_ElDayAlphaPair`＋α詳細データ表`_ElRecoAlphaDetail`／母数は前日まで全期間）が `if (!_iaSigs.length) return null;`（＝その日の`cd.signals`が空なら非表示）で当日エントリーの有無に依存して隠れていた。DayViewの`_elBaseAlphaDayBlockV2`は「前日まで or 本日」ガードで既に無記録日でも表示していたので非対称だった。
- **修正**（app-02:6060付近）: ガードを `if (!_elStockRecsBefore(data, stock, date).length && !_iaSigs.length) return null;` に変更＝前日まで全期間の履歴（v2・算入）があれば当日エントリー0でも表示。履歴ゼロ（前日まで・当日とも記録なし）の銘柄だけ非表示（空ブロック回避）。日経(hideSignals=true)は従来どおり対象外。
- **検証**: preview(:3457・データ空)で実マウント console 0。合成12記録＋無記録閲覧日で `_elStockRecsBefore`=12→`guardShows=true`（表示）、履歴ゼロ銘柄=`guardShows=false`（非表示）、`_elBaseAlphaPeriodBlockV2`をReactDOM renderし例外0・見出し「α 推奨α値（TEST）」＋本日の採用α値欄＋α詳細data表(table)presenceを確認。

### 2026-07-18b 📡シグナル総合タブにKPI早見（8枚＋頻度）＋浮き足/RN表をα詳細表スタイルへ刷新（sw v183→v184）
ユーザー要望「分析を充実させたい・KPI（勝率/平均損益/頻度）を出す・既存表もα詳細表を参照して刷新」。母数=サブタブごと（浮き足%→浮き足〇[浮き値あり]／RN→RN〇）。
- **KPI早見**: `_kpiBlockOf(rs, _freqHoli)`（app-06）に第2引数を追加＝holiSetを渡すと頻度カードを差し込み8枚(4×2)→9枚(3×3)。未指定（集計タブの既存2呼出）は従来の8枚(4×2)のまま不変。頻度＝`_elBizSpanDays`(活動営業日)÷`_elEnteredDays`(採用αでEP到達した実日数)。シグナル総合タブ本体（app-06 `_isSigTotal`分岐）で`_sigHoliSet`/`_sigUkiPool`/`_sigRnPool`を作り、集計タブと同様に見出しの前へKPI見出し＋`_kpiBlockOf(pool,_sigHoliSet)`を置く（カード外にそのまま並ぶ）。
- **浮き足加算率表の刷新**: `_elUkiPctSweep(pool, aiOf, holiSet)`に第3引数追加＝`span`(`_elBizSpanDays`)を返し各行に`entDays`(`_elEnteredDays`)を持たせる。`_elUkiPctSweepNode`の列を α詳細表(`_elBaseAlphaDetailV2`)と同じ「浮き足%/E成立/到達率/**頻度**/利確率/損切り率/**最終損益(平均/中央/Σ)**/**勝ち/負け平均**/スコア」へ（共通セル`_elFreqCell`/`_elH2AmtCell`/`_elH2WinLossCell`/`_elScoreCell`を流用）。旧列「件数/平均最終/想定損益(計)」は最終損益セルに集約。★推奨/次点/推奨バーは不変。`_elUkiPctBoardV2`/`_elUkiPctBoardScoped`にholiSet引数を追加し透過。銘柄別グループ内の`_elFloatReasonSectionV2`のスイープ表もholiSetを渡し同刷新。
- **RN①EP位置スイープ表**: `_elRnBoardV2(recs, aiOf, holiSet)`に第3引数追加＝各行に`af`(αオフセット関数)を持たせ`_span`＋「**頻度**」列（E成立の隣）を追加。②寄与内訳・③RN距離別は不変。
- **呼出更新**: シグナル総合（app-06 5762/5759相当）＋記録フォーム📊詳細表（app-05）＋EPナビ📊詳細表（app-04）の`_elUkiPctBoardScoped`呼出にholiSet(`_buildHolidayDateSet(data.trades,(data.custom||{}).eventCategories)`)を追加。
- **検証**: node無し→preview(:3457別オリジン=データ空)で実マウント（console 0・シグナル総合の空状態にKPI見出し描画）＋合成12〜10記録で`_elUkiPctSweepNode`/`_elRnBoardV2`/`_elUkiPctBoardV2`をReactDOM detached renderし例外0・ヘッダ9列/セル9個一致・頻度列presence・span/entDays実値・KPIの9枚(3×3)/8枚(4×2)分岐をassert。損益は合成値なので参考。

### 2026-07-15 推奨α★の選定を「膝（B案）」へ全面変更（基本・応用・追加・浮き足）＋詳細表/見出しの説明更新
ユーザー方針: 平均が高くてもエントリー数が少なく累計が痩せる高α側を★にしない。ゲート（黒字・到達率・損切り率・E成立・頻度）通過候補の中で「累計損益Σがほぼ最大（既定95%以上）を保てる範囲」に絞り、その中で質（スコア=0.7×損切り回避+0.3×利確率）最良のα＝膝を理想とし、推奨（★）＝理想−1（指値フィルマージン）。旧＝「到達率下限ギリギリまで最も高いα−1」／旧追加α＝「平均最終損益最大」。
- 新規 `_EL_KNEE_FRAC=0.95`（膝の閾値・調整可）＋共通ヘルパー `_elKneeFilter(cands, sigOf)`（app-06・`_EL_ALPHA_OFFSET`直後）。Σが候補中の最大のこの割合以上に絞る純関数。
- `_elBaseAlphaPick`/`_elSpecialAlphaPick`（app-06）: okCands→`_elKneeFilter`→スコア最大ソート→`_finish`既定オフセット（膝＝理想／推奨＝理想−`_EL_ALPHA_OFFSET`・応用のクランプは温存）。両`_finish`に第3引数off（既定=`_EL_ALPHA_OFFSET`）を追加。
- `_elAddAlphaPickDate`（app-06）: sel.cand→`_elKneeFilter`→スコア最大（旧avgH2最大を置換）。
- `_elUkiPctSweep`（app-06）: qual行→`_elKneeFilter`→スコア最大（best/runnerUpとも膝内）。
- 説明文更新（膝ロジックに統一）: 基本α詳細表サブ見出し/解説ボックス（`_elBaseAlphaDetailV2`）・応用α詳細表note/insight/label（`_elTotalAlphaSectionV2`）・期間表note・到達率設定ラベル・セクションヘッダー。**当初offset=0で「膝＝推奨」（理想表記を消した）が、ユーザーが指値フィルマージンを復活選択→`_finish`既定オフセットに戻す（`, 0`削除・2026-07-15b/sw v160→v161）＝理想＝膝／推奨＝★＝理想−1・「（理想 X −1）」注記と「理想」タグは従来どおり表示（表データで理想4円・推奨3円）。**
- 検証: node無し→ブラウザV8で `new Function`構文OK＋実ロード関数で膝単体テスト。画面表データ（gate通過0〜10円）で膝集合={0,3,4}・★=4円（膝無しなら質最大で8円=Σ34%減）。実機はユーザー。
- **2026-07-15c（同指示のフォロー・a18419fの次/sw v162）＝★選定を「膝(累計Σ95%足切り＋質)」→「平均50:累計50の合成スコア」へ置換**: ユーザー「平均も累計も同じぐらい重要」＝膝は累計寄りすぎ（Σ95%足切りで高α=平均側を除外、平均はタイブレークのみ）。`_EL_KNEE_FRAC`/`_elKneeFilter`を廃止→`_EL_W_SIGMA=0.5`/`_EL_W_AVG=0.5`＋`_elBalancedScorer(cands, sigOf, avgOf)`（Σと平均を各候補中の最大で正規化し等ウェイト合算するスコア関数を返す）を新設。基本/応用/追加/浮き足の4pickでゲート通過候補をこのスコアでソート→最大＝理想（理想−1＝推奨は維持・応用クランプ維持）。安全ゲート（黒字/到達/損切り/E成立/頻度）は不変。説明文8箇所＋セクションヘッダーを「50:50合成」表記に。検証＝V8構文OK＋実関数`_elBalancedScorer`で理想4円（Σ99%×平均87%=score0.933）・推奨3円・膝残骸0・console0。ウェイトは`_EL_W_SIGMA`/`_EL_W_AVG`で調整可。
- **2026-07-15d（同・b04a333の次/sw v163）＝重み微調整＋頻度ゲート厳格化**: ①ユーザー「平均を少し重めに」＝`_EL_W_SIGMA` 0.5→**0.4**／`_EL_W_AVG` 0.5→**0.6**（平均寄り）。②ユーザー「頻度は5未満」＝頻度ゲートを`(_fspan/ed) <= _EL_FREQ_MAX`→`< _EL_FREQ_MAX`（基本/応用/追加の`_freqOk`/`_freqOkX` 3箇所・`_EL_FREQ_MAX=5`は据置＝5未満に厳格化）＋表示文の「以内」「≤」を「未満」に統一（8箇所・replace_all）。表データは理想4円/推奨3円で不変（4円はΣ99%×平均87%で両立・重み0.4/0.6でscore0.920）。検証＝V8構文OK＋実定数0.4/0.6・実関数理想4円・`< _EL_FREQ_MAX`×3・`<=`残0・`頻度≤`残0・console0。
- **2026-07-15e（同・相談で採用/sw v164）＝★選定を「正規化した重み付き合成」→「順位和（ボルダ得点）」に置換**: ユーザー提案「前提を満たす中で累計上位5→平均で順位付け→順位ポイント(1位5,2位4…)を足して最大＝理想」。実装＝前提ゲート＋頻度<5通過候補を累計Σ上位`_EL_BORDA_TOPN`(=5)に絞り、pool内で累計順位点と平均(avgH2)順位点(トップ=n…1点)を出し`_EL_W_SIGMA`×累計点＋`_EL_W_AVG`×平均点(**0.4/0.6＝平均を少し重め＝平均1.5倍**)の合計最大＝理想／推奨＝理想−1。同点は平均点→累計点→高α。**透明・外れ値に強い・累計トップN足切りで低累計α(例:平均最高の高α)は土俵外**。`_elBalancedScorer`廃止→`_elBordaBest(cands, sigOf, avgOf, aOf)`新設。基本/応用/追加/浮き足の4pick(応用/浮き足の次点も順位和で再計算・addのp2も)。説明文9箇所を「順位和」表記に(replace_all 5＋個別4)。検証＝V8構文OK＋実関数`_elBordaBest`で理想4円(累計トップ5={3,4,0,5,6}・4円=累計2位+平均1位で合計点最大)・推奨3円・_elBalancedScorer残0・console0。`_EL_BORDA_TOPN`/重みは調整可。
- **2026-07-15f＝応用α詳細表の列を基本αに合わせる（ユーザー要望）**: `_elTotalAlphaSectionV2`（記録帳の応用αセクション＋EPナビ「推奨応用α 詳細データ」ポップアップ app-04:3457 が共用）の表が7列で、基本α詳細表`_elBaseAlphaDetailV2`にある「H1勝率」「平均H1損益」「スコア」の3列が無かった→追加して10列に統一（列順も基本αと同一：…損切り率(最終)/H1勝率/平均H1損益/利確率(最終)/最終損益(平均/Σ)/スコア）。応用プールのH1評価を`_h1ByA[e.a]=_elBaseAlphaEval(pool, _elAnaAiOf(aiOf), e.a)`で算出しセル追加（基本αと同じ前提損切り値・H1勝率=_h1.h1win/平均H1損益=_h1.pnl÷_h1.scN/スコア=_elScoreCell(_h1.score)）。茶テーマ・★/理想マーカーは応用のまま。注記に「H1勝率・平均H1損益・スコア＝旧H1基準の参考列」追加。**今後基本α表の列を変えたら応用も追随させる**。検証＝V8構文OK＋実関数(_elBaseAlphaEval空プールでh1win/pnl/scN/score返す・_elScoreCell throwなし)・ヘッダー10列一致・console0。sw v165。
- **2026-07-15g＝頻度ゲートを表示と一致(祝日も除外)＋淡色を全最低条件に拡張（ユーザー指摘: JX金属で4円が理想のはず・頻度≥5を薄く）**: 【根因】選定の頻度ゲート`_freqOk`は`_elBizSpanDays(pool)`＝**土日のみ除外で祝日を営業日に数えていた**ため、表示の頻度列`_elBizSpanDays(pool, holiSet)`＝**祝日も除外**より高く出て、表示4.7の4円等がゲート内部で≥5扱い＝誤除外→理想が1円低く出ていた（JX金属で理想3円→本来4円）。【修正】休場日カレンダーをモジュール同期`_elHoliCur`(app-06・`_elAnaCutCur`隣・null時は土日のみにフォールバック)＝`_elAlphaInfo`(app-05)で**dataごとにメモ構築**(`_elHoliMemo`・毎レコード呼びのため identity メモ)し代入（前提cut/到達率下限と同じ同期点）。基本/応用/追加の3つの`_fspan`を`_elBizSpanDays(…, _elHoliCur)`に。**これで選定頻度＝表示頻度**。【淡色】`_elTotalAlphaSectionV2`と`_elBaseAlphaDetailV2`の`pass`（淡色でない条件）を「到達率≥下限&黒字」だけ→**「到達率≥下限・黒字・損切り率≤上限・E成立≥下限・頻度<`_EL_FREQ_MAX`」の全最低条件**に（表示と同じ`_freqVal=_span/ed`で頻度判定・_edは頻度セルと共用）＝頻度≥5等の行も薄くなる。基本サブ見出しの「淡色は下限未満/赤字」→「最低条件のどれかを満たさない」。応用ラベルは既に「淡色＝全条件未達」で今回正確化。**今後、選定の頻度と表示の頻度は同じ休場日で計算すること（食い違うと★がズレる）**。検証＝V8構文OK(app-05/06)＋`_elBizSpanDays`が祝日オブジェクト`{日付:true}`でspan20→18(頻度4.0→3.6)＋`_elAlphaInfo`が`_elHoliCur`代入＋淡色2箇所に頻度条件＋console0。sw v166。
- **2026-07-15h＝★選定を「順位和（ボルダ）」→「平均最終損益 最大」に置換（ユーザー要望・相談で採用）**: ユーザー「理想は“1件あたりの成績”で決めたい」＝順位和は累計Σ上位5足切り＋Σ重み0.4で低α（総額の出る側）に寄り、平均ピークの5円等が土俵外/負けになっていた（1枚目の表で理想2円・本来5円）。実装＝`_elBordaBest(cands, sigOf, avgOf, aOf)`本体を書き換え＝ゲート通過候補のうち**平均(avgOf=avgH2)が最大の候補を返す**（同点は累計Σ(sigOf)大→値(aOf)小）。定数`_EL_W_SIGMA`/`_EL_W_AVG`/`_EL_BORDA_TOPN`と順位和ロジックを廃止。関数名`_elBordaBest`は互換のため据え置き（4pick=基本/応用/追加/浮き足が共通で呼ぶ単一選定点・応用の≥基本+1クランプ/次点/推奨=理想−1は温存）。説明文の「累計上位N/累計順位＋平均順位を点数化し合計最大」を「平均最終損益（1件あたり）が最大」に更新（基本α詳細`_elBaseAlphaDetailV2`・応用α詳細`_elTotalAlphaSectionV2`のinsight/note/label・期間トレンド`_elBaseAlphaTrendBody`・サマリー`_elBaseAlphaSummary`・到達率下限ラベル・内部コメント／guard付きPython 25置換・残存`_EL_BORDA_TOPN`/`累計上位`/`順位和トップ`=0）。安全ゲート（黒字/到達率≥下限/損切り率≤上限/E成立≥10/頻度<5）は不変。検証＝V8構文OK＋実関数`_elBordaBest`で1枚目の表のgated(0〜7円)→理想5円(平均1179最大)/推奨4円(=採用中)・同点Σ大→α小OK・定数undefined・mounted/console0。sw v167。
- **2026-07-15i＝★選定の損切り率ゲートを25%→30%に緩和（ユーザー要望）**: `_EL_BASE_MAX_STOPRATE` 0.25→**0.30**（app-06・単一定数＝基本α`_confFull`/`_conf`・応用α`_confFull`/`_conf`・追加α`_selFrom`第1段・淡色`pass`条件・詳細表/insight/labelの表示`Math.round(_EL_BASE_MAX_STOPRATE*100)`が全て30%に自動追従）。20%(07-13)→25%(07-14e)→30%。`_EL_BASE_MAX_STOPRATE_2`(=0.30・追加αのna緩和上限)は据置＝主ゲートと同値になり損切り率のna緩和は実質無効化（頻度ドロップのna緩和のみ残る・無害）。浮き足`_elUkiPctSweep`は元々損切り率ゲート無し＝不変。旧inlineコメント`(20%)`も`(30%)`に。検証＝V8構文OK＋実定数`_EL_BASE_MAX_STOPRATE`=0.3/表示30%・mounted/console0。sw v168。
- **2026-07-15j＝★選定の到達率を「60%目標＋50%フォールバック(参考/青★)」に（ユーザー要望）**: `_EL_ANA_REACH_DEF` 50→**60**（🎯到達率下限ステッパーの既定・調整可は不変）＋新定数`_EL_ANA_REACH_FLOOR2`=**50**（安全網下限）。`_elBaseAlphaPick`/`_elSpecialAlphaPick`のゲートを`_confFull`固定→`_confAt(rf)`ファクトリ化し、①目標到達率(reachFloor)で該当α有→従来どおり`ok`(赤★)／②無ければ50%まで引き下げ再選定→`na`(青★・参考)＝返り値reachFloorも0.5にして表示/淡色を50%基準に／③50%でも無ければ従来どおり`nomin`(条件適合無し)。**2026-07-14dで全廃した na緩和を「到達率だけ・50%まで」限定復活**（ユーザーは50%を青★=参考で表示を選択）。na表示機構(青★`_elStarNode`/橙字/参考バッジ/`_elBaseAlphaSummary`警告)は07-14dでも残置＝そのまま再活性。ガード`_rf2 < reachFloor`でステッパー≤50%時は素通り。追加α(`_elAddAlphaPickDate`)は別の固定`_EL_BASE_MIN_ERATE`=0.5フロアのまま＝不変。ステッパー注記に「満たすαが無ければ50%まで引き下げて参考=青★」追記。検証＝V8構文OK＋実定数60/50＋実関数`_elBordaBest`で三段決定(target60→該当無→50%na/5円・target50→ok・target70→50%na)＋mounted/console0。sw v169。
- **2026-07-16＝時間かぶり除外の判定を「5分固定窓」→「早い方の保有時間（EP〜手じまい）」に（ユーザー要望）**: `_elCollisionExcludedSet`(app-05)のペア条件を`(B.min - A.min) <= 5`→**`B.min <= A.exitMin`**（遅い方Bのシグナル時刻が早い方Aの手じまい足の時刻以内＝Aの保有[EP〜手じまい]にBのシグナルが入ったら被り・境界=手じまい足ちょうども含む）。各記録の手じまい足は`_elRideVals`に**`exitIdx`(=r.epIdx+exitD・0基点レグindex・OS1=0)**を追加して取得、`exitMin = _toMin(s.time) + exitIdx`（OS足=1分前提＝ユーザー確認済・EP以降は元々1分足基準 app-05:8045）。保有はH2(EPの2本後)が最大＝exitIdxの上限はepIdx+2。greedy上限2ペア/keep=早い方/同時刻drop=損益大 は不変。説明文更新（ヘッダーコメント/明細バッジtooltip×2/app-06凡例＝「5分以内」→「早い方の保有時間(EP〜手じまい)に入った」）。検証＝V8実関数`_elRideVals`(EP=OS2/H1→exitIdx2)＋実`_elCollisionExcludedSet`(A:9:46→EP9:47→H1手じまい9:48／B@9:49=除外せず・B@9:48=除外[境界inclusive]・B@9:47=除外)・mounted/console0。sw v170。※5分足記録が混じる場合はpre-EPが5分刻みで手じまい時刻が過小＝将来対応（ユーザー1分足前提でOK確認済）。
- **2026-07-16b＝エントリー記録表に「損切り」「保有」列を追加＋記録カード(EntryLogCard)を案B右パネル型に刷新（ユーザー選択）**: ①新helper(app-05・_elRideMiniNode直後)＝`_elCutValNode(cutLine)`（採用損切り値セル・null→—）／`_elHoldMinNode(s,alpha,cutLine)`（EP足〜手じまい足の保有時間「N分」＝`_elRideVals.exitD`・1分足換算=時間かぶり判定と同基準・損切り手じまいは緑「切」添え・E成立v2以外は—）。②共通セル`_elPnlDetailCells`に3つ目td（保有）追加＝使用3表(app-02週明細/app-04取引タブ/app-06 _elOsTradeMini)は自動反映。③インライン3表(app-02 ChartSection🎯/app-04 日別P&L明細/app-06 _recTable full)は損切りtd(α値の隣)+保有td(OS・損益詳細の隣)を個別追加。**全6表**でヘッダーth追加（保有thは1分足換算tooltip付き）・合計行の先頭colSpan+1と保有位置に空td・展開行colSpan+2（ChartSection 9/10→11/12・週明細11→13・取引タブ10→12・P&L明細11→13・_recTable colN 11→13）。**今後この6表に列を足すときはヘッダー/合計行/展開colSpanの3点セットを揃えること**。④EntryLogCard刷新＝旧v1チップ帯(OS値/確定値/結果/EP損益/H1/H2/実現損益チップ)を廃止→右パネル（最終損益`_elHold2AmtNode`(13px)+決済`_elRideMiniNode`+保有`_elHoldMinNode`＋点線区切りで`_elDetailFlowStack`＋実現損益(グレード+`_tradeAlphaChip`)＋価格）／左カラム＝メモ類(H1H2/α/スルー/審議/根拠/振り返り・v1記録のみOS値/確定値チップ温存)／シグナルチップの括弧詳細(`_elTagDisp`)を素のタグ名に＝下の詳細タグ行と二重だった表示を一本化／ヘッダー時刻に分足バッジ`_minBarBadge`・α採用チップの隣にE判定`_epECell`追加。旧planPnl/holdPnl/_dispResult/_dispHP/_resultBadge/_hpBadge/_pnlColor/_pnlFmtローカルは撤去。カードは7呼出箇所（表4種の展開行・ChartSectionカード一覧・記録帳一覧・週明細展開）共通で全反映。sw v174。
- **2026-07-16c＝シグナル総合のRNサブタブにRN〇記録一覧＋時間帯/曜日サブタブ撤去（ユーザー要望）**: ①RNサブタブ（app-06 EntryLogView `_tabBody`の`_isSigTotal`分岐）に「🗂 RN〇の記録一覧（全銘柄）」カードを追加＝`_recTable(_v2recsAll.filter(_elRnYes).sort(_byDateDesc), "full", "rntab_")`＝RNボードの母数そのものを記録帳標準の一覧（行タップで明細カード→カードタップで`setEditTarget`→EntryRecordForm）で直接編集可能に。②`_SIG_TABS`から tod(時間帯)/dow(曜日) を撤去＋`_tabBody`の両分岐削除（旧sigSub値はelseで浮き足%に落ちる・既定"uki"は不変）。`_elTimeOfDaySectionV2`/`_elDowSectionV2`本体は**シグナル別グループ分析(_groupPanel)内で使用中のため残置**（削除したのはシグナル総合の全銘柄版のみ）。sw v175。
- **2026-07-16e＝α詳細表の列再編（ユーザー指示＋提案3件採用・9エージェント調査＋28エージェント敵対レビュー）**: 【ユーザー指示】①H1勝率・平均H1損益を削除（最終損益があるので不要）②利確率と損切り率を隣に・利確率が左 ③E成立を到達率の左 ④**スコア列は残す**（ユーザー判断・私の推奨は削除だった）。【新列3件】中央値／勝ち・負け平均／未達。**新列順(10列・変更前と同数)＝ 基本α|E成立|到達率|頻度|利確率(最終)|損切り率(最終)|最終損益(平均/中央/Σ)|勝ち/負け平均|スコア|未達**。
  - **エンジン**`_elH2EvalByFn`(app-06:1442)に`medH2`(中央値)/`avgWin`/`avgLoss`を追加＝既存1パス内で`h2Vals`/`winSum`/`lossSum`/`lossN`を収集（プール再走査なし・α毎のsort1回のみ）。**キー追加のみ＝既存consumerに影響なし**。母数は`h2Cnt`(=`_elHold2TotParts.main`非null)でE成立(`decided`)とズレうる。勝ち/負けの境界は利確率と同一(`main>0`が勝ち・**0円トントンは負け側**)。実機で恒等式「平均 = 利確率×勝ち平均 +(1−利確率)×負け平均」の成立を確認。
  - **共通セル3件**を`_elScoreCell`直後に新設＝`_elGateBadges(ngs)`(未達バッジ・ngs=[[記号,tip]]・空=緑✓)／`_elH2AmtCell(h2)`(平均/中央/Σの3段)／`_elH2WinLossCell(h2)`(勝ち/負け平均2段)。**3表で共用**。
  - **未達列**＝各表の`pass`の&&連結を`_ng`配列にほどいただけ（判定は等価・追加計算ゼロ・記号=到/件/頻/損/赤）。「なぜ隣のαが★を取れないか」が読める。**バッジ⇔淡色の一致を全26行で検証済み**。
  - **応用α表`_elTotalAlphaSectionV2`も同形に追随**（2026-07-15fの追随ルール）。**落とし穴＝主スイープが逆**: 応用は`e`自体が`_elH2EvalByFn`なので`e.medH2`直参照／基本は`_h2ByA[e.a]`ルックアップ。機械的コピペするとundefined列になる。スコア列を残したので応用側の`_h1ByA`も残置。
  - **祝日バグ修正**: α値タブ基本αゾーン(app-06:5791)だけ`_elBaseAlphaDetailV2(_drs,_ai)`とholiSet未渡しで、頻度列と淡色が祝日を営業日として数え★とズレていた。`_alHoliSet`の宣言を`if(_alSel==="base")`の外へ出して両ゾーンに渡す。**全10呼出でholiSet渡し済みを確認**。
  - **⚠️`_elAddAlphaDetailV2`は呼び出し元ゼロの死んだ関数**（2026-07-13fの応用α移行で配線が外れ「当面残置」＝FILEMAP:77）。同型なので列は揃えたが**画面には出ない**。関数先頭に未配線マーカーを明記した。専用の`_elAddAlphaPickDate`/`_elAddAlphaRecoScore`も同様。**ここの文言/列を直しても無意味＝将来繰り返さないこと**。
  - **⚠️`_secH`の副題は「※」で始まらないと描画されない**(app-06:4987・2026-07-03「見出し下の定型説明は非表示」)。よってセクション副題の文言修正はコード上のみで画面に出ない。**「見出しの文言がおかしい」系の指摘を受けたら、まず`_secH`の※ルールを確認すること**。
  - 敵対レビューで棄却した指摘: 「0〜4円行に★が出ないのに✓が出る」→**誤り**。★選定は`full`=`_EL_BASE_ALPHAS_FULL`(0〜20)で行われ(app-06:1759)、`pick.sweep`(5〜20)はH1参考列用に運ばれるだけ＝0〜4円も★になりうる（現に理想5円→推奨4円が★）。sw v177。
- **2026-07-16f＝損切りタブに「📉 逆行(MAE)」セクション新設（ユーザー承認）＋2表食い違いの調査**:
  - **新関数2本**（app-06・`_elStopOvershootSectionV2`の直前）＝`_elMaeStats(entered, aiOf, C)`（素値・{n,stopN,rows,arr,max,mean,med,p90}）／`_elStopMaeSectionV2(entered, aiOf)`（本体・カード4＋分布表＋個別`_SNCollapse`＋insight）。挿入位置＝`simTbl`と`📐上振れ`の間。**simTbl に「平均逆行」「最悪逆行」列も追加**（6列化）。
  - **逆行の定義**＝`_elRideVals(s,α,C).mx − α`（EP足〜手じまい足の最高値−α・**水準線比の円＝損益ではない**）。母数＝`v.stopped===false`（損切りを免れた取引）＝`_elStopOvershootSectionV2`(損切りした記録・`_elHoldMaxHigh.all`基準)と**排他**。
  - **中核定理（実測検証済み）**: ①逆行≥0（`_epResolve`が`h>=alpha`でEPを立てる）②非損切りなら**逆行 < C**（損切り判定`h−α≥C`の対偶・mxの走査範囲0..exitDと判定の深さが同じ土俵）③**C'=最悪逆行+1に詰めても生存者は顔ぶれ・損益とも完全不変**（C=20/生存12件/最悪14→C'=15で12→12件・損益完全同一を実測）④損切り組の損失は**縮むか同額**（実測: −12,000→−12,000＝同額もある。「必ず縮む」は誤り）⑤**単調性**＝Cが広いほど最悪逆行は非減少（実表: C5→4/C8→7/C10→8/C12→11/C15→14/C20→14）＝**バグの検算に使える**。
  - **⚠️色の規約が逆**: このアプリの`_elPnlColor`は赤=利益。逆行は損益ではなく**深いほど悪い＝深い方を赤**。`_elPnlFmt/_elPnlColor`を逆行に通すと利益に見える大事故。最終損益列だけ`_elPnlColor`。単位も混在（逆行=水準線比の円／最終損益=100株換算）＝列見出しで明示。
  - **⚠️`_elRideVals`はcutLine省略で15円に化ける**（app-05:4802）＝スイープでCを渡し忘れると全行15円基準になる。
  - **⚠️「損切り回数」カード(`_isStop`＝期待度キャップ無し)と`M.stopN`(`v.stopped`＝期待度×で先に降りた後の接触は「免れた」側)は基準が違い一致しないことがある**＝noteに明記済み。
  - n<`_EL_BASE_MIN_N`(10)は「詰められる下限」を出さず参考表示（最悪値は1件の外れ値で動くため）。
  - **2表食い違いの調査結論**: 既定状態(根拠=全体/詳細=まとめて/詳細タグ別OFF)なら集計タブ(`_baRecs`=`_selSigRecs`)とα値タブ(`_alReasonRecsFull`=`_reasonFilter(_selSigRecs)`)は**同一配列＝完全一致**（実測で14行diff 0）。**修正前は頻度列が全行ズレていた**（営業日22→18・頻度1.2→1.0）＝f552d16が原因ではなく食い違いを直した側。**今も差が出る条件＝①α値タブの根拠セレクタが「全体」以外（集計タブに同フィルタが無い・告知は見出し末尾の小さな注記のみ）②「詳細:」プルダウンが表ごとに独立state(`gp_ba`/`al_baD`)③集計タブの詳細タグ別モード④SWの旧JSキャッシュ**。sw v178。
- **2026-07-16d＝RN分析ボード`_elRnBoardV2`(app-06:1531)を全面刷新（ユーザー相談→①②③全採用・旧2表置き換え）**: RNまたぎ＝EP下二桁90台(91〜99)→RN加算(rnVal=100−下二桁)でEPをRNちょうどに乗せる運用（ユーザー定義をヘッダーコメントに明記）。**①EP位置スイープ**＝EPをRN−3〜RN+3に置き直して`_elH2EvalByFn(pool, aiOf, α+off)`で再判定＋RN無し(α−rnVal)参考行。現行=RNちょうど行は琥珀ハイライト・★=E成立≥`_EL_BASE_MIN_N`かつΣ黒字で平均最終損益最大（RN無しが★なら「そもそも不要」のサイン・薄いうちは（仮））。**②寄与の内訳**＝記録単位で現実/RN無しの最終損益を`_pnlAt`(engine同一判定=_epResolve epIdx0-2→_elDynResult ok/ng/draw→_elHold2TotParts.main)で並べ、「両方成立の値幅改善」「RN待ちで入れなかった取引の仮想損益（負=待って正解）」「差し引き(寄与)」の3カード＋記録ごとの得/損/同件数（Σの1件支配チェック）。恒等式: 差し引き=Σ現実−ΣRN無し=bothDiff−cfOnlySum+realOnlySum。**③RN距離別の寄与**＝rnVal別にΣ現実/ΣRN無し/寄与差。旧「RN込みvsRN無し2行表」「RN加算値別(現実のみ)表」は①③に吸収＝撤去。呼出側_secH注記も更新。sw v176。

### 2026-07-13f 追加α（基本αへの増分）→ 特段α（独立α値）へ全面移行（一括切替・過去の特段記録のEP/損益が変わる移行含む）
「基本α＋追加α増分」モデルを廃止し、**特段の事情がある局面で使う独立したα値＝特段α**へ作り替え。base-levelα ＝ `specialUsed ? specialAlpha : baseAlphaVal` を正本に集約。採用α(alphaVal)＝ base-levelα ＋ 浮き足(_elUkiAdd) ＋ RN(_elRnAdd)。「+追加」増分は消滅。sw v111→**v112**。
- **①データ土台（app-01/05）**: 新helper `_elSpecialUsed(s)=specialUsed===true` / `_elBaseLevelAlpha(s)`（正本）/ `_DEF_SPECIAL_REASONS` を app-05 α-hub に新設。旧3状態判定 `_elAddAlphaYes/No/Unset`・`_addAlphaUnsetBadge`・`_DEF_ADD_REASONS` は撤去。app-05 EntryRecordForm: `fAddAlpha/fAddAlphaUsed/nAddReasons` → `fSpecialAlpha/fAlphaKind("base"/"special")/fAddReasons(=specialReasons由来)`・`_fBaseLevel=(fAlphaKind==="special")?_fSpecialA:_fBaseAInput`・`_fAlpha=_fBaseLevel+浮き+RN`・doSaveは `specialUsed/specialAlpha/specialReasons` を保存（旧addAlpha*撤去）。新 `_refSpecial`（銘柄全体母数の推奨特段α＝§9で移行値と一致）。
- **移行 `_migSpecialAlpha`（app-01 migrateData・return直前・`typeof _elSpecialAlphaPick==="function"`ガード・冪等）**: 旧 `addAlphaUsed===true` の記録のみ、原本を `_almig` にbackup → `specialAlpha`＝各銘柄の推奨特段α（銘柄全体母数・`_elSpecialAlphaPick`／浮き足〇・RN〇は母数除外／不足時は推奨基本α→旧base+add）→ base-levelα=specialAlpha として alphaVal=specialAlpha+浮き+RN 再計算 → 旧 addAlpha* を delete。`custom.addAlphaReasons`→`custom.specialReasons`。**`_migUkiAlpha` を一回性フラグ化して先行**（順序: uki→special）。EPは非保存で alphaVal から都度導出(_epResolve)。
- **②推奨（app-06/05/04）**: `_elTotalAlphaPick`→`_elSpecialAlphaPick`（推奨特段α本体に昇格）。母数フィルタ `_elAddAlphaYes`→`_elSpecialUsed` を全ファイル語境界置換（浮き足〇/RN〇除外は維持）。`_elBaseAlphaA.add` を増分shape{add,total,improved}→特段shape{alpha,alpha2,status,avgH2,…}へ・全下流を `.alpha` 参照へ。二重/三重実装同期: app-05 `_refSigAlpha`/`_autoBase`・app-04 `autoPick`/`_baseDefault`/`_epnSpecialRecoFrom`(旧_epnAddRecoFrom)/`_epnRecalcBase`。
- **③入力UX（app-05/04）**: 記録フォームα値セクション先頭に**「採用α値」セレクタ（基本α/特段α）**（案A＝特段α選択時は下に根拠欄）。EPナビ `_EpnCalcForm`: `nAddUsed/nAdd/nAddReasons`→`nSpecialUsed/nSpecialAlpha/nSpecialReasons`・特段〇×トグル・base-level=特段α置換。`_EpnAddSection`（早見カードインライン）special化。根拠マスター `custom.addAlphaReasons`→`custom.specialReasons`（app-05/app-04二重・改名は過去記録追従）。
- **④記録帳分析（app-06）**: α値タブ「② 追加α」→「② 特段α」・`_elTotalAlphaSectionV2`（推奨合計α→推奨特段α）に配線。母数トグル addAlphaFil/osDistFil を3状態(〇/×/未選択)→2状態(特段あり/なし)。`_elAlphaBreakdownNode`（app-05）単一α＋特段マーカー。撤去（呼び出し停止・関数は当面残置）: `_elAddAlphaReco/_elAddAlphaPickDate/_elAddAlphaRecoScore/_elAddAlphaDetailV2/_elAddAlphaPeriodTableV2/_elAddAlphaSectionV2/_elBaseAlphaH2Pick/_elAddAlphaH2Pick`。
- **⑤シミュ（app-06 `_elKabuLadderSimV2`）**: 増分上乗せ→基底置換（意味反転）。`_simAddOf`=浮き足のみ・新 `_simBaseLevel(r,cand)`＝特段〇なら specialAlpha で候補基本αを置換。`_manTiersOf`/自動combo/`_autoTiersOfFor` の3系統に適用。`_elKabuRecoBaseFn.addAt`→`specialAt`（絶対値）。recoSig署名に specialAlpha 追加。addOn上乗せピル廃止・addFilラベル 特段用語へ。
- **⑥日別/記録表（app-02/04/06）**: α値セル背景ハイライト `_elAddAlphaYes`→`_elSpecialUsed`（app-02×2/app-04×2/app-06）。実効α文字列「+追」→base-level+RN・EPナビ実効α「特段/基」。
- 検証: node無し→自前http.server別オリジン+SW全消しで実マウント console 0。**移行チェックサム**（`migrateData`合成データ）＝specialUsed/specialAlpha変換・`_almig`backup・冪等・alphaVal===specialAlpha+浮き+RN・通常記録不変 全pass。記録帳/α値タブ(推奨特段α20円・母数内訳)/シミュ(特段なし・特段あり・全記録)/EntryRecordForm(採用α選択→特段αで特段α値+根拠+推奨特段α)/_EpnCalcForm(特段α・追加α消滅) 隔離マウント全pass。
- **既知の残作業（follow-up）**: 撤去対象の死関数（_elAddAlphaReco 等 約8本）は呼び出しを全停止したが定義は残置（削除は別パスで）。シミュ手動ラダーの取引ごと「追加α」行UIは無効化済み(結果列非表示)だが行設定UIは残置。

### 2026-07-13g 特段α移行の敵対的レビュー後の修正（3件・sw v112→v113）
独立レビュー（12エージェント・移行/母数/二重三重/base-level/シミュ）で確定した実害3件を修正。二重/三重実装（推奨特段αの _refSpecial/_epnSpecialRecoFrom/_migSpecialAlpha/_elBaseAlphaA.add/_elTotalAlphaSectionV2）は一致で問題なし。
- **[medium] 移行フォールバック（app-01 `_migSpecialAlpha`）**: 最深フォールバック（推奨特段α/推奨基本αとも算出不能な薄い銘柄）を `_oldBase+_oldAdd`（baseAlphaVal欠損=0扱いで旧採用αを取りこぼす）→ `_oldTot(=旧alphaVal)−浮き足−RN`（＝旧base-levelαを厳密復元）へ。baseAlphaValがnull（採用αに畳込み済み）の特段記録でもEP/損益を厳密維持。合成データで検算: base欠損+浮き足込みの記録で旧採用α15を厳密保持。
- **[low] `_elBaseAlphaPeriodTableV2` basePool（app-06）**: 想定損益(1日/1件)列の母数 `!_elSpecialUsed` のみ→`!_elSpecialUsed && !_elUkiYes && !_elRnYes` の三重除外へ（推奨基本α選定 `_elBaseAlphaPick` と母数一致・2026-07-12に `_elBaseAlphaDetailV2` で入れた修正の取りこぼし分）。
- **[medium] シミュ手動ラダーの取引ごと「追加α」入力UI（app-06 `_elKabuLadderSimV2`）**: 特段α化で `_manTiersOf` が addMethod を参照しなくなった（機能停止）のに入力UI＋説明文が残り誤誘導 → 取引ごと追加α行（_addSel/_addInputs）を削除・見出し/注記を「基本αのみ・特段〇は特段α値を基底に採用」へ更新。
- 残（follow-up・dead code）: `_elAddAlphaSectionV2/_elAddAlphaPeriodTableV2` 等の死関数内の increment 残骸・sim の未使用 addOn state は呼び出し無しのため別パスで物理削除。
- 検証: 自前http.server別オリジンで console 0・移行フォールバック検算（旧採用α厳密保持）・_elKabuLadderSimV2 隔離マウント（追加α行消滅・console 0）pass。

### 2026-07-13e EPナビ計算フォーム調整（浮き足を全シグナル表示・③底抜けを畳みの外・早見EP右に底抜け・特段α詳細表ポップアップ）
- **浮き足加算を全シグナルで常時表示（app-04 `_EpnCalcForm`）**: `showUki=_ukiSigNames.indexOf(nTag)>=0`（底抜け系シグナルのみ）→`showUki=true`に。記録フォーム(app-05 `_showUki=true`)と同じ＝全シグナルで浮き足欄＋推奨/次点/手入力%が出る。`_ukiSigNames/_elUkiSignalNames`のEPナビ用途は撤去（関数自体は分析母数で存続）。
- **③底抜けを「詳細を入力」畳みの外へ（app-04 `_EpnCalcForm`）**: ③底抜けチップを`detOpen`畳みブロックから出し、②シグナルの直下に常時表示（`nTag`選択時）。畳みは③起点/④その他/⑤ライン併存だけに＝トグル文言「③〜⑤」→「③起点〜⑤」・`_detSummary`から`底:`除外・空ヒント文言も更新。
- **早見カードのEP価格の右に底抜け（app-04 `_renderCard`）**: `EP ○○円`のdivをflex化し、右に`e.b`（③底抜けの選択値）をEP価格と同サイズ(16px)・プレーン(#475569)で表示（「EP 4232円　前日安値」＝ユーザー指定「VWAPみたいに表示するだけ」）。
- **「特段α 詳細データ表」ポップアップ（app-04 `_EpnCalcForm`・基本αの下）**: 基本α行の下にボタン→state`showSpTable`のオーバーレイ(position:fixed/zIndex10000)で記録帳と同じ`_elTotalAlphaSectionV2`（＝推奨合計α＝特段αの実体・母数=追加α〇[浮き/RN除外]）を表示。母数=`_epnCascade(data,stock,null,null,date).all`＋`_buildHolidayDateSet`。**追加α→特段αの本移行後は自動で正式な特段α表に切替わる**。「本日の採用α値」の「表を参照」と同方式。
- 検証: node無し→自前http.server(:8849別オリジン)で全8ファイル実マウントconsole0・`_EpnCalcForm`隔離レンダー（浮き足/特段αボタン/トグル改称/③底抜けが畳みの外[②シグナル<③底抜け<トグル]）・特段αモーダル開閉（空データで空メッセージ非throw）・EpNaviPanelをEPカード入りでマウント「EP 4232円→前日安値」の順で表示 全pass。sw.js APP_CACHE v110→**v111**。

### 2026-07-13d EPナビ「本日の採用α値」欄＋「表を参照」ポップアップ（記録帳と同じ推奨α値詳細表から選択）＋頻度ゲート理想 4→5
- **「本日の採用α値」欄（app-04・新規`_EpnDayAlphaField`）**: EPナビ各銘柄列の見出し直下に常設。既定＝推奨基本α（銘柄全体＝`_epnCascade(data,stock,null,null,date).stk`＝計算フォームautoPickのstk段と同一・この日より前の記録が母数）。数値入力＋`_stepBtn`▲▼で±1（長押し連続・即保存）。空欄＝推奨に追従（stored=null）／数値を入れると`charts[銘柄_日付].epNaviDayAlpha`へ即保存。並び＝入力欄＋▲▼ → 「表を参照」ボタン → 現状の推奨α値（ユーザー指定）。
- **保存ヘルパー（app-04）**: `_epnDayAlphaGet(data,stock,date)`／`_epnDayAlphaSet(save,stock,date,val)`＝EP保存(epNavi)と同じチャートオブジェクトの別フィールド`epNaviDayAlpha`（数値・null=未設定）。日付ごと・銘柄ごと＝「本日の」は日付キーで自動リセット。migration不要（既存に無ければnull＝推奨追従）。
- **計算フォームへ反映（app-04 `_EpnCalcForm`）**: `dayAlpha` propを受け、`_baseDefault = dayAlpha!=null ? dayAlpha : autoPick.a`。基本αのbaseV/placeholder/▲▼ステッパー/実効α注記/ラベル「空欄＝(本日の採用α値|推奨値)を自動採用」が_baseDefault起点に。**手入力nBaseは常に最優先／dayAlpha未設定は従来どおりautoPick（シグナル別に絞れる）＝純追加で既存不変**。`_cellsForm`で`dayAlpha:_epnDayAlphaGet(data,st,date)`を配線。※早見カードのインライン編集`_epnRecalcBase`はautoPick起点のまま（本欄は計算フォーム初期値のみ・既知の割り切り）。
- **「表を参照」ポップアップ（app-04 EpNaviPanel）**: state`tableStock`（null=閉）。オーバーレイ（position:fixed/inset0/zIndex10000・背景タップ＝閉）に記録帳と**全く同じ**推奨α値詳細表`_elBaseAlphaDetailV2(_c.all,_c.aiOf,_hs,onPick,curEff)`を表示。母数＝`_epnCascade(...).all`（この銘柄v2・この日より前＝フォーム推奨と同一）。行タップでその基本α値を本日の採用α値へ取り込み閉じる。現採用値は青ハイライト。
- **`_elBaseAlphaDetailV2`に選択機能を追加（app-06）**: 引数を`(recs,aiOf,holiSet,onPick,curSel)`に拡張（**後方互換＝既存3引数呼び出しは非選択で不変**）。onPick時のみ各行を`createElement.apply`でクリック可能化＋末尾に「選択/採用中」列・ヘッダーに「選択」を`.concat(onPick?["選択"]:[])`。curSel一致行を青#EFF6FF背景。
- **頻度ゲート理想 4→5（app-06 `_EL_FREQ_MAX`）**: ユーザー方針変更「頻度の理想は5以下」。`_EL_FREQ_MAX=4→5`（≤5営業日/回）。全推奨★（_elBaseAlphaPick/_elTotalAlphaPick/_elAddAlphaPickDate）＋UI文言は`_EL_FREQ_MAX`補間で自動追従（コメントのみ手修正）。
- 検証: node無し→自前http.server(:8848 別オリジン)で全8ファイル実マウント＝console 0・`_EpnDayAlphaField`隔離レンダー（入力/▲▼/表を参照→onOpenTable発火/推奨データ無し表示）・`_elBaseAlphaDetailV2`新シグネチャ空データ非throw・`_elBaseAlphaDetailV2.length===5`・`_EL_FREQ_MAX===5`全pass。sw.js APP_CACHE v109→**v110**。

### 2026-07-13c 推奨合計α（追加α〇局面）新セクション＋★色分け（赤/青）＋頻度ゲート（≤4営業日/回）＋頻度数字表記
- **推奨合計α（新セクション・app-06）**: ユーザー要望「追加α〇記録向けの推奨基本α分析」→AskUserQuestionで「基本/追加を分けず合計α1本・0〜20円・表示のみ・追加αサブタブに新設」に確定。新規=`_EL_TOTAL_ALPHAS`(0〜20)・`_elTotalAlphaPick(pool,aiOf)`（母数=追加α〇[浮き足〇/RN〇除外]・各合計α0〜20を前提損切り値で一律評価・_elH2EvalByFn手じまい基準・★選定は_elBaseAlphaPickと厳密同一条件式＋平均最終損益最大）・`_elTotalAlphaSectionV2(recs,aiOf,holiSet)`（結論バー＋母数内訳＋合計α別総当たり表）。配線=追加αゾーン先頭に`_secH/_detBody("al_totA", _alReasonRecsFull)`。**表示のみ＝フォーム/EPナビ/シミュ/推奨値の自動入力には非配線**（純加算・0削除・敵対的レビュー2観点[正しさ/副作用なし]でPASS＝既存推奨は1円も不変）。母数は下の追加α詳細と件数一致。
- **★色分け（赤/青・全推奨α詳細表）**: ユーザー指定「濃い字の中での推奨=赤★・薄い字でも推奨があれば青★」。新helper`_elStarNode(status)`＝status ok→赤#C0392B（条件充足＝濃い字の推奨）／na→青#0369A1（条件緩和の参考推奨）。適用=_elBaseAlphaDetailV2/_elBaseAlphaSummary/_elAddAlphaDetailV2(日付別主表)/_elTotalAlphaSectionV2。red★は常にpass行(濃)に載る（ok=strict通過）・blue★は緩和pick(na)に載る。
- **頻度ゲート（≤4営業日/回・★選定に組込・app-06）**: ユーザー指定「理想は4営業日以内に1回」。`_EL_FREQ_MAX=4`。_elBaseAlphaPick/_elTotalAlphaPick/_elAddAlphaPickDate の選定に頻度条件を追加＝活動営業日span(`_elBizSpanDays(pool)`・holiSet無し平日ベース)÷そのαの到達実日数(`_elEnteredDays`)≤4。**フォールバック順=損切り20%＋頻度≤4(ok) → 損切り30%＋頻度≤4(na) → 頻度ゲート解除(na)**＝頻度の理想を最後まで温存。**不変条件（検証済）＝第3段`_selFrom(0.3,false)`は旧na段`_selFrom(0.3)`とバイト同一→頻度ゲートで「none」への退行は起きない**（推奨が消えることはなく、頻度不足なら青★の参考に降格するだけ）。これは_elBaseAlphaPick経由で全画面（日別ページ/取引・銘柄別テーブル/フォーム/EPナビ/シミュ）の推奨に波及＝疎な（めったに入れない）高αは青★に落ち、頻度の高いαが赤★で選ばれやすくなる。
- **頻度セル数字のみ表記（`_elFreqCell`）**: ユーザー指定「『3.8』とか数字のみ」。「X営業日に1回」→数字のみ（10未満は小数1桁・以上は整数・ツールチップに意味を残す）。全詳細表の頻度列に波及。
- 条件文言/凡例更新: 各詳細表のconcl na/banner/insight/主表ラベルに「頻度≤4営業日/回」と「★赤=条件充足／★青=緩和参考」を追記。
- 検証: esprima＋実マウント（console 0）＋単体＝★色(ok赤rgb(192,57,43)/na青rgb(3,105,161))・頻度セル"3.8"・**頻度ゲート切り分け（同じ勝ちトレードで密[毎営業日到達]=ok/赤★・疎[4日集中でfreq5.5]=na/青★）**・合計αpick(母数22=〇24−浮き足2・0〜20レンジ)・3詳細表レンダー(頻度条件文言/★/凡例)全PASS。敵対的レビュー(正しさ/副作用)PASS。sw.js APP_CACHE v108→**v109**。

### 2026-07-13b 推奨追加αを「日付別カウンターファクタル（各記録日の推奨基本α＋加算X・手仕舞い基準）」へ全面刷新＋②追加αタブに根拠セレクタ・詳細データ表
- ユーザー要望「それぞれの記録での推奨基本α値（新★基準で再計算されたもの）に何円を追加していれば手仕舞いでこうだった、という分析」＋「基本αと同じような分析・全体と根拠別・詳細データ表」。AskUserQuestionで確定＝★基準は基本αと完全統一／範囲0〜10円（0円=足さない基準行）／固定スイープは参考で残す／根拠別UI=案Aセレクタ／旧④⑤根拠別は撤去／日付別表（旧H1版）は主表に昇格。
- **新コア（app-06・_elAddAlphaRecoScoreの直後）**: `_EL_ADD_SWEEP_MAX=10`・**`_elAddAlphaPickDate(pool, aiOf, recoFn, baseAlpha)`**＝X=0〜10を「実効α=recoFn(記録日)+X」で_elH2EvalByFn（手仕舞い）＋_elAlphaEvalByFn（H1参考）並走→基本αと同一条件式（到達≥50%・損切り(手じまい)≤20%・E成立≥20→10緩和・黒字→Σ最終損益最大・同点E成立多→小X・緩和30%=na）。★X=0→improved:false＋**zeroBest:true**（「足さない方が良い」）。返り値旧互換＋{zeroBest,status,minN,nBase,decided,takeRate,h2Sum,avgH2,h2Sum2,sweepDate}。**`_elRecoFnCached(recs,aiOf)`**＝recoFn（日付時点推奨基本α）のモジュールLRUキャッシュ（内容署名・上限24）。**`_elAddAlphaReco(recs,aiOf,baseAlpha,fullRecs)`＝新正本のラッパー**（fullRecs=recoFn母数・全呼び出し元が新方式に）。旧・段階選抜は**`_elAddAlphaRecoScore`**に改名保存（「旧基準 +X円」チップ用・_elOldPickChipにpfx引数追加）。
- **`_elKabuRecoBaseFn`再構築（再帰爆発の防止・必須）**: 窓ごとの基本αを`_elBaseAlphaA`→**`_elBaseAlphaPick`直**（追加α計算を含まない）に変更し、`addAt`は遅延評価＝窓の〇プールへ`_elAddAlphaPickDate(pool, aiOf, fn自身, base)`（fnは基本α部分のみ参照＝再帰しない）。**_elBaseAlphaA/_elAddAlphaReco経由で_elKabuRecoBaseFnを呼ぶ経路を作らないこと**（wrapperは_elRecoFnCached→_elKabuRecoBaseFnで終端）。`_elBaseAlphaA`は第4引数としてrecsをwrapperへ渡す。
- **`_elAddAlphaDetailV2(recs, aiOf, holiSet, fullRecs)`刷新**: 結論バー（新基準統計・＋0円（足さない）表現・na=30%緩和参考・旧基準チップ）＋母数内訳1行（〇N→浮き足/RN除外→M件・うち推奨基本α算出可nBase件）＋**主表=日付別（sweepDate流用・列=追加α/到達率/頻度/E成立/損切り率(最終)/H1勝率/平均H1損益/利確率(最終)/Σ最終損益・★/淡色=条件式）**＋参考表=旧固定スイープ（★なし・H1基準と明記）＋読み取り（+0円との差=上乗せ効果）。呼び出し=集計タブ(_baRecs渡し)＋②追加αタブ新設。
- **②追加αタブ（EntryLogView）**: 根拠セレクタバーを全サブタブに適用（`if(_alSel==="base"||"tools")`→無条件）。`_alAdd`=根拠フィルタ後プール＋recoFn母数=_selSigRecs全体で`_elAddAlphaReco`。ゾーンヘッド=zeroBest/na/旧基準チップ（`_elAddAlphaH2Pick`バッジ廃止・関数は残置）。新セクション「🔬 推奨追加α 詳細データ（日付別・手仕舞い基準）」＋期間別/📐分析を_alReasonRecs*に配線。`_alHoliSet`=_buildHolidayDateSetを追加。
- **`_elAddAlphaSectionV2`**: ②効果検証・③幅別実績を**手じまい基準化**（_h2At=_elHold2TotParts.main・_stopAtにH2損切り追加・文言更新）・**④根拠別成績/⑤根拠別明細を撤去**（_numReason等も除去）。**`_elAddAlphaPeriodTableV2`**: 列を「損切り率(最終)/利確率(最終)/到達率/E成立/Σ最終損益」に（統計=新pick同梱値・固定α再評価廃止・＋0円（足さない）/参考バッジ）。
- **KPIカード_kpiAdd（集計）**: zeroBest「+0円（足さない）」/na参考/旧基準チップ。**DayView🎯推奨α値/銘柄別期間ブロックの説明文2箇所**＝旧スコア式の記述を新基準（基本α=条件式Σ最終・追加α=日付別）に全面書き換え・見出し統計を「損切(最終)/利確/E成立」に。**app-04 `_epnAddRecoFrom`**＝第4引数baseLevelRecs（EPナビも日付別方式に）。フォームは_elBaseAlphaA経由で自動追随。
- **同日追記1＝★選定をΣ最終損益→「平均最終損益（1件あたり）」最大に変更（ユーザー指示「最終損益は合計額でなく平均額」）**: `_elBaseAlphaPick`/`_elAddAlphaPickDate`のbyBestをavgH2比較に（黒字条件h2Sum>0は同値なので不変）。表示＝詳細表/期間まとめ/トレンド/追加α主表/期間別表の損益列を「最終損益(平均/Σ)」2段セル（平均=太字・Σ=小書き）・結論バー/KPI/insightは平均主表示＋Σ併記・「〜最大」系文言をreplace_allで平均表現に統一。
- **同日追記2＝前提損切り値（ユーザー指示・既定15円）**: 「損切り値が◯円である前提として推奨α値はこうなった」＝推奨系スイープのcutを各記録の実損切り値→**固定の前提値**に。新設（app-06 consts直後）＝`_EL_ANA_CUT_DEF=15`／`_elAnaCutCur`（モジュール共有・**同期は_elAlphaInfo(app-05)内**＝全画面のaiOf構築時にdata.custom.anaCutPremiseから更新）／`_elAnaCut(data)`／`_elAnaAiOf(aiOf)`（cutLineだけ前提値に差し替えるラッパー）／`_ElAnaCutCtl`（↑↓ステッパー・custom.anaCutPremise保存＝全端末同期・α値タブのピル直下＋集計タブ銘柄別パネル_addFilBar直下に設置）。適用＝`_elBaseAlphaPick/Score`・`_elAddAlphaPickDate/RecoScore`の内部でaiOfをwrap＋BaseDetailV2の表示スイープ(_lowSweep/_h2ByA/母数集計)＋AddDetailV2参考表。**非適用＝実績損益・損切りタブ・_elCutPick・意思決定表/感応度・効果検証**。キャッシュは前提値をキーに含める（`_elRecoFnCached`のsig＋シミュrecoSigに"c15!"前置）＝前提変更で全推奨が即再計算。表ラベル/noteに「前提損切り値X円で評価」明記。
- **同日追記3＝シミュ除外チェックの件数バッジ**: ユーザー報告「除外が効いてない」→単体＋実クリック検証で**機能自体は正常**（8件→☑ライン併存で5件・マスター表/通算とも連動）。見えづらさ対策として除外ON時に「N件を除外中（対象M件）」琥珀バッジ／該当0件なら「除外0件＝チェック中のフラグ（〇）が付いた記録がこの母数にありません」を表示（`_poolBeforeEx`/`_exCount`）＝フラグ未設定・母数トグルとの絡みをその場で判別可能に。
- 検証: esprima＋new Function（app-04/05/06）＋実マウント（console 0）＋単体＝合成35記録（基本25＋〇10）でrecoFn=10円/★=+3円/旧基準=+1円（チップ発火）/addAt=3/0円ケースzeroBest=true/wrapper一致＋**平均vsΣ対立ケース（X1=Σ12,200円・平均249円49件 vs X2=Σ7,500円・平均300円25件→★=+2円＝平均選定を実証）**＋前提損切り値（既定15・2円に絞るとX0損切り率51%＝スイープに効く・ステッパー↑で16保存）＋レンダー（詳細表・📐・期間別・ステッパー・除外バッジ）全PASS。sw.js APP_CACHE v107→**v108**。
- ⚠️注意: recoFn構築は日付×窓×32評価で重い＝_elRecoFnCachedの署名キャッシュ前提。もたつき報告があれば_epLegsのWeakMapメモ化が次の手。

### 2026-07-13 記録帳6点改修（①損切りタブ対象記録＋保有ならルール化／②★選定を条件式＋Σ最終損益へ刷新／③OS分布の実現+生同時表示／④決済サマリー行／⑤母数内訳の透明化／⑥シミュ除外チェック）＋横スクロールヒント全廃
- **_HScrollBox（app-06）**: 「👆タップで横スクロール」ピル・タップロック・右端フェードを全廃＝常に素の`overflowX:auto`（ユーザー指示・plainプロップは互換で残すが挙動同一）。先行して単独push（10ac3d7・sw v106）。
- **①損切りタブ（app-06 `_elStopTabSectionV2`）**: サマリー直下に「📋 対象記録（E成立N件）」の`_SNCollapse`＋`_elOsTradeMini`一覧を追加。「保有なら」を新helper **`_elRuleHoldPnl`（app-05・_epHoldLadder直後）**＝「損切りせず、確定値が前の足より悪化（上昇）したらその足の確定値で手仕舞い・悪化しなければ最後まで」の実運用ルールに変更（旧=無条件最終足finalPnl）。適用箇所=上振れ検証の保有なら/サマリーsavedArr/⑤理想損切りの「広げて保有なら」・note文言も更新。
- **②推奨基本α★選定の全面刷新（app-06）**: 旧スコア方式は**`_elBaseAlphaPickScore`**に改名保存（乖離確認用）。新**`_elBaseAlphaPick`**＝母数従来（追加α〇/浮き足〇/RN〇除外）で各α5〜20円を最終損益基準`_elH2EvalByFn`で評価し「到達率≥50%(_EL_BASE_MIN_ERATE)・損切り率(手じまい)≤20%(**_EL_BASE_MAX_STOPRATE**)・E成立≥20件(**_EL_BASE_N_PREF**・無ければ10件=_EL_BASE_MIN_Nに自動緩和)・Σ最終損益>0」の中で**Σ最終損益最大**（同点E成立多→低α・次点=より大きいαで同条件Σ最大）。該当なしは損切り率≤30%(**_EL_BASE_MAX_STOPRATE_2**)へ緩和=status na参考→件数最大参考。返り値shape旧互換＋新fields（decided/takeRate/h2Sum/avgH2/h2sweep/h2Sum2・stopRateは手じまい基準に変更）。**`_elOldPickChip`**新設＝新旧が異なる時だけ「旧基準 X円」グレーチップ（結論バー/KPIカード/α値タブゾーンヘッド＝旧`_elH2AgreeNode`+`_elBaseAlphaH2Pick`バッジを置換・_elBaseAlphaH2Pickは未使用で残置）。表示更新=詳細データ表（E成立/損切り率(最終)列・★条件で淡色判定・結論バーΣ最終損益系）・期間まとめ（カード/表を最終基準列に）・週別/月別トレンド表（利確率/Σ最終損益/E成立列）。フォーム自動入力・EPナビ・シミュrecoOf・日別ページは_elBaseAlphaPick経由で自動的に新基準。
- **③OS値分布の同時表示（app-06）**: `_elOsHistV2`に**rawValsプロップ**追加＝案A重ね棒（濃淡逆・ユーザー指定）：背面=生の最高OS（色棒・全幅）／前面=実現OS（白地+濃枠#9A3412・幅64%）・数字は上=実現(橙太字)/下=生(灰)・共通スケール・×見送り点線は実現バー内・25円〜展開は両系列の和集合・凡例行追加。rawVals無し呼び出し（未達タブ等）は従来単系列。**OS基準トグルは廃止**＝EntryLogViewの`osValMode` state撤去（`_osValFn`=実現固定・互換変数残置）・`_elOsSectionV2`（集計タブ）と`_kpiOs`（銘柄別パネル）のトグルUI撤去＋統計行に「（生X円）」併記＋α目安は生固定のまま「(生基準)」常時表示。帯別成績表・棒クリックの取引一覧は実現基準。
- **④決済サマリー行（app-05）**: **`_elRideSummaryNode(s,alpha,cutLine)`**新設＝`_elDetailFlowStack`最下段（H部の下・点線区切り）に「最高↑18 → 決済↓3（H2）」（損切り記録は「最高↑26 → 損切（H1）」）。最高=EP足〜手仕舞い足の高値max（どこまで逆行したか）・決済=最終損益(_elHold2TotParts.main)と同じ手仕舞い足の確定値（損切り足/期待度○のみ次足へのカスケード・H2まで）。文言はユーザー指定で「保有/手仕舞い」不使用（最高/決済）。全エントリー記録表（取引/銘柄別/記録帳/シミュ展開）に波及。
- **⑤母数内訳の透明化（app-06 `_elBaseAlphaDetailV2`）**: α別総当たり表の上に「母数の内訳: 算入N件 → 追加α〇a・浮き足〇u・RN〇r除外 → 素の記録M件」の1行（36件→有効15件の疑問への回答＝仕様通りだが見えなかった）。銘柄別パネルOS分布の母数注記も修正（「×+未選択＝▲と同じ母数」→「▲はさらに浮き足〇/RN〇を除いたもの」）。
- **⑥シミュ除外チェック（app-06 `_elKabuLadderSimV2`）**: 追加α母数ピルの下に「除外: ☐浮き足加算〇 ☐ライン併存〇 ☐RN〇」トグルボタン（state exFlags・既定オフ=従来値不変・poolをフィルタ・floatModeでは浮き足除外非表示・切替でautoExp/mtExpリセット）。損切り値欄に「※損切りライン＝各取引のEP＋この損切り値（円）」注記追加（⑦の確認結果＝現行解釈はユーザーの具体例と一致・コード変更なし）。
- 検証: new Function構文OK（app-05/06）＋実マウント（クリーンSWで起動・console error 0）＋単体（_elRuleHoldPnl=500 vs finalPnl=900／決済サマリー「最高↑12→決済↑5（H1）」「最高↑22→損切（H1）」／新★=合成25件でΣ最大のα10円・旧スコア=5円・minN20・h2Sum20000／ヒスト二層=白枠凡例+25〜帯・単系列後方互換／損切りタブ対象記録/シミュ除外行/詳細表内訳・Σ列・★・旧チップ表示）。sw.js APP_CACHE v106→**v107**。
- 備考: 検証中の「useState is not defined」クラッシュログはpush済みコードでも再現するプレビュータブ固有のアーティファクト（新規タブでは非再現・実機影響なし）と切り分け済み。

### 2026-07-10b ⚡EPナビ 追加α値欄に▲▼ステッパー（早見カード＋計算フォーム・ユーザー要望）
- app-04.js: 早見カード`_EpnAddSection`＝▲▼クリックで即`_epnPut`保存（onValue・○×やチップと同じ即時反映＝onBlur待ちだと押しただけでは保存されないため）。長押しリピート対策で表示値(addStr)/確定値(e.add)は`_addStrRef`/`_addCurRef`参照・blur前の入力途中値からもステップ・0〜50クランプ。計算フォーム`_EpnCalcForm`の追加α欄＝水準線/基本αと同形の`_stepBtn`（空欄は推奨追加α起点＝基本αのautoPick起点と同流儀・0〜50）。RN・浮き値は対象外（ユーザー指定＝追加αのみ）。
- sw.js: APP_CACHE v95→v96。

### 2026-07-10 ⚡EPナビ 計算フォームの③〜⑤をデフォルト畳みに（案A・ユーザー選択）
- app-04.js: `_EpnCalcForm`に「▷ 詳細を入力（③〜⑤）」トグル（detOpen state・保存対象外）＝③底抜け/③起点/④その他/⑤ライン併存を琥珀枠に格納。畳み中は選択値を要約表示（`_detSummary`）・✎編集読込（loadForEdit）は詳細値があれば自動展開・リセット/保存で畳みに戻る。推奨基本αの段階フォールバック（詳細別→シグナル別→銘柄全体）により最短入力（②シグナル＋水準線）で保存可。
- sw.js: APP_CACHE v94→v95。

### 2026-07-08j ニュース記事画像の鍵マーク（★保護）が横長画像で消える不具合を修正（ユーザー報告）
- 症状: ニュースタブの「主要ニュース」記事画像で、一部の画像だけ鍵マーク（★保護トグル）が出ない。
- 原因: ニュースタブ(app-03 NewsTab)/ニュース履歴(app-07 NewsHistoryView)の記事カードは `overflow:hidden` + `width:cardW`(220)。`ImgGrid`(app-02)は画像を `height:IMG_H`(180)固定・`width:auto` で表示するため、**横長画像は幅がカード幅を超え**、画像右上に `-7` ではみ出し配置した鍵/削除ボタンがカードの角丸クリップで切れて見えなくなっていた（縦長画像は幅が収まるので鍵が見える＝「ものによっては出ない」の正体）。実機で再現確認（横長500×360→表示249×180・鍵ボタンがカード右端+10px＝clipped）。
- 修正: `ImgGrid` に `boxed` prop を追加。boxed時は画像を `maxWidth:100%`/`height:auto`/`maxHeight:IMG_H` でコンテナ幅に収め（横長も全体表示・アスペクト維持）、鍵/削除ボタンを画像の内側(`top:2 right:2`)に配置。`NewsTab`(app-03:4539)と `NewsHistoryView`(app-07:1907)で `boxed:true`。**他のImgGrid使用箇所（メモ添付 app-02:2140 等）は既定(boxed無し=従来の-7はみ出し)で見た目・挙動とも不変**。本文リッチテキスト内画像は元々自動削除対象外のため対象外。
- sw.js: APP_CACHE v79→v80。
- **追補(v81)**: v80で boxed時にボタン(★/削除)を画像右上内側(top:2 right:2)へ置いたら、記事カードの移動(⇄)/削除(×)ボタン(右上 top:4)と密集して**被った**(ユーザー報告)。boxed時のボタンコンテナを**画像右下(bottom:4 right:4)**へ移動（`top:boxed?"auto":-7` / `bottom:boxed?4:"auto"` / `right:boxed?4:-7`）＝記事カード上部ボタンと分離。非boxed(メモ等)は従来の-7はみ出しで不変。実機で★がヘッダーと非重複・非クリップ(画像218×158に収まる)を確認。sw v80→v81。
- **追補(v82)**: ×ボタンが2つ（画像個別削除×＋記事削除×）で紛らわしいとのユーザー指摘→boxed時はImgGridの画像個別削除×を非表示(`(!boxed && onRemove)`)＝鍵★だけに。画像1枚削除は記事ごと削除(delNews)で代替。非boxed(メモ添付等)は従来の鍵＋×で不変。実機でboxed=ボタン1個(鍵のみ)/非boxed=2個(鍵+×)を確認。sw v81→v82。

### 2026-07-08i 推奨の件数フロア _EL_BASE_MIN_N を 3→10 に引き上げ（ユーザー要望・全推奨一律）
- **app-06.js**: `_EL_BASE_MIN_N` 3→10。推奨基本α（件数フロア=`max(10, maxScN×_EL_BASE_MIN_FRAC(0.5))`）・推奨追加α（各加算 `scN≥10`）・推奨損切り（各損切り値 `n≥10`）の絶対下限＝EPナビ/記録フォーム/記録帳分析すべてに共通で効く（使用箇所: 1405 floorN / 1443 追加α black / 1500 損切り cand / 1575・1623・1696 minN表示 / 5266 lowN判定）。10件未満は status="ok" にならず（仮）/参考表示。件数窓（直近50→100→全期間 _EL_PERIOD_COUNTS[1],[2]）は不変。
- 影響: 母数の少ない推奨追加α（追加α〇の記録だけが母数）は「データ無し」表示が増える（ユーザー了承＝AskUserQuestionで「全部一律10件」を選択）。推奨ロジック・保存データ・α計算は無改修。
- sw.js: APP_CACHE v78→v79。

### 2026-07-08h「RNまたぎ加算」を第5のα要素として新設（記録フォーム＋EPナビ計算フォーム＋早見カード・ユーザー要望）
- RN＝ラウンドナンバー（キリ番株価）をまたぐ動きへの加算。〇×（既定×）→〇で加算値（既定5・手動可・▲▼ステッパー/長押し連続）を**そのまま**合計α/実効αに加算（浮き足の÷2のような計算なし）。全シグナルで表示。基本α＋浮き足加算＋追加α＋ライン併存に続く第5要素。alphaVal/effA畳み込みで下流（採用α・EP・分析・シミュ）は無改修で反映。
- **app-05（EntryRecordForm）**: 追加α（根拠含む）行の下・合計α値バーの上に「RNまたぎ加算」欄（青系#EFF6FF）。state `fRnUsed`（既定 initSig.rnUsed===true?"○":"×"）/`fRnVal`（initSig.rnVal）。`_fRnAdd`を`_fAlpha=_fBaseA+_fUkiAdd+_fAddA+_fRnAdd`に合流。saveに`rnUsed`/`rnVal`。合計α値バー内訳に「＋RN」（_fRnAdd>0時）。**欄のすぐ横に「暫定EP」＝水準線値+合計α（既存の予定EP欄と同値・RN含む）を小さく表示＝EPを見てRN要否を判断（ユーザー要望）**。〇でfRnVal空なら5補完。範囲0〜50。
- **app-04（_EpnCalcForm）**: 浮き足加算の下・予定EPの直前に「RNまたぎ加算」欄（_lrow・_oxBtns・〇でnRnVal空なら5補完）。state `nRnUsed`/`nRnVal`。`rnAddV`を`effA=baseV+ukiAddV+addV+rnAddV`に合流。_resetForm/doSave(`rn:rnAddV`,`rnUsed`)/loadForEdit(rnUsed明示 or rn>0で推定)。実効α表示に「＋RN」。
- **app-04（早見カード）**: 新コンポーネント`_EpnRnSection`（追加αの_EpnAddSectionを簡略化＝根拠なし・値はローカルstate/onBlur確定・青系）を追加αインラインの下に配置。`onSetRnUsed`（〇＝rn5または既存値でEP再計算/×＝rn0）/`onSetRn`。カードのalphaSum/bk内訳に`e.rn`（＋RN）。**EP計算箇所を全て`+rn`化＝_epnRecalcBase・onSetAdd・onSetAddUsedにも`rn=Number(e.rn)||0`を合算（rnを保持しつつ他要素編集してもEPが崩れない）**。
- **migrateData**: 変更なし。既存signal/itemにrn無し→`Number(e.rn)||0=0`で加算0＝既存alphaVal/EP不変（新規保存分のみrnが入る）。
- sw.js: APP_CACHE v77→v78。
- 配置はユーザー選択（EPナビは浮き足の下＝加算系の最後／早見カードはインライン編集あり／記録フォームは追加αの下＋暫定EP併記）。

### 2026-07-08g「ライン併存ルール」を独自の〇×欄として新設＋既存記録を移行（ユーザー要望）
- 旧来は「併存ライン/ライン併存」チップ（シグナル詳細等）で暗黙に基本α=1にしていたが、チップ表記「ライン併存」とコード定数「併存ライン」が食い違い実質未機能だった。今回それを明示欄化し正す。
- **app-05（EntryRecordForm）**: ③その他（「特徴」の語を削除）の下に「④ ライン併存ルール」を〇×（既定×）で新設。state `fLineCoexist`（既定 initSig.lineCoexist===true）＋effectで〇→setFBaseAlpha("1")/×→base=="1"なら""へ戻し_baTouchedRef=false（推奨復帰）。旧`_KYOZON`/`_isKyozon`チップ検知は撤去。saveに`lineCoexist:fLineCoexist`。`_EL_SIG_SECS`のfラベルを「③ その他特徴」→「③ その他」（記録フォーム/app-06分析の見出し共通）。
- **app-04（_EpnCalcForm）**: ④その他（「特徴」削除・複数可）の下に「⑤ ライン併存ルール」〇×（常時表示・_oxBtns）。state `nLineCoexist`＋同effect（〇→setNBase("1")）。_resetForm/loadForEdit(e.lineCoexist===true)/doSave(lineCoexist:nLineCoexist)。
- **app-04（早見カード）**: インライン編集に⑤ライン併存ルール〇×を追加（起点/特徴の下・追加αの上）。`onSetLineCoexist(st,e,used)`=_epnPut(_epnRecalcBase({...e,lineCoexist:!!used}))。`_epnRecalcBase`は「_K='併存ライン'をtag/b/k/f検知→base=1」を「item.lineCoexist===true→base=1」に変更。**回帰注意=else分岐がitem.fを_fで参照するため関数先頭の`var _f=Array.isArray(item.f)?item.f:[]`は必須（一度消して"_f is not defined"回帰＝起点/特徴のカード編集も壊す→実マウントで発見・修正）**。
- **app-01（migrateData `_migLineCoexist`）**: 既存の詳細(sigDetail b/k/f・新obj/旧string/旧array)・EPナビitem(b/k/f)・候補マスター(custom.sigDetails/sigDetails2)から「ライン併存」「併存ライン」を除去し signal/item.lineCoexist=true を付与。**実トレード履歴のbaseAlphaVal/alphaVal/epは書き換えない（過去損益の整合保持・ユーザー了承）**。条件ベース冪等・in-place mutate・try/catch。
- sw.js: APP_CACHE v76→v77。
- 検証: V8構文OK＋実マウント（EpNaviPanel/EntryRecordFormをReactDOM.render）＝移行7ケース(新obj/旧string/旧array/単一b/EPナビitem/候補/履歴base保持/冪等)・記録④〇でbase1×で復帰・編集読込往復・計算⑤〇でbase1/EP4001×で復帰・カード⑤〇でbase1/EP3867/src・起点変更(else分岐)回帰無し・「その他特徴」消滅・console警告0。多エージェント敵対レビュー実施。**preview検証の教訓=SWがstale配信＝SW unregister後もその読込は旧SW制御下＝確実には`fetch(...,{cache:"reload"})`＋`(0,eval)(src)`で変更ファイルのグローバルを更新してからマウント。**
- ユーザー選択（show_widgetモック＋AskUserQuestion4問）: 両表記移行／③から外し候補も削除／1自動入力手修正可／早見カードにも追加。

### 2026-07-08f EPナビ早見カードの追加αインライン編集を計算欄と同じ〇×→根拠に統一（ユーザー要望・案A完全一致）
- **app-04.js**: 早見カードのインライン追加α編集を旧・±ステッパーのみ→計算フォーム(_EpnCalcForm)と同一の「〇×→(〇のとき)数値入力＋根拠チップ(_EpnChipMgr・追加/改名/削除/並替)＋推奨追加α表示」に。新コンポーネント`_EpnAddSection`（値入力はローカルstate＝onBlur確定で1文字ごとの全体保存を回避・casc/recoはuseMemo）。
- epNavi itemに`addUsed`(boolean)を追加＝計算フォームdoSave(`addUsed:nAddUsed==="○"`)・loadForEdit(`e.addUsed`優先、旧itemはadd>0で推定)。カードの〇×はaddUsedから導出。
- 〇初期値＝推奨追加α（あればその値）／無ければ5円・あとで手動変更可（ユーザー確定）。onSetAddUsed(true)=_epnAddReco→無ければ5、(false)=add:0/reasons:[]/ep再計算。
- 二重実装ドリフト回避で共有ヘルパーへ抽出＝推奨追加α`_epnAddRecoFrom(casc,baseV,reasons)`/`_epnAddReco(...)`（計算欄の旧inline addRecoを置換・autoPick.key=_epnBaseLevelKeyで同一導出）、根拠マスター操作`_rsnAddData/_rsnDeleteData/_rsnReorderData/_rsnRenameData`（計算欄_rsn*とパネル_rsn*Gが共用）。**_rsnRenameDataは改名を過去記録(charts.signals)＋保存EP(charts.epNavi.reasons)まで一括追従＝旧計算欄改名のepNavi取りこぼしも解消**。
- sw.js: APP_CACHE v75→v76。
- **性能（多エージェント敵対レビューのCONFIRMED 1件で対応）**: `_epnCascade`が`_elCollectAllSignals(data)`をカード/列ごとにキャッシュ無し走査＝同一data参照の重複フル走査。単一スロット同一性キャッシュ`_epnCollectSignals(data)`（dataはsaveで必ず新オブジェクト＝参照変化で自動失効・純粋関数で結果不変）に集約＝計算フォーム/カード/`_epnRecalcBase`の全走査を共有。挙動非破壊。
- 検証: V8構文OK＋実マウント（EpNaviPanelを合成データ12記録＋epNavi1件でReactDOM.render）＝カード〇で推奨値8自動入力・EP再計算(3879)・根拠トグル/推奨表示/値入力blur確定(add12→EP3883)/×でadd0・reasons空・EP3871/根拠追加改名削除並替・_rsn*Data純データ変換7項目(改名がmaster/signals配列/signals文字列/epNavi.reasons追従・既存名中断・prev不変)・計算フォームも〇で推奨表示＋保存でaddUsed永続化・キャッシュ同一性/失効/内容一致・修正後も〇→推奨8で不変・console警告0。多エージェント敵対レビュー(候補19→確証1[上記性能のみ]・不確実0)。
- ユーザー選択（show_widgetモック3案＋AskUserQuestion2問）: 案A計算欄と完全一致／〇初期値=推奨値そのまま・無しは5円。

### 2026-07-08e 詳細の一括バーを撤去→各分析セクション見出しの小型プルダウンへ（まとめて/各詳細/詳細ごと比較・セクション独立・ユーザー選択＝モック案C）
- **app-06.js**: EntryLogView。detSub→detScopes（secKey→選択のマップ）・_detCtl/_detCtlRow/_detBody新設・_secHに第3引数right。_groupPanelに第4引数useDet（シグナル別集計のみtrue・詳細タグ別モードは非表示）＋KPI+OS値分布を_kpiOs(rs,baRs)へ関数化＝詳細選択時は推奨基本α/追加αカード・OS分布▲/損切りマークも詳細母数に追従。設置＝集計(KPI+OS値分布/基本α詳細/追加α詳細/詳細分析7セクション/記録一覧)・α値(OS→α分位/基本α詳細/追加α期間別/追加α分析/浮き足/意思決定表/感応度カーブ)・損切り・未達・深掘り7セクション・シミュ。「📊詳細ごと比較」＝全詳細（1件以上）を琥珀枠カードで縦積み・0件は非表示。旧バー右の「この詳細の推奨基本α」参考ピルは廃止（KPIカードが詳細母数に追従して代替）。
- sw.js: APP_CACHE v74→v75。
- 検証: V8構文OK＋実マウント（合成データ6記録・旧バー撤去・集計3＋詳細分析展開7=10プルダウン・KPI件数まとめて5→9EMA3・比較5バケット・セクション独立（1つ変えても他は不変）・α値2/追加α2/ツール2・損切り1・未達1・深掘り7・シミュ1・詳細タグ別モードでは非表示・console警告0）。
- ユーザー選択（show_widgetモック3案＋AskUserQuestion3問）: UI=案C（プルダウン＋詳細ごと比較）／セクション独立／全タブの全セクションごと。

### 2026-07-07h ⚡EPナビ 表示銘柄をドラッグで並び替え（ユーザー要望）
- **app-04.js**: 「⚙表示銘柄」ピッカーを2段化＝上段「選択中（ドラッグで並び替え・タップで外す）」＝`epnStocks`のチップを`data-epnstk`＋pointer drag（フォームのsigDetailチップと同方式・setPointerCapture遅延でタップ選択と両立）で並べ替え→`_epnStockReorder`が`custom.epnStocks`へ保存＝早見カラム順に即反映。下段「追加」＝未選択銘柄（最大3で満杯時はdisabled）。新規state`_stkOrd`（ドラッグ中プレビュー）＋refs`_stkDragRef`/`_stkMovedRef`。列順は`custom.epnStocks`の並び（既定はallStocks先頭3から古河電工除外）。
- sw.js: APP_CACHE v53→v54。
- 検証: V8構文OK＋実マウント（合成PointerEventでSBGを末尾へドラッグ→epnStocks=["7203","6501","SBG"]・カラム順も一致・選択中/追加の2段表示）pass・console0。

### 2026-07-07g ⚡EPナビ 早見の操作性改善＝カードタップ編集・銘柄ごと＋計算・表示銘柄ピッカー(最大3)・計算画面は常時表示（ユーザー要望）
- **app-04.js**: (1)**早見カードはカード全体タップで編集**（`_renderCard`にonClick=loadForEdit＋cursor:pointer・旧✎ボタン削除・×は`ev.stopPropagation()`で伝播止め）。(2)**「＋計算」を各銘柄カラムの下に**（`marginTop:auto`で下端揃え・onClick=setNStock(st)+_resetForm・ヘッダーの旧グローバル＋計算トグルは撤去）。(3)**計算画面は常時表示**＝epnOpen既定true・閉じるボタン撤去（開閉トグル廃止）。(4)**保存でリセット**は既存doSave→_resetForm（維持）＋**①銘柄の右に「↺リセット」ボタン**（_resetForm＝銘柄以外を初期化）。(5)**早見は横並び最大3銘柄＝`custom.epnStocks`**（EPナビ右上「⚙表示銘柄」ピッカーで選択・最大3で4つ目はdisabled・空/未設定は既定＝全銘柄先頭3から**「古河電工」を除外**・ピッカーで戻せる）。`_EPN_MAX_STOCKS=3`/`_epnStockToggle`（save→custom.epnStocks）/`showStockPicker`。savedViewは`epnStocks`（≤3）を列挙・EP無し銘柄も列は出す（「EPなし」＋＋計算）。
- ユーザー要望（会話・逐次）: 早見はEP欄タップで編集／＋計算は各銘柄の下／全銘柄→最大3＋EPナビ内で設定／現状は古河電工を既定除外／計算画面は常時表示／保存でリセット＋①銘柄右にリセットボタン。
- sw.js: APP_CACHE v52→v53。
- 検証: V8構文OK（全app＋sw）＋実マウント（計算常時表示/閉じるボタン無し・列は最大3で古河電工除外・＋計算×3・⚙ピッカー開閉/最大3cap/deselectで補充/古河電工復帰/列即更新・カードタップで編集読込・↺リセットでバナー消去+水準線クリア・2タップ削除）すべてpass・console0。

### 2026-07-07f ⚡EPナビ大改修＝独立配置＋選択肢管理＋基準分足＋追加α根拠＋早見の銘柄横並び/色分け/編集（ユーザー要望・モック案C継続）
- **app-04.js**: EpNaviPanelを全面刷新。**配置**＝エントリー記録テーブルの右サイドバー→**エントリー記録カードの「上」に独立表示**（full幅・DayView取引タブ先頭）。**番号**＝①銘柄／**②基準分足（新設・"1"/"5"トグル・両方可・フォームfMinBarsと同形）**／③シグナル／④底抜け／④起点／⑤その他特徴（それに伴い番号ずらし）。**水準線**＝旧「起点（水準線）価格」を「水準線」に改名し基本α欄の【上】へ移動。
- **選択肢管理（新設ヘルパー`_EpnChipMgr`）**: シグナル（custom.signalTags）・底抜け/起点/その他特徴（custom.sigDetails2[タグ].{b,k,f}）・追加α根拠（custom.addAlphaReasons）の各チップに「＋追加・✎改名・×削除・ドラッグ並び替え」。改名は過去記録も追従＝`_sigRename`（signals[].tags/tag＋sigDetails2/sigDetailsキー＋各signal.sigDetailキーをrekey）／`_detRename`（`_elSigDetailRenameSig`で全charts走査・app-05）／`_rsnRename`（signals[].addAlphaReasons/addAlphaReason）。ドラッグはフォームのsigDetailチップと同方式（setPointerCapture遅延でタップ選択と両立）。マスター外の記録由来タグはorphansで選択可・編集不可。
- **追加α根拠**: 追加α〇で根拠チップ（custom.addAlphaReasons・既定["指標線支え"]）を表示。**推奨追加αは選択根拠で絞った母数から算出**＝採用基本α段の記録を`_elAddAlphaYes && !_elUkiYes && 選択根拠を含む`で絞り`_elAddAlphaReco(pool,aiOf,baseV)`（app-06）。根拠未選択なら従来どおり全追加α〇。空欄なら推奨値を自動採用。
- **早見刷新**: 保存を`_epnSave`→**`_epnPut`（同一id除去→追加＝新規/更新/銘柄変更を一本化）**。表示＝**銘柄を横並びカラム（各flex 160px・横スクロール）／各銘柄内はEP高い順（ep降順）／銘柄の並びは最高EP降順**。**色分け＝追加αあり(add>0)はEP価格を赤字＋赤枠、5分足(minBars⊇"5")は緑「N分」バッジ＋緑左3pxライン＝別チャンネルで両立（ユーザー選択）**。各カードに**✎編集**＝`loadForEdit`で全項目を計算パネルへ読込み更新モード（編集中バナー＋「更新保存」＋「編集をやめる」・あとから追加α後付け）／×は2タップ削除。保存itemに`minBars`/`reasons`追加。
- **placement**: DayView取引タブ＝`tab==="trades" && div(null, EpNaviPanel, Card(エントリー記録...))`。前版のテーブル右flex配置は撤去。
- ユーザー選択（AskUserQuestion2回＋show_widgetモック）: 色競合＝別チャンネルで両立／早見編集＝カード→計算パネル読込／その他特徴は④→⑤。
- sw.js: APP_CACHE v51→v52。
- 検証: V8構文OK＋実マウント（配置=EPナビがエントリー記録の上・番号①〜⑤・水準線が基本αの上・シグナル/底抜け/起点/特徴/根拠のチップ管理・追加α根拠別推奨（byReason）・保存/更新（id重複なし）/編集読込・早見の銘柄横並び同一行/EP降順・追加α赤字/5分緑左枠・詳細改名37件追従・シグナル改名マスター追従・2タップ削除）すべてpass・console警告0。

### 2026-07-07e ⚡EPナビ新設＝場中のEP計算・保存早見サイドバー（日別ページ取引タブ・エントリー記録テーブルの右・ユーザー要望＝モック案C採用）
- **app-04.js**: 新設トップレベル5定義（StockQuickRefTableWithChartとDayViewの間）＝`_epnTagsOf(s)`／`_epnPickWin(rs,aiOf)`（直近50→100→全期間の件数窓で最初のstatus okの推奨基本α＋同窓の推奨追加α。okが無ければ全期間の仮値ok:false）／`_epnCascade(data,stock,tag,sel,beforeDate)`（銘柄のv2算入記録をbeforeDateより前に絞り{all,stk:銘柄全体,sig:シグナル別,det:詳細組合せ}を返す。**ロジックはEntryRecordFormの_refSigAlpha/_pickWin（app-05）と同一＝変更時は両方直す**）／`_epnSave`/`_epnDelete`（charts[銘柄_日付].epNavi配列への追加/除去＝_apSave/_apDeleteと同型・Firebase同期に乗る）／`EpNaviPanel({data,save,date,stocks,activeStock})`。
- EpNaviPanel: 右サイドバー（flex:1 1 200px・maxWidth320・青系#BFDBFE）。上=「⚡EPナビ」＋「＋計算」開閉ボタン。中=保存済みEP早見カード（銘柄・EP◯円を大きく・起点＋α内訳（基/浮/追）・シグナル/詳細/採用段・×は2タップ確認「削除?」3秒＝window.confirm不使用）＝その日の全銘柄のcharts[*_日付].epNaviを保存時刻順に常時表示。下=計算パネル（開いた時のみ・casc計算も開いている間のみ）：①銘柄セレクト②シグナルセレクト（その銘柄の過去記録タグ頻度順・「—（銘柄全体で計算）」可）③詳細＝底抜け/起点セレクト＋その他特徴チップ複数（候補=custom.sigDetails2[タグ]→旧sigDetailsフォールバック・読み取りのみ）→推奨基本α3段表示（詳細別/シグナル別/銘柄全体・ok=赤太字・不足=「データ不足（仮Y円）(n=X)」・★採用）→基本α入力（空欄=推奨自動採用・全段不足時は仮値を（仮）表示で採用＝フォームと異なり値を出す・±ステッパ0〜50）→追加α〇×（〇=推奨追加α表示＋空欄=自動採用の上書き入力）→浮き足加算（シグナル=custom.ukiSignalName「底抜け水準線OS」の時のみ・〇＋浮き値→floor(v/2)）→起点（水準線）価格（_toHankakuDecimal・±1）→予定EP＝起点＋実効α（Math.round(*100)/100・内訳表示）→「💾保存（早見に追加）」（EP算出時のみ活性）。
- DayView取引タブ: エントリー記録カード内の（記録なし/テーブル/合計行）をflexコンテナ｛flexWrap:wrap・左flex:1 1 460px・minWidth:0｝で包み右にEpNaviPanel＝狭い画面では下に回り込み。保存item={id:"epn_..",tag,b,k,f[],base,add,uki,ukiVal,level,ep,src,at,minBars[],reasons[],done,addUsed(2026-07-08f)}。
- ユーザー選択（AskUserQuestion2回＋show_widgetモック3案）: 出力=EP価格＋浮き足加算まで／詳細=3セクション対応／フォールバック=フォームと同じ3段／名称=⚡EPナビ／レイアウト=案C早見サイドバー型／保存=日付紐づけ・Firebase同期（過去日も当時の保存が残る）・×でいつでも削除。
- sw.js: APP_CACHE v50→v51。

### 2026-07-07d 追加α根拠に「詳細」セクションを新設（根拠ごと・複数可＝例: 指標線支え→50EMA・ユーザー要望）
- **app-05.js**: ~~追加α根拠の詳細（_elAddReasonDetailList/signal.addReasonDetail/custom.addReasonDetails）~~ **2026-07-09 完全廃止（ユーザー要望・記録のみで分析未組み込みだったため）**＝EntryRecordFormのフォームUI（詳細チップボックス・state fRsnDetail/fRsnDetOrder/fRsnDetAdd/fRsnDetEdit）・ヘルパー_elAddReasonDetailList・保存・根拠改名時の詳細キー追従を全削除。既存データ（signal.addReasonDetail・custom.addReasonDetails）はmigrateData._migDropAddReasonDetail（app-01・フラグ一回性・冪等）で全記録から一掃＝addAlphaReasons本体（根拠名）は不変。sw v84。
- ユーザー選択（AskUserQuestion）: 根拠ごとの詳細は複数可。
- 検証: V8構文OK＋実マウント11/11 pass（ボックス表示/未選択根拠は非表示/保存値の選択復元/複数選択/再タップ解除/＋追加のマスター書込と自動選択）・console警告0。

### 2026-07-07c シグナル詳細を①底抜け/②起点/③その他特徴の3セクション化＋推奨基本αの自動入力を詳細別→シグナル別→銘柄全体の段階フォールバックに（ユーザー設計決定・レイアウトはモック案A）
- **app-05.js**: `signal.sigDetail[タグ]`を{b:底抜け(単一),k:起点(単一),f:[その他特徴...](複数)}へ変更。新設`_EL_SIG_SECS`（セクション定義）/`_elSigDetailSec(s,t)`（セクション読み取りの正本・旧文字列/フラット配列は③(f)として読む＝ユーザー決定・保存で新形式に置き換わる）/`_elSigDetailRenameSig(s,t,secKey,old,new)`（セクション内改名の記録追従・③は旧形式も対象）。`_elSigDetailList`は全セクションのフラット化＝従来読者（詳細サブタブ等）は無改修で継続・`_elTagDisp`は「タグ（底:X・起:Y・特徴...）」表記。EntryRecordFormの詳細欄は案A縦積み3段（各セクションに専用チップ行/＋追加/編集/ドラッグ＝fDetOrder・fDetAddにsec追加）・候補はcustom.sigDetails2[タグ]={b,k,f}（_writeSecで書き込み・旧custom.sigDetailsは旧端末互換のため凍結＝シード/フォールバック読みのみ）。
- 推奨α: `_refSigAlpha`を段階フォールバック化＝_pickWin（直近50→100→全期間の件数窓でstatus okの推奨基本α＋同窓の推奨追加α）を「①②③選択の組み合わせ」→「シグナル」に適用し、`_autoBase`＝詳細別(ok)→シグナル別(ok)→銘柄全体(_defBaseA)の順で基本αの自動入力に使用（シグナル別参考行に★自動入力バッジ・_fBaseA/ステッパ/入力欄フォールバックも_autoBaseAへ・自動入力useEffectは_refSigAlpha後へ移動）。データ不足の段は「データ不足（仮Y円）(n=X)」表示で下の段へ。選択値ごと（底＝/起＝/特＝）の参考pickは全期間・表示のみ。推奨追加αも同カスケード（_refAddAlphaSrc・採用段ラベル付き・表示のみ）。日別ページ/シミュのrecoOfは銘柄全体のまま＝値がズレうる点はユーザー了承済み。
- **app-01.js**: migrateDataに`custom.sigDetails2`シード＝旧custom.sigDetailsを{b,k,f}へ複製（タグ単位で冪等・旧キーは温存）。
- **app-06.js**: 詳細サブタブの候補`_detNames`にsigDetails2（b/k/f全セクション）を合流（サブタブは従来どおり名前ベース＝どのセクションでも同名は同バケット）。
- ユーザー選択（AskUserQuestion4問＋show_widgetモック3案）: レイアウト=案A縦積み3段／旧データ=③として読む／候補プール=セクション別×初期複製／推奨α=自動入力もシグナル/詳細別に。
- 検証: V8構文OK（app-01/05/06/sw）＋ヘルパーunit 20/20 pass（レガシー正規化/フラット化dedup/セクション別改名/表示/migrateシードと冪等）＋実マウント15/15 pass（①②③表示・単一置き換え/再タップ解除/セクション独立/複数選択・シグナル別参考のデータ不足表示）・console警告0。sw.js v49→v50。

### 2026-07-07b 推奨基本α/追加α詳細表に「頻度（何営業日に1回エントリーできたか）」列を追加（ユーザー要望・全営業日ベース）
- **app-06.js**: 新設ヘルパー3つ＝`_elBizSpanDays(recs,holiSet,validOf)`（母数の活動期間[初回〜直近の記録日]の営業日数・`_fmIsBizDay`で平日かつ非祝日・holiSet省略時は土日のみ・validOfで推奨α算出不能記録を除外）／`_elEnteredDays(recs,alphaOf)`（各記録にαを当てEP到達したdistinct日付数＝分子）／`_elFreqCell(span,enteredDays)`（span÷enteredDays＝X→「約X営業日に1回」・1.3未満はほぼ毎営業日・10以上は整数）。
- `_elBaseAlphaDetailV2`と`_elAddAlphaDetailV2`に第3引数`holiSet`（任意）を追加し、両表（基本α総当たり・追加α日付別スイープ）の「到達率」列の右に「頻度」列を挿入。分母（span）は表ごとに1回・分子（到達実日数）は行のαごと＝高αほど到達日数減→頻度の間隔が広がる。母数＝各表と同じ（基本α＝×+未選択／追加α日付別＝addPool）。
- 呼び出し: `_groupPanel`で`_holiSet=_buildHolidayDateSet(data.trades,custom.eventCategories)`を1回作り、`_elBaseAlphaDetailV2(_baRecs,_ai,_holiSet)`（2か所）と`_elAddAlphaDetailV2(_baRecs,_ai,_holiSet)`へ渡す。~~α値タブ①の`_elBaseAlphaDetailV2(_alReasonRecsFull,_ai)`はholiSet無し＝土日のみ除外にフォールバック（許容）。~~ **←2026-07-16eに失効・修正済み**: これが「集計タブとα値タブで頻度列/淡色が食い違う」の原因だった（★は`_elHoliCur`で祝日除外済み＝表とも不整合）。`_alHoliSet`をゾーン分岐の外で宣言し全呼出でholiSetを渡すよう修正（実測: 営業日22→18・頻度1.2→1.0で全行ズレていた）。**「許容」ではなくバグだったので、同種の“片方だけ引数を省略”は今後許容しないこと。**
- ユーザー選択（AskUserQuestion）: 分母＝全営業日ベース（活動期間の営業日で割る／シグナルが出なかった日も分母に含む）。列見出しは「頻度」・セルは「X営業日に1回」・各表のラベルに算出式注記。
- 検証: V8構文OK＋ヘルパーunit（span=平日inclusive5/holiday除外4/single1・freq「2営業日に1回」「ほぼ毎営業日」「15」「1.8」「—」）＋実マウントで両表の頻度列が到達率低下に連動して増加（例: 到達100%→1.4営業日に1回・58%→2.4営業日に1回）・警告0。sw.js v48→v49。

### 2026-07-07 追加α分析: 「各記録日の推奨基本α＋追加α（0〜10円）」別のスコア表を新設（基本α詳細表を模倣・ユーザー要望）
- **app-06.js**: 新設`_elAlphaEvalByFn(recs, aiOf, alphaOf)`＝`_elBaseAlphaEval`の一般化（記録ごとにαを変えられる版・`alphaOf(r)`→そのレコードのα・null=母数外でnに数えない・返り値shape同一でa=null）。固定α版と挙動一致（`_elBaseAlphaEval`は無改修＝コア不変）。
- `_elAddAlphaDetailV2`に日付別スイープ表を追加（既存の単一基本α＋加算表の下）: 実効α＝`_elKabuRecoBaseFn(recs,aiOf)`の`recoOf(記録日)`＋追加α（0〜10円・記録ごとに推奨基本αが変わる反実仮想）。列は画像の基本α表と同じ＝追加α/到達率/有効件数/損切り率/H1勝率/平均H1損益/スコア内訳(0.7×(1−損切り率)+0.3×H1勝率)/スコア。★＝件数フロア(minN)を満たす中でスコア最大。母数＝addPool（追加α〇・数値根拠/浮き足除外）＝既存の追加α分析と一致。推奨基本α算出不能な記録（履歴不足）は母数外。
- ユーザー選択（AskUserQuestion2問）: 1つの表（行＝追加α0〜10）／母数＝追加α〇の記録のみ。セクション見出し副題も追記。
- 検証: V8構文OK＋実マウント（scheme:3実データで自動描画→表・★・スコア表示・エラー0）＋`_elAlphaEvalByFn`が固定α版と一致するunit。sw.js v47→v48。

### 2026-07-07 株数シミュ: 自動配分のα候補を3種に限定＝両取引とも候補セットから選ぶ配分に再設計（ユーザー要望）
- **app-06.js**: `_elKabuLadderSimV2`の自動配分を、第1取引0〜推奨基本α未満の全整数×第2取引固定＝推奨α、から変更。両取引とも候補α方式14種（絶対値0/3/5・各日の推奨基本α＝推奨α±0・推奨α±1〜±5）から選び、総株数を100株刻みで2取引に配分して総当たり。正準形（`_seen`シグネチャ=同方式は株数合算・第1/第2入替を集約）で重複排除（500株で378通り）。`_AUTO_CANDS`/`_candIdx`/`_candBaseFromReco`/`_candLabel`/`_autoTiersOfFor`（tranches化）は全て`_elKabuLadderSimV2`内（トップレベル定義なし）。ランキング表を「配分（α方式×株数）」1列に再構成・combo key=`cb.sig`。`_s1AlphaMax`削除。
- 検証: V8＋実マウント（自動モード切替→ランキング/★最適/tranche表示）＋アルゴunit。sw.js v46→v47。commit c621234。

### 2026-07-06g OS連鎖セル: OS3まで期待度を必ず表示＝未記録スロットの自動×をグレーで明示（`_epOsChainCell`・ユーザー要望）
- 旧記録は「保有域外（H2手仕舞い後）」「×連鎖後」の足に判断が記録されておらず、06fの案A表示でOS3の下が空になっていた→**計算上×扱いのものは「次×」をグレー（#9CA3AF）で表示**。事前パス`_dispEx/_autoEx`＝①記録時EPの保有域外(x>recEp+1) ②前の足（表示値）が×/損切り済（×連鎖・グレー×からも連鎖）→自動×。どちらでもない真の未記録（例: 未達記録で待ち足○○の後続）は従来どおり空＝×と断定しない。`_expSym(sym, muted)`にmuted引数追加。
- 検証: 実マウント4ケース＝未達×連鎖（次×/次×/次×グレー）・EP=OS1&hold2Exp×・EP=OS1全○（OS3=保有域外の自動×）・未達○○（OS3空のまま）・React警告0。sw.js v38→v39。

### 2026-07-06f 表のOS連鎖セルも次足期待度表記に統一（案A=位置も統一・`_epOsChainCell` app-05・ユーザー選択）
- **表記**: 接頭辞「α○/H○」→「次○」に統一・配色はフォーム_expBと同一の1本（○赤/△琥珀/×緑）＝旧「H期待は○緑/×赤」の文脈反転を廃止。
- **位置（案A）**: 各足の下に「その足の引けの次足期待度」（_epNextExpAt(s,i)＝entered-at・フォームのスロット配置と1:1）。**EP足は「↑EP」＋その下に次足期待度（=旧H1期待）の縦2段**・旧H2期待はH1足の下へ＝記号が1つ左に移る。保有域外（旧記録の未記録）は空・新記録はnextExp1〜3が各足に表示（OS4=nextExp4はこのセルの表示範囲外=OS1〜3チェーン）。×見送りの「↑EP（×）」・「実E」併記は維持。
- **_epExpAt削除**: 最後の利用者（このセル）が_epNextExpAtへ移行し全消費者の統一が完了＝旧・役割固定の期待度取得関数を削除（死コード掃除）。
- **検証**: 実マウント4ケース＝EP=OS1（↑EP＋次○/次○/空）・未達（次×が同位置）・×見送り（次×/↑EP（×）＋次○/次×）・新記録（nextExp1〜3表示）・React警告0・esprima構文OK。sw.js v37→v38。

### 2026-07-06e 次足期待度統一のアプリ全体反映＝期待度参照の全数監査と37箇所置換（ユーザー依頼「徹底的にチェックして反映」）
- **全数棚卸し**: app-01〜08の`os1/2/3Exp`・`holdExp/hold2Exp`直読み61箇所＋`_epExpAt/_epAsTraded/_epHoldView/_elWinBucket/_elOsMaxFiltered/_elOsMaxCapped`呼び出しを分類。**行αシミュ（simAlpha per-record上書き・一括α）が集計ループへ流れることを確認**＝生`s.holdExp`読みはシミュα時に役割ズレする経路だった（2026-07-03vでもTotParts×3しか直っていなかった）。
- **共有ヘルパー新設（app-05）**: `_elH1ExpAt(s,alpha)`/`_elH2ExpAt(s,alpha)`＝H1/H2保有ガバナンス期待度（=_epNextExpAt(s,epIdx)/(s,epIdx+1)・TotPartsと同一規約・採用αでは生holdExp/hold2Expと同値）。
- **置換37箇所**: ①集計ループ＝_elTotAccum(app-05)・_elCalcStats H1/H2(app-05)・早見表アグリゲータ(app-05 _aSig)・銘柄別記録合計(app-02 _esAlpha)・週間/本日/今週のH列合計(app-02×1・app-04×3 _aR/_alphaRec) ②表示セル＝_elHold2Cell・_elHoldStackInner・_elHoldBoth(exp/hexp/_h1Exited→_elH1/H2ExpAt) ③`_elWinBucket`＝tri判定と出口ウォークを_epNextExpAt化（足iの保有=足i-1の引け） ④`_elOsMaxFiltered`/`_elOsMaxCapped`＝役割インライン割当→「保有側=足i-1の引け/待ち側=足iの引け」の足固定解決（新記録のnextExpN対応） ⑤`_epAsTraded`＝待ち足のnextExpNもnull化（EP足以降の保有判断は温存＝×見送り「取引していたら」分析が新記録でも正しい）。
- **対象外（意図的）**: `_elHoldMaxHigh`（α非依存・記録時表示）・`_elH2ExpCounts`等の記録済み判断カウンタ・app-06 _elTriangleHoldSectionV2等の採用α限定セレクタ・app-01 migrateData:699（scheme2/3除外ガード済み）・_epOsChainCellの表示（従来契約）。
- **検証**: **恒等性証明＝71バッテリー記録全件で`_elH1ExpAt(s,採用α)===s.holdExp`/`_elH2ExpAt===s.hold2Exp`フェイル0**→全機械置換は採用αで数値不変。WinBucket/OsMaxFiltered/OsMaxCapped独自バッテリー＝**採用α diff0件**・シミュα差分42件は全て役割ズレ解消カテゴリ（例: EP前進時に旧はholdExpを誤った物理足に割当てOS値を過小評価）。×見送り→_epAsTraded＝nextExp1剥がし・nextExp2温存・H2フォールバック正。EntryLogView実マウントで集計/α値/損切り/深掘り/シミュ全タブ巡回エラー0・React警告0・esprima構文OK。
- **sw.js**: `APP_CACHE` v36→v37。

### 2026-07-06d 期待度を「次足期待度」に統一（データモデル＋フォーム＋読み取り層・ユーザー設計決定）＝いつでもEPが変わってよい記録へ
- **概念統一**: 旧・α値到達期待度(os1/2/3Exp)・H1期待度(holdExp)・H2期待度(hold2Exp)を「**次足期待度**＝各足の引けで下す“次の足へ継続するか”の判断（○=継続/△=中間/×=この足で降りる=見送り・手仕舞い）」1本に統一。待ち足の×=見送り宣言・EP以降の×=手仕舞い＝同じ概念の両面（ユーザー確認済）。
- **データモデル（app-05）**: 新フィールド `signal.nextExp1〜4`（足固定 OS1..OS4・正本）。保存時にレガシー（os1Exp/os2Exp=EP位置でゲート・holdExp=EP足スロット・hold2Exp=EP+1足スロット・os3Exp=null廃止）を現在EP位置から自動導出＝**下流・旧版アプリと完全互換**。既存記録の一括migrateはしない（Firebase帯域保護）＝読み取り層フォールバックで対応。
- **読み取り層（app-05）**: 新 `_epNextExpAt(s, x)`＝足xの引けの次足期待度（nextExpN優先→レガシー導出。保有域外(x>recEp+1)はnull=×扱い＝**「自動×は既存記録だけ」**のユーザー指示どおり）。**足iを保有するかのガバナンス＝_epNextExpAt(s, i-1)**（off-by-one解消）＝`_elHold1TotParts`/`_elHold2TotParts`/`_elH1HeldBase`のH1/H2期待参照を`_epExpAt(s,epIdx+1/+2)`→`_epNextExpAt(s,epIdx/epIdx+1)`へ。`_epIsTriEntry`も_epNextExpAt化（EP後退シミュで元EP足のholdExp△を△確信度として正しく拾う）。**2026-07-06cの○補完（i===recEp分岐）は撤去＝正式解に置換**（EP前進時の新H1保有は「その前の足の引けの実判断」＝記録済みのos1Exp/os2Expを使う。○補完は△を○扱いする近似だった）。`_epExpAt`は表のOS連鎖セル等の表示専用に降格（従来契約のまま）。
- **フォーム（EntryRecordForm）**: 期待度5stateを足固定 `fNxs[0..3]`＋`_setNx`に統一。**スロットはOS1〜OS4常時表示**（ラベル「次足期待度」統一=案A・EP位置で出たり消えたりしない・**OS4も常時選択可**=ユーザー指示）・OS5はスロット無し（次足が無い）。編集ロード=_epNextExpAt（レガシー導出込み）＝**EP（α）が変わっても入力値は足に残る**。旧名互換の導出値（fOs1Exp/fOs2Exp/fHoldExp/fHold2Exp）を_epFormState直後に定義しプレビュー_fVSig/保存/損益変化効果/非表示バッジは無改修。カスケードを足固定に一般化（×連鎖=EP境界は跨がない＝×見送りでもH判断を参考記録可・△継承=EP以降・△確信度→EP足○不可・待ち足側の損切り済→×正規化）。バリデーション文言=「OS1の次足期待度」「次足期待度（OS3・H1保有）」等（必須集合は従来どおり・EP+1より深いスロットは任意）。
- **効果**: αを上げるシミュ（EP後退）で新記録のOS3/OS4スロットが新H1/H2の保有判断として使える＝両方向で「いつでもEPが変わってもいい」が完成。既存記録の保有域外は×扱い（記録が無いため）。
- **検証**: 採用α不変バッテリー74記録（EP=OS1/2/3×holdExp5値×hold2Exp4値×待ち足exp変種＋損切り＋miss）＝**編集前後で完全一致（diff 0件）**＝本日の損益データ欄・記録帳・週間の実損益は不変。シミュα±3の差分16件は全て設計どおり（EP前進=実判断尊重で△→（）内・×/未設定→保有せず／EP後退=△確信度精密化）。+1,000円問題記録＝os1Exp○の実判断で+4,500円維持。新フィールド優先・レガシーfallback・α上げシミュのOS3/OS4スロット動作をエンジン直呼びで確認。フォーム実マウント＝スロット4個常時・α7→5でEP移動しても値が足に固定・OS4選択可・保存ペイロード（nextExp1〜4=正本＋レガシー導出＝EP=OS1でos1/os2/os3Exp=null・holdExp/hold2Exp正値）確認・React警告0・esprima全8ファイルOK・シミュタブ回帰OK。
- **sw.js**: `APP_CACHE` v35→v36。

### 2026-07-06 株数シミュ: 浮き足加算・追加α上乗せ＝シミュの入力αを「基本α部分」に再定義（実効α＝入力＋記録固有の上乗せ）
- **背景（ユーザー相談）**: シミュは実効αを直接当てるため、浮き足〇の記録（前足浮きサブタブ＝全記録）は実運用（基本α＋浮き値÷2切捨て）より浅いαで建てる想定になりEP到達率・損益が楽観に歪む。追加α〇の記録（母数トグル〇のみ/全記録時）も同様。実運用ルール「α＝基本α＋浮き足加算＋追加α」の基本α部分だけ差し替える反実仮想に統一（AskUserQuestionで確定＝浮き足は常時上乗せ／追加αはセレクタ選択・既定実追加α／全方式適用）。
- **`_simAddOf(r)`（`_elKabuLadderSimV2`内・新設）**: 記録固有の上乗せ＝`_elUkiAdd(s)`（常時・浮き足×/対象外は0）＋追加α上乗せ（`_elAddAlphaYes`の〇記録のみ・`addOn` state: act=実追加α`s.addAlphaVal`(既定)/reco=記録日時点の推奨追加α`recoOf.addAt(date)`/none=0）。**既定母数（×+未選択・その他サブタブ）では全記録0＝従来の数値と完全一致**。
- **適用箇所**: 手動`_manTiersOf`（abs/reco/recobase全方式・0クランプ後に加算）／自動配分の総当たり（`_exAs`・第1取引`_a1+_ex`・第2取引`_recoAs+_ex`）／`_autoTiersOfFor`（マスター表・ランキングと一致）。**損切りは無改修**＝cutLineはαからの相対幅なので実効α起点に自動追従。従来列（実採用α=三要素込み）も無改修＝公平な比較。
- **`_elKabuRecoBaseFn`拡張**: cacheを`{a,add}`化し返り値関数に`fn.addAt(dateStr)`追加（同じ窓の推奨追加α・`_elBaseAlphaA().add`のimprovedのみ・無ければnull→上乗せ0）＝フォーム`_refBaseAlpha`の「同じ窓からaddも取得」（2026-06-27窓統一）と同ルール。既存呼び出し（数値返し）は無改修互換。
- **UI**: 「追加α上乗せ:」ピル行（実追加α/推奨追加α/なし・#9A3412）＝**〇記録がシミュ対象poolにいる時だけ表示**（`_poolHasYes`）。floatMode注記に「シミュαには各記録の浮き足加算を上乗せ」。マスター表シミュα列に上乗せ内訳の小書き「（浮8+追3込）」（`m.simUki`/`m.simAddOn`）。手動・自動の説明文に上乗せルール追記。
- **sw.js**: `APP_CACHE` v32→v33。検証: esprima＋V8全9ファイル構文OK・実マウント6テスト＝既定母数で従来値+1,500円一致（後方互換）／〇のみ→セレクタ表示・実追加α+1,700円（入力5+追3=実効8）・シミュα「8円（追3込）」／なし+1,400円／推奨追加α（addAt=null→0）+1,400円／floatMode+2,000円（入力5+浮8=実効13）・「13円（浮8込）」／自動配分★+10,000円（実効13×500株）検算一致・React警告なし。

### 2026-07-06c EP前進時の損益過小評価を修正＝元EP足のα到達期待を○補完（`_epExpAt` app-05.js・ユーザー指摘）
- **症状**: 株数シミュでα（基本α部分）を採用αより下げるとEPが早い足へ前進。ユーザー報告＝ある記録で従来+12,500円がシミュ+1,000円に激減。
- **原因特定（実マウント検証）**: 記録＝`OS1(高値6)→OS2(高値8)→OS3(高値2)`、採用α7でEP=OS2/H1=OS3(確定-18)/holdExp○＝従来+2,500円/100株。シミュα5では高値6のOS1で先に到達しEP=OS1へ前進→新H1=OS2。だが**OS2は記録上「α到達待ち足」でos2Expが空**（EP足は入力UIがEP表示になりα到達期待が保存されない）→`_epExpAt(s,新H1)`=null→H1保有せず`_elDynPlanned`＝EP想定額+200円/100株＝**+1,000円**。合成でos2Exp="○"なら+900円/100株(+4,500円)が正しく出ることを確認＝**ルール自体は正しく実装済み・データ欠損が原因**。
- **修正（`_epExpAt`に1行）**: `if (recEp >= 0 && i === recEp) return (legs[i] && legs[i].exp) || "○";`＝**記録時EP足(i===recEp)はα到達済み(h≥α)なのでα到達期待が空でも○とみなす**＝ユーザーのルール「α到達期待をH期待として流用」をEP足でも完成。明示の×/△/○はそのまま尊重。
- **影響範囲＝EP前進時のみ発火**: `i===recEp`が新H1/H2になるのは`epIdx<recEp`（EP前進＝αを下げた分析）だけ。**採用α(EP不動)ではi=recEpは損益に使わない(H1=recEp+1〜)ので本日の損益データ欄/週間/記録帳の実損益は完全不変**（検証: 採用α7→+12,500不変）。**EP後退(αを上げる)では2026-07-03vの足固定を維持**＝新H1が元hold2Exp=×足なら×のまま保有しない（検証: α8後退→expAtH1=×・バグ再発なし）。`_elHold1TotParts`/`_elHold2TotParts`/`_elH1HeldBase`経由でシミュ・α感応度カーブ・α意思決定表・損切り値別シミュにも一貫適用。
- **検証**: esprima+V8構文OK・実マウントで問題記録がシミュα5→**+1,000円→+4,500円**改善／従来+12,500不変／os2Exp明示×は+1,000尊重・△はmain200/ref700／EP後退α8はexpAtH1=×維持／React警告なし。
- **sw.js**: `APP_CACHE` v34→v35。

### 2026-07-06b 株数シミュ: 自動配分の合計株数に▲▼ステッパ追加＋計算基準の同一性を徹底検証（ユーザー依頼）
- **自動配分の合計株数入力に▲▼（±100）**: 手動の各取引株数(`_stepCum`)にはあったが自動の合計株数(`total`)には無かった（ユーザー指摘「株数の入力方式に↑↓のやつもない」）→ `_stepTotal`（±100・0クランプ・`setAutoExp(null)`同伴）新設＋inputを手動株数欄と同じ`_stepBtn`ラッパ化。実マウントで▲押下500→600確認。
- **計算基準の同一性を徹底検証（従来列 vs シミュ列 vs 自動配分）**:
  - **エンジンレベル**: 6記録タイプ（○H1○/H1△/損切り/EP△/未到達miss/×見送り）で `_elKabuTierEval(s,α,cut).main100/ref100` == `_elHold1TotParts(s,α,cut).main/ref` を全MATCH＝**シミュ列も従来列も同一の`_elHold1TotParts`を通る**ことを実証。
  - **実効α=採用αで完全一致**: 上乗せ0（×+未選択・絶対値α=採用α）／浮き足〇（絶対値α=基本α→実効α=採用α）／追加α〇（絶対値α=基本α+実追加α→実効α=採用α）の3ケースで従来列合計==シミュ列合計（4,200/7,800/7,800・1円一致）＝**「実効αと採用α・cutを揃えればシミュと従来は完全一致」＝計算方法は同一**。
  - **自動配分**: 総当たりランキング1位・★最適ブロック・マスター表バー・独立エンジン再現（総当たりロジック／`_autoTiersOfFor`→`_elKabuLadderCalc`）が**四者とも +18,500円（（）内+22,500円）で1円一致**。総当たりの`_evAt(pi,a+ex)`とマスター表の`_autoTiersOfFor`が同じ実効α・同じcut（`aiOf`＝損切りオフセット非適用）を通ることを確認。
- **sw.js**: `APP_CACHE` v33→v34。検証: esprima＋V8構文OK・上記実マウント全パス・React警告なし。

### 2026-07-04 株数シミュ: 従来列を「損益データ欄の理論値×総株数按分」に単純化＋シミュ列に取引内訳（案B）＋差額の符号バグ修正
- **従来列（`_kbConvParts`/`_kbConvSums`新設・`_elKabuLadderSimV2`内）**: 旧「採用αで総株数を単建てしたラダー再計算（`_elKabuLadderCalc`経由）」を廃止し、**`_elHold1TotParts(s, 採用α, cutLine)`（＝損益データ欄・銘柄別記録/取引テーブルと同一の（）外/（）内理論値・100株当たり）×総株数÷100の単純按分**に変更（ユーザー仕様「従来の損益をそのまま株数スケール・第1/第2取引は無関係」・AskUserQuestionで実現損益ベース案は不採用＝理論値を選択）。マスター表の従来列/合計行/`_mtBar`/KPI差額（旧`_baseManCalc`→`_convMan`）を同一基準に統一。採用αがnullの記録も損益データ欄と同じフォールバック（保存値）で拾われる（旧はnoalphaスキップで—）。
- **シミュレーション列（`_mtSimBreakdown`新設）**: 複数取引の記録は合計（（）内併記維持）の**下段に取引ごとの内訳「+1,200円/+2,400円」を同サイズで表示**（案B・show_widgetモック3案から選択・「小さくしない・中央ぞろえ」指定）。建たなかった/判定不可の取引は「—」で位置を残す（順序は取引内訳と同じ）。シミュ列全体（見出し/本体/合計行）を中央ぞろえ化。
- **差額の符号バグ修正**: 2026-07-03uの「片側だけ数値ならその値」を撤回＝シミュ—（建たず）・従来+1,500円のとき差額が+1,500円になっていた → **null=0円扱いで常に「シミュ−従来」**（例では−1,500円）。
- **`_origPnl`（元の損益(実際)）**: `signal.realizedPnl`のみ→**`item.pnl`優先**（対象取引リスト`_elOsTradeMini`・取引テーブルと同じ優先順位）に統一。
- **sw.js**: `APP_CACHE` v22→v23。

### 2026-07-05 株数シミュ: 手動ラダーに損切り値（推奨基本α値+X円）＋表に「シミュ損切」列
- ユーザー要望＝シミュで損切り値も設定。「+取引を追加」ボタンの真下に損切り入力（α値と同じ±1ステッパ`_stepStopOff`・「推奨基本α値 + X 円」）。state `stopOff`（空欄=各記録の実際の損切り値）。
- **損切り解決 `_simCutOf(r)`**: 空欄→`aiOf(r).cutLine`（各記録の実際の損切り値）／設定時→`recoOf(r.date)`（その記録日時点の推奨基本α）+ オフセット（1円未満は1にクランプ・推奨α不明は記録値へフォールバック）。`_aiSim(r)={alpha,cutLine:_simCutOf}` を **manCalc の aiOf に差し込む**＝手動シミュのP&L/KPI/損切り率が損切り設定を反映（自動配分は据え置き＝各記録の実際の損切り値）。
- **表「シミュ損切」列**（シミュα列の右）: `_kbMasterTable` に第4引数 `cutOf`（`{cut,ov}` を返す・省略時=記録の実際値・ov=false）。手動は `_simCutInfo`（ov=推奨基本α+オフセットで上書き中）を渡す。列は `recoOf+オフセット`（上書き=橙#B45309／記録の実際値=灰#94a3b8）。例: 推奨基本α5＋オフセット10→**15円**（推奨基本α10なら20円）。列追加に伴い ヘッダ11列・合計行 colSpan 6→7・展開 colSpan 10→11・minWidth 680→730。
- 脚注に「シミュ損切」列の説明を追記。**従来列は各記録の実際の損切り値のまま**（損切りオフセットはシミュ側のみ＝差額はフル『もし全部こうしていたら』比較）。
- sw.js v30→v31。検証: esprima+V8構文OK・実マウントで 履歴ありでrecoA=5→simCut空欄10円(灰)/オフセット+10で15円(橙)・recoA不明は10円へフォールバック・**エンジンがcut反映（stop-sensitive記録: cut10→-1,300(損切)、cut15→+500）**・オフセット切替でシミュ損益が-1,300→+500に変化・自動配分は列灰10円/損切り入力は非表示・React警告なし。

### 2026-07-05 株数シミュ: シミュ列セルを「内訳=合計」形式に再設計（合計を少し大きく）
- ユーザー要望＝シミュ列セルを「上段: （）外の内訳＝合計／下段: （（）内の内訳＝合計）を丸括弧で囲む」に。例「+300円/+3,200円=+3,500円」＋「（0円/+2,000円＝+2,000円）」。合計（=の右）は少し大きく(`_SIM_SUM_FS`=13px・セル既定10.5)。
- `_mtSimBreakdown`(合計は別行`_mtPnlNode2`＋内訳のみ)を廃し **`_mtSimCell(m)`** に統合＝セル全体を描画。合計は行の正準値 m.cfgPnl(（）外)/m.cfgPnl+m.cfgRef(（）内)を使用（内訳の和と一致・株数100倍数で端数なし）。取引1件（内訳なし）は「=」無しで合計のみ。ref皆無（△/損切り済なし）は（）外の1段のみ・全未成立は「—」。並びα昇順・同一α合算は従来どおり。cell 呼び出しは `_mtTd(_mtSimCell(m), "center", true)`。**合計行(tfoot `_sumRow`)・下部バー・従来列は据え置き**（アグリゲートは内訳が無いので `_mtPnlNode2` のまま）。
- sw.js v29→v30。検証: esprima+V8構文OK・実マウントで △記録「+300円/+3,200円=+3,500円（0円/+2,000円＝+2,000円）」・合計とも13px・all○記録は1段（点線なし）・単一取引は「=」無し・合計行 従来+5,500(+4,000)/シミュ+4,500(+3,000)/差額-1,000 整合・React警告なし。

### 2026-07-05 株数シミュ: 累積(累計)方式を廃止し「各取引の株数を足す」方式へ＋展開行にフル計算式
- **累積廃止（ユーザー指摘「累計というシステムがおかしい。第1取引100株＋第2取引400株なら合計500株」）**: `_manTiersOf` の累積差分（add = cum_i − max(前まで)）を廃し **add = 入力株数そのまま**。第1取引100株＋第2取引400株＝**合計500株**（旧: 累積400なら第2=300株）。第2取引 α5円×400株 = 800円/100株×4 = **+3,200円**（＝ユーザーが当初「800×4のはず」と言った通りに）。α昇順ソートは表示整合のため残す（足し算なので合計は順序不変）。`_manTot` は `Math.max(cum)`→**Σ株数(総和)**。`_ladderPrev`（「α0円で100株 ＋ 推奨基本α値で400株 ＝ 合計500株」）・`_addRow`(既定cum:"100")・`_rowInputs`(ラベルから「累積」削除)・注記文言を更新。state フィールド名は歴史的に `cum` のまま=意味は「各取引の株数」。**自動配分(_autoTiersOfFor)は元から s1+s2 の足し算＝無変更**＝これで手動と自動が同じ足し算方式に統一。
- **展開行のフル計算式は撤回（2026-07-05・同日中）**: 一旦 `_mtFormula`/`_per100Explain`/`_fmtSigned` で「従来＋シミュ各取引＋合計＋差額＋100株あたりの根拠」を式表示したが、ユーザー「計算式の表示は不要」で撤回＝展開行は元の `_mtTierNodes`（取引内訳＝各取引のα×株数と（）外損益・（）内括弧）に戻した。従来欄が正しい（800円/100株×株数）ことの確認自体は有効。sw v28→v29。
- **従来欄の検証結果**: 従来＝`_elHold1TotParts(採用α)`×総株数按分で正しい。例(07/03 10:51・α5・H1が△): 100株あたり+800円＝EP想定額(α5−EP確定−3)×100 → ×株数。（）内は H1保有 +500円/100株。
- **sw.js**: v27→v28（v27は同セッションで内訳2段化 _mtSimBreakdown＝上段（）外/下段（）内・点線）。検証: esprima+V8構文OK・実マウント7シナリオ（100+400=500株/第2=800×4=3200/式表示/同一α合算/未到達=建たない/自動500株/ref無し1段）＋React警告なし。

### 2026-07-04d 株数シミュ: 手動/自動トグルを対象取引セクションの下へ移動
- `_elKabuLadderSimV2`の`head`から手動ラダー/自動配分トグルを抜き、`_modeToggle`として`_targetList`（対象取引）と脚注の間に挿入。並び＝対象期間→追加α母数→対象取引→**手動/自動トグル**→脚注→body。母数を決めるフィルタ（対象期間・追加α母数＝poolを変える）を上に、分析方式の選択（modeはpool不変）を母数確認の後に置く意図。pool空の早期returnはhead（フィルタのみ）＋「該当なし」で従来どおり（トグル不要）。
- **sw.js**: `APP_CACHE` v25→v26。検証: esprima+V8構文OK・実マウントで表示順（対象期間154<追加α母数182<対象取引224<トグル356<脚注368）＋トグル切替（自動⇔手動）動作＋React警告なしを確認。

### 2026-07-04c 株数シミュ徹底監査＝整合性の不備7件を修正（ユーザー依頼「他にも整合性取れてない箇所がないか徹底的にチェック」）
- **①推奨基本α値（自動配分の表示＋第1取引α上限_s1AlphaMax）を日別ページ「今日の推奨」と同一基準に**: 旧`_elBaseAlphaPick(baseRecs)`直当て＝生母数（**非v2記録がeRate分母nを膨らませ到達率フロアを歪める・不算入(includeInTotal:false)記録も混入**）・窓なし・当日込み → v2＋算入・本日の前日まで・直近50→100→全期間で最初に値が出た窓（`_elBaseAlphaA(...).pick`・status保持＝na参考バッジ動作は不変）。recoOf（記録日ごと・2026-07-03tで統一済み）と同じ思想に単一値も揃えた。
- **②EP△（△確信度エントリー）記録の表示不一致**: シミュ列だけ「0円（+X円）」・従来列は「—」だった → `_elKabuLadderCalc`のrows.recPnlを「（）外を持つ取引が皆無ならnull」に（hasMainフラグ）＝3列とも「—」で統一。**合計への算入は0円のまま不変**（sum/sumRefは変わらない）。
- **③推奨αキャッシュ署名（recoSig）にOS/H足の実データが無い**: OS値・確定値・期待度・浮き足〇だけ編集しても再マウントまで古い推奨αが残った → `_epLegs`のh,c,expフィンガープリント＋`_elUkiYes`を署名に追加。
- **④推奨α±Xのマイナス実効α**: エンジン(`_elKabuTierEval`)は0クランプだがシミュα列/積み増しプレビューはマイナス表示 → `_manTiersOf`のreco経路でも`Math.max(0,…)`。
- **⑤シミュ列内訳の並び・重複**: 自動配分で第1α＞その日の推奨αに逆転するとシミュα列（昇順・重複除去）と内訳（第1→第2順）の対応がズレる／同一α2取引が2値で出る → `_mtSimBreakdown`をα昇順ソート＋同一α合算に（シミュα列と1:1対応）。全取引が建たない記録は内訳自体を出さない（—/—抑止）。
- **⑥自動配分のマスター表に注記行が無い**: 手動のみ`_notesLine`（判定不可/推奨α不明/×見送り件数）だった → 選択配分の`_elKabuLadderCalc`をIIFEで共有し表＋注記を表示。
- **⑦追加α母数トグルで展開が残る**: mtExpキー（pool index）が別記録を指す → addFilピルで`setMtExp(null)`（期間ピルは対応済み）。
- **監査で確認した「問題なし」**: pool母数チェーン（_v2recsAll=v2+_elInclTotal→シグナル→浮き足/その他→追加αトグル）／α・損切り値=_elAlphaInfo（全表と同一）／シミュ損益=_elHold1TotParts（損益データ欄と同一・2026-07-03v足固定込み）／自動配分オプティマイザとマスター表再計算の一致（同一評価関数・同一丸め順）／ランキング列=8ヘッダ8セル／recoOf=日別ページ見出しと完全一致（実装突合）／フォーム自動入力はok限定（na不使用）だが表示・recoOfは「最初に値が出た窓」＝日別ページと同じ（意図された差）。**既知・意図的に残す**: 記録帳分析系の実現損益が生値（株数100運用で実害なし＝旧A群判断のまま・_kbConvPartsのα=null保存値フォールバックも同穴）／マスター表は新しい順・対象取引リストは古い順（並び逆・要望があれば統一）／①基本αタブの推奨サマリーはシグナル別・全期間・当日込み（軸が違う設計・シミュ／日別ページとは別物）。
- **sw.js**: `APP_CACHE` v24→v25。検証: esprima+V8構文OK・実マウント7シナリオ＝EP△行3列「—」統一／recobase同一α統合（+3,000円・内訳なし）／全null内訳抑止／トグルで展開解除／自動配分の推奨基本α値=履歴ベース手計算一致（ok・参考バッジなし）＋注記「推奨α不明1件」／OS値編集→シミュα 8円→6円追従／**不算入＋旧形式レコード混入でもpool 3件・推奨値不変（除外が効く）**・React警告なし。

### 2026-07-04b 株数シミュ: 差額も（）外/（）内の2値表示に（行/合計行/下部バー/KPIカード）
- **差額の（）内側**: 従来・シミュ列は（）外（（）内）併記済みだったが差額は（）外のみだった → 4か所すべてに追加。行=`diffRef=(m.cfgRef||0)-(m.baseRef||0)`を`_mtPnlNode2(diff, diffRef)`で表示／合計行・下部バー=`uplRef=cfgRef-baseRef`（`_mtBar`に`uplRef`引数追加・`_in(upl, uplRef)`）／KPI差額カード=`_uplManRef=manCalc.sumRef-_convMan.sumRef`（通算損益カードと同じ17px同色で（）併記）。**表記ルールは既存と同一＝（括弧内）は（）内合計同士の差額で、（）外差額と同値（ref差0）なら括弧非表示**。
- **sw.js**: `APP_CACHE` v23→v24。検証: esprima+V8構文OK・実マウント（△記録・α0×300株+α20×500株）で行/合計行/バー/KPIすべて「-1,700円（-2,100円）」＝手計算一致（（）外=−1,200−500／（）内=−600−1,500）・ref差0ケースは括弧非表示・React警告なし。

### 2026-07-03v 損益計算のH1/H2期待度を「足固定(_epExpAt)」に統一＝シミュでαを上げEPが前進してもH2×の足を保有算入しないよう修正（ユーザー報告バグ）
- **不具合（ユーザー報告）**: 株数シミュで採用αより高いαを当てると、EPが前の足へ前進し、**元々「H2＝×」と記録した足が新しい"H1"扱いになって保有益を算入**していた（例06/18: OS1=5→OS2=7→OS3=10・H1○H2×・採用α5でEP=OS1・H1=OS2→+1,500／シミュα6でEP=OS2に前進しH1=OS3（本来H2×）を+2,300で算入）。「最高値」表示は足固定で7止まりなのに損益はOS3を算入する矛盾。
- **原因**: 足ごとの期待度を「記録時EP位置」で固定する`_epExpAt`（2026-07-03作成）が**最高値表示等にだけ使われ、損益計算(`_elHold1TotParts`/`_elHold2TotParts`/`_elH1HeldBase`)では生の`s.holdExp`/`s.hold2Exp`を直接参照**していた＝αでEPが動くと別の物理足に誤適用。
- **修正（app-05.js）**: `_elHold1TotParts`・`_elHold2TotParts`・`_elH1HeldBase`が、`_r=_epResolve(s,alpha)`で解決したEPに対し**`_epExpAt(s, _r.epIdx+1)`（H1）/`_epExpAt(s, _r.epIdx+2)`（H2）**を期待度として見るように変更（v2＋α有りのみ。v2以外/α無しは従来どおり生の記録値）。**採用α（EP不動）では`_epExpAt=s.holdExp/s.hold2Exp`で完全に不変**＝本日の損益データ欄・週間・記録帳の既定表示は値が一切変わらない。αを変える分析（株数シミュ・α感応度カーブ・α意思決定表・損切り値別シミュ等）だけが「元々×だった足は×扱い（EP想定額にフォールバック）」に正される。AskUserQuestionでユーザーOK。
- **sw.js**: `APP_CACHE` v21→v22。検証: `new Function`構文OK＋実関数テスト＝報告例06/18はシミュα6が+2,300→**+1,600**（EP=OS2で手仕舞い＝EP想定額(6−(−10))×100）・従来α5は+1,500不変。**採用αでの無影響を50バリアント（holdExp×hold2Exp×EP2種）で不変条件`_epExpAt@採用α===s.holdExp/hold2Exp`ミスマッチ0で証明**。

### 2026-07-03u 株数シミュのKPIはみ出し修正＋マスター表の列整理（実現削除・改名・差額を必ず数値化・合計は下部のみ）
- **KPIカード（`_card`）**: 文字はみ出し修正＝幅拡大（flex `1 1 110px`→`1 1 148px`・minWidth 105→138・padding広め）＋値を折返し可（whiteSpace normal・`wordBreak:keep-all`で数字は途中改行しない）。通算損益の（）内も同サイズ17pxのまま折返し可に（外span normal・内span nowrap+inline-block＝(）外/（）内の境界でだけ改行）。KPIが2行に回り込んでOK（ユーザー了承）。差額カードの副文言「シミュ−実α単独」→「シミュ−従来」。
- **マスター表（`_kbMasterTable`）**: ①**実現列を削除**（ユーザー要望＝不要）。②列名変更「実α単独」→**従来**／「シミュ」→**シミュレーション**／「差」→**差額**（`_mtBar`の「実際のα単独」→「従来」も）。③**差額を必ず数値化**＝旧`(cfg!=null&&base!=null)?差:null`を「両方—なら—・片側だけ数値ならその値・両方あればシミュ−従来」に（ユーザー例: 従来+100/シミュ— → 差額+100）。④**合計は下部だけ**＝先頭の`_sumRow("h")`（thead内合計行）と先頭の緑バー`_mtBar(...,false)`を削除、tfootの`_sumRow("f")`＋下部の`_mtBar(...,true)`のみ残す（`_mtBar`ラベルは「合計（再掲）」→「合計」に）。列削減に伴い合計行colSpan 7→6・展開明細colSpan 11→10・テーブル枠は上角丸め+下枠なし。手動の`cfgLabel`「シミュ・N株」→「シミュレーション・N株」。
- **sw.js**: `APP_CACHE` v20→v21。検証: `new Function`構文OK＋実レンダーでマスター表ヘッダ＝日付/OS連鎖/E/推奨α/採用α/シミュα/**従来/シミュレーション/差額**（実現なし・10列=本体10セル一致）・thead合計行なし（tfootのみ）・下部バー「合計」「従来」・差額が片側—でも数値（両方あればシミュ−従来＝例 −6,000−(−4,400)=−1,600）を確認。**デプロイ確認はユーザー指示時のみに変更（[[feedback_github_pages_check]]更新）**。

### 2026-07-03t 株数シミュの推奨αを日別ページ・記録フォームと一致（母数をシグナル別→銘柄全体へ）＝ユーザー指摘の値ズレ修正
- **不具合**: シミュの「推奨α」列（記録日ごと・`recoOf`）が日別ページの「今日の推奨」／記録フォームの推奨基本αと値が違う。原因＝**母数の範囲**: 日別ページ`_elBaseAlphaDayBlockV2`・記録フォーム`_refBaseAlpha`(app-05:5509)は**銘柄全体（全シグナル・`r.stock===銘柄`）**で算出するのに対し、シミュ`_elKabuRecoBaseFn`は`baseRecs=_selSigRecs`＝**そのシグナルだけ**。加えて窓の選び方も違った（日別=直近50→100→全期間で最初に非null／シミュ=okを優先）。ユーザーが実際に記録時に見て使う推奨α（記録フォーム＝銘柄全体）が正準＝AskUserQuestionで「銘柄全体に合わせる」を採用。
- **修正（app-06.js）**: ①`_elKabuRecoBaseFn`を`_elBaseAlphaDayBlockV2`の見出しロジックと**完全一致**に書き換え＝`baseRecs`を前日まで(look-ahead回避)で日付順に並べ、直近50(`_EL_PERIOD_COUNTS[1]`)→100→全期間の順で最初に`_elBaseAlphaA().pick.alpha!=null`の窓を採用（`_elPeriodWindows`＋ok優先を廃止）。②シミュ呼び出し(`EntryLogView`)の`baseRecs`を`_selSigRecs`→**`_isAllStock ? _selSigRecs : allRecs.filter(r=>r.stock===_selStock)`**（銘柄全体）へ。`baseRecs`はシミュ内で推奨α算出（`recoOf`/`_recoBasePick`/`recoSig`）にしか使わないので影響は「推奨α」列・手動「推奨基本α値」方式・自動配分の第2取引/推奨基本α表示のみ（シミュ母数`recs`は従来どおりシグナル別）。コメント2箇所更新。
- **sw.js**: `APP_CACHE` v19→v20。検証: `new Function`構文OK＋実レンダーで`_elKabuRecoBaseFn(銘柄全体)(記録日)`＝日別ページ見出しロジックの独立再現と**完全一致**（2データセットで`match:true`）を確認。

### 2026-07-03s 株数シミュ手動ラダー: α値欄に▲▼(±1)＋横スクロールヒント削除＋KPI拡充（差額/勝率/損切り率%/1株あたり損益）
- **app-06.js `_elKabuLadderSimV2`（手動ラダー）**: ①**α値入力欄（`off`）に▲▼ステッパ**を追加＝`_stepOff`（**±1**・`_stepBtn`流用・絶対値は0未満不可/推奨α±Xはマイナス可・functional setState）。株数欄の±100(`_stepCum`)と対で、新規記録画面と同じ縦▲▼。
- **横スクロールヒント削除**: `_HScrollBox`に`plain`プロップ追加（trueで早期return＝ロック無し・「👆タップで横スクロール」ピル無しの素の`overflowX:auto`）。`_elOsTradeMini(recs,aiOf,opts)`に第3引数`opts`追加し`opts.plain`を`_HScrollBox`へ伝播。**対象取引リスト**の呼び出しを`_elOsTradeMini(pool,aiOf,{plain:true})`に＝ヒント表記を消去（他の`_elOsTradeMini`呼び出しは従来どおりヒントあり）。
- **KPI拡充**（手動カード）: `_pnlNode2`の代わりに**通算損益の（）内損益を（）外と同じサイズ(17px)・同色(`_elPnlColor(main)`)**でインライン描画（従来は9〜11px灰）。**「上乗せ」→「差額」に改名**（`_mtBar`）。新規KPIカード＝**差額**（`_uplMan`=シミュ−実α単独＝`_baseManCalc`で総株数を採用α単建てした基準との差・`_mtBar`の差額と一致）／**勝率**（建玉あり中`recPnl>0`の割合%・≥50%緑）／**損切り率**（`stopRecN÷builtRecN`%＋件数を副に併記＝旧「損切り取引あり」カードを置換）／**1株あたり損益**（`sum÷_manTot`・小数1位）。追加KPIはAskUserQuestionで勝率/損切り率%/1株あたり損益を採用（最大DDは見送り）。
- **sw.js**: `APP_CACHE` v18→v19。検証: `new Function`構文OK＋実レンダー（合成6記録・abs方式選択）＝α欄▲▼出現・対象取引リストにヒント無し・KPI（差額/勝率/損切り率/1株あたり損益）表示・_mtBar「差額」/「上乗せ」消滅を確認。

### 2026-07-03r 株数シミュに対象取引の期間絞り込み（本日〜全期間）＋対象取引リストを先頭表示
- **app-06.js `_elKabuLadderSimV2`**: 対象取引記録を**期間で絞り込み**（`period` state・既定"all"）。選択肢＝**本日/1週間/1か月/3か月/6か月/1年/全期間**。位置＝手動ラダー/自動配分トグルの**上**（`head`の先頭に「対象期間:」ピル行を追加・`_pill`流用・teal）。絞り込みは記録日基準（`_periodRecs`＝本日は`r.date===todayStr()`／相対は`todayStr()`から`setDate/setMonth/setFullYear`で遡ったcutoff以降／全期間は無制限）→`pool`は`_periodRecs`に追加α母数トグルを適用。**推奨α（`baseRecs`/`recoOf`）は各記録日の履歴で別途算出するため期間絞り込みの対象外**（look-ahead整合を維持）。
- **対象取引を先頭表示**: 期間選択後、`head`の直後・結果（手仕舞い注記/ラダー）の前に「対象取引（N件）」＋`_elOsTradeMini(pool, aiOf)`（本日の損益データ欄と同じ詳細表）を`maxHeight:300`スクロールで表示＝ユーザー要望「選択したら対象取引がまず表示される」。
- **sw.js**: `APP_CACHE` v17→v18。検証: `new Function`構文OK＋実レンダー（合成6記録＝今日/−3/−10/−40/−100/−200日）＝期間ピルがトグルの上・対象取引リストが結果より前・絞り込み動作（全期間6→本日1→1か月3→全期間6）を確認。

### 2026-07-03q 株数シミュ マスター表を推奨α/採用α/シミュαの3列に分離＋手動ラダーに株数▲▼ステッパ・リセット・未選択初期状態
- **app-06.js `_kbMasterTable`（🧮シミュ・手動/自動共用）**: 2026-07-03pで「シミュα」→「推奨α」に改名した列を撤回し、**3列に分離**＝列順「E→**推奨α→採用α→シミュα**→実現…」。**推奨α**（新規列・`m.recoA=recoOf(記録日)`＝その記録日時点の推奨基本α・青#0369A1）／**採用α**（`alphaNode`＝実際の採用α・基本+浮+追加内訳）／**シミュα**（`m.simA`＝その配分が各記録で使ったα・teal#0F766E）。列追加に伴い合計行 colSpan 6→7・展開明細 colSpan 10→11・table minWidth 620→680。recoAは`recoOf(r.date)`（`_elKabuRecoBaseFn`・既にrecoRefキャッシュ）を`mrecs`に追加。
- **手動ラダー（`_elKabuLadderSimV2`）**: ①**株数（累積）入力に▲▼ステッパ**（新規記録画面と同じグローバル`_stepBtn`・`_stepCum`で**±100**・長押しリピート・functional setStateで多重加算対応）。②**各取引にリセットボタン「↺ リセット」**（`_upd(i,{method:'',off:'',cum:''})`で条件を無に）。③**初期状態を「何も選択していない」に**＝初期`rows`を`[{method:'',off:'',cum:''}×2]`（未選択2行）。方式`<select>`に「（未選択）」オプション追加（未選択時グレー表示）・未選択の間は入力欄を出さず「入力方式を選択」ヒント表示。`_manTiersOf`は未選択(method空)を建てない（`else if reco`＋`else a=null`に修正）・`_ladderPrev`/`_manTot`も未選択行を除外。
- **sw.js**: `APP_CACHE` v16→v17。検証: `new Function`構文OK＋**実レンダー**（vendor+app01〜06を注入し`_elKabuLadderSimV2`をReactDOMでマウント→合成6記録）＝初期は未選択2行・（未選択）/↺リセット/ヒント表示、方式選択で▲▼ステッパ出現、株数入力で3列表（ヘッダ 日付/OS連鎖/E/**推奨α/採用α/シミュα**/実現/実α単独/シミュ/差・11列=本体11セル一致・推奨α5円/採用α9円/シミュα5円）を確認。

### 2026-07-03p 株数シミュ マスター表「シミュα」列を「推奨α」に改名＋採用α列の右へ移動
- **app-06.js**: `_kbMasterTable`（🧮 シミュタブのマスター表・手動/自動 両モード共用）のヘッダとボディで列名「シミュα」→「推奨α」に改名し、列順を「E→**採用α→推奨α**→実現…」に変更（従来は E→推奨α→採用α）。ヘッダ(`_mtTh`)とボディ(`_mtTd(alphaNode)`＝採用α と simA セル＝推奨α)を入れ替え。合計行は colSpan:6 領域内の入れ替えのため無改修。表示のみ・計算ロジック不変。
- 補足（この列の中身＝ユーザー確認済）: 「推奨α」列(`m.simA`)はその配分が各記録で使ったα。手動/自動とも第2取引が「推奨基本α値」方式のとき `recoOf(記録日)` ＝**その記録日時点の推奨基本α**（`_elKabuRecoBaseFn`→`_elPeriodWindows(baseRecs, 記録日, false)`＝前日まで・直近50→100→全期間の順で `_elBaseAlphaPick`）。**母数はシグナル別**（`baseRecs=_selSigRecs`＝銘柄×シグナル）で、日別ページの銘柄別記録テーブル/取引テーブル（`_elBaseAlphaPeriodBlockV2`＝銘柄のみ・全シグナル合算・表示日1点）とは**計算エンジンは同一だが母数と基準日が異なる**。
- **sw.js**: `APP_CACHE` v15→v16。検証: `new Function`構文OK・「シミュα」残0・ヘッダ順 採用α→推奨α を確認。

### 2026-07-03o 浮き足加算α値を第3のα要素に昇格（基本α＋浮き足加算＋追加α）＝旧・追加α根拠「底抜け前足浮き（数値根拠）」の置き換え
- **app-05.js**: 新規トップレベル `_elUkiYes`/`_elUkiVal`/`_elUkiAdd`（signal.ukiUsed=〇×/ukiVal=前足浮き値の生値・実効加算=floor(浮き値/2)＝半額切捨て。合計採用α alphaVal に畳み込み済み＝下流の損益/EP計算は無改修）。`_DEF_ADD_REASONS`から「底抜け前足浮き」削除。`_elAlphaBreakdownNode`を3要素化（例（10+浮8+5）・整合チェック=base+uki+add）。EntryRecordForm: `fUkiUsed`/`fUkiVal` state＋浮き足行（**基本α行と追加α行の間**・エントリーシグナルに「底抜け水準線OS」（`data.custom.ukiSignalName`で変更可・既定固定文字列）選択時のみ表示・〇×→〇で浮き値入力→「→＋N円を加算（半額・小数切捨て）」表示）・合計α値バーを3要素内訳に拡張（案B）・`_fAlpha=_fBaseA+_fUkiAdd+_fAddA`・保存 ukiUsed/ukiVal・旧数値根拠欄（fReasonVal入力・addAlphaReasonVal保存・addAlphaNumericReason改名追従）を撤去。
- **app-01.js**: `migrateData`末尾に浮き足移行`_migUkiAlpha`相当（**フラグ無し条件ベース・冪等**）: 追加α〇＋根拠に底抜け前足浮き＋数値>0 → ukiUsed=true/ukiVal=数値・根拠から当該名除去・単独根拠→追加α×(addAlphaVal=null)／複合→addAlphaVal=旧値−半額(下限0)・**alphaVal=基本α＋半額＋新追加αに再計算**（基本α導出不能なら合計据え置き）・custom.addAlphaReasonsマスターから削除・custom.addAlphaNumericReason廃止。数値未入力の底抜け根拠は変換対象外（手動調整）。
- **app-06.js**: `_elHasNumReason`を signal.ukiUsed 判定へ載せ替え（`_EL_NUM_REASON`廃止・関数名は互換で据え置き）＝シグナル内サブタブ（表記「浮き足/その他」に改名）・推奨追加α母数除外・上乗せ幅/根拠別除外がそのまま新判定で機能。`_elBaseAlphaPick`の母数から**浮き足〇も除外**（旧・追加α〇として除外されていた挙動を維持）。乖離表`_elBaseAlphaSimpleBoardV2`: 種別バッジ3種（浮き足〇=緑/浮＋追α〇）・現実/推奨行とも＋浮き足加算（「浮N」小書き）。浮き足専用分析`_elFloatReasonSectionV2`: 母数=浮き足〇（旧: 追加α〇＋数値根拠）・浮き値=ukiVal・%別シミュを`Math.floor`（現行50%ルールと整合）・_baseOf/_addOfは浮き足加算込みで導出・見出し/注記を浮き足表記へ。期間別表などの説明文「基本αは追加α〇・浮き足〇以外が母数」に更新。
- **app-08.js**: `_EL_NUM_REASON`同期を削除。**sw.js**: `APP_CACHE` v14→v15。
- 帰結（ユーザー決定）: 過去の底抜け記録の採用α（alphaVal）は「基本α＋浮き値の半額」ベースに変わる＝EP判定・シミュ・乖離も新ルールで再計算。実現損益(item.pnl等)の保存事実は不変。記録帳の扱いは「現行分析の載せ替え」（推奨基本α母数組込みは将来案）。
- 検証: `new Function`構文OK（vendor+app01〜08+sw.js）。実関数テスト＝移行6ケース（単独/複合/他根拠のみ/×/数値なし/旧addAlphaReason文字列）・冪等・マスター掃除・`_elUkiAdd(17)`=8・内訳（10+浮8+1）/（未選択）優先・`_elBaseAlphaPick`の浮き足/追加α除外・浮き足専用分析＆乖離表のレンダリング すべてpass（途中`_numReason`残存参照を検出→修正済）。実機確認はユーザー。

### 2026-07-03n 株数シミュの損益を本日の損益データ欄と同じ（）外/（）内方式に統一（案C・両方式併記）
- **app-06.js**: シミュの損益計算が期待度ブラインド（全記録H1保有＝△/×も満額算入で過大）だった問題を、本日の損益データ欄と同一基準へ統一。中核＝`_elKabuTierEval`のP&L行を`_elDynHold`→**`_elHold1TotParts(s,α,cut)`**（app-05・{main:（）外, ref:（）内差分}）に差し替え。返り値に`main100`（（）外＝○のみH1本算入・△/×/損切りはEP想定額）/`ref100`（（）内差分＝△・損切り済のみH1保有額との差）を追加（`pnl100`はmain100の別名で既存互換）。集計`_elKabuLadderCalc`に`recRef`/`sumRef`、自動配分オプティマイザ（インライン）に`_refSum`/combo`sumRef`を追加。表示＝マスター表の「シミュ」「実α単独」列・上下合計バー(`_mtBar`)・合計行(`_sumRow`)・取引内訳(`_mtTierNodes`)・手動「通算損益」カード・自動★最適/ランキングで**「（）外（（）内）」併記**（`_mtPnlNode2`/`_pnlNode2`新設・（）内は△/損切り済がある時だけ括弧表示）。「差」「上乗せ」は（）外基準。脚注も期待度ブラインド→（）外/（）内方式の説明に差し替え。AskUserQuestionで案C採用。検証: `new Function`構文OK。

### 2026-07-03m 株数シミュ マスター表に「シミュα」列追加＋「この配分」→「シミュ」改名
- **app-06.js**: `_kbMasterTable`に、その配分（シミュ）が各記録で実際に使ったα値 `simA`（tierの`c.t.a`＝「推奨基本α値」方式なら`recoOf(日付)`・複数tierは「/」連結・無ければ「—」）を算出し、**採用αの左に「シミュα」列**を追加＝この配分の損益が実α単独（採用α）と違う理由（日々再計算した推奨基本α≠保存済み採用α・初期記録はrecoOf=null→建たず「—」）を可視化。列見出し「この配分」→「**シミュ**」・手動バーのラベル「この配分・N株」→「シミュ・N株」に改名。合計行colSpan5→6・展開行colSpan9→10。ユーザー要望。検証: `new Function`構文OK。

### 2026-07-03k 補強: 新規エントリーフォームにOS3到達期待度（os3Exp）入力欄を追加（2026-07-03iの残タスク完遂）
- **app-05.js**: `EntryRecordForm`に os3Exp を入力・保存できるよう3点追加＝①state `fOs3Exp`（`useState(initSig.os3Exp||null)`・os2Exp stateの直後）②OS3列（`_legCol("OS3",…)`）に**EP=OS3の時だけ**「到達期待」ボタン行（`_ef.epIdx===2 ? _row("到達期待", _expB(fOs3Exp, setFOs3Exp, fOs2Exp==="×"), true) : null`＝OS1/OS2到達期待と同形・前段OS2が×なら自動×のdisabled）を高値/確定値の後・H1/H2期待の前へ挿入③保存に `sig.os3Exp = (_efS.epIdx===2) ? (fOs3Exp||null) : null`（EP=OS3の到達足のときだけ保存。EP≠OS3ではOS3はホールドでholdExp/hold2Expが担うのでnull）。編集時はstate初期値でinitSigから復元。これで2026-07-03iで配線済みの`_epLegs`(OS3 leg exp=s.os3Exp)/`_epExpAt`が実データで機能＝株数シミュでαを下げEPが手前化した時、元OS3足がその到達期待度をH期待度として表示。
- 検証: app-05はJScript非対応（既存・HEADでも同エラー）のため、CORSサーバ＋ブラウザ`new Function`で全文パースOK（構文エラー0）＋3編集が各1件で存在を確認。表示・保存の最終確認はユーザーが実機で。

### 2026-07-03j 株数シミュのデッドコード整理（案A=マスター表刷新で未使用化した4関数を削除）
- **app-06.js**: `_elKabuLadderSimV2`内の定義のみ・呼び出し0件の4関数を削除＝旧比較カード`_compareCard`＋専用セル`_cmpCell`＋同株数基準`_baselineAt`（→`_mtBar`が内部で`baseCalc`直接計算に置換）／旧「記録ごとの内訳」テーブル`_kbDetail`（→マスター表の展開行`_mtTierNodes`に置換）。いずれも2026-07-03h（案A刷新）で置換済み。`_origPnl`/`_mtBar`/`_mtTierNodes`/`_elKabuLadderCalc`/`_elKabuTierEval`/`_notesLine`は生存＝残す。表示・ロジックとも変化なし（純粋なデッドコード除去）。検証: Grepで4関数の呼び出し0件を再確認＋`new Function`構文OK。

### 2026-07-03i シミュでEP移動時の期待度を「記録時EP基準」で足に固定（案A・表示修正）
- **app-05.js**: 共有ヘルパー `_epExpAt(s, i)` を新設＝物理足iの期待度を記録時EP位置(`_epOwnAlpha`のαでEP判定)で固定割当（OS1/2＝α到達期待度os*Exp／記録時EP直後1本目=holdExp・2本目=hold2Exp／それ以外=leg.exp・無ければnull）。`_epOsChainCell`(旧3817)の期待度選択を`(i===epIdx+1?holdExp:...)`→`_epExpAt(s,i)`に変更＝シミュでαを下げEPが手前にずれても、新たにEP後ホールド化した元OS足に「記録時の別足用holdExp」でなく、その足自身の期待度を表示。`_epLegs`のOS3 legに`s.os3Exp`を配線（補強os3Exp用・現状フォーム未対応で不活性）。
- 据え置き＝OS値打ち切り側(`_elOsMaxFiltered`/`_elOsMaxCapped`/3466)の同型パターンは変更せず（採用αでは`_epExpAt`と同値で無変化・シミュ表示のOS連鎖には出ない・OS値集計を動かさない）。期待度はP&L計算(`_elDynHold`等・株数シミュ)に不使用＝損益不変。
- 前提訂正（調査Workflow）＝α到達期待度はOS1/2のみ(os1Exp/os2Exp)・OS3にはos3Expが元々無い・OS3以降の足の期待度はholdExp/hold2Expが「記録時EPからの相対」で担う。ユーザー選択=案A+補強。**残＝補強（新規エントリーフォームにOS3到達期待os3Exp入力欄を追加：EP=OS3記録でOS3がシミュで手前ホールド化した時の欠測を埋める）は別タスク（app-05フォーム＋2保存経路＝JScript非検証のため集中セッションで）**。検証: app-05はJScript非対応(既存・HEADでも同エラー)のため編集3件を目視でカッコ均衡確認。

### 2026-07-03h 株数シミュ「🧮 シミュ」タブを案A=マスター表レイアウトに刷新（記録詳細＋シミュ3列＋上下合計）
- **app-06.js**: `_elKabuLadderSimV2`に`_kbMasterTable(cfgCalc, cfgLabel, nShares)`＋補助`_mtBar`/`_mtTh`/`_mtTd`/`_mtPnlNode`/`_mtTierNodes`＋state`mtExp`を新設。1枚の表＝本日の損益データ欄準拠の列（日付/OS連鎖(期待度)`_epOsChainCell`/E`_epECell`/採用α`_elAlphaBreakdownNode`/実現`_elSignedVal`）＋シミュ3列（実α単独=採用α単建ての`_elKabuLadderCalc`recPnl／この配分=cfgCalc.recPnl／差）。行タップで取引内訳を展開（`mtExp`・元index基準の安定キー`oi`）。合計は上下の`_mtBar`（#0F766E）＋表内ヘッダ/フッタ合計行の計4か所。手動＝`_kbMasterTable(manCalc,…)`で旧`_compareCard`＋`_kbDetail`折りたたみを置換。自動＝`_selCb`（autoExp選択 or best）の配分をマスター表表示・ランキング行はインライン展開廃止しautoExpで選択（ラベル「▷ 表示/● 表示中」）。mtExp（マスター表展開）とautoExp（ランキング選択）は別state。
- 損益色＝当アプリ慣習（利益赤/損緑・`_elPnlColor`）。列多いためモバイルは`overflow-x`＋`minWidth620`。
- 決め方＝Workflow（調査3並列→設計4案→統合3案）＋show_widgetモック3案提示→**ユーザー選択=案A**。実装後にWorkflow敵対的レビュー3観点＝実バグ無し（「ソート後キー不安定」はstable sortで誤検出と判定・保険で元index基準キーに堅牢化）。残=デッドコード`_compareCard`/`_cmpCell`/`_baselineAt`/`_kbDetail`（別タスクで整理予定・害なし）。検証: `new Function`構文OK。

### 2026-07-03g 株数シミュをα値タブ④から独立タブ「🧮 シミュ」へ昇格（深掘りの右）
- **app-06.js**: `EntryLogView`の`_tabs`（銘柄選択時）末尾に`["sim", "🧮 シミュ"]`を追加＝深掘りの右。`view === "sim"`分岐を新設（ゾーン見出し＋`_elKabuLadderSimV2`・空ガードはα値タブと同形＝`_selSigRecs`）。α値タブの`_alphaSubs`から④株数シミュを除去（①〜③に純化）・`_alSel === "kabu"`分岐を削除（旧保存alphaSub="kabu"は`_alSel`がbaseへフォールバック＝無害）。母数は不変＝recs=`_selSigRecsScoped`（内訳スコープ）/baseRecs=`_selSigRecs`（シグナル全体）。
- ユーザー相談（場所で悩み）→AskUserQuestion（案A独立タブ推奨・タブ名「🧮 シミュ」）＋show_widgetモック2案で確定。検証: `new Function`構文OK・"kabu"残存はコメントのみ。

### 2026-07-03f 株数シミュ④ 自動配分の監査＋「元の損益」比較カードを追加（自動/手動両モード）
- 監査結果: 自動配分ロジックは**内部整合・損益バグ無し**（総当たりの合計損益＝展開「内訳」の合計／1段損益は`_elKabuTierEval`→`_elDynHold`で手動・全分析と同一／母数は`_v2recsAll`＝`_elInclTotal`適用済で他分析と一致／組合せ重複排除・推奨α不明の扱いも一致）。設計上の注記（バグでない）＝①第2取引αは記録ごとの日付時点推奨（列見出しは「推奨α」一語）②第1取引α上限は単一の推奨基本α−1なので日付時点推奨<の記録で「第1>第2」の深さ逆転あり（独立ポジション計算で損益は正）③「最適」は通算損益最大のみ＝構造的に低α・全展開へ寄る（リスクは損切り列のみ）④手動＝累積／自動＝配分(加算)でαが非単調の端でのみ差。
- **app-06.js**: `_elKabuLadderSimV2`に比較カード`_compareCard(simSum, simLabel, nShares)`＋セル`_cmpCell`＋同株数基準`_baselineAt(n)`（各記録を実際の採用α`aiOf(r).alpha`でn株・単建て＝`_elKabuLadderCalc`流用）＋`_origPnl`（母数のsigned realizedPnl合計・`_elIsEntered`/`_elSignedVal`＝実際の実現損益）を新設。自動＝`autoRes.best.sum`(nShares=totalN)／手動＝`manCalc.sum`(nShares=最大累積`_manTot`)で「実際のα単独／シミュ配分/上乗せ(シミュ−実際のα)」＋元の損益（実際・参考）を表示。スケール差（シミュ=総株数／実現損益=実際株数）を明示し誤解回避。
- ユーザー選択（AskUserQuestion）＝レイアウト案A(同株数比較＋元の損益2段)・比較基準=実際に採用したα単独。show_widgetモック提示。検証: `new Function`構文OK。

### 2026-07-03e 見出し下の定型説明文を全画面で非表示（「※」注記は残す）
- **app-06.js / app-04.js**: 見出しヘルパー（app-06 `_secH`・`_h`×2／app-04 `_hdr`）を「subが※で始まる文字列の時だけ表示」に変更＝記録帳・DayView分析の全セクション見出しの灰色説明を一括非表示・※注記/補足のみ残す（`(sub && typeof sub === "string" && sub.indexOf("※") === 0)`）。
- **app-06.js**: 記録帳の導入文（α分析導入・シグナル軸バナー・内訳バナー）＋α値タブのゾーン見出し説明（基本α/追加α/株数シミュ/共通ツールの4ゾーン・推奨値要約`_alBaseSum`/`_alAddSum`は残す＝subをnull/要約のみに）＋成立率目安の説明divを削除。
- **app-04.js**: DayView「📊α比較・深掘り」の見出し説明（stale＝理想α/期間推移に言及）を削除。
- 残したもの（ユーザー方針＝※は残す・操作/入力ヘルプは温存）: 「※」注記（5分足補足等）・設定画面のメンテ操作ヘルプ・入力フォームのα説明/ラベル・空状態メッセージ・カードのキャプション（KPIカードsub）。
- ユーザー要望（範囲＝アプリ全体・「※」は残す）。app-06は`new Function`構文OK。app-04はJScript非対応（既存の相性問題・HEADでも同エラー）のため編集2件はカッコ対応を目視確認（`_hdr`は条件ラップのみ・キャプションは1子要素＋カンマ削除）。

### 2026-07-03d 株数シミュ④手動ラダー: 取引ごとに入力方式（絶対値/推奨α±X/推奨基本α値）を選択可能に＋「基準」トグル廃止
- **app-06.js**: `_elKabuLadderSimV2`の手動ラダーを再設計。グローバルな「基準」トグル（`baseMode`/`baseOff`＝絶対値/推奨α±X）を廃止し、各取引（第N取引）が独立に入力方式`method`を持つ（`rows`=[{method,off,cum}]）。方式＝abs(絶対値でα○円)/reco(推奨α±X)/recobase(各取引日の推奨基本α値でN株・α入力なし)。`_manTiersOf`は記録ごとに各取引の実効α（abs=off / reco=推奨α+off / recobase=推奨α）を算出→α昇順ソート→累積差分で積み増し（reco系で推奨不明の取引はその記録では建てない）。行内の方式は`<select>`で変更、「＋取引を追加」は縦並びピッカーで方式選択（`addPicker` state）。旧「＋段を追加」の直し漏れも解消。
- ユーザー要望（案A＋取引ごと選択・追加時は縦並びピッカー）。show_widgetモック2回で確認。検証: `new Function`構文OK。

### 2026-07-03c 株数シミュ④ 推奨基本α値の表示＋「段N」→「第N取引」改名＋自動配分の第1取引αを0〜推奨基本α未満に
- **app-06.js**: `_elKabuLadderSimV2`。①自動配分に「推奨基本α値＝〇円」を表示（recoRefに`_elBaseAlphaPick(baseRecs)`を同梱キャッシュ＝①基本αタブと同じ単一値・status na時は「参考」）。②自動配分の第1取引α探索上限を固定0〜20→`_s1AlphaMax`=推奨基本α−1（＝推奨基本α未満・推奨不明時のみ0〜20フォールバック）。③UI表記「段1α/段1株/段2株(推奨α)」→「第1取引α値/第1取引株/第2取引株(推奨α)」、手動ラダーの「段N」→「第N取引」、内訳表ヘッダ/注記/最適配分サマリー/脚注の「段」も「取引」へ統一（`_kbDetail`/`_notesLine`は手動・自動で共有）。内部コメントの「段」＝tierは据え置き。
- ユーザー要望。検証: `new Function`構文OK。

### 2026-07-03b 集計タブ銘柄別パネル: 追加α母数トグル〇で基本α詳細を折りたたみ＋推奨追加α詳細データを新設
- **app-06.js**: `_elAddAlphaDetailV2(recs, aiOf)`を新設＝`_elBaseAlphaDetailV2`の追加α版（結論バー＋加算値別の総当たり[基本α＋1〜30円ごとの到達率/件数/損切り率/H1勝率/想定損益・★＝推奨]＋読み取り）。母数＝追加α〇（数値根拠=底抜け前足浮きは除外）で`_elAddAlphaReco`/`_kpiAdd`と一致。想定損益＝ΣH1損益（`_elBaseAlphaEval.pnl`＝`_elSimPnlByDay.sum`同値を流用）。
- `_groupPanel`の「🔬推奨基本α詳細データ」を条件分岐化＝`!_floatMode && osDistFil==="yes"`のとき基本α詳細を`_SNCollapse`で折りたたみ（要約はKPIカードに常時表示・母数固定`_baRecs`は不変）＋その下に推奨追加α詳細をフル表示。×+未選択/全記録/前足浮きタブは従来どおり基本α詳細をフル表示。
- ユーザー選択（AskUserQuestion）: 基本α詳細=折りたたみ（簡略版でなく）／追加α詳細=基本と同じ総当たり表を新規（既存`_elAddAlphaSectionV2`流用でなく）。
- 検証: `new Function`構文OK。

### 2026-07-03 OS値分布ヒストを0円から1円刻みに＋推奨基本α詳細データ表に0円行を追加
- **app-06.js**: `_elOsHistV2`の「0〜4円」帯を0/1/2/3/4の個別バーに分割（共有部品＝集計タブ「OS値の分析」/未達タブ「最高値の分布」/銘柄別パネル「OS値分布」の全3か所）。▲マーカーのキー対応も0〜24=個別バー（`String(markVal)`）に更新。25円〜は従来どおり帯（クリックで内訳展開）。
- `_elBaseAlphaDetailV2`のα別総当たり表を1〜20円→0〜20円に（`_lowSweep`を`[1,2,3,4]`→`[0,1,2,3,4]`・ラベルも「0〜20円」）＝集計タブ銘柄別パネル＋α値タブ①基本αの2か所に反映。
- 検証: `new Function`構文OK。commit 16832b7でpush済。

### 2026-07-02j αシミュレーター（独立機能）を全削除（ユーザー要望・既存の損益データ表の行αシミュ＋一括αと重複のため）
- **app-06.js**: `AlphaSimBody`／`AlphaSimulatorModal`／`_alphaSimBtn` の3関数を削除。記録帳見出しの`_alphaSimBtn`呼び出しも削除。**`_elOsTradeMini`は存置**（OS値別成績の展開・履歴ポップアップで使用／2026-07-02eのα値内訳＝基本+追加＋追加α〇黄ハイライトの修正はそのまま維持＝本日の損益データ欄と統一済）。
- **app-04.js**: DayViewのタブ配列から `["sim","🧪 シミュレーション",true]` を削除＋`tab==="sim"`の描画分岐を削除。
- **app-02.js**: WeeklyPnlPanel（今週の損益データ）見出しの`_alphaSimBtn`呼び出しを削除。
- **app-08.js**: App の `simInit`state／`window.__openAlphaSim`effect／`AlphaSimulatorModal`描画を削除。
- **sw.js**: `APP_CACHE` v13→v14。
- 備考: 下記2026-07-02e〜iのαシミュレーター関連の実装はこの削除で無効（履歴として残す）。**残存する「シミュレーション相当」＝本日/今週の損益データ表の行ごとα値・損切り値シミュ＋「一括 推奨基本α」（app-04・非永続）と、記録帳の推奨α/α別早見（集計）**。分割エントリー機能は完全に消滅（将来必要なら別途検討）。
- 検証: `new Function`構文OK（app-02/04/06/08）＋実アプリ起動でApp/DayView描画・sim識別子全undefined・_elOsTradeMini等の共有関数存置・DayViewに🧪タブ無しを確認、コンソールエラー0。

### 2026-07-02i αシミュ 各カードに「本日の損益データ欄」と同じ詳細行を常時表示（OS1〜3の上）
- **app-06.js**: `AlphaSimBody` の各記録カードに `_elOsTradeMini([r], _ai)` の詳細行（日付/時間/シグナル/α値[基本+追加内訳]/OS[連鎖＋最高]/E/取引/EP損益/H損益/実現損益＝本日の損益データ欄と同一）を**常時表示**。折りたたみ時（既定・6件以上）もヘッダ直下に見える＝一覧で詳細把握。展開時は詳細行を**OS1〜3の大きめ表示の上**に配置（順＝ヘッダ→詳細行→OS1〜3→分割エントリー→合計）。`detail`変数を追加し折りたたみ/展開の両returnに挿入。
- **sw.js**: `APP_CACHE` v12→v13。
- 検証: 実アプリ(3457)で7記録（既定折りたたみ）＝折りたたみカードに詳細行（α値/EP損益/H損益/最高/実現損益）表示・展開でEP損益(64)<OS1(296)<分割エントリー(338)<水準値(380)<合計(422)の順を確認、コンソールエラー0。

### 2026-07-02h αシミュ 分割エントリー列を1建て値α→5項目（株数/水準値/基本α/追加α/損切り）に拡張
- **app-06.js**: `AlphaSimBody` の分割エントリー列を、単一「建て値α」入力から**縦積み5項目**へ（上から 株数→水準値→基本α値→追加α値→損切り値）。既定は**実際の記録と同値**（`_tierDef`＝baseAlphaVal/addAlphaVal〇分/cutLine/levelPrice、旧記録はalphaValから逆算・株数は100）。tier構造 `{a}`→`{b,a,c,lv,sh}`。有効α=**基本α+追加α+水準線シフト(sim水準値−実水準値)**（OS記録が実水準線基準のため線移動を有効αに変換）、損切りはエントリーごと（`_entryCalc`）。P&L=`_elDynHold(s,有効α,その段の損切り)`×株数/100・○△×/損切×は結果。水準値は小数入力(`_toHankakuDecimal`)・シフト時は「線±N円シフト」注記。列幅116→120。`_miniStep`にdecimal引数追加・`_fieldLbl`新設。手仕舞い（H1）は記録どおりで不変。
- **sw.js**: `APP_CACHE` v11→v12。
- 検証: 実アプリ(3457)合成記録で 項目順・既定値(株数100/水準1200/基本3/追加2/損切10)・各項目の再計算(基本8→+1,200・追加0→+300・水準1198で線-2円シフト→+100・損切1で損切×-400・株数300で-1,200)・＋追加/🗑/↺・カード計/全体計を確認、コンソールエラー0。

### 2026-07-02g αシミュを分割エントリー版に全面刷新（単一α/記録ごと/段階の3モードを廃止し一本化）
- **app-06.js**: `AlphaSimBody` を全面書き換え。**期間タブ（最上部・下線タブ this日/今週/直近50/全期間）→銘柄タブ（ピル）→絞り込み（シグナル/追加α）** の順で母数を決める（「まず期間から」）。②以降＝**記録ごとのカード**（件数≤5で自動展開・6件以上折りたたみ`recOpen`）。各カード＝ヘッダ（日時/銘柄/シグナル/実際→シミュ/↺/✎）＋**OS1〜3を大きめ表示**（`_osBig`＝記録の実αでEP位置確定・値+期待度○△×/↑EP・`_vcol`色）＋**分割エントリー（横ならび列・各列に建て値α/株数の入力欄が縦に並ぶ`_entryCols`）**＝案B（各エントリーに建て値αを`_miniStep`で入力・株数±100・「＋」で列追加・2列以上は🗑）。**株数配分は記録ごと**（`recTiers`マップ・未設定=実α+100株が既定・↺で戻す）。各エントリー損益=**記録の損益計算どおり**`_elDynHold(s,建て値α,記録のcut)`×株数/100（手仕舞い変更不可）・○△×は`_elDynResult`/損切り。カード合計＋**全記録合計（実際→シミュ・差分・延べ株数）を上(sticky)と下(一番下)に表示**`_grandBarEl`。✎は`EntryRecordForm`モーダル（`save`必須）。**削除＝単一αモード・基本α別早見表(sweep)・旧記録ごとモード・旧段階エントリー(global tiers)・①母数ライブ一覧`_elOsTradeMini`呼び出し**（`_elOsTradeMini`関数自体は他所[OS値別成績/履歴]で使用のため存置）。
- **sw.js**: `APP_CACHE` を `sn-app-v10`→`sn-app-v11`（端末が新app-*.jsを確実に取得＝stale-while-revalidate対策）。
- 検証: 実アプリ(3457)に合成3記録をマウントし、期間/銘柄タブ切替・OS1〜3表示・分割エントリー＋/🗑/↺・建て値α/株数ステッパでの損益再計算（+300/+800→カード計+1,100・全体+2,500）・✎フォーム起動を確認、旧モード文言なし・コンソールエラー0。表示最終確認はユーザーが実機で。

### 2026-07-02f αシミュに「記録ごと」モード新設＝各記録の実値を既定にカード単位でシミュ
- **app-06.js**: `AlphaSimBody` に第3モード「記録ごと」（`simMode:"cards"`・トグルは単一α/記録ごと/段階エントリーの3並存）。③＝記録カード縦積み（レイアウトA+B自動＝件数≤5は全展開・6件以上は折りたたみ既定`_rcDefaultOpen`・ヘッダタップで開閉`recOpen`）。各カード＝ヘッダ（日付時刻/銘柄/シグナル/α実→シミュ/損益実→シミュ/実現損益/↺個別リセット/✎編集）＋縦フォーム行（水準線値・基本α・追加α・損切り＝「実際値表示→ステッパ±/直接入力(type=text+inputMode・`_toHankakuNum`/`_toHankakuDecimal`)/推奨・実際クイック」`_rcInRow`・**既定＝実記録と同値**（`_rcDef`＝baseAlphaVal/addAlphaVal(〇のみ)/cutLine/levelPrice・旧記録は合計αから逆算）・変更欄は枠色+黄背景）＋OS連鎖ライブ（`_epOsChainCell(s,有効α)`＝EP位置が入力に追従）＋手仕舞い深さピルEP/H1/H2（`_depPnl`＝`_elDynPlanned`/`_elDynHold`/`_elDynHold2`・既定H1）＋株数±100（既定100）＋足元4セル（EP/H1/H2損益 実→シミュ・選択深さに★・判定 実→シミュ`_judgeMark`）。**水準線値の変更＝線シフトΔ**（有効α=基本+追加+Δで再計算・OS記録値は元の線基準のため・シフト中は注記バッジ）。上書きは`recOv`マップ（key=`_rcKey`＝signal.id優先）・`_rcSim`が解決。②＝一括バー`_cardsBar`（推奨基本α/全±1/追加αなし・推奨/損切り推奨を全カードへ・↺全部実際値に戻す＝`_rcBulk`/`setRecOv({})`）。合計バー（実際→シミュ・株数/手仕舞い反映・差分円）を**カード群の上(sticky top0)と下**に配置`_rcTotBar`＋メトリクス4カード（到達率/損切り率/H1勝率/想定損益H1＝`_agg`をカードごとシミュ値で）＋α別早見`_sweepTableC`（基本α5〜20総当たり・追加α/損切り/線シフトは各カード値固定・**行クリック=全カードへ一括適用**）。✎編集＝`simEdit`stateで`EntryRecordForm`モーダル（`save`プロップ必須・無ければ非表示）。
- **app-06.js**: `AlphaSimulatorModal`が`save`を受けて`AlphaSimBody`へパススルー。
- **app-08.js**: App→`AlphaSimulatorModal`に`save: save`追加。**app-04.js**: DayView🧪タブ→`AlphaSimBody`に`save: save`追加。
- 検証: `new Function`構文OK（app-04/06/08）＋実アプリ(3457)で合成3記録マウント＝カード描画/ステッパ(α7→8)/手仕舞い★移動/↺個別・一括/全+1/線シフト(1200→1198でα7→5+注記)/折りたたみ/早見行クリック一括/✎フォーム開閉/単一α・段階回帰、全passコンソールエラー0。

### 2026-07-02e αシミュを銘柄タブ式に＋①ライブ一覧のα値内訳を本日損益データ表と統一
- **app-06.js**: `AlphaSimBody` の①の上に銘柄ピルタブ（`_stockTabs`＝全体`__all__`＋各銘柄・案A＝記録帳の銘柄タブと同形）を追加。母数`_stockOpts`（選択中期間の銘柄）＋選択中銘柄は常に表示（`_stkList`）・件数は`_byPeriod`基準（`_stkCnt`）。既存の①内「銘柄」ドロップダウンは役割重複のため削除（`stockFil`/`setStockFil`はタブが操作）。タブ切替`_pickStock`で②の手入力（`simBase`/`simAdd`/`simCut`）を空にリセット＝その銘柄の推奨（`_recBaseA`/`_recAddA`/`_recCutV`）が②へ自動入力。①見出しに「選択中：〇〇」を併記。既定は全体（`initial.stock||"__all__"`は不変＝呼び出し側が銘柄指定時のみその銘柄開始）。
- **app-06.js**: `_elOsTradeMini`（αシミュ①ライブ一覧／OS値別成績の展開／履歴ポップアップで共用）のα値セルを「本日の損益データ」表と統一＝`_elAlphaBreakdownNode(s, a)` の（基本+追加）内訳を追加＋追加α〇（`_elAddAlphaYes`）行は背景`#FEF3C7`ハイライト。OSの「最高」（`_epOsChainCell`→`_epOsMaxChainNode`）は元から表示済で不変。3呼び出し先すべてに反映。
- 検証: `new Function`構文OK（全app-*.js）。表示確認はユーザーが実機で。

### 2026-07-02d αシミュに段階エントリー（株数配分）モードを追加
- **app-06.js**: `AlphaSimBody` の②に「単一α／段階エントリー（株数）」モード切替（`simMode`）を追加。段階モード＝可変N段テーブル（`tiers`＝各段 {offset:水準線から+円, shares:株数}・±ステッパー`_step`・「＋段を追加」・2段以上は🗑削除）＋全段共通の損切り。各段のクイック入力＝推奨基本α（`_recBaseA`）/＋推奨追加（基本+`_recAddA`）/＋よく使う（基本+最頻addAlphaVal `_freqAdd`）。③は段構成を母数の各取引に当て `_ladderRec`＝段別損益（`_elDynHold`(off) per100 × 株数/100・到達/損切り/未達）＋記録ごとの計＋合計損益（現実の実現損益`_elSignedVal(realizedPnl)` vs シミュ合計・延べ株数）を表示。水準線`levelPrice`は損益未使用でα＝水準線からの円なのでoffsetをαとして再計算。非永続。単一αモードは従来どおり（現実vsシミュ/α別早見/記録明細）。
- 検証: `new Function`構文OK＋合成データで単一αモード（回帰）＋段階モード（トグルクリック→段テーブル/＋段追加/合計損益/記録別内訳）の描画を確認。

### 2026-07-02c αシミュ①に該当取引記録のライブ一覧を追加
- **app-06.js**: `AlphaSimBody` の①母数フィルタ内「→ 対象 N件」の直下に、選択中の母数`recs`を`_elOsTradeMini(recs, _ai)`（読み取り専用ミニ取引表）でライブ表示（marginTop6・maxHeight260のスクロール枠・recs空はnull）。期間/銘柄/シグナル/追加α状態を変えると`recs`が変わり即時更新。
- 検証: `new Function`構文OK＋合成データで①内に該当2件の取引行（時刻/シグナル/α/OS/損益）が描画されることを確認。

### 2026-07-02b αシミュレーターを日別ページの「🧪 シミュレーション」タブとして常設（本体を殻から分離）
- **app-06.js**: `AlphaSimulatorModal` を本体`AlphaSimBody`（①母数フィルタ/②条件/③結果の3セクションのFragmentを返す・殻なし）＋薄いモーダル殻`AlphaSimulatorModal`（`AlphaSimBody`をオーバーレイ＋×閉じるで包む）に分離。ロジック・計算は不変。
- **app-04.js**: DayViewのタブ配列(app-04:3773)に`["sim", "🧪 シミュレーション", true]`を「📋 取引」の右へ追加＋`tab==="sim" && React.createElement(AlphaSimBody, {data, initial:{date, period:"day"}})`をインライン描画（この日/今週/直近50/全期間を節内で切替＝「その週のシミュ」も可）。日別ページの本日/今週見出しに2026-07-02で付けた`_alphaSimBtn`（モーダル起動）はタブと重複するため撤去。
- 残す: モーダル(App/app-08 `window.__openAlphaSim`)＋`_alphaSimBtn`は app-02 WeeklyPnlPanel（ChartSection銘柄別）と app-06 記録帳ヘッダーで継続使用。
- 検証: `new Function`構文OK(app-04/06/08)＋合成データで`AlphaSimBody`インライン描画（①対象/③結果/α別早見・対象2件）＋モーダル殻描画を確認。

### 2026-07-02 αシミュレーター（独立モーダル）を新設
- **app-06.js**: `AlphaSimulatorModal({data, onClose, initial})`＝シナリオ型αシミュレーター（末尾に追加）。①母数フィルタ（期間 この日/今週/直近50/全期間・銘柄・シグナル(tag)・追加α状態 全部/〇/×のみ/未選択）②仮条件（基本α/追加α/損切りを±ステッパー＋推奨/理想ワンタップ＝推奨は`_elBaseAlphaPick`/`_elAddAlphaReco`/`_elCutPick`・理想は`_elIdealAlpha`/`_elIdealCut`の母数中央値）③結果（現実vsシミュのサマリー[到達率/損切り率/H1勝率/想定損益]＋α別早見[基本α5〜20を振り到達率/損切り率/H1勝率/件数/想定損益・★最大・行クリックで基本αに反映]＋記録明細[現実/シミュ2段で各記録のEP/H1/判定]）。母数＝`_elCollectAllSignals`→`_elFilterPeriod`等。集計は内部`_agg`（EP到達OS3まで・`_elDynPlanned`/`_elDynHold`/`_elPlanIsStop`/`_elHoldIsStop`）＋`_elBaseAlphaEval`/`_elSimPnlByDay`。非永続。`_alphaSimBtn(initial, label)`＝起動ボタン（`window.__openAlphaSim`経由）。
- **app-08.js**: App に`simInit` state＋useEffectで`window.__openAlphaSim(init)`を公開＋SettingsModalの後にモーダルを条件描画（`initial={period,date,stock,signal,addAlphaFil}`）。
- **起動ボタン配置**: app-02 WeeklyPnlPanel「📅今週の損益データ(銘柄)」見出し／app-04 DayView「📊本日の損益データ」「📅今週の損益データ」見出し／app-06 記録帳ヘッダー（📒エントリー記録帳の右）。各`_alphaSimBtn`でstock/date/periodを既定として渡す。
- 旧`VirtualAlphaCalc`（本日表内・単一α・旧計算式）は据え置き（別物）。
- 検証: `new Function`構文OK（app-02/04/06/08）＋合成データでモーダル全3セクション描画＋起動ボタン→`window.__openAlphaSim(init)`呼び出しを実マウントで確認。

### 2026-07-01f 推奨基本α「期間推移」セクションを全3か所から削除
- **app-06.js**: 推移グラフ`_elBaseAlphaTrendV2`の描画を全廃＝①集計タブ`_groupPanel`「🎯推奨基本α値（期間ごとの傾向）」②α値タブ基本αサブタブ「🎯推奨基本α 期間推移」③DayView`_elDayStockBenchV2`「📈推奨基本α 期間推移」(trendBlock)。各`_secH`＋グラフを除去（末尾要素はFragment/returnの閉じを調整）＋`_elDayStockBenchV2`の説明文とタブ概要コメントから期間推移の記述を削除。ユーザー指示（3か所すべて）。
- `_elBaseAlphaTrendV2`定義(1540)は呼び出し無しのデッドコードとして残置。推奨基本α詳細(`_elBaseAlphaDetailV2`)は各所に残す。
- 検証: `new Function`構文OK＋`_elBaseAlphaTrendV2`の描画箇所0件（定義とコメントのみ）。

### 2026-07-01e シグナル別パネルのOS値分布ヒストの棒クリックで取引一覧を展開
- **app-06.js**: `_groupPanel` のOS値分布ヒスト（`_elOsHistV2` 呼び出し）に `recs: _osFilRecs, aiOf: _ai` を追加＝各棒クリックでその帯（OS1〜3最高値＝`_elOsMaxAll`基準）の取引ミニ表（`_elOsTradeMini`）を直下に展開（`_elOsHistV2` 内蔵の `_histClickable` を有効化）。母数はvals（`_elOsStatsV2(_osFilRecs, _elOsMaxAll)`）と同じ `_osFilRecs`＝棒と展開内容が一致。25円〜の内訳展開は従来どおりトグルリンクで可能。
- 検証: `new Function`構文OK＋実マウントで6円バーをクリック→「OS 6円 の取引（2件）」ミニ表が展開することを確認（全22本クリック可能）。

### 2026-07-01d 本日/今週の損益データ表からOS値（中央/平均）列を削除
- **app-02.js**: WeeklyPnlPanel（今週の損益データ・単一銘柄）のサマリー表からOS値列（`_wkTh`ヘッダー＋`_sumRow`の`_td(_elOsMMCell(...))`セル）を削除。
- **app-04.js**: DayView/取引タブの「今週の損益データ」(`_wkRow`)と「本日の損益データ」(`_pbRow`)のサマリー表からOS値列（ヘッダー`_wkTh`/`_pbTh`＋各行のOSセル）を削除。展開行の`colSpan:12`は元々列数未満のクランプ運用のため変更不要。
- ユーザー指示。同パターンの本日/今週表に一括適用（[[feedback_apply_all_instances]]）。`_elOsMMCell`は記録帳(app-06)の分析表で継続使用のため定義は残置。明細(per-record)のOSチェーン列は対象外（サマリーの中央/平均列のみ）。
- 検証: `new Function`構文OK（app-02/app-04）＋「中央/平均」「_elOsMMCell」消滅を確認。

### 2026-07-01c 🔻底抜け表の乖離度を「理想の追加α」廃止→現実/推奨それぞれのOS差に
- **app-06.js**: `_elFloatReasonSectionV2` の乖離度列を刷新＝理想の追加α（`_elIdealAddForRec` winMin＝不要(0)/勝てず/+X円必要）の表示を撤去し、rowSpanをやめて2段それぞれに「到達最高OS−その段のα」を表示（現実行=`_devNode(osMax, 採用合計α)`／推奨行=`_devNode(osMax, 推奨合計α)`・＋到達/−未達）。ユーザー指摘「勝てず表現／理想が不要」への対応で論点ごと解消。idealPcts収集(下の📐%シミュ上限用)は不変。
- 検証: `new Function`構文OK＋実マウントで各行にOS差が出る・「勝てず/理想」非表示を確認。

### 2026-07-01b 推奨α・損切り・想定損益のEP到達判定を「OS2まで」→「OS3まで」に拡大
- **app-06.js**: EP到達の母数判定 `epIdx <= 1`（OS1/OS2）→ `epIdx <= 2`（OS1〜OS3）を4関数で統一＝`_elBaseAlphaEval`(推奨基本α/追加αの損切り率・H1勝率・到達率・合成スコアの母数)・`_elCutEval`(推奨損切り値)・`_elBaseAlphaDetailV2`の②母数集計・`_elSimPnlByDay`(想定損益ΣH1)。OS3でしか到達しない記録も「到達・分析対象」に含める。関連ラベル/コメントの「OS2まで/OS1〜2/OS1またはOS2」表記も「OS3まで/OS1〜3」へ統一(到達率フロア注記_EL_BASE_MIN_ERATE/_EL_ADD_MIN_ERATE・詳細データのインサイト文・推奨基本α期間推移の説明)。ユーザー承認で推奨損切り(_elCutEval)も一緒にOS3化。
- 期待度×(judge=x・見送り)の扱いは不変＝到達率(entered)の分子には入るが損切り率/H1勝率/想定損益/合成スコア(scN)には入らない(_elDynHold等がjudge≠okでnull)。実測 recX=entered1/scN0。
- デメリット(承知の上): 遅い(OS3)到達が母数に入り損切り率↑/H1勝率↓/想定損益悪化に振れうる・黒字ゲート(pnl>0)を割り推奨がna化しうる・到達率が甘くなり高αが候補に残りやすい・過去数値と不連続・推奨追加α/損切りも連動して変化。
- 検証: `new Function`構文OK＋OS3のみ到達の合成記録が entered1/scN1 で母数入り確認(旧は0)。

### 2026-07-01 🔻底抜け前足浮き表を現実/推奨2段へ刷新＋シグナル別パネルの母数トグルを全セクション追従
- **app-06.js**: `_elFloatReasonSectionV2`（🔻底抜け前足浮き）の上表を「%別・理想%分析」から**現実/推奨2段テーブル**へ全面刷新＝列＝日付(＋「記録」ボタンで`EntryLogCard`行内展開・`recCtx.expKey`で開閉)/種別/基本α/追加α(下に前足浮き値)/合計α/OS(=`_elOsMaxFiltered`＝OS1〜3・×で打ち切り)/乖離度(上＝`_elIdealAddForRec`の理想追加α〔不要(0)／勝てず／+X円必要〕と採用の過不足・下＝到達最高OS−採用合計αの`_devNode`)。推奨基本α=呼び出し元`_baPick`(`_elBaseAlphaPick`)を第5引数で受領／推奨追加α=前足浮き値×推奨%(下の📐%シミュ`_best.P`)。下の📐%別シミュ表は不変。呼び出し(3988)に`basePick`/`recCtx{expKey,setExpKey,onEdit,onGoDate}`を追加。
- **app-06.js**: `_groupPanel`の追加α母数トグル(`osDistFil`/`_addFilOf`→`_osFilRecs`)を全セクション追従へ拡大＝従来はKPI/OS分布のみ→詳細分析7ブロック(EP位置/累積損益/α感応度/時間帯/曜日/×見送り/△保有)と記録一覧も`_osFilRecs`に切替(パネル全体を1母数に統一＝従来KPIだけ〇除外・詳細は〇込みの不整合を解消)。`_addFilBar()`を詳細分析ブロック先頭と記録一覧直上にも再掲。推奨基本α(設計上つねに固定母数`_baRecs`)と🔻底抜け表(〇記録専用で常時表示)は追従対象外。
- 検証: `new Function`構文OK＋新コード参照ヘルパー19個の実行時定義確認＋合成の底抜け記録で2段レンダー/記録ボタン展開(`EntryLogCard`)まで実マウント確認・括弧バランス差分0。

### 2026-06-29b DayViewのα比較窓を件数ベースへ＋シグナル別パネルの遅延描画（体感速度）
- **app-06.js**: ①`_elDayStockBenchV2`（DayViewの「📊α比較・深掘り」表）の今週/今月列を件数ベース（直近50件/100件・前日まで・当日除外）へ＝他の分析表(`_EL_PERIOD_COUNTS`=25/50/100)と統一（カレンダー窓の取り残しを解消・前回保留分をユーザー判断で確定）。表ヘッダー/読み取り欄/説明文のラベルも更新。変数名wk/moは据え置き（描画差分最小化）。②シグナル別パネルの「詳細分析」を、#5のネイティブ`<details>`(閉でも子をDOM生成して計算)から新コンポーネント`_SNCollapse`の**遅延描画**(`render`関数を開いた時だけ呼ぶ＝閉じている間は7セクションを計算しない)へ＝stale無しで体感速度を改善。③`_apAllRows`(`_apCollectAll(data)`・全chart走査の純粋計算)を`useMemo([data])`でメモ化。

### 2026-06-29 アプリ全体の安全網（エラーバウンダリ・容量メーター・同期ステータス強化・Firebaseルール手順）
- **app-08.js**: ①Reactエラーバウンダリ`_SNErrorBoundary`（ES5プロトタイプ方式・class構文/JSXを使わないため）を新設し `ReactDOM.createRoot().render` で `App` を包囲＝どこかのコンポーネントが描画中にthrowしても白画面にせず「⚠エラー表示＋🔄再読み込み」に切替（過去の白画面事故の安全網。labelで場所名表示も可）。②同期バッジ(fbBadge)を強化＝オンライン/オフライン(`navigator.onLine`＋online/offlineリスナーの`_snOnline`)で「📴オフライン」表示、最終同期時刻(`_snLastSync`・`fbStatus`→okのuseEffectで更新)をtitleツールチップに。
- **app-04.js**: SettingsModalのメンテナンスタブ先頭に💾localStorage使用量メーター（全キー走査でbytes算出÷目安5MB・65%/85%で色変化＋満杯前の警告文。タブを開いた時だけ計測）。事後の保存失敗アラートだけだったのを事前見える化。
- **FIREBASE_RULES.md（新規）**: RTDBは`?auth=<fbSecret>`(Database secret)でルールをバイパスするため、ルールを`.read/.write:false`に締めてもアプリは動き第三者だけ締め出せる、という確認・推奨手順書。Storageを締めるには匿名認証が要る点も明記。コードからは現ルールを確認できないためユーザーがConsoleで要確認。

### 2026-06-28f シグナル別パネルのスリム化（2段構成＝汎用分析を折りたたみ・提案#5）
- **app-06.js**: `_groupPanel`（シグナル別タブのパネル）を2段構成に＝コア（KPI/OS値分布/推奨基本α傾向+詳細/🔻底抜け前足浮き/🗂記録一覧）は常時表示、汎用7ブロック（📍EP位置/📈累積損益/📉α感応度/🕘時間帯別/📅曜日別/🚫期待度×/🔺期待度△＝各専用タブにも全体スコープ版あり）をネイティブ`<details><summary>`「詳細分析（…）」に既定折りたたみ（React状態管理不要・閉時もDOMには存在）。並びはコア→詳細分析→記録一覧に整理。esprima構文OK。

### 2026-06-29e 追加α分析トグルをスクロール追従(sticky)化＋各セクション見出しに絞り込みバッジ
- **app-06.js**: ユーザー相談→案A採用（見た目のみ・計算/母数は不変）。記録帳の「追加α分析」トグル(`addAlphaFil`)の行を `position:sticky; top:0; zIndex:5`（白背景＋薄い影、絞り込み中は従来どおり橙#FFF7ED）にしてスクロールしても画面上部に貼り付いて見えるように。加えて`_secH`（セクション見出しヘルパー）に、トグルが効いている時だけ＝`addAlphaFil!=="all" && view!=="alpha" && !_isAllStock` の条件で現在の絞り込みバッジ「🔍〇要のみ／×不要のみ／未選択のみ」を見出し横に付与＝下方の見出しを読んでいても今の母数が分かる。**α値タブ（母数固定・トグル非適用）と損益＝全銘柄合算（トグル非表示）には付けない。** 検証＝preview `new Function`構文OK＋EntryLogViewにv2フィクスチャをmount→集計タブで〇要に切替→トグルのcomputed `position==="sticky"`・見出しバッジ3個出現・α値タブはバッジ0＆トグル非表示・ランタイムエラー0。sticky は祖先に overflow があると効かない可能性があるが、効かなくても従来の非固定挙動に戻るだけで無害。

### 2026-06-29d 推奨追加αの選定を段階フォールバック化（損切り0%→≤30%→最低率・黒字必須）
- **app-06.js**: `_elAddAlphaReco`（α値タブ②追加α・各銘柄期間別表・DayView「今日の推奨」で共用）の選定を2段→**3段フォールバック**に再設計（ユーザー方針）。全段共通の必須条件＝**黒字(想定損益ΣH1>0)かつscN≥`_EL_BASE_MIN_N`(3件)**（赤字の上乗せは推奨しない＝AskUserQuestionで「黒字優先」選択）。**第1**=損切り率0%(完全回避)の中で**最小加算**／**第2**=損切り率≤**`_EL_ADD_STOPRATE_2`(=0.30・新定数で調整可)**の中で**最小加算**／**第3**(それも無し)=黒字の中で**損切り率が最も低い**加算(同率は最小加算)。黒字加算が皆無→`improved:false`(推奨無し)。旧=「損切り0%→最小加算／無ければ黒字のみ→最小加算」。返り値の形(add/total/improved/stopRate/h1win/eRate/scN/pnl/sim/…2)は不変＝UI側(`_elAddAlphaPeriodTableV2`/`_alAddSum`/`_elBaseAlphaSimpleBoardV2`等)は無改修。次点＝選抜プール内で1番目より大きい最小加算(全段共通)。30%の閾値は`_EL_ADD_STOPRATE_2`を変えるだけで調整可。検証＝preview`new Function`構文OK＋`_elBaseAlphaEval`/`_elSimPnlByDay`をスタブして6シナリオ(第1/第2は最小加算・第3は最低率・黒字無し→推奨無し・境界30%含む・件数フロア除外)を実関数で全pass。

### 2026-06-29c α値タブをタブ内サブタブ式に再設計（基本α/追加α/共通ツールを分離）
- **app-06.js**: 記録帳📐α値タブで推奨基本αと推奨追加αが「ごっちゃ」な問題を解消（ユーザー選択=案C「タブ内サブタブ」。6案を分離性/情報階層/実装適合の3観点で審査し採用）。`view==="alpha"`分岐を、ピル型サブタブ`[① 基本α(青#0369A1)｜② 追加α(茶橙#9A3412)｜③ α早見・ツール(グレー#64748B)]`で別画面に分離する形へ再構成（新state `alphaSub`=base/add/tools・`_uAlS`、アクティブピルはゾーン色で塗る）。各サブタブ冒頭にゾーン見出し帯＋推奨値サマリー（基本=`_alBaseSum`／追加=`_alAddSum`。いずれも`_elBaseAlphaA(_v2recsAll,_ai)`由来＝B1結論バー/A群と同値で二重表示のズレ無し）。割り当て＝基本α:成立率の目安(`_elOsAlphaPctlTableV2`)+詳細(`_elBaseAlphaDetailV2`)+期間推移(`_elBaseAlphaTrendV2`)／追加α:期間別(`_elAddAlphaPeriodTableV2`)+分析(`_elAddAlphaSectionV2`)／ツール:意思決定表(`_alphaTable`)+感応度カーブ(`_elAlphaCurveSectionV2`)。混在の発生源だった`_elBaseAlphaSummary`(基本α期間まとめ)の「追加α目安」カードを撤去＋insightの追加α文を「（追加αは「② 追加α」タブで）」へ差し替え（L2749/L3893のシグナル別・深掘りパネルにも一律反映）＋未使用化した`var add`を削除。計算ロジックは不変（表示の再配置のみ）。検証＝CORSサーバ+preview `new Function`構文OK＋全バンドル(vendor+app01〜07)ロード→EntryLogViewにv2フィクスチャをmountし 基本/追加/ツール 3サブタブをクリック巡回でランタイムエラー0を確認。

### 2026-06-28e 記録帳タブの仕上げ（時間帯/曜日を指定期間追従・死コード整理・トグル強調&損益で非表示）
- **app-06.js**: ①期間タブの🕘時間帯別/📅曜日別を、指定期間(custom)モードのときその日付範囲(`_timeScope`=cFrom/cToで絞る)に追従（従来は全体`v2recs`固定のほころびを修正）。②死コード整理＝旧「OS連鎖」タブ専用で孤児化した`_elOsLegsSectionV2`(85行)を削除＋未使用state(`listExclOnly`/`apKindFil`/`apNameFil`/`calYM`)を削除（`_elOsChainSection`はDayView(app-04 L5347)で使用継続のため残置・`setSelDate`は銘柄/損益ボタンのリセットで使用のため`selDate`は残置）。③追加α分析トグル＝絞り込み中(`addAlphaFil!=="all"`)は橙背景＋枠線で強調し注記を「🔍〇要のみで母数を絞り込み中（「全部」で解除）」と明示。④「損益(全銘柄合算)」(`_isAllStock`)ではトグル非表示＋損益ボタン押下で`addAlphaFil`を"all"へリセット（全体額が部分額に紛れるのを防止）。⑤シグナル別パネルのスリム化は保留（後日）。esprima構文OK(346,590 bytes・約7KB減)。

### 2026-06-28d 追加α分析トグルをサブタブバー直下へ移動
- **app-06.js**: 記録帳の「追加α分析」母数トグル(`addAlphaFil`・全部/〇/×/未選択)を、銘柄タブとサブタブバーの「間」から「サブタブバーの直下（内容の真上）」へ移動＝母数フィルタを絞り込み対象の内容に隣接させ、α値タブ切替時のレイアウトシフトも軽減。効くタブ(`view!=="alpha"`で表示)は現状維持。esprima構文OK。

### 2026-06-28c 記録帳タブ内容の配置整理（期待度×/△・EP位置→深掘り／時間帯別・曜日別→期間／OS値のα目安→α値）
- **app-06.js**: 配置監査で判明した「集計タブの肥大」を是正＝集計(銘柄別 `_sumStockContent`)から以下を移設。①🚫期待度×(`_elXSkipSectionV2`)・🔺期待度△(`_elTriangleHoldSectionV2`)→🔬**深掘り**(期待度キャリブレーションの隣＝"期待度の精度"3兄弟を集約)。②📍EP位置(`_elEpPosSectionV2`)→🔬**深掘り**(計画EP vs 実エントリーの執行乖離の隣＝執行ペア)。③🕘時間帯別(`_elTimeOfDaySectionV2`)・📅曜日別(`_elDowSectionV2`)→📆**期間**(期間IIFEの後にスコープ全体 `v2recs` で append)。④OS値の分析(`_elOsSectionV2`)内「成立率の目安(OS→α分位)」を新関数 `_elOsAlphaPctlTableV2(recs)` に切り出し📐**α値**タブへ(集計のOS分析からは `aTable`＋"α設定の目安"insight行を削除・OS本体/分布/帯別成績は集計に残置)。シグナル別 `_groupPanel` と期間行展開 `_detailOf` の各セクションは設計上の重複として不変。集計タブの残り＝KPI/OS値の分析/累積損益/連勝連敗DD。esprima構文OK。

### 2026-06-29b 本日αボードのメインを3列横並び化／推奨α欄をマスター全銘柄に拡張
- **app-06.js**: `_elBaseAlphaSimpleBoardV2` のメインブロックを縦3行→3列グリッド(`repeat(3,1fr)`)に変更（ユーザー選択=案A）。`_baseNode`/`_addNode`/`_cutNode` の `big` 表示を「値(20px)＋補足(11px)の縦積み」に再構成（基本α=次点／追加α=計X・次点+Y／損切り=参考のみ）。`_lbl` をインライン(minWidth70)→ブロックの上ラベルへ。サブ行(`big=false`)のインライン表示は不変。
- **app-04.js**: 本日αボードの対象銘柄 `_alphaBoardStks` を「本日エントリー銘柄のみ／無ければフォールバック」→**マスター登録の全銘柄**(`data.custom.stocks`||`_DEF_STOCKS_FROZEN`)に拡張（ユーザー選択）。本日エントリー銘柄(_pbStks)を先頭、その後にマスター順の残り（本日取引なし）。日経平均株価＝指数(エントリーα対象外)は除外。本日取引なし時のバナー文言も更新。`_elStocksWithV2Before`(app-06)は不使用化（定義は温存）。検証＝app-06は全体JScript構文OK・app-04はES3誤検出で全体不可のため追加分を単体構文OK＋HEAD比較で同一エラー（＝編集起因でない）を確認。

### 2026-06-29 取引テーブルの推奨基本α値欄に推奨損切り値を併記（期間別表に列追加＋ボード/🎯ブロック）
- **app-06.js**: 推奨基本α(`_elBaseAlphaA`/`_elBaseAlphaPick`)を表示する各所に推奨損切り値(`_elCutPick`の`cut`＝実現H1損益をほぼ維持できる最小タイト損切り・10〜30円)を併記（ユーザー要望「値のみ簡潔に」）。新ヘルパー `_elCutPickCell(recs,aiOf)`（期間別表セル用・値のみ・na=参考・データ無し—・赤系で基本α青と区別）。(A) `_elBaseAlphaPeriodTableV2` に「推奨損切り」列を推奨基本αの右へ追加（主行=`_elCutPickCell(pd.recs,aiOf)`・次点行はαの2番手なのでdash・ヘッダ8列化）＝ChartSection(app-02)/DayViewチャートタブ/取引テーブルの共用表すべてに反映。(B) `_elBaseAlphaSimpleBoardV2`（取引タブ・本日のαボード）にローカル`_cutNode(p,big)`を新設しメインブロックに「推奨損切り」行・サブ行に「切」を追加（窓は`W.periods[1|sd.i].recs`＝基本αと同窓）。(C) `_elBaseAlphaDayBlockV2`（🎯ブロック）はheadに`recs`を保持し見出しに「推奨損切りN円」を併記。母数＝`_elPeriodWindows`が`_epIsV2&&_elInclTotal`で絞り済みのpd.recs（追加α〇も含む全記録＝フォームの推奨損切りと同母数。基本αだけ〇を除外する点は既存設計どおり）。説明文にも「推奨損切り＝…」を追記。JScript `new Function` 構文OK。

### 2026-06-28b 記録帳タブ整理（出現・一覧テーブル削除／未達を損切りの右・期間を集計の右へ並べ替え）
- **app-06.js**: EntryLogViewのサブタブ`_tabs`(非_isAllStock)を `集計→期間→α値→損切り→未達→シグナル別→深掘り` に再構成＝「📡出現(appear)」「🗂一覧(list)」を削除し、未達(miss)を損切り(stop)の右、期間(period)を集計(sum)の右へ移動。`view==="appear"`本体分岐とデフォルト`else`(=一覧listの本体)を削除（miss分岐を閉じる`}`は保持）。タブバッジ`cnt`を`miss=_missCnt`のみへ簡素化（list/dateの死分岐を除去）、追加α分析トグルの表示条件を`view!=="alpha"`へ簡素化＋注記から「出現・一覧」削除。⚠️旧記録(非v2)は一覧タブだけで表示していたため記録帳から非表示に（旧記録oldCnt案内文も撤去・oldCnt計算は据え置き）。`_elMissSectionV2`等の本体関数は不変。esprima構文OK。

### 2026-06-28 底抜け前足浮き分析をシグナル別へ移設／OS連鎖・カレンダータブ削除／OS値分布に推奨基本αマーク
- **app-06.js**: (A) `_elAddAlphaSectionV2`（α値タブ）の「🔻底抜け前足浮き（数値根拠）」専用ブロック(旧floatNode)を新トップレベル関数 `_elFloatReasonSectionV2(recs,aiOf,data,secH)` に切り出し、シグナル別パネル `_groupPanel`（推奨基本α詳細の直後）へ移設＝そのシグナル(底抜け水準線OS)の記録だけが母数（`_baRecs`＝追加α分析トグル非依存）。floatRecs(追加α〇かつ数値根拠を含む記録)が無ければnull＝該当シグナルのタブにだけ出る。α値タブの④/⑤根拠別除外(`_elHasNumReason`/`!== _numReason`)は不変。(B) 記録帳サブタブの `_tabs` から `["date","📅 カレンダー"]` と `["oschain","🔗 OS連鎖"]` を削除＋`view==="date"`/`view==="oschain"` の本体分岐も削除（else-ifチェーンは sum→alpha→stop→signal→period→deep→appear→miss で連結。`_elOsLegsSectionV2`/`_elOsChainSection`/カレンダーstate(calYM等)は未使用化＝据え置き）。(C) `_elOsHistV2` に `markVal` propを追加＝現在の推奨基本α(`_elBaseAlphaPick(...).alpha`)に当たるOS値帯を青字+▲マーカー+青枠で強調（注記「▲青字＝現在の推奨基本α（N円）」）。配線は描画2箇所＝`_groupPanel`のOS値分布(markVal=`_baPickAlpha`・`_baRecs`基準)と`_elOsSectionV2`(集計タブ・recs基準)。未達タブの`_elOsHistV2`はmarkVal無し。esprimaで構文検証OK。

### 2026-06-22 α値セクション再設計（見立て廃止→追加α〇×一本化／推奨追加α二プール／推奨損切り合成スコア）
- **app-05.js**: `_elAddAlphaUsed`（追加α〇判定＝signal.addAlphaUsed優先・旧記録はaddAlphaVal>0）を新設（`_elInclTotal`近く）。EntryRecordFormの「α到達の見立て(reachPred ◎○△×)」セクションを廃止し「α値」セクションを縦積みに再構成＝基本α値（右に推奨基本α）／追加α値（〇×トグル→〇選択時のみ数値＋推奨追加α）／合計α値／損切り値（右に推奨損切り）／αメモ。新state `fAddAlphaUsed`（既定×）・signal.addAlphaUsed保存。合計αは〇選択時のみ追加αを算入。フォームのreachPredバリデーション/保存(signal.reachPred)/state(fReachPred)、表示ヘルパー`_reachPredBadge`/`_reachPredHead`/`_RP_CELL_COLORS`/`_RP_CELL_TITLE`、EntryLogCardの「到達見立」チップを削除。`_refBaseAlpha`/`_refAddAlpha`(旧_refAlphaAux)/`_refCutPick` useMemoで参考値を算出。**追記(1001ed5)**: 追加α〇選択時に「根拠（理由）」欄を表示＝選択制(**複数選択可**・22dで単一→複数化)・既定`_DEF_ADD_REASONS`(指標線支え/底抜け前足浮き)＋「その他」テキスト・選択肢の追加(window.prompt)/削除(✎編集モード)は`data.custom.addAlphaReasons`に保存・記録は`signal.addAlphaReasons[]`(旧`addAlphaReason`文字列も後方互換で読込・マスター外の値は「その他」テキストとして復元・複数のその他は / 区切りで1欄)。推奨基本αの表記を「推奨基本α値　1週間：N円　1カ月：N円　3カ月：N円　全期間：N円」に修正。
- **app-06.js**: `_elBaseAlphaPick`の母数から追加α(〇)記録を除外。`_elBaseAlphaA`を二プール化＝基本αは「追加α無し」母数・推奨追加αは「追加α〇」母数で算出。`_elBaseAlphaSummary`/`_elBaseAlphaDetailV2`も`_elBaseAlphaA`経由に。推奨損切り`_elCutPick`/`_elCutEval`/`_EL_CUT_CANDS`(5〜30円)を新設＝実現H1損益の平均が最大の損切り値を主軸選定（損切り回避率/H1勝率は併記）。`_elExpCalibSectionV2`の(A)α到達予想◎○△×の的中検証(`_rpActual`等)を撤去しH期待のみに。EntryLogViewの到達見立列(colN 12→11)を削除。
- **app-06.js 追記(2026-06-22b)**: `_elBaseAlphaPick`の件数フロアを固定3件→**実データ連動**(`_EL_BASE_MIN_FRAC`=0.5＝最も件数scNの多いαの半分以上・最低3件)に変更し、その帯の中でスコア最大を選定。高αは到達率低下で標本が薄く「いいとこ取り(選抜バイアス)」でスコアが上振れる問題を是正(例: 件数10件の19円が選ばれる→13円)。旧の僅差±EPS同点→到達率優先のタイブレークは撤去(同点は件数最大→低α)。
- **app-06.js / app-02.js 追記(2026-06-22c)**: 推奨基本αに**到達率フロア**(`_EL_BASE_MIN_ERATE`=0.5＝EP到達率(OS2まで)50%未満のαは推奨対象外・約定しにくい高αを除外)を追加。**`_elBaseAlphaPeriodTableV2`新設**＝recsをrefDate以前に絞り直近1週/1か月/3か月/全期間で推奨基本α(`_elBaseAlphaA`)を出す期間別表(期間/推奨基本α/損切り率/H1勝率/到達率/件数)。**第4引数includeToday=trueで先頭に「本日」(=refDate当日)行を追加(app-02のChartSection表のみtrue・DayView早見ブロックは前日まで据え置き)2026-06-24**。**`_elBaseAlphaPeriodBlockV2(data,stock,refDate)`新設＝「α 推奨基本α値（銘柄・期間別）」ブロック(見出し＋説明＋_elBaseAlphaPeriodTableV2・includeToday=true)をChartSection(app-02)と取引タブ本日損益データ(app-04)で共用＝同じ見た目に統一。取引タブは旧クロス銘柄表`_elBaseAlphaTableV2`→取引した銘柄ごとに期間別ブロックを並べる形へ作り直し(`_pbStks.map`)。`_elBaseAlphaTableV2`は今週欄/WeeklyPnlPanelで継続使用 2026-06-24**。app-02のChartSection銘柄別記録の推奨基本α欄を「この日の記録だけ」→「その銘柄の前日まで（当日は含めない）の全履歴を期間別」に差し替え(`_elCollectAllSignals(data).filter(stock)`・aiOf=`_elAlphaInfo`)。各`_elBaseAlphaTableV2`の注記も新方式(件数フロア＋到達率50%)に更新。**フォーム(EntryRecordForm)修正(2026-06-22c)**: `_refBaseAlpha`の全期間・`_refAddAlpha`・`_refCutPick`の母数を fDate(その日)の前日までに統一(当日を含めず・過去記録の編集時に未来/当日データを使わない＝look-ahead回避・depsにfDate追加)。推奨基本αの母数は`_elBaseAlphaPick`内で追加α〇を除外（母数＝×＋未選択＝追加αを使っていない記録。未選択はaddAlphaVal無しで基本αのみ＝2026-06-24に未選択も算入へ変更。旧「×記録のみ」から拡大）。**基本α自動入力の既定基準を1週優先→1か月優先に変更(ユーザー方針)**＝`_defBaseA`=m1→m3→all(1週間は標本が薄くブレやすいので自動入力には使わず表示のみ)。
- **想定損益列＋推奨追加α期間別表 追記(2026-06-24c・commit後)**: 期間窓ロジックを **`_elPeriodWindows(recs,refDate,includeToday)`** に抽出(前日まで+includeToday時は先頭「本日」窓・基本α表/追加α表で共用)。**`_elSimPnlByDay(recs,aiOf,alpha)`**=指定αを母数に当てたH1想定損益(`_elBaseAlphaEval`のpnlと同基準=OS1〜2でEP到達しH1判定可能な記録のΣ`_elDynHold`)と異なる営業日数→{sum,n,days,avg}。**`_elSimPnlCell(sim)`**=「+X円/日（計+Y円・N日）」セル(利益赤/損緑)。**`_elBaseAlphaPeriodTableV2`に「想定損益(1日/累計)」列追加**＝推奨基本αを母数(追加α〇以外=×・未選択)に当てた値。営業日＝エントリー成立した異なる日(記録の無い日・ノーシグナル日は除外)。**`_elAddAlphaPeriodTableV2(recs,aiOf,refDate,includeToday)`新設**＝推奨追加α値の期間別表(母数＝追加α〇の記録だけ＝基本α表と別プール)。各期間で「基本α＋推奨追加α(`_elBaseAlphaA().add`)」を〇記録に当てた損切り率/H1勝率/到達率/件数/想定損益。改善なしは+0(基本αで十分)・〇記録の無い期間は—。`_elBaseAlphaPeriodBlockV2`の下段に追加表示(ChartSection app-02/DayView app-04の両方に出る)。想定損益列は`_elBaseAlphaPeriodTableV2`共用のためDayView早見`_elBaseAlphaDayBlockV2`にも出る。ユーザー方針＝推奨追加αは〇記録のみが母数なので基本α表とは別欄。JScript(UTF-8/ADODB.Stream)でapp-06.js全体PARSE_OK。
- **根拠別の追加α分析＋根拠の数値入力 追記(2026-06-24d)**: 新規記録(EntryRecordForm app-05)の追加α根拠で、数値欄を出す根拠名=`data.custom.addAlphaNumericReason`(既定「底抜け前足浮き」・`_renameR`で改名追従)を選択中のとき、その選択肢にインライン数値入力(追加α欄と同じ`_toHankakuNum`半角強制・幅32px)を表示→`signal.addAlphaReasonVal`(円)に保存(`fReasonVal`state)。**app-06に`_elIdealAddForRec(s,base,cut)`新設**=各記録の理想の追加α＝winMin(損切り回避&H1黒字の最小追加α・無→null=勝てず)＋fillMax(EP到達を保てる最大追加α=約定上限・αを上げるほどEP難化のため約定観点は上限で表現)。**`_elCorr(pairs)`**=ピアソン相関。**`_elAddAlphaSectionV2`に⑤「根拠別の追加α明細」追加**=母数poolを根拠別にまとめ、各根拠ヘッダに件数/推奨追加α(`_elAddAlphaReco`を根拠の〇サブプール×基本α基準で算出)/前足浮き中央＋記録明細表(日付/実際/理想①勝つ最小/約定上限/[数値根拠のみ]前足浮き/H1損益)＋数値根拠は前足浮きvs理想追加αの相関(`_elCorr`)を併記。記録帳α値タブの追加α分析に出る。JScript(UTF-8)でapp-06.js全体PARSE_OK＋app-05追加分snippet単体PARSE_OK。
- **追加α〇/×全体トグル＋数値欄ステッパー 追記(2026-06-24e)**: 根拠の数値欄(app-05)に基本/追加α欄と同じ`_stepBtn`(▲▼長押し連続増減)を追加。**EntryLogView(app-06)に「追加α分析」全体トグル(全部/〇/×/未選択)を新設**(`addAlphaFil`state・銘柄タブの下に配置)＝`v2recs`を`_v2recsAll`(銘柄/期間で絞ったv2算入・トグル非適用)と`v2recs`(トグル適用＝addAlphaFilで_elAddAlphaYes/No/Unset絞り)に分離。集計・損益・OS値・損切り・シグナル別等の分析タブは`v2recs`(絞り込み)に従う。`_stockAllV2`(集計今月)も同トグルで絞る。**推奨基本α/追加αタブ(view==="alpha")と`_alphaTable`は`_v2recsAll`を使い母数固定＝トグル独立**(ユーザーQ3=推奨α母数は現行維持・〇モードで空になるのを回避)。シグナル別`_groupPanel`の埋め込み推奨αはグループrecs由来でトグルに従う(許容)。記録帳α値タブ推奨追加α表は`_elAddAlphaPeriodTableV2(_v2recsAll,...)`。app-06全体PARSE_OK・両ファイルCRLF。
- **チャート画面レイアウト移動＋次点(2番目)推奨α 追記(2026-06-24f)**: (A)ChartSection(app-02)の📝メモ欄をタグの下/エントリー記録の上へ・今週の損益データ欄(WeeklyPnlPanel)をエントリー記録(＋追加)の下/推奨基本α値欄の上へ移動。新順=タグ→メモ→エントリー記録→今週の損益→推奨基本α→出現。(B)**次点(2番目の推奨α・1番目より大きい値限定)を全箇所表示(ユーザー方針)**。`_elBaseAlphaPick`に`alpha2`(同フロア候補で1番目より大きいαのスコア最大)、`_elAddAlphaReco`に`add2`/`total2`(1番目より大きい加算でスコア最大・ループをcands収集に改修)を追加。表示ヘルパー`_elReco2Node(text,fontSize,color)`=1番目の真下に（）で同サイズ小書き。適用先=`_elBaseAlphaPeriodTableV2`/`_elAddAlphaPeriodTableV2`/`_elBaseAlphaSummary`(cards)/`_elBaseAlphaDetailV2`(concl)/`_elBaseAlphaTrendBody`/`_elBaseAlphaTableV2`(_aLine+追加α)/`_elBaseAlphaDayBlockV2`/`_elAddAlphaSectionV2`(recoNode+根拠別はインライン「（次点+X円）」)＋フォーム(app-05 _refBaseAlpha.pickOfにalpha2/各期間インライン（）・_refAddAlpha表示にインライン（次点+X円）)。app-02 reorder snippet・app-06全体・app-05 form snippet すべてPARSE_OK。
- **推奨追加α再設計＋次点の全データ行＋表記統一 追記(2026-06-24g)**: ユーザー指摘「追加αを付けたのに想定損益がマイナス／本日1件で推奨無しは変／概念が間違い」。**`_elAddAlphaReco(recs,aiOf,baseAlpha)`を再設計**(baseScore引数廃止)＝合成スコア改善でなく「損切り率0かつ想定損益(ΣH1)>0になる**最小加算**」を返す(無ければ想定損益>0の最小・それも無ければimproved:false=推奨無し)。`_elBaseAlphaEval`+`_elSimPnlByDay`を加算ごとに評価し損切り率/H1勝率/到達率/件数/想定損益と次点(1番目より大きい最小加算)を同梱。**`_elBaseAlphaA`はpick.status="na"でも追加αを算出**(件数フロアgate撤廃・本日1件でも参考表示)。これで追加α表の想定損益はbase+推奨追加αの**プラス値**に(例:本日 記録09:21 基本12→推奨追加+13(計25)で損切り回避→想定+2,700円)。`_elBaseAlphaPick`返り値にeRate2/scN2追加(次点行用)。**期間別表(基本α/追加α)は次点を点線区切りの全データ行で表示**(_elBaseAlphaPeriodTableV2/_elAddAlphaPeriodTableV2をforEachで主行＋次点行push・次点なしはα列に「（次点なし）」)。次点表記は全箇所「（次点：〇円）」/「（次点なし）」(_elReco2Node)・今日の推奨(_elBaseAlphaDayBlockV2)は次点横並び。推奨追加α=改善なしは「推奨無し」表示。波及修正=詳細結論バーのスコア表示→想定損益・根拠別`_elAddAlphaReco`呼び出しのbaseScore引数除去。app-06全体PARSE_OK・CRLF維持。⚠損切り判定/損益はユーザー確認済みで現状(高値ベース・高値までの逆行幅)を維持。
- **追加α5円刻み＋期間区切り線＋底抜け前足浮きを分析除外 追記(2026-06-24h)**: (A)`_elAddAlphaReco`の加算ループを1円刻み→**5円刻み**(add=5;add+=5＝5/10/15/20/25/30)＝ユーザー方針→**2026-06-25再び1円刻み(add=1;add+=1＝1〜30)＝ユーザー方針。注記の「+5〜30円（5円刻み）」も「+1〜30円（1円刻み）」へ。③上乗せ幅別分析は記録の実add値で集計のため不変**。(B)期間別表(基本α/追加α)の主行に`i>0`で`borderTop 2px solid #7DD3FC`を全セルに付与＝**期間どうしの明確な区切り線**(次点行はdashed据え置き)。(C)`_elAddAlphaSectionV2`で**底抜け前足浮き(=_numReason)を根拠別④/⑤から除外**(rKeys/detKeysを`.filter(k!==_numReason)`)＋専用ブロック`floatNode`新設＝各〇記録で「実際の追加α→さらに何円足せば損切り回避&H1黒字だったか(_gapOf=実際の合計αから+0..30をsweepし損切り回避&hd>0の最小・不要(0)/足りず)」を 日付/前足浮き値/実際の追加α/さらに必要/H1損益 の表で表示。_numReason定義を④の前へ移動。app-06全体PARSE_OK・CRLF。
- **DayView早見ブロックを取引テーブル並みに充実 追記(2026-06-24i)**: `_elBaseAlphaDayBlockV2`（DayViewチャートタブの銘柄クイック参照表の下に出る「🎯 推奨基本α値」ブロック・app-04.js:3947で呼出）を、取引/損益テーブルの`_elBaseAlphaPeriodBlockV2`と同等に拡充＝(1)基本α/追加αの説明文を追加、(2)期間別表`_elBaseAlphaPeriodTableV2`を`includeToday=true`にして先頭「本日」行を表示、(3)`_elAddAlphaPeriodTableV2`による「α＋ 推奨追加α値（期間別）」独立テーブルを下段に追加。「今日の推奨◯円（次点）」の大見出し(headNode)は残す＝いいとこ取り(ユーザー選択)。🎯見出しから「前日まで」表記を削除。早期returnガードを`!all.length`→`!all.length && !_todayN`に緩和し、本日のみ記録の銘柄でも本日行を表示(履歴ゼロ時headNodeは「データ不足」)。新規トップレベル定義の追加なし(既存関数の改修のみ)。ブラウザ実描画テスト(ReactDOM.render・有効v2合成データ)で本日行/説明文/「推奨追加α値（期間別）」見出し/想定損益すべて描画確認＋app-06全体PARSE_OK・CRLF維持。さらに`_elPeriodWindows`の「本日」行ラベルを`本日（M/D）`(先頭ゼロ除去)に変更＝「本日」は開いているページの日付(refDate)なので過去日を開いているときの誤認を防ぐ(ユーザー指摘)。`_elPeriodWindows`は基本α表/追加α表の共用なので早見ブロック(`_elBaseAlphaDayBlockV2`)も取引テーブル(`_elBaseAlphaPeriodBlockV2`)も一括反映。実描画でrefDate2026-06-08→「本日（6/8）」/2026-12-31→「本日（12/31）」を両表で確認。さらに**推奨追加α値の母数から数値根拠『底抜け前足浮き』を除外**(ユーザー指摘＝記録帳の根拠別分析④/⑤と揃える)。モジュール変数`_EL_NUM_REASON`(既定『底抜け前足浮き』・App(app-08:225)のレンダリングで`data.custom.addAlphaNumericReason`から実値設定＝リネーム追従・App描画は子より先なので各α関数の母数算出時に最新)＋判定`_elHasNumReason(s)`を新設し、`_elBaseAlphaA`(app-06:1302)と`_elAddAlphaPeriodTableV2`(app-06:1672)のaddPoolフィルタに`&& !_elHasNumReason(r.signal)`を追加＝推奨追加α値の全経路(期間別表/各「＋追加X」見出し/記録帳③recoNode/今週欄WeeklyPnlPanel/フォーム)へ一括波及。判定基準はfloatNode/④⑤と同じ`addAlphaReasons`に_numReasonを含むか(複数根拠・旧`addAlphaReason`文字列も対応)。記録帳「追加α分析」の①概況KPI/②効果検証/③上乗せ幅別の『効果』集計は追加α〇全体のまま(④/⑤で底抜けを分離・floatNodeで別表示する既存設計を維持)。等価テスト＝底抜け記録を足してもaddPool件数3/`_elBaseAlphaA().add`不変(addEqual)・反証で`_EL_NUM_REASON=""`にすると母数5へ増・app-06/08全体PARSE_OK・CRLF維持。さらに記録帳「追加α分析」の🔻底抜け前足浮きブロック(floatNode・`_elAddAlphaSectionV2`内)を**「前足浮きの何%を追加αにすべきだったか」分析に拡張**(ユーザー要望)＝数値根拠は前足浮き値(`addAlphaReasonVal`)を別途入力しているので固定+X円でなく前足浮き比で評価。(1)記録ごとに 前足浮き値/実際の追加α(実際%)/**理想の追加α**(=損切り回避&H1黒字の最小`_elIdealAddForRec().winMin`)/**理想%**(=理想追加α÷前足浮き値)/H1損益 を表示＋**理想%の中央値**。(2)**%別シミュ**＝前足浮きのP%を追加α(addAmt=round(F×P/100))にして基本αに足したときの 到達率/損切り率/H1勝率/想定損益(計ΣH1) を10刻み(上限は理想%最大に追従・最大200%)で出し**想定損益最大のPを★推奨**(母数=前足浮き値と基本αが揃う底抜け記録)。旧「さらに必要だった額(gap)」列は理想追加α(winMin=実際+gap)に置換。実描画で理想%(winMin/F)が外部計算と一致(0/60/70%)・推奨70%/損切り0%/想定+3,900円・app-06全体PARSE_OK・CRLF維持。さらに**追加α分析トグル(全部/〇/×/未選択)を、トグルが効かない3タブで非表示**に(ユーザー要望＝ややこしさ回避)＝EntryLogViewのトグルUIブロックを`(view !== "alpha" && view !== "appear" && view !== "list")`でゲート(α値=母数固定／出現=v2無関係の出現データ／一覧=旧記録含む全件)。タブバーは独立した別の子要素なので非表示の影響を受けない(目視＋実行中ソースでゲート位置確認)。注記文を「α値・出現・一覧タブは対象外」に更新。残りのタブ(集計/損切り/期間/カレンダー/シグナル別/OS連鎖/深掘り/未達)は従来通りトグル反応。app-06全体PARSE_OK・CRLF維持。さらに**推奨基本α詳細データ②「採用αでの全記録の内訳」(`_elBaseAlphaDetailV2`)の母数を×＋未選択に修正**(ユーザー指摘＝②が入力recs(αタブは_v2recsAll)を全行ループしていて追加α〇まで母数に混入・「対象外」はOS3/未到達のみで〇は普通のOS1/OS2行に紛れる・結論バーのpick.scN(〇除外)と②下insightの母数件数(〇込み)がずれていた)＝`recsSorted`を`recs.filter(!_elAddAlphaYes)`で〇除外し`_elBaseAlphaPick`の母数(×＋未選択)と一致。見出しを「② 採用αN円 での母数記録の内訳（追加α〇を除く ×・未選択のみ…）」に改名＋注記「推奨基本αの母数は追加αを使っていない記録(×・未選択)だけ…追加α〇は除外（追加α〇 N件を除外）」を追加(ユーザー要望＝対象記録だけ表示と明記)。実描画で〇記録の日付が②に出ない・×/未選択は出る・除外件数表記を確認・app-06全体PARSE_OK・CRLF維持。
- **整合性監査（多次元・敵対的検証）＋12系統の不整合修正 追記(2026-06-24j)**: 「②型(表示母数≠計算母数)」の同種を全app-*.jsで徹底監査(並列finder→候補19→敵対的検証で確定15→重複整理12系統・全て確立ルール照合/file:line根拠付き)。修正内訳:
  - **【母数】**(1)`_elBaseAlphaTrendBody`の件数列を`b.n`(バケット全件・〇込み)→`pick.scN`(×+未選択かつH1判定可能)に＝同行のα/損切り率/勝率/スコアと母数一致。(2)`_elAddAlphaSectionV2`③上乗せ幅別表を`eff`→`effNoNum`(=eff.filter(!_elHasNumReason))に＝推奨追加α(_A.add)と母数一致／④byR・⑤detByR・radd(reasonRecs)も`!_elHasNumReason`で数値根拠(底抜け前足浮き)併持記録を根拠別分析から除外＝floatNode専用に統一。
  - **【損益基準】**(3)`_elStopStatsV2`の損切り率分母を`o.os`(OS値入力件数・未達込み)→`o.entered`(E成立=_elDynResult ok/ng/draw)に＝時間帯別/損切りタブ等と統一(検証 any2÷entered4=50%・旧any÷os=33%)。(4)`_elPeriodStatsV2`の実現損益に`_elIsEntered(s,r.item)`ゲート追加＝未エントリーで残ったrealizedPnl混入を排除(他の全実現損益集計と統一・検証realCnt1)。
  - **【OS基準】**(5)app-04 銘柄別OS値分析「本日vs全期間」の↑↓矢印を平均(.avg)→中央値(.med)に＝セル主表示(中)・確立ルール(OS比較は中央値)と一致。
  - **【recsソース】**(6)シグナル別タブの推奨基本α2パネル(_elBaseAlphaTrendV2/_elBaseAlphaDetailV2)を`_groupPanel`の第3引数`fixedRecs`(=`_sigGroupsAll`←_v2recsAll由来・トグル非適用)で母数固定＝α値タブと一致(addAlphaFil=〇でも「データ無し」にならない・検証dataNashi0)。`_sigGroups`を`_buildSigGroups(arr)`へ抽出し`_sigGroupsAll`新設・推奨基本α見出しにトグル非適用の注記追加。
  - **【表記/注記/コメント・数値不変】**(7)推奨追加α説明文(app-06:1733/1775の2か所)「+1〜30円…有意に改善…無ければ+0」→「+5〜30円(5円刻み)…損切り率0かつ想定損益が黒字になる最小の加算…無ければ推奨無し」。(8)α値タブ「推奨基本α期間推移」注記「件数3件以上」→「件数フロア(最も件数の多いαの半分以上・最低3件)かつ到達率50%以上」(集計タブの同コンポーネント表記と統一)。(9)フォーム未選択ボタンtitle/(10)追加α未選択バッジtitle(app-05)「基本α母数から除外」→「基本αには算入・推奨追加αの母数からのみ除外」。(11)app-05旧コメント(3122/5404/5435)「推奨基本αの母数=×のみ」→「×＋未選択(=〇以外)」。
  - 計算ロジック変更は(1)〜(6)、(7)〜(11)は表記/注記/コメントのみ。実証=#1件数pick.scN(6件/全8件)・#3/#4/#5数値一致・#6/#7実描画(EntryLogViewマウント→シグナル別→〇トグルで母数固定確認)・app-04/05/06全PARSE_OK・コンソールエラー無・全行CRLF維持。
- **app-04.js / app-02.js**: 各記録表（取引/本日損益/EntrySignalSection/WeeklyPnlPanel明細）の「到達見立」列（ヘッダ/セル/展開colSpan/合計colSpan）を削除。app-02の並び替え「到達見立順」(`_esSortByDifficulty`/`_DIFF_RANK`/sortMode="difficulty")を撤去。
- **記録帳に「損益」タブ追加(2026-06-22d・app-06 EntryLogView・旧称「全体」)**: 銘柄タブの一番左に「💰損益」タブ(センチネル`_ALL_STOCK`)を追加＝選ぶと全銘柄合算(`_isAllStock`で`filtered`=全期間記録)。既定表示も損益に変更。集計ビューの先頭に「💰全体損益（期間別）」テーブル(`_ovPnlTbl`=day/week/month切替・期間/件数/勝率/EP損益/H1/H2/実現損益(各セルに合計＋平均併記)/損切り(件数・平均損切り額・損切り率)＋合計行・行タップでその期間の取引記録(`_recTable` full・state`ovExp`)を展開・損益基準は_elTotAccum)を追加。損益タブは損益情報のみ(KPI＋全体損益表＋累積損益＋連勝連敗DD)＝OS分析/EP位置/時間帯/曜日/×/△の分析は`!_isAllStock`で非表示(ユーザー方針2026-06-22d:損益だけ)。銘柄を選ぶと従来どおり全分析。α値/出現等のサブタブは残存(全体対応)。
- **推奨損切りをタイト優先に変更(2026-06-22d・app-06 `_elCutPick`)**: 「平均実現H1損益が最大」だと1回の損失額(リスク)を見ず大きい損切りに張り付く(回復玉が多いと損切りが大きいほど平均が上がる)問題→「平均が最大値−tol以内の中で最小の損切り値」に変更＝P&Lをほぼ維持できる範囲で一番タイト。tol=max(`_EL_CUT_TOL_MIN`=200円, |最大平均|×`_EL_CUT_TOL_FRAC`=0.2)。値を大きくするほどタイトに(調整可)。
- **期間ビューのグラフを刷新(2026-06-22d・app-06 EntryLogView 期間タブ)**: 5本重ね折れ線（読みにくい）→指標トグル(`chartMet`=実現/EP/H1/H2・既定H1)で選んだ1指標を3種で表示＝①損益バー＋勝率(`_elBarChartV2`新設・赤利益/緑損失の上下バー＋勝率を破線右軸0〜100%)②累積損益カーブ(`_elLineChartV2`流用・単線の資産曲線)③曜日揃えヒートマップ(`_elWeekdayHeatV2`・カレンダー型＝列=月〜金で縦揃え・行=週(上が古い→下が最新・2026-06-22にreverse廃止)・セルは「M/D＋損益円」フル表記(略さない・2026-06-22)・日別の色濃淡で曜日ごとの傾向を見る・gran非依存で常に日別・記録無しの営業日枠は薄い空セル)。オフライン(file://)前提のため全て自前SVG/HTML(Chart.js不使用)。
- **期間テーブルに「日数」列追加(2026-06-22d・app-06 期間タブ)**: 週/月の右に、その期間に市場が開いていた営業日数(平日かつ非祝日・記録の有無に関係なく数える・当日todayStr()まで頭打ち)。営業日判定は`_fmIsBizDay`(app-03)＋`_buildHolidayDateSet`(app-01・ユーザー記録の祝日/休場イベントから生成)。週は月曜起点Mon〜Fri・月はその月の全日・日は1。期間明細の展開colSpan 9→10。
- **推奨追加αに到達率40%フロア追加(2026-06-30・app-06 `_elAddAlphaReco`)**: 段階フォールバックの第1段(損切り0%)・第2段(損切り≤30%)に「到達率(EP到達率OS2まで)≥`_EL_ADD_MIN_ERATE`(新規トップレベル定数・既定0.4＝当初0.5から変更)」を追加＝届かない高αを除外(基本αの`_EL_BASE_MIN_ERATE`と同思想・独立つまみ)。第3段(最後の砦＝黒字で損切り率最低)には課さない(ユーザー選択＝安全網)。黒字(想定損益ΣH1>0)＆scN≥`_EL_BASE_MIN_N`(3件)の全段共通必須条件は不変。判定ヘルパー`_reachOk`を選抜直前にローカル新設(t1/t2フィルタのみに適用)。`_elAddAlphaPeriodTableV2`の説明注記2か所(銘柄別/全銘柄)も新条件に更新。返り値の形・母数・他経路は不変でUI無改修。
- **推奨α値ブロックを統合＝下半分(追加α独立テーブル)を廃止し上半分に集約(2026-07-01・app-06)**: ユーザー要望「上半分で事足りる／追加αの次点が上半分に無い／見出しを推奨α値に」。(A)`_elBaseAlphaPeriodTableV2`の「└ +追加α」行(本体)の直後に**「└ +追加α 次点」行**を新設＝`_elAddAlphaReco`が既に同梱する次点フィールド(`add2/total2/stopRate2/h1win2/eRate2/scN2/sim2`)をそのまま使い**再計算なし**・8セル(推奨損切り列は`dash`)・ガード`if(add&&add.improved&&add.add2!=null)`・key`"a2"+i`・スタイル`_dadd2`(1px dashed #FDE68A)・paddingLeft18。行順は 本体→└+追加α→**└+追加α次点**→└次点(基本α)。これで上半分が下半分の情報を完全包含。(B)両ブロック(`_elBaseAlphaPeriodBlockV2`=ChartSection/`_elBaseAlphaDayBlockV2`=DayView早見)から**追加α独立テーブル`_elAddAlphaPeriodTableV2`の呼び出し＋その見出し＋注記を削除**(基本α表の末尾`,`→`)`に修正)。(C)見出しを改称＝DayView最上位「🎯 推奨基本α値（日付）」→「🎯 推奨α値（日付）」/ ChartSection「α 推奨基本α値（{stock}・期間別）」→「α 推奨α値（{stock}・期間別）」。(D)削除で消える追加α説明を基本α注記末尾(2か所・replace_all)に1文追記(「└ +追加α」「└ +追加α 次点」＝追加α〇母数の推奨加算とその次点・到達率40%以上等)。`_elAddAlphaPeriodTableV2`自体は**記録帳②追加αサブタブ(app-06:4034)で単独使用のため存置**(基本α表と非同居・改称対象外)。記録帳①基本αサブタブの`_secH("🎯 推奨基本α値（期間ごとの傾向）")`(app-06:3935)も**据え置き**(追加αは別サブタブで「推奨基本α値」が正)。レイアウトはshow_widget2案(案A=次点を独立行/案B=金額インライン)提示→案A採用。app-06全体PARSE_OK・CRLF維持。新規トップレベル定義なし。
- **浮き足加算を底抜けラインOSにも適用（2026-07-07・両方に付ける）**: 「前足浮き半値」の欄を出す対象シグナルを単一→集合化。新設 `_elUkiSignalNames(custom)`（app-05・定数 `_UKI_DEFAULT_SIGNALS=["底抜け水準線OS","底抜けラインOS"]`・`custom.ukiSignalNames`配列を優先＋旧 `custom.ukiSignalName`単一も常に包含＝後方互換）。ゲートをフォーム(app-05 `_showUki=fTags.some(t∈names)`)とEPナビ(app-04 `showUki=names.indexOf(nTag)>=0`)で集合判定に変更。下流(`_elUkiYes`/`_elUkiAdd`/合計α畳み込み/記録帳の浮き足・その他サブタブ/推奨基本α・追加αの母数除外)は `signal.ukiUsed` 駆動でシグナル名非依存＝底抜けラインOSの浮き足記録も自動追従（無改修）。ユーザー決定＝AskUserQuestion「両方に付ける」。sw v55→v56。
- **#2 時間かぶりの合計除外（2026-07-07・[[project_scalping_total_pnl_system]]の#2実装）**: 同一日・全銘柄でv2算入記録を時刻昇順に並べ、隣接≤5分を「上限2個の貪欲ペアリング」でペア化（例 9:31/9:34/9:39/9:42/9:47→{9:31,9:34}{9:39,9:42}{9:47}）。各ペアは（）外手じまい損益（`_elHold2TotParts`のmain・採用α/採用損切り）の**残す方を選定（2026-07-08ユーザー指示で変更）＝時刻が異なれば早い方(ペア先頭)を残す（損益に依らず）／同時刻(分が同じ)のときだけ損益が小さい方を残す（同額は先）＝残さない方を合計額から除外**（金額もCntも全スキップ・件数系=`_elCalcStats`由来の件/到達等は両方残る。旧は「損益の悪い方を残す」P&L基準だったが後知恵回避のため時刻優先へ・app-05のA.min===B.min分岐）。単独は除外なし。新設（app-05・`_elTotAccum`直前）: `_elCollKey`/`_elCollisionExcludedSet(data)`（memo化）/`_elCollExcluded`/`_elCollMarked`/`_elCollMarkNode`（残した側の明細行に「※被り有」バッジ）。`_elTotAccum`に`get.excluded`を追加し**表示総計だけ**配線＝app-02 `_detailTotRowFor`・app-04 `_trTT`(取引テーブル)・app-06 `_elWeeklyTargetSummaryV2`(第3引数dataOpt追加)/`_ovPnlTbl` totOf/`_grpTable`/`_kpiBlockOf`/`_groupPanel`/`_periodTot`。α総当たり/理想α/推奨基本α系（`_elIdealAlphaV2`・α感応度カーブ・損切り値別シミュ・α意思決定表・`_elBaseAlpha*`）には付けない＝不変。inline複製にも反映＝app-02 `_sumRow`(_recsM/_stM)・app-04 `_wkRow`(_wkRecsM/_wkStM)/`_pbRow`(_pbRecsM)/`_pnlDetailTableEl`フッター(_collXpb)。明細行の「※被り有」＝app-02 `_detailRowsFor`/EntrySignalSection行・app-04 `_pnlDetailTableEl`と取引テーブル行・app-06 `_recTable`（時間セル・除外された良い方側は無印）。**追い配線（同日ユーザー承認＝表示系は全部反映）**: app-02 EntrySignalSection合計行(_esTotループ冒頭skip)・app-08 ホーム月次集計(_mAgg)・app-04 検索日カード（実現損益合計のみ除外・勝敗カウントは全件）・カレンダー日別ランク(app-05 gradeByDate)＋チャート早見表(`_elCalcChartGrades`に第4引数exclFn追加・app-04の3セルで配線)・記録帳 累積損益カーブ(`_elCumPnlSectionV2`にdata prop追加＝渡すと被り除外でフィルタ・呼び出し3箇所配線)。Sig直渡し用ヘルパー`_elCollExcludedSig(data,stock,date,s)`追加（app-05）。**可視化強化（同日・記録帳監査後のユーザー選択①②④⑤）**: `_elCollMarkNode`を両側化（残した側=琥珀「※被り有」/除外側=灰「被り除外」）・`_elCollisionExcludedSet`の返り値に`info`（key→{role,own,oStock,oTime,oMain}＝ペア相手情報）追加・`_elCollExclCountRecs(data,recs)`（除外件数）・`_elCollPairNode(data,r)`（EntryLogCardのペア相手1行・data無し呼び出しはnull）。記録帳KPI3箇所（集計タブ`_kpiBlockOf`/シグナル別`_groupPanel`/期間タブ`_periodKpi`）の件数カードに「被り除外n件」サブ表示・`_recTable`下に凡例（該当行がある時のみ）。EntryLogCardヘッダ下にペア情報行（app-05）。**記録帳監査の追補（同日）**: EntryLogCardに採用α内訳（`_elAlphaBreakdownNode`＝基本+浮+追加）と詳細タグ行（`_elSigDetailSec`から底/起/特徴を1行合成＝明細表の2行表示と対称）を追加。監査結論=スルー母数(_elInclTotal)・浮き足判定(_elHasNumReason=_elUkiYes)・明細2行化(_elSigCell共有)は追従済み／△保有分析の生holdExp参照は保存時導出で同値（技術負債メモ）／検索→詳細タグ分析ジャンプ(Ph2b)は既知の保留。実V8合成データ検証（ペア除外/件数/単独不変/理想α不変/同時刻同額/main無しペア）全PASS。sw v60→v61。**2026-07-08 銘柄別ビューは同一銘柄内の被りだけ除外（別銘柄との時間かぶりでは落とさない）**: 全ヘルパーに任意の第3引数`scope`（銘柄名）を追加＝`_elCollisionExcludedSet(data, scopeStock)`がscope指定時は`byDay`をその銘柄の記録だけに絞ってペアリング（memoはscopeごと`_elCollMemo`マップ）。scope省略/null＝全銘柄横断（従来＝本日/今週/取引テーブル/検索日カード/ホーム月次/カレンダーは不変）。配線＝**銘柄別のみのビューにscopeを渡す**: app-02 ChartSection（EntrySignalSection/WeeklyPnlPanel＝`stock`）・app-04 早見表`StockQuickRefTable`（`_elCalcChartGrades`のexclFnに`activeStock`）・app-06 EntryLogView（`_collScope=_isAllStock?null:_selStock`を1つ定義し全セクションの`excluded`/`_elCollExclCountRecs`/`_elCollMarkNode`/`_elRowStyleWithColl`/`_elWeeklyTargetSummaryV2`(第4引数)/`_elCumPnlSectionV2`(scopeStock prop)へ渡す＝全体タブ→全銘柄横断・銘柄タブ→自銘柄のみ）。`EntryLogCard`に`collScope`propを追加（`_elCollPairNode`へ）。`_elDayStockBenchV2`は元々被り除外なし＝対象外（据え置き）。V8実証: クロス銘柄被りはscope時に除外されない/同一銘柄被りはscope時も除外/global不変。sw v67→v68。**2026-07-08c 被り除外件数の可視化を拡張（sw v70/v71）**: `_elCollExclCountRecs`を使い①本日/今週の損益データ見出し（app-04・不算入バッジの右に紫「被り除外N件」・全銘柄横断）②ChartSection単一銘柄「今週の損益データ」見出し（app-02・scope=stock）③検索の日付カード見出し（app-04・その日の全銘柄・_collExclN）に追加。**紫ドット新設**（app-05）`_elCollDot`/`_elDayStockCollCount(data,stock,date,scope=既定stock)`/`_elDayCollCount(data,date)`＝不算入(水色)と別チャンネルで両立→銘柄タブ（app-03 StockTabsにcollCount prop追加・app-04で`_elDayStockCollCount`配線＝scope=その銘柄）とホームカレンダー（app-05 Calendar・`_elDayCollCount`＝全銘柄横断）に点表示。**記録帳🗂記録一覧に「被り除外のみ」トグル**（app-06 collOnly state・_osFilRecsを`_elCollExcluded(...,_collScope)`で絞る・表示のみで集計/KPIは不変・0件時は非表示）。旧メモの「不算入のみN件フィルタ(listExclOnly)」は現行app-06に存在しない（旧EntryLogView削除で消失）＝この被り除外トグルは単独新設。
- **シグナル名の統合改名（2026-07-07f・`_migSignalRename2`＝app-01 migrateData）**: 底抜け水準線OS→底抜けラインOS／底つきサイン水準線OS→底つきラインOS／寄り付き足OS→寄り付きラインOS。ユーザーがマスター名だけ変更→過去記録が別シグナル扱いになった件の追従＝旧名の全保存箇所を新名へ一括書き換え（旧「底抜けラインOS」既存記録とも統合）。対象=記録(`tag`/`tags`dedupe/`sigDetail`キー)・`custom.signalTags`・詳細候補(`custom.sigDetails`=和集合/`custom.sigDetails2`=セクション別和集合)・浮き足対象(`custom.ukiSignalNames`/`ukiSignalName`)・出現欄メモ(`charts[*].apMemos`の「s|id|名前」キー)・手動出現(`appearances[].name`)・EPナビ保存(`epNavi[].tag`)。キー衝突は統合（配列=和集合・単一値=新名側優先）。冪等。`_UKI_DEFAULT_SIGNALS`(app-05)は["底抜けラインOS"]の1本に。実V8検証24項目（各書き換え・カスタム記録不変・非シグナルキー不変・冪等性）全PASS。sw v61→v62。
- **マスター改名の全域追従・恒久版（2026-07-07g）**: 今後の改名は自動で過去記録に追従（完全削除は従来どおり記録に触らない）。新設 `_elSignalRenameData(prev,oldNm,newNm)`（app-05・`_elSigDetailRenameSig`直後）＝シグナル名改名を旧名の全保存箇所（記録tag/tagsのdedupe/sigDetailキー・signalTags/ukiSignalNames/ukiSignalName/sigDetails/sigDetails2・apMemosキー・appearances[kind=signal].name・epNavi[].tag）へ一括適用した新dataを返す純関数（copy-on-write・改名先が既存でも統合＝配列和集合/単一値新名側優先）。EPナビ選択肢管理の`_sigRename`(app-04)をこれに差し替え（旧実装は①既存名で黙って中断②apMemos/appearances/epNavi/uki未追従）。詳細タグ改名`_detRename`(app-04)も既存名中断→統合(dedupe)に変更。テクニカル改名（TechnicalManageModal・app-02）に`onRename`プロップ追加＝AppearanceSection側で全チャートの手動出現行(kind=tech)の名前を追従。実V8検証20項目（統合/単純改名/no-op/copy-on-write参照同一性）全PASS。sw v62→v63。
- **改名追従の全UI展開（2026-07-07g後段・「どこで名前を変えても追従」）**: 残っていた「同名が既にあると黙って中断」を全て「統合(dedupe)」へ変更＝①エントリー記録フォームの詳細タグ改名(app-05 fDetAdd.old)②追加α根拠改名(app-05)③~~根拠詳細改名~~。**注: ②の根拠詳細キー追従（custom.addReasonDetails/signal.addReasonDetail）と③根拠詳細改名は、追加α根拠の詳細機能ごと2026-07-09に廃止済み＝現存せず（②は根拠名の改名のみ生きている）。**④銘柄改名(handleRenameStock app-04)の追従漏れ3つを補完＝custom.epnStocks（EPナビ表示銘柄）/stockSubCatRefs/stockInfoTabs（銘柄名キーのmap）。チャート形状タグ/銘柄タグ/ニュースカテゴリの改名は従来から記録追従済み（同名時はalertで明示中断＝据え置き）・イベントカテゴリはID参照で改名自動追従。sw v63→v64。
- **被り除外の行全体色分け（2026-07-08・ユーザー要望）**: 新設 `_elRowStyleWithColl(data, r)`（app-05・`_elNotInclRowStyle`直後）＝スルー(灰)/不算入(水色)に加え、時間かぶりで除外された記録の行を薄紫（#F5F3FF＋左#A78BFA・opacity0.68）で全体色分け。優先順位=スルー/不算入が先。行スタイル適用5箇所（app-02 EntrySignalSection行/WeeklyPnlPanel明細行・app-04 取引テーブル行/_pnlDetailTableEl行・app-06 _recTable行）を `_elNotInclRowStyle(s)`→`_elRowStyleWithColl(data, r)` に差し替え。「被り除外」バッジと凡例も紫系に統一。実V8検証（除外側=紫/残す側・単独=null/不算入優先/data無しは従来）全PASS。sw v64→v65。
- **シグナル統合（2026-07-17・`_migSignalRename6`＝app-01 migrateData・sigRename5直後）**: 「水平線耐えOS」「水平線底抜け」→既存「底つきライン」へ吸収（ユーザー指示＝元は同一シグナル）。各々の旧世代名（底つきラインOS/底つきサイン水準線OS/水平線耐えラインOS/水平線耐えライン）も同じ新名へ集約＝先行rename3/4/5のフラグ先行同期・実行順に依らず1本化。正規化照合済み`_elSignalRenameData`(app-05)で記録tag/tags/sigDetailキー・signalTags・sigDetails/sigDetails2・uki・apMemos・appearances・epNaviの全経路を移行（既存「底つきライン」へは統合＝配列和集合/単一値新名側優先）。冪等（一回性フラグ）。合成データ検証16項目（統合/dedupe/和集合/既存名不変/旧世代名吸収/冪等）全PASS。sw v178→v179。
- 構想メモ（未実装）: 基本αのプリセット複数化（α-A/α-B・Aがより大きいα値）。

- **指値不成立リスク（OS値＝α値）2026-07-20**: ユーザー要望「α値がOS値と同じ記録が何件あるか、それを除外した場合の損益」。予定EP（水準線＋採用α）にちょうど到達しただけで一度も上抜けなかった記録＝実際の指値注文が約定しなかった可能性がある記録を数え、除外後の保守的な損益を併記する。**判定（AskUserQuestionでユーザー決定）＝`_elOsMaxAll`（OS1〜3高値の最大・アウトカム盲目）===`_epOwnAlpha`（採用α）の「ちょうど一致」**。後の足でEPを上抜けた記録は「刺さった」とみなし対象外。**実エントリー済み（`_elIsEntered` or 実現損益あり）は約定した証拠につき対象外**＝仮想損益（見送り等）にだけ効かせる。新設（app-05・`_elTotAccum`直前）: `_elFillRisk(s,item)`/`_elFillRiskRec(r)`/`_elFillRiskCountRecs(recs)`/`_elFillRiskNode(r)`（明細行の「指値注意」バッジ・teal #0F6E56）。**時間かぶり(`_elCollisionExcludedSet`)と違い記録単体で完結する判定＝セット構築もmemoも不要**。配線は時間かぶりと同じ線引き＝表示総計のみ（α総当たり/理想α系には付けない）。UI（レイアウトはshow_widget3案提示→案C採用）: ①app-06 `_fillRiskSection(rs, scopeNote)`＝`_secH("🎯 指値不成立リスク（OS値＝α値）")`＋4セル（該当件数／通常の最終損益／除外後／差額）＋「該当のみ表示」トグル（`riskOpen` state・`_recTable`で該当記録だけ表示）。通常側は既存KPIと同じ配線（時間かぶり除外あり）＝除外後は「そこに指値リスクを足しただけ」で必ず同一母数・同一基準。配置＝損益タブ（`v2recs`・期間別表の直後）と`_groupPanel`（`_addFilOf(recs)`＝上のKPI `_kpiOs`と同母数）。②`_kpiBlockOf`の件数カード副文言に「・指値注意N件」（`_friskN`）＝`_kpiBlockOf`を使う全パネルに波及。③`_recTable`の時間セルに`_elFillRiskNode(r)`バッジ（`_elCollMarkNode`の隣）。**④週間・月間・累積の件数（追加要望 2026-07-20）＝`_ovPnlTbl`に「指値注意」列を新設**（損切り列と最終損益列の間・`friskCell(n,tot,ex)`＝件数＋対件数の率・0件は「—」）。日別/週別/月別トグルでそのまま週間・月間の件数になり、tfootの合計行が累積。展開明細の`colSpan`を8→9に追随。実機マウント検証: 判定4パターン（ちょうど一致=true／上抜け=false／実エントリー=false／未達=false）・セクション数値（通常0円3件→除外後-400円2件・差額-400円＝KPI最終損益と一致）・トグルが該当1件だけ表示・週別（7/20週1件50%・7/6週1件100%・6/8週1件50%・合計3件60%）・月別（07月2件67%・06月1件50%・合計3件60%）全PASS。console error無し。sw v203→v204。
- **指値不成立リスクセクションに日別/週別/月別の内訳（2026-07-20b）**: ユーザー要望「指値不成立リスクセクションでは、日・週・月別に分けて見れるようにしたい」。**期間の区切りロジックを共通化**＝`_ovPnlTbl`のローカル`keyOf`/`labelOf`をapp-06コンポーネント直下へ抽出（`_granKeyOf(ds,g)`/`_granLabelOf(k,g)`・週=月曜起点5営業日・月=YYYY-MM）。`_ovPnlTbl`は`keyOf=function(ds){return _granKeyOf(ds,g);}`の薄いラッパに置換＝挙動不変。日別/週別/月別のセグメントUIも`_granSeg(cur,setFn,keyPfx)`として共通化し、全体損益のインライン実装（3案ボタン）を置換（[[project_scalping_dedup_backlog]]の方針＝二重実装を作らない）。新設: `_friskTotOf(x,exFill)`（通常/指値リスク除外の集計器＝4カードと内訳表で共用）・`_fillRiskGranTbl(rs,g)`（期間／件数／指値注意／通常の最終損益／除外後／差額＋tfoot合計行＝累積）。granは`riskGran` stateで**全体損益の`gran`とは独立**（別々の粒度で見られる・既定week）。**合計行は上の4カードと同じ母数rs全体＝カードと必ず一致**（`_ovPnlTbl`の合計は4月＝EMA参考期間を抜くが、こちらはカード整合を優先して抜かない・コメントに明記）。実機マウント検証: 週別（7/20週2件中1件50%・-400→-800・差額-400／7/6週1件中1件100%・+400→—／6/8週2件中1件50%）・月別（07月3件中2件67%・0円→-800・差額-800／06月2件中1件50%）・日別（07-20/07-06/06-10）・合計行は粒度によらず不変（5件・3件60%・-400→-1,600・差額-1,200）・**2つのトグルが独立動作（全体損益=月別／リスク=日別を同時に確認）**・全体損益表が`_granSeg`置換後も従来どおり切替可能。console error無し。sw v204→v205。
- **指値不成立リスクを全体損益表へ統合＋独立の内訳表を廃止（2026-07-20c）**: ユーザー指摘「平均とかないね。独立のセクションとするのではなく、元の表に組み込む方がいいかな？」→統合案を採用（show_widgetでモック提示・セクションは「縮小して残す」を選択）。**`_ovPnlTbl`に「除外後（指値注意を除く）」列を新設**＝`totExOf(x)`（`totOf`の除外条件に`_elFillRiskRec`を足しただけ＝母数/基準はtotOfと完全に同一）＋`exclCell(a,b,days,ex)`（`_yenNR`＋既存`avgDayLine`の1日平均＋差額の小書き）。**元の表に載せたことで1日平均が自動で付く**（要望の「平均が無い」を解消）＝pnlCellと同じ体裁。**差額は0のとき行を出さない**＝該当記録が無い期間は最終損益と同額であることが一目で分かる。前日追加の「指値注意」列と隣接し、同じ行で 最終損益→除外後→差額 が横並びに読める。展開明細の`colSpan`を9→10に追随。**削除**: `_fillRiskGranTbl`（セクション内の日別/週別/月別 内訳表）と`riskGran` state、および`_fillRiskSection`内の`_granSeg`呼び出し＝件数/指値注意列とトグルの二重持ちを解消。`_fillRiskSection`は**4カード（該当件数／通常の最終損益／除外後／差額）＋「該当のみ表示」トグル**に縮小（一目での件数確認と対象記録の確認に専念）。`_friskTotOf`は4カード用として存置。**2026-07-20bで抽出した`_granKeyOf`/`_granLabelOf`/`_granSeg`は`_ovPnlTbl`＋損益タブの単独利用として存置**（挙動不変・`_ovPnlTbl`が短く読みやすくなるため差し戻さない）。実機マウント検証: 週別で 7/20週=指値注意1件50%・最終損益-400(1日平均-400)→除外後-800(1日平均-800)・差額-400／**7/6週=該当0件で除外後が最終損益と同額-1,600・差額行なし**／6/8週=1件50%・-400→-800・差額-400／合計=2件33%・-2,400→-3,200・差額-800。**セクションの4カードが表の合計行と完全一致**（-2,400／-3,200／-800）。ページ内のtableが1つだけ＝内訳表の消滅を確認。銘柄パネル(`_groupPanel`)のセクションも4カードで正常描画。console error無し。**注意: 期間内訳は「全体損益（期間別）」を持つ損益タブのみ＝銘柄パネルは4カードのみ**（元表が無いため）。sw v205→v206。
- **「指値注意」→「指値同値」改称＋2列を1欄に統合（2026-07-20d）**: ユーザー指示「『指値注意』でなく『指値同値』ね。指値同値欄は最終損益欄の右に移動して、除外後欄と統合してひとつの欄にして。上に件数／下に除外後の最終損益額」。①**全文字列を`指値注意`→`指値同値`に一括置換**（app-05/app-06・11箇所＝`_elFillRiskNode`の明細行バッジ・`_kpiBlockOf`件数カード副文言・表ヘッダ/title・コメント。関数名`_elFillRisk*`/変数`_friskN`等の内部名は不変）。②**`friskCell`と`exclCell`を1つの`friskCell(n,tot,a,b,ex)`に統合し、列位置を損切りの右→最終損益の右へ移動**＝上段に該当件数（+対件数の率を同じ行に小さく併記）・下段に除外後の最終損益（`_yenNR`）・差額は小書きで3行目。**差額0（該当0件）のときは差額行を出さない**＝最終損益と同額であることが一目で分かる。**該当0件でも下段の金額は出す**（列全体が「—」だと欠測と紛らわしいため・0件はグレー表示）。1日平均の小書きは統合で外した（隣の最終損益列に残っており、同じ行で対比できるため）。列数10→9に戻り展開明細の`colSpan`も10→9へ追随。最終列順＝週/日数/件数/到達/利確/損切り/**最終損益**/**指値同値**/実現損益。実機マウント検証: 列順・セル2段構造（7/20週=1件50%→-800円・差額-400／7/6週=0件→-1,600円・差額行なし＝最終損益と同額／合計=2件33%→-3,200円・差額-800）・KPI件数カード副文言「v2記録のみ・指値同値1件」・旧名がページ上に0件・展開明細のcolSpan=9・console error無し。※セクション見出し「🎯 指値不成立リスク（OS値＝α値）」は改称対象外（ユーザー指摘は列名のみ）。sw v206→v207。
- **セクション見出しも「指値同値」に統一（2026-07-20e）**: 2026-07-20dの列名改称に続き、ユーザー指示「揃えて」でセクション見出し側の旧表現も統一。`指値不成立リスク`→`指値同値`に一括置換（app-05/app-06・8箇所＝`_secH("🎯 指値不成立リスク（OS値＝α値）")`→`_secH("🎯 指値同値（OS値＝α値）")`と、`_uFR`/`_granKeyOf`/`_granSeg`/`_friskN`/`_friskTotOf`/`_groupPanel`配線まわりのコメント）。**これで表示・コメントとも用語が「指値同値」1本**（関数名`_elFillRisk*`・変数`_friskN`等の内部名は不変＝挙動に影響なし）。実機マウント検証: 見出しが「🎯 指値同値（OS値＝α値）」・ページ上に旧名（指値注意／指値不成立）が0件・4カードとトグルは従来どおり・console error無し。sw v207→v208。
- **指値同値欄に1日平均を追加（2026-07-20f）**: ユーザー指示「指値同値欄でも一日平均額は表示して。差額行の下」。`friskCell(n,tot,a,b,ex)`→`friskCell(n,tot,a,b,days,ex)`に引数`days`を足し、末尾に既存`avgDayLine(b.hold2, days)`を追加（他の損益列と同じ体裁・平均の分子は**除外後**の金額＝その列自身の数字）。呼び出し2箇所に日数を渡す＝明細行`dn`(_bizDaysIn(k))・合計行`_ovTotDays`。2026-07-20dで統合時に一度外したものを復帰（隣の最終損益列とは分母は同じ営業日数・分子が違うので値も異なる）。**段の順＝件数(＋率)／除外後の最終損益／差額／1日平均**。差額行は0（該当0件）のとき出さない仕様は不変なので、該当0件の期間は 件数/金額/1日平均 の3段になる。実機マウント検証: 7/20週=1件50%・-800円・差額-400・1日平均-800（-800÷1日）／7/6週=0件・-1,600円・1日平均-320（差額行なし・-1600÷5日）／6/8週=1件50%・-800円・差額-400・1日平均-160（-800÷5日）／合計=2件33%・-3,200円・差額-800・1日平均-291（-3200÷11日）。すべて算術一致・console error無し。sw v208→v209。
- **「指値同値」バッジを記録の明細表 全5表に展開（2026-07-25）**: ユーザー指示「この表でも指値同値は表示されるようにして」（日別ページの🎯エントリー記録の画面）。バッジ`_elFillRiskNode(r)`(app-05)は記録帳の記録一覧(app-06:6065)だけに配線されていたので、同じ並び＝**時間セルの`_elCollMarkNode`（被り除外バッジ）の直後**へ4箇所追加＝`app-02:4677`(EntrySignalSection 🎯エントリー記録)・`app-02:5014`(WeeklyPnlPanel 今週の損益データ)・`app-04:5779`(DayView 取引タブのエントリー記録)・`app-04:6128`(DayView 💰損益タブの記録表)。4表とも`r={stock,date,signal,item}`が既にスコープ内（`_elCollMarkNode(data,r,…)`が同じ`r`を使用）なので**判定・集計の配線は一切変更なし＝表示のみ**（`_elFillRisk`は「表示総計」限定でα総当たり系に配線しない方針を維持）。sw `v264`→`v265`。
- **⚡浮き足%分析を株価帯別に＋推奨%も帯優先（2026-07-25）**: ユーザー要望「エントリー記録帳の浮足％分析も株価帯別にしようと考えている」。AskUserQuestionで①配置=案A（浮き足%タブに帯ピル）②推奨%も帯優先③母数は全部そろえる、を確定。**①帯グルーピングを単一源化**＝`_bandAxisBody`内のインライン実装を`_pbSplitByBand(pool)`（app-06・EntryLogView直下）へ抽出し💴株価帯別タブと⚡浮き足%タブで共用（返り値`{bounds,groups}`・実帯は0件でも表示／材料・不明は0件なら非表示＝旧挙動と同一）。**②⚡浮き足%タブ**＝新state`ukiBand`（既定`"all"`）＋`_subTabBar`の帯ピル［すべて/💴帯…/⚡材料あり/帯不明］。選択帯は**KPI早見・加算率ボード・記録一覧の3つ全部**に効く（`_ukiRecs`単一変数）。ボードの母数を`_v2recsAllData`→`_ukiRecs`に変更（`_sigUkiPool`⊂`_v2recsAllData`＝v2かつデータ算入なので`"all"`時は従来と同結果）。**③推奨%の株価帯レッグ**＝`_elUkiPctPickScoped(data,refDate,mode,reasons,**stock**)`に第5引数を追加。`_pbDayBandOf(data,stock,refDate)`で当日の帯を取り、同帯の記録（銘柄横断）が`_EL_BASE_MIN_N`以上なら`pool`を帯へ差し替え（`byBand`/`bandLabel`を返す）。**帯不明・⚡固有材料日・帯が薄い場合は全銘柄プールへ純フォールバック＝stock未指定なら完全に従来と同一（後方互換）**。根拠別(_sp)の絞り込みは帯プールの上に重ねて効く。応用の「基本+10クランプ」再帰も同じ`stock`スコープで取る。呼び出し2箇所に銘柄を配線＝記録フォーム(app-05 `fStock`)・EPナビ計算フォーム(app-04 `stock`)＋両方の「推奨%」ピル右に`💴〜4000円`バッジ（`byBand`時のみ・title に件数と条件）。**⚠️頻度(spanOverride/bandSpan)は浮き足%ボードには無関係**（頻度カードを持たないため）。検証＝esprima構文OK＋実マウント（SW unregister+cache削除で新版確認）・帯レッグの合成データ検証［stock無し=24件/byBand=false、帯12件=帯採用、帯9件=全体へ戻る、材料日・帯不明=全体］・UIで帯ピル選択→KPI早見/ボード見出しが追従。sw `v265`→`v266`。
- **OS欄の「最高」→「実現最高」＋OS1〜5へ拡張（2026-07-25）**: ユーザー指摘「浮き足〇の記録一覧でのOS・損益詳細欄の最高OS値は、手じまいまでの最高値ではないの？違うなら『実現最高』と名前を変えてそうしてほしい」。調査結果＝**（）外は元から×/損切り打ち切り版(`_elOsMaxCapped`)＝ほぼ手じまいまで**だったが、**`slice(0,3)`でOS1〜3しか見ておらず、EPがOS2/OS3にずれた記録では手じまい足(H1/H2)がOS4/OS5に来るため高値が抜けていた**のが違和感の正体。AskUserQuestionで①改名＋OS1〜5拡張②×宣言した足自身の高値は含める（現状維持）を確定。**①`_elOsMaxCapped`の`slice(0,3)`を撤去**＝OS4・OS5も対象（`_epNextExpAt(s,x)`の有効域はx=0〜3なので`i-1`は最大3で範囲内・打ち切り規則は不変）。**②表示専用`_epOsMaxRawAll(s)`を新設**（app-05・`_epOsMaxChainNode`直前）＝（）内の生の最高値を全足に揃える（揃えないと実現＞生になり得る）。**③ラベル「最高」→「実現最高」**＋ノードにtitleツールチップ。分析側の語彙「実現OS」(`_elOsMaxFiltered`)／「生の最高OS」(`_elOsMaxAll`)と揃えた。**⚠️分析用の`_elOsMaxAll`（OS分布/α目安/指値同値判定など20箇所超）と`_elOsMaxFiltered`は一切不変**＝`_elOsMaxCapped`は`_epOsMaxChainNode`専用（表示のみ）なので波及なし。※「実現最高」は×足自身の高値を含む／分析の「実現OS」は含まない＝1足ぶん定義が違う（ユーザー確定で表示は含める側を維持）。セルは記録帳/本日/今週/銘柄別/取引の全表で共有＝全部に効く。**EPがOS2/OS3の記録は表示値が上がる**（回帰ではなく仕様修正）。検証＝esprima OK＋実マウントで3ケース［A: EP=OS2/打切なし→実現最高25(旧12)・B: EP=OS1従来型→12で不変・C: EP=OS2/OS4で打切→25（99）］＋`_elOsMaxAll`は全ケース12で不変＋レンダー文字列「実現最高 25（99）」・console 0。sw `v266`→`v267`。
- **最終損益を「H2まで」→「手じまいまで」に（N段カスケード化・2026-07-25）**: ユーザー要望「最終損益欄、H2までにするのではなく手じまいまでにできる？現状H2までになってるよね？」。AskUserQuestionで①単一源を直す＝表も分析も全部②ロジック＋詳細セルまで、を確定。**旧モデルはEP→H1→H2の2段固定**で、EPがOS1/OS2の記録は次足期待度が○のまま続いてもEPの2本後で打ち切っていた（実測: 4本とも○の記録で 旧+2,000円 / 実際は+4,000円）。**①名前の付け替え**＝旧`_elHold2TotParts`を`_elHold2TotPartsFixed`（2段固定・不変）に退避し、**新しい正本`_elHoldFinalParts`**を追加。呼び出し41箇所(app-02/04/05/06)は機械的に改名しただけで配線は不変＝表・合計・推奨α・推奨損切り・浮き足%・RN・シミュ・ランキング・累積カーブが全部同じ基準で揃う。**②depth汎用プリミティブ**（app-05・`_elHas2Data`直後）＝`_elHoldMaxDepth(s,alpha)`（足数と`_epNextExpAt`有効域x=0〜3の両方で決まる・epIdx=0かつOS5まであれば4）／`_elHoldIsStopAt(s,a,cut,d)`／`_elDynHoldAt(s,a,cut,d)`（d<=2は`_elDynHold`/`_elDynHold2`へ委譲＝従来の正本と完全一致）。**③1段ぶんの規約は旧H1→H2と同型**: 保有判断が×/損切り済/未設定→1段下のまま／その足で損切り→1段下のまま（損切り＝×と同一視）／△→（）外=1段下・（）内=そこまでの差／○→完全○チェーンなら（）外を更新。**④`_elRideVals`の手じまい足もN段化**（`_expD`/`_stopD`をdepth3以降へ延長）＝実現到達・手じまい足ラベル(H3/H4)・保有時間・**時間かぶり判定**が最終損益と同じ深さに。**⑤詳細セル**＝`_epHoldView`/`_elHoldParts`/`_stopRow`に`depth`引数を追加（未指定は従来＝depth<=2は1行も変わらない）＋`_elHoldStackInner`がH３/H４行を描画（**その足まで実際に保有した記録だけ**＝×/損切り/未設定で降りた記録・EPがOS3の記録は従来と同じ見た目）。集計側の縦積みラベル「H２：」→「最終：」＋3表のtitle文言を更新。**後方互換**: OS4/OS5未入力・nextExp3/4未設定（＝未設定は×扱い）・EPがOS3（maxDepth=2）の記録は`_elHold2TotPartsFixed`をそのまま返す＝**従来と完全に同一**。**⚠️これで実現最高(_elOsMaxCapped・同日にOS1〜5へ拡張)と最終損益・実現到達の深さが再び一致**（2026-07-16に揃えた不変条件を復旧）。検証＝esprima OK＋実マウントで6ケース［4本とも○: 2,000→4,000/手じまいH4・H2で×: 2,000のまま・nextExp3未設定(旧記録): 2,000のまま・H3引けで△: main3,000(ref+1,000)・H2引けで△: main1,000(ref3,000)・EPがOS3: maxD=2で完全一致］＋詳細セルがH３/H４行を描画（旧記録は3行のまま）＋`_elTotAccum`のEP-300/H1 1,000/最終4,000＋実現最高と実現到達mxの一致＋console 0。sw `v267`→`v268`。
- **【実バグ・当日回帰】実現最高が「降りた後の足」を1本含んでいた＋OS連鎖を手じまい足まで（2026-07-25b）**: ユーザー指摘「OS・損益詳細欄も手じまいまでのHが表示されるべきかな？」の調査中に発覚。提示された記録（高値5/7/7・次○→次○→次×・OS4高値9）で**実現最高が9＝保有していない足の高値**を表示していた（H行はH2までで正しい＝次×が手じまいの合図）。**真因＝`_elOsMaxCapped`が「その足を保有したか」を判定する前に高値を算入していた**。2026-07-06の次足期待度統一以降 `_epNextExpAt(s,i-1)` は「足iを保有するか」の意味なので、×なら足iは未保有＝入れてはいけない。旧コメント「×になった足自身の高値は含める」は統一前の（足自身の到達期待度）解釈の名残で、`slice(0,3)`がEP=OS1の記録で露見を防いでいたのを **2026-07-25のOS1〜5拡張が表に出した**。**修正＝EP以降の保有足だけ「算入の前に」打ち切り判定**（待ち足・EP未成立の経路は従来どおり算入してから）。これで `_elOsMaxCapped` ≡ `_elRideVals.mx`（実現到達）が常に成立。**⚠️2026-07-25のAskUserQuestion「×宣言した足自身の高値は含めますか？→含める」は、当該の足を「手じまいした足＝まだ保有していた」と誤って説明した上での回答だった**（実際は降りた次の足）。ユーザーの意図（手じまいまでの最高値）に沿って含めない側へ修正。**併せてOS連鎖セル**＝`_epOsChainCell(s, alpha, cutLine)` に第3引数を追加し、**手じまい足がOS3より後まで伸びた記録だけ**その足まで表示（既定は従来どおりOS1〜3・保有していない足は出さない）＝H３/H４行・実現最高と足並みが揃う。app-06の呼び出し2箇所のうちシミュ明細(5380)は損切り値がスコープに無いため2引数のまま（既定15・表示範囲の判定にのみ使用）。検証＝esprima OK＋実マウント［①画像の記録: 実現最高 9→**7**・実現到達mxと一致・OS連鎖は3本のまま・最終損益-100円で不変 ②最後まで保有: OS連鎖が5本に伸び・実現最高10・手じまいH4・最終+4,000円］・console 0。sw `v268`→`v269`。
- **手じまい拡張の反映漏れ監査＋2件修正（2026-07-25c）**: ユーザー確認「エントリー記録帳の合計損益など、反映されるべき場所に反映されてる？」。**単一源に乗っている経路＝OK**（`_elHoldFinalParts`が41箇所＝1記録セル/合計行`_elTotAccum`/`_elCalcStats.sumHold2`/KPI/推奨α評価器`_elH2EvalByFn`の`_h2At`/累積カーブ/ランキング/株価帯/指値同値除外後/シミュ）。**反映漏れ①`_elRealizedOutcome`（app-05・実現結果セル`_elOutcomeCell`＋損切り分析のフィルタ）**＝利益/損失をH2の損益で判定し、H2より後の損切りを"stop"にしていなかった → 手じまい足(`_elRideVals.exitD`)が3以上なら`_elDynHoldAt(exitD)`で判定＋`v.stopped&&exitD>=3`を"stop"に追加。**exitD<=2は従来と完全に同一**。**反映漏れ②損切り率の判定19箇所（app-06）**＝`_elPlanIsStop || _elHoldIsStop || _elHoldIsStop2`＝EP/H1/H2固定で、H3/H4の損切りを数えていなかった（**損切り率は推奨α/推奨損切りのゲート条件**）→ 新ヘルパー`_elStoppedDeep(s,alpha,cutLine)`（app-05・`_elHoldIsStop2`直前＝`_elRideVals`の`stopped&&exitD>=3`）を**既存式にORするだけ**の形で全19箇所へ機械挿入。**⚠️`_elHoldIsStop2`自体は変更しない**（`_elRideVals`が内部で使うため＝書き換えると`_stopD`が壊れ`_elStoppedDeep`と循環する）。H2期待度別分析(app-06 `hold2Exp`の`_calc`)はその足自身のstop率なので対象外。**据置（意図的にH2スコープのまま）**: 記録フォームの「H2結果損益」自動計算(`_elDynHold2`)・H2期待度別の深掘り分析・`_elCalcStats`のh2Yes/h2Mid/h2No/hold2CapSum（外部利用なしの死フィールド）。検証＝esprima OK＋実マウント［H4まで保有: 追加分false＝従来どおり・H3で損切り: 旧判定false→**新判定true**／実現結果stop／手じまい「H3・損切」・旧記録(OS4/5なし): 3項目とも従来と同一］＋最終損益セル4,000＝合計`_elTotAccum`4,000＝実現結果profitの一致・console 0。sw `v269`→`v270`。
- **【実バグ】損切り件数が「降りた後の足の損切り」を数えていた（2026-07-25d）**: ユーザー指摘「ここの損切り数は、最終損益で見たときの損切り数じゃないの？」。実例（07-24 フジクラ 底つきライン α1/損切15）＝**次×でH1で降りた**のに、その先のOS3の高値(18)が損切り値を超えていたため損切り1件に数えられていた（最終損益は−500円＝H1で、損切りではない）。**真因＝集計側の`_elPlanIsStop || _elHoldIsStop || _elHoldIsStop2`は「EP/H1/H2の足のどれかが損切り値に触れたか」で手じまい足を見ていない**。表示側(`_elRideVals`)は2026-07-16に「損切りは期待度手じまいより後には起こり得ない」と修正済みだったが、集計側が取り残されていた（2026-07-25bの`_elStoppedDeep`追加は"深い側"だけ埋めた対症療法だった）。**修正＝`_elIsStopFinal(s,alpha,cutLine)`（app-05・`_elHoldIsStop2`直前）を損切り判定の単一源に**＝E成立v2は`_elRideVals.stopped`（期待度の手じまい足と損切り足の早い方）／それ以外は従来式へフォールバック。`_elStoppedDeep`は廃止し、app-06の**19箇所すべてを`_elIsStopFinal`へ置換**（損切り件数/率・isStop分類・`_stopAt`・`_isStop`・期間別表の`stopsOf`・株価帯・時間帯・シグナル別）。`_elRealizedOutcome`(app-05)も同関数へ統一（未使用化した`_sp/_sh1/_sh2`は削除）。**⚠️`_elHoldIsStop`/`_elHoldIsStop2`自体は変更しない**（`_elRideVals`が内部で使うため＝書き換えると循環する）。**今後、損切り件数・損切り率は必ず`_elIsStopFinal`を使う**。**影響**: 損切り率は推奨α/推奨損切りのゲート条件（≤40%）なので★の選定に効く。過大計上が消えるぶん損切り率は下がる方向。検証＝画像の4記録を再現し**最終損益合計が+4,400円＝画面と一致**（−500/−100/+2,700/+2,300）した上で、**損切り件数 旧1件→新0件**・①の実現結果 stop→loss・EP足で本当に損切りした記録は stop のまま・console 0。sw `v270`→`v271`。
- **同型バグの全数監査＋4件修正（2026-07-25e）**: ユーザー「ほかに似たようなバグがないか徹底的にチェックして」。観点＝**①手じまい足を見ずに「足が損切り値に触れたか」で判定している所 ②表示側と集計側の二重実装のドリフト**。`_elPlanIsStop`（EP足）は**EP足が必ず保有されるため常に安全**／`_elHoldIsStop`（EP足orH1足）・`_elHoldIsStop2`（H2足）は**その足を保有したかを見ていない**、が判定基準。**修正4件**: (a)`_elStopStatsV2`＝損切り内訳(EP足/H1足/H2以降)と損切り率。旧は`h1 = !p && _elHoldIsStop(...)`で、EP足で降りた記録でもH1足の高値が損切り値超えなら「H1で損切り」に計上→`_elIsStopFinal`で先にゲートしてから足へ割当（**KPI早見・株価帯・分析パネル等5箇所で使用**）。(b)`_elKabuTierEval`のstopフラグ（損益は`_elHoldFinalParts`＝手じまい基準なのにフラグだけEP/H1足だった・シミュの損切り表示/anyStop）。(c)`_elStopOvershootSectionV2`の`avoidCut`（「この損切り値なら避けられた」の反実仮想）。(d)`_elMemoPerfSectionV2`の`lost`判定。**問題なしと確認**: `_elDynHold`/`_elDynHold2`/`_elHold*TotParts`内の`_elHoldIsStop*`は「H1/H2まで保有した場合」の反実仮想なので足チェックで正しい／`_elWinBucket`は全足を歩く／`_elDynResult`はEP足のみ／`_elCalcStats`のper-leg cap集計・app-04の損切り済み表示はper-leg表示で整合／`epStopN`(EP足)は安全。**要判断として据置（設計事項・勝手に変えない）**: ①**推奨損切り`_elCutPick`はH1基準**（`_elDynHold`＋EP/H1足stop・`_elCutPickH2`は一致マーク用のみ）②**推奨α詳細表`_elBaseAlphaDetailV2`の損切り率/勝率もH1基準**（選定側`_elBaseAlphaPick`系は最終損益基準＝表示と選定で基準が違う）③`_elOsMaxFiltered`（実現OS・OS分布の主基準）はOS1〜3どまりでH3/H4まで保有した足の高値が入らない。**死コード**: `_elIdealAddForRec`／`_elBaseAlphaH2Pick`／`_elAddAlphaH2Pick`（呼び出し無し）。検証＝esprima OK＋実マウント3ケース［①EP足で降りた(H1足は損切り値超え): 旧true→**新false**・②H1で降りた(画像の記録): 新false・③本当にEP足で損切り: 新true(不変)］＋`_elStopStatsV2`が EP足1/H1足0/H2以降0/率33%（3件中③のみ）＝**旧なら3件100%**・シミュstopフラグも追従・console 0。sw `v271`→`v272`。
- **据置3件を全部やる＋死コード削除（2026-07-25f）**: ユーザー「ぜんぶやって」。2026-07-25eの監査で"要判断"として残した3件を実施。**Ⓐ推奨損切りをH1基準→最終損益基準**: `_elCutEval`（`_elCutPick`の中身）を`_elDynHold`＋EP/H1足stop → `_elHoldFinalParts.main`＋`_elIsStopFinal`へ。母数ゲートも`_elDynResult`の決着判定に統一。返り値の`h1win`フィールド名は互換のため据置（**意味は「最終損益プラス率」に変わった**）。基準が一本化されたので`_elCutPickH2`との一致バッジ（`_elH2AgreeNode`）は無意味になり撤去（app-05記録フォーム/app-06 `_elCutPickCell`の2箇所）。**Ⓑ推奨α詳細表のインサイト文をH1基準→最終損益基準**: 表本体の★ゲート(`_ng`)は元から`_elH2EvalByFn`＝最終基準だったが、**下の説明文だけ`epStop||h1Stop`＋`_elDynHold`のH1基準**で食い違っていた。`_elDynResult`＋`_elHoldFinalParts`＋`_elIsStopFinal`へ統一し、文言「H1勝ち」→「最終損益プラス」・「H1判定可能」→「決着」。**Ⓒ実現OS(`_elOsMaxFiltered`)をOS1〜3→OS1〜5**: EPがOS2/OS3にずれた記録やH3/H4まで保有した記録で到達足の高値が母数から抜けていた。打ち切り規則（保有しない足は算入しない）は不変。**⚠️`_elOsMaxAll`（生の最高OS＝α到達確率・α目安・指値同値の母数）はOS1〜3のまま**＝EPはOS1〜3でしか成立しない性質上それが正しい。**Ⓓ死コード6関数を削除**: `_elIdealAddForRec`／`_elBaseAlphaH2Pick`／`_elAddAlphaH2Pick`（元から未使用）＋Ⓐで不要化した`_elCutH2Eval`／`_elCutPickH2`／`_elH2AgreeNode`。app-06は661,031バイトに。検証＝esprima 4ファイルOK＋実マウント［削除6関数がすべてundefined／Ⓒ実現OS=25（OS4の高値・旧なら12）・`_elOsMaxAll`=12で不変／Ⓐ`_elCutEval` n=12・mean=−1,500＝1記録の最終損益×12で整合・stopRate=1／`_elBaseAlphaDetailV2`と`_elCutPickCell`("16円"・バッジ無し)がエラー無く描画］・console 0。sw `v272`→`v273`。
- **⚡浮き足%タブに「🔁 応用α換算」サブタブ（2026-07-25g）**: ユーザー要望「浮足％でなくすべて応用αを用いた場合の各取引の記録が見れるようにしたい」＋「浮足％を用いた場合との差額が各記録・全合計とも見れるように」。AskUserQuestionで①帯の判定＝**その記録の水準線価格**(`signal.levelPrice`)②換算αに**RNまたぎ加算を含める**③置き場＝浮き足%タブの新サブタブ、を確定。**仕組み**＝既存の表示/損益ヘルパーは全て`(signal, α, 損切り値)`を受けるので**αを差し替えて再計算するだけ**（シミュと同じ機構・保存データは一切変えない）。**新設(app-06・`_ukiScopeToggle`の直前)**: `_EL_UKI_ALT_BANDS`＝[{max:5000,a:3},{max:7000,a:5},{max:Infinity,a:7}]（**〜5000=3円/5001〜7000=5円/7001〜=7円**・ユーザー指定。値を変えたくなったらこの表だけ直す）／`_elUkiAltBandOf(s)`（水準線価格→{price,alpha,idx}・未入力はnull＝換算不可）／`_elUkiAltBandLabel(i)`／`_elUkiAltAlpha(s)`＝帯別応用α＋`_elRnAdd(s)`／`_elUkiAltRow(r,aiOf)`＝{band,curA,altA,cur,alt,diff}（cur/altとも`_elHoldFinalParts().main`＝最終損益・損切り値は記録の採用値のまま）。**UI**＝`ukiSub==="alt"`分岐。KPIカード3枚（浮き足%（実際）／応用α換算／差額（換算−実際）＋1件あたり）＋明細表9列（日付・時刻／銘柄／シグナル／水準線（株価帯）／実際の採用α／換算α／最終損益（実際）／最終損益（換算）／差額）＋合計行。差額プラス行は薄赤・マイナス行は薄緑。**帯ピル(`ukiBand`)の絞り込みがそのまま効く**（母数は`_ukiListRecs`）。水準線未入力の記録は「換算不可」として合計から除外し件数を注記。検証＝esprima OK＋実マウント［合成4件: 水準線4600→帯〜5000/換算α3(実際10)/実際+2,600→換算+1,900/差額−700・6200→5(実際8)/+2,400→+2,100/−300・7800→7(実際5)/+2,100→**+2,300/+200**・水準線なし→換算不可。合計 実際+7,100／換算+6,300／差額−800（3件・換算不可1件除く）］＋サブタブ描画（見出し/説明/カード3枚/表9列/合計行）・console 0。sw `v273`→`v274`。
- **🔁 応用α換算の差額を全件で埋める（2026-07-27）**: ユーザー指摘「ちゃんと手じまいまでにしてる？差額欄もしっかり埋めて」。**手じまいは元から正しい**（`_elUkiAltRow`は`_elHoldFinalParts().main`＝1記録セル/合計行と同じ正本ヘルパー）。**実バグ＝片側が無エントリーの行を丸ごと捨てていた**: 最終損益の（）外(main)がnullになる①×見送り(スルー)②α未達(EP不成立)③（）外が立たない△エントリー、を「換算不可」として差額—・合計からも除外していたため、**「αを下げたから取れていた取引」（例: 05/20 JX金属 実際α12=未達→換算α3なら+1,800）が応用α換算の効果ごと集計外**になっていた。AskUserQuestionで**①0円扱いで全件算入 ②セルは「0円」＋理由バッジ**を確定。**新設(app-06・`_elUkiAltRow`の直前)**: `_elUkiAltWhy(s,a,mainV)`＝不成立理由（"スルー"/"未達"/"△のみ"・成立と α==null はnull・`_epIsXSkip`→`_elH2Miss`の順で判定）。`_elUkiAltRow`は`cur`/`alt`を main!=null ならmain・nullなら**0**へ（**nullになるのはα不明＝帯不明(水準線未入力)の時だけ**＝真の換算不可）＋`curWhy`/`altWhy`を返す。①②は「そのαなら取引しなかった＝0円」、③は（）外＝0の既存規約（`_elTotAccum`と同じ）で、いずれも（）外への寄与は0円＝算入して整合。**UI**＝`_altAmtWhy(v,why)`でグレーの「0円」＋下段に小さく理由バッジ、合計行に「（うち無エントリー0円算入 N件）」、実際カードの副題にも同注記、説明文に0円算入の規約を明記。`_altT.ne`＝0円算入件数。**効果**（ユーザー実データ23件）＝実際+11,300→**+14,200**／換算+10,400→**+17,100**／差額**−900→+2,900**（結論が反転）。検証＝全app-*.js `new Function` OK＋実マウントconsole 0＋`_elUkiAltRow`単体5ケース［α未達→0円(未達)／os1Exp=×の見送り→0円(スルー)／nextExp1=△の△エントリー→0円(△のみ)／両側成立→従来どおり／水準線なし→cur/alt=null＝合計から除外］。sw `v274`→`v275`。
- **💰全体損益（期間別）の展開明細を「対象期間の全記録」へ（2026-07-27b）**: ユーザー指摘「ここに不算入やスルー記録がないのはなぜ？全記録を表示してほしい」＋「表示だけしてくれればいい。算入云々でなく」。**確認結果＝スルーだけが表示専用として出ていた**（2026-07-20b・`_thruByP`は`_epIsV2(s) && _elIsThru(s)`）ので、**不算入(includeInTotal=false)・②データのみ・旧記録(非v2)・母数トグルで外れた記録は一切見えていなかった**。**変更**＝`_thruByP`→`_extraByP`＝**filtered − rs**（集計母数に入らなかった記録すべて）に一般化。`_extraLbl(r)`＝理由ラベル（スルー／不算入／データのみ／旧記録／対象外・この優先順）・`_extraBrk(rows)`＝内訳文言。**母数rsは一切触っていない**＝件数/到達/利確/損切り/最終損益/実現/1日平均/合計行/`_aggKeys`は完全不変（「表示だけ」の据置方針そのまま）。**表示**＝明細見出し「N件＋表示のみM件」／注記「※淡色の行＝集計に入らない記録（スルー2件・不算入1件・データのみ2件）。件数・損益・率のどれにも入りません」／期間行バッジは全部スルーなら「スルーのみ」・混在なら「算入記録なし」。**`_recTable`に第5引数`dimOf(r)`を新設**（既存7箇所の呼び出しは未指定＝挙動不変）＝ラベルを返した行に淡色(opacity .6/`#FAFAF9`/左線`#D6D3D1`)＋グレーのバッジ。スルー/不算入は`_elNotInclBadge`＋`_elRowStyleWithColl`が元から色分けするので`_elIsExcluded`で二重付けを回避。**検証**＝全app-*.js `new Function` OK＋合成データ実マウント（TESTA 5件＝通常2/スルー2/不算入1）で ①既定: 見出し「2件＋表示のみ3件」注記「スルー2件・不算入1件」・不算入行が水色`#38BDF8`で出現（**変更前は非表示**）②`custom.rotatingStocks=["TESTA"]`で②データのみ化: 「0件＋表示のみ5件」「スルー2件・不算入1件・データのみ2件」・データのみ行がグレー左線`#D6D3D1`＋「データのみ」バッジ・期間行バッジが「算入記録なし」へ・console 0。sw `v275`→`v276`。
- **記録一覧の並び順を「日付が新しい順」既定＋トグル（2026-07-27c）**: ユーザー指示「デフォルトは日付が新しい順／並び替えは古い順と選べるように／各日付の中では時間が早い順」。**2段ソート**＝日付だけ反転し、**どちらの順でも各日付の中は常に時間が早い順**（日付＋時刻を繋げた文字列の単純降順にすると日内まで逆順になるのでNG・`_byDateDescTimeAsc`は日付と時刻を分けて比較）。AskUserQuestion＋show_widget 2案提示で**①コントロール＝表の右上セグメント（日別/週別/月別と同じ形・案A）②適用範囲＝全ての記録一覧**を確定（旧「記録一覧は日時昇順に統一 2026-07-18」を上書き）。**新設(app-06・EntryLogView内)**: state `recOrder`（既定`"desc"`・**全一覧で共通の1状態＝どこで切り替えても全部に効く**）／`_byDateDescTimeAsc`／`_recSorted(rs)`／`_recSortBar()`。**`_recTable`が並べ替えとトグル表示を一手に引き受ける**＝`_recSorted(recs)`してから`limit`で切る（「新しい順の先頭N件」になる・旧は昇順の先頭N件）＋2件以上のとき表の頭に`_recSortBar()`を自動表示。呼び出し側7箇所は無改修（残っている`.sort(_byDateAsc)`は二度手間なだけで無害）。🔁応用α換算だけ`_elv2Table`製で自動表示に乗らないので`_ukiListRecs`を`_recSorted(_ukiRecs)`にし、明細表の上に`_recSortBar()`を明示的に置いた。**検証**＝`new Function` OK＋合成データ実マウント（TESTA 7件×3日）で ①既定: 07-17(09:50→10:15)→07-16(09:05→13:40)→07-15(09:10→10:20→11:30)＝**日付降順×日内昇順**・セグメントは「新しい順」が白 ②「古い順」タップ: 07-15→07-16→07-17の完全昇順へ ③銘柄タブの🗂記録一覧へ移動しても「古い順」が維持＝状態共有を確認 ④損益/集計・銘柄タブ・📡シグナル総合・⚡浮き足%・🔁応用α換算を順に開いてconsole 0。sw `v276`→`v277`。
